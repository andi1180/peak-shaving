// Platzhalter-Startseite — nur zur Build-/Deploy-Verifikation. KEIN Design,
// keine Farben, keine Navigation, kein echter Content (folgt in späteren
// Schritten, Pflichtenheft §11 Phase 1).
export default function Home() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '2rem',
        textAlign: 'center',
      }}
    >
      <h1>COOLiN ENERGY – Website in Aufbau</h1>
    </main>
  )
}
