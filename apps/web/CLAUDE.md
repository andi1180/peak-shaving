# CLAUDE.md — coolin.at Website

> Wird bei jeder Session automatisch geladen. Bewusst kurz.
> **Maßgebliches Detaildokument: `./Pflichtenheft_Website_Coolin.md`** — bei Widerspruch gilt das Pflichtenheft.
> Diese Datei enthält Regeln und Leitplanken; das Pflichtenheft enthält Details, Entscheidungen (mit Begründung) und offene Punkte.
> Exakte Design-Tokens: `./DESIGN.md`.
>
> **Dateiname-Hinweis:** Diese Datei heißt `CLAUDE.md` (damit Claude Code sie in `apps/web/` automatisch lädt). Es darf KEINE Parallel-Datei `CLAUDE_Website.md` o. Ä. im Repo liegen — sonst driftet der Stand. Immer nur diese eine Datei pflegen.
>
> **Übergeordnet: `../../Fahrplan_2026.md`** — kanonische Quelle für Reihenfolge/Umfang aller Bauabschnitte seit 20.07.2026. Für `apps/web` relevant: **B1–B3** (Lead-/Einwilligungsfundament, Segmentierung, Lead-Erfassungskomponente), **B4** (Vertragsablauf-Erinnerung), **B5** (Förder-Check), **B6** (E-Control-Widget). Der Haushalts-Tarifmonitor (`/strom-check`, T3) ist seit 20.07.2026 **RUHEND** — bleibt bestehen, bleibt `noindex`, bleibt unverlinkt, wird nicht weitergebaut; s. Banner in `../../Pflichtenheft_Monitor_MVP.md` und `../../packages/tariff-monitor/CLAUDE.md`.
>
> **Naming-Hinweis (nur claude.ai-Projektordner, nicht Repo):** Im flachen claude.ai-Projektordner liegt diese Datei bewusst unter dem Namen **`CLAUDE_Website.md`**, weil dort bereits eine `CLAUDE.md` für das Kalkulator-Schwesterprojekt existiert und zwei gleichnamige Dateien im selben flachen Ordner nicht unterscheidbar wären. **Im Repo** heißt sie korrekt `CLAUDE.md` (in `apps/web/`) — dort eindeutig, weil eigener Ordner. Beim Hochladen hier im Projektordner also immer `CLAUDE_Website.md` verwenden, nie `CLAUDE.md`.

---

## Was wir bauen

Der professionelle Neubau von **coolin.at** — der Marketing-Plattform von COOLiN ENERGY. Klassische Marketing-Seite mit echter Menüstruktur/Unterseiten (kein 1:1-Rebuild der alten Scroll-Seite), plus Fundament für einen späteren Login-/Subscription-Bereich. Die Website „dreht sich" um den bestehenden **Peak-Shaving-Kalkulator** (Schwesterprojekt), der als bezahltes Produkt hinter Login integriert wird. **Erster Bauabschnitt: die Marketing-Website (`apps/web`).**

## Zweites Produkt in diesem Ordner: Monitor-Gratis-Check

Seit T3 beherbergt `apps/web` zusätzlich zur Website eine zweite Produkt-Oberfläche: den **Gratis-Strom-Check** des Haushalts-Energiemonitors (Route `/strom-check`, `noindex` — noch nicht offiziell verlinkt). Das ist **kein Website-Feature**, sondern ein eigenes Produkt mit eigener Spec, das sich nur den App-Ordner + das Designsystem teilt.

- **Maßgeblich für Monitor-Arbeit:** `../../Pflichtenheft_Monitor_MVP.md` + `../../packages/tariff-monitor/CLAUDE.md` (Arbeitsregeln, „Stand & offene Entscheidungen"). Für Monitor-Arbeit in `apps/web` gelten **BEIDE**: das apps/web-Designsystem (diese Datei/`DESIGN.md`) UND die Monitor-Spec — bei Widerspruch zwischen Website-Pflichtenheft und Monitor-Pflichtenheft gilt für Monitor-Code die Monitor-Spec.
- **Reinheitsgrenze:** Die Rechenlogik (Normalisierung, Vergleich, Plausibilität) liegt in `packages/tariff-monitor` (rein, isomorph) und läuft **im Browser**. `apps/web` hält nur den Leserand (`lib/monitor/`: server-only Supabase-Zugriff + snake_case→camelCase-Mapping) und die UI-Komponenten (`components/monitor/`) — keine Rechenlogik hier nachbauen.
- **Dataless-Prinzip (Monitor-Variante):** Verbrauchseingaben verlassen das Gerät im Gratis-Check nicht (localStorage, Client-Rechnung) — anders als das spätere Abo (Server-Persistenz, eigenes Kapitel im Monitor-Pflichtenheft). Supabase-Zugriff für die Tarif-Tabelle läuft ausschließlich server-seitig, Env nicht-präfixt (`SUPABASE_URL`/`SUPABASE_ANON_KEY`, nicht `NEXT_PUBLIC_*`).
- **Design-Token:** `--color-navy-subtle` stammt aus der Monitor-Arbeit (Abo-Teaser-Karte), bereits in `DESIGN.md` dokumentiert — gehört zum geteilten apps/web-Tokensystem, nicht nur dem Monitor.
- **Bewusst NICHT gebaut:** die Marketing-Einbindung des Monitors (Nav/„Produkte", Landingpage, Verlinkung von `/strom-check`) — das gehört in die Website-Session, nicht in die Monitor-Bau-Session.

## Nicht verhandelbare Prinzipien

1. **Zwei Achsen, nicht vermischen.** *Leistungen* (Beratung/Umsetzung, Lead-Gen) vs. *Produkte* (Software/Daten-Abos, bezahlt). IA, CTAs und Ton trennen beide sauber.
2. **Peak Shaving ist das Flaggschiff** — eigener Top-Level-Punkt (nicht in den Leistungen) + prominenter Startseiten-Block mit Teaser. Zwei Rechner sauber getrennt: **Schnellrechner** (frei, Teaser) vs. **Pro-Kalkulator** (echte Analyse).
3. **SEO ins Fundament.** Intent-getrennte Seiten (Leistungen/Branchen/Wissen/Flaggschiff kannibalisieren sich nicht), JSON-LD (Organization/LocalBusiness Wien), sitemap/robots, Core Web Vitals, 301-Redirects von alten `.html`-Pfaden. **Flaggschiff-Content: „Leistungstarif 2027 / SNE-GV"** — bester AT-Hebel, erster Blog in Phase 1.
4. **Für Skalierung bauen, ohne Over-Engineering.** Produkte als datengetriebene Collection; Login später als Multi-Entitlement-Container; Reseller-Mandantenfähigkeit (Tenant-Overlay) nicht verbauen — aber jetzt nicht bauen. Keine Demo-Abkürzungen im Fundament, keine spekulativen v2/v3-Features vorbauen.
5. **i18n-vorbereitet.** Phase 1 nur Deutsch (AT), aber Struktur so, dass ein Sprach-Toggle (europäische Sprachen, Phase 3/4) ohne Umbau geht. Texte übersetzbar strukturieren, Locale-Routing vorbereiten.
6. **Seriös & ehrlich.** Professionell, ruhig, keine Gradienten, keine verspielten Emoji-Icons, gedämpfte Profitöne (Navy + ein Grün-Akzent + neutrales Off-White). Mobile/Tablet/Desktop gleichwertig. **Keine erfundenen Kennzahlen/Referenzen.**
7. **Rechtstexte:** Datenschutz aus Bestand als Ausgangstext (referenziert in `reference/`), muss vor Live-Gang von Daten/Analytics/Login/Stripe erweitert werden `[rechtlich]`. **Impressum: der Bestand ist selbst unvollständig** (siehe OP#13) — nicht kopieren, bis Pflichtangaben vorliegen.

## Stack (bestätigt)

Next.js 15 (App Router), TypeScript, Tailwind 3, pnpm-Workspaces (Monorepo), shadcn/ui (Radix), lucide-react, next-intl, next/font, next/image. Supabase (Phase 1: `contacts`; später Auth/Stripe/RLS). Resend (E-Mail). Cloudflare Turnstile (Bot-Schutz). Plausible/Umami (Analytics → **kein Cookie-Banner**). Deploy: Vercel (Projekt `peak-shaving-web`, Root Directory `apps/web`; Domain später).

**Monorepo:** `apps/web` (Marketing + Konto/Auth + Admin + **der eingeloggte Portalteil**, aktiv) · `apps/website` (Standalone-Peak-Shaving-Funnel — **läuft parallel weiter**, eingebettet per iframe) · `packages/engine` (wiederverwendet) · `packages/shared` · `supabase/`.

> **`apps/portal` gibt es nicht mehr** (gelöscht mit B10-3, 22.07.2026). Der Ordner war ein leeres Next-Gerüst ohne Abhängigkeiten und ohne Vercel-Projekt; der eingeloggte Bereich ist hier in `apps/web` entstanden (Auth T4-2, Konto, Entitlements, Gutscheineinlösung, Zugangsschutz des Kalkulators B10). Eine zweite App für denselben Zweck wäre eine zweite Auth-Domain und ein zweites Deployment für nichts.

**Harte Bau-Constraints (aus DESIGN.md, gelten für jeden Prompt):** KEIN `/alpha` auf `var()`-Hex-Tokens (nur `*-subtle`-Tokens; Alpha nur auf Tailwinds eigener Palette wie `white/5`). Keine Gradienten. Keine Emoji-Icons (nur schlichte einfarbige lucide-Line-Icons oder keine). **Signature-Motiv max. 1× pro Seitenansicht** und nie hinter Text laufend. Akzent (Teal) sparsam.

## Arbeitsregeln (Kurzform)

- **Rollen:** claude.ai = Advisor/Architekt (schreibt Prompts). Claude Code = Implementierung (investigiert selbst). Andreas testet jeden Schritt live (Vercel-URL), bevor der nächste Prompt läuft.
- **Kommunikation:** Deutsch, kurz, präzise. Advisor statt Assistent — Position halten, kein Zustimmungs-Opener, Confidence-Tags bei echter Unsicherheit. Rückfrage nur bei echter fachlicher Lücke.
- **CC-Prompts:** klare AUFGABE + Kontext, expliziter NICHT-TUN-Abschnitt, konkrete Verifikation (gemessene Belege) im Bericht. ABSCHLUSS: `pnpm build && pnpm test && git add -A && git commit`, dann `git push`. Sonnet/Standard-Effort als Default; Opus nur bei echter schwieriger Einzelentscheidung. Ein Prompt = ein Schritt.
- **Gates:** Bugs → Root Cause durch CC, nicht Symptom-Fix.
- **Design:** frontend-design-Skill konsultieren. Mehr Freiheit als Kalkulator-Projekt, außer bei Rechtstexten.

## Stand & offene Entscheidungen

> Lebendiger Handover-Anker. Neueste offene Punkte. Erledigtes wandert raus. Vollständige OP-Tabelle: Pflichtenheft §13.

- **[GESAMTSTATUS]** Prompts 1–12 + **13a/b/c** + **14–17** (drei Startseiten-Grafiken, Rückbau zweier davon, Hero-Grafik PV-Erzeugung vs. Verbrauch auf `/leistungen/pv-speicher`) + **18 (Grafik-Platzierung korrigiert + Aggregationsschema `/leistungen/energiemanagement`)** + **19–24** + **25 (Wortmarke-Neuanordnung, Hero-Text, Branchen-Neuordnung 4→5, Speicheröfen, Kalkulator-Zugangscode)** + **26 (Nav-Zentrierung, Login ohne Funktion, Kalkulator-Gate als Popup)** + **28 (Kalkulator-Gate: Code bei JEDEM Aufruf, keine Persistierung)** abgeschlossen, live auf `peak-shaving-web.vercel.app`. **Das SEO-Fundament (§6.4) ist damit technisch vollständig**, und die in 13b berichtete Wissen-Verlinkungslücke ist dort geschlossen, wo sie inhaltlich passte — offen bleiben inhaltliche Punkte (OP#6 Keyword-Gegencheck, OP#13 Impressum) und die restlichen, in 13c erneut geprüften Lücken (s. u.).

- **[GEBAUT: B16-1 — Partner-Attribution (Fahrplan B16), NUR DATENBANK]** (24.07.2026). Modell A: Fachbetriebe verweisen ihre Bestandskunden per personalisiertem Link an COOLiN; COOLiN führt Analyse und Kundenbeziehung, der Partner bekommt das erste Zugriffsrecht auf die Montage. Migration `supabase/migrations/20260724190000_create_partner_attribution.sql` + DB-Gate `packages/db-tests/src/partner-attribution.test.ts` (38 neue Tests, 328 → **366**) + `supabase/database.types.ts` neu erzeugt. **Keine Route, keine Landingpage, kein UI, kein Admin-Screen** — das sind B16-2 und B16-3, die gegen genau diese Wrapper bauen. **KEIN `tenant_id`, kein Partner-Login, keine Partner-eigene Sicht auf Leads** (B13, unverändert zurückgestellt), **kein neuer `consent_purpose`** (die Rechtsgrundlage einer über einen Partnerlink entstandenen Anfrage ist dieselbe wie beim Kontaktformular: Vertragsanbahnung).
  - **[DIE ENTSCHEIDUNG, DIE DEN ABSCHNITT TRÄGT: ZWEI SPALTEN, NICHT EINE]** `platform.leads.partner_slug` ist die BESTÄTIGTE Zuordnung (Fremdschlüssel), `referred_by_text` der FREITEXT des Interessenten („Empfohlen durch"). Dieselbe Trennlinie wie in B7 zwischen Extraktion und Interpretation: die Kundenangabe ist BEOBACHTUNG, die Zuordnung ist URTEIL. Der Freitext trifft oft keinen Slug („Fa. Raymann Elektro", „mein Elektriker aus Wiener Neustadt") und ist trotzdem der Beleg, auf den sich die Zuordnung stützt — in einem Feld vermischt liesse sich nicht mehr feststellen, ob „raymann" dort steht, weil der Kunde es schrieb oder weil jemand es zuordnete. Folgerichtig: `admin_update_lead` kann `partner_slug` setzen (elftes bearbeitbares Feld) und hat für `referred_by_text` **bewusst gar keinen Parameter** — dieselbe Konstruktion wie bei `email` seit B2-1.
  - **[DIE ZWEITE ENTSCHEIDUNG: DIE ANONYMISIERUNG BEHANDELT DIE BEIDEN SPALTEN GEGENSÄTZLICH]** `referred_by_text` wird von `platform.anonymize_lead` genullt und steht in `guard_anonymized_lead` (jetzt **17** geschützte Spalten): Freitext einer Person kann Namen Dritter enthalten. `partner_slug` **überlebt** und steht ausdrücklich NICHT im Guard — ohne E-Mail, Name und PLZ ist „kam über Partner X" keine personenbezogene Angabe mehr, und die Partner-Statistik muss die 24-Monats-Frist überdauern; sonst verlöre ein Fachbetrieb rückwirkend den Nachweis über die von ihm gebrachten Kontakte, ausgerechnet für die ältesten. Die Kehrseite ist im Gate offengelegt (die Spalte bleibt technisch beweglich), der Weg dorthin bleibt trotzdem eng: `admin_update_lead` antwortet bei einem anonymisierten Lead mit `{status: anonymized}`, bevor es irgendetwas schreibt.
  - **[`platform.partners`: der SLUG ist der Primärschlüssel, es gibt keine zweite Kennung]** Struktureller Zwilling von `lead_sources` — ein stabiler Schlüssel, der in URLs wandert, von `leads` referenziert wird, nie gelöscht und über `is_active` stillgelegt wird. Eine zusätzliche `id uuid` wäre eine zweite Identität für etwas, dessen ganzer Zweck ein einziger, unwiderruflicher Schlüssel ist. **Derselbe Format-CHECK `^[a-z0-9-]+$` wie `lead_sources.key`** — die Regel, an der B10-5 real mit SQLSTATE 23514 aufgeschlagen ist; hier wiegt sie schwerer, weil der Slug in einem Link steht, den ein Fachbetrieb an hunderte Bestandskunden verschickt. **Kein `delete`-Grant für IRGENDEINE Rolle** (Muster `monitor.scrape_targets`): ein gelöschter Partner machte jede bereits erfolgte Zuordnung unerklärbar.
  - **[DER TRIGGER SCHÜTZT DEN FALL, DEN DER FREMDSCHLÜSSEL NICHT SCHÜTZT]** `guard_partner_slug` blockt jede Umbenennung. Der FK allein reichte nicht — er greift nur bei einem BEREITS referenzierten Slug. Ausgerechnet ohne Leads ist der Schaden am grössten: die verschickten Links zeigten ins Leere, und der Fehler äusserte sich als **Ausbleiben** von Leads, was niemand bemerkt.
  - **[KONTAKTPERSON GETRENNT VON ANFANG AN]** `contact_first_name`/`contact_last_name`, nicht ein `contact_name`. Die Auftrennung genau dieses Feldes hat `platform.leads` eine Migration zuvor einen brechenden Spaltenwechsel gekostet; ein zweites zusammengesetztes Namensfeld anzulegen hiesse, denselben Defekt sofort neu einzuführen. **Bewusst KEINE `contact_email`/`contact_phone`:** es gibt in B16-1 keinen Benachrichtigungspfad, der sie benutzte (die Weitergabe eines Montageprojekts ist B16-3), und eine Spalte auf Vorrat wäre eine Angabe, von der niemand weiss, ob sie gepflegt ist. Additiv nachrüstbar.
  - **[EIN LINK MIT TIPPFEHLER DARF KEINEN LEAD KOSTEN — und die bewusste Gegen-Entscheidung]** `capture_lead` VERWIRFT einen unbekannten oder **inaktiven** Slug und legt den Lead trotzdem an; der Freitext bleibt stehen und wird ausdrücklich NICHT durch den verworfenen Slug ersetzt. `admin_update_lead` dagegen **WIRFT** (22023) bei einem unbekannten Slug — dort steht kein Interessent auf dem Spiel, sondern eine bewusste Handlung, und eine still verworfene Zuordnung sähe aus wie eine erfolgte. **Ein INAKTIVER Partner ist dort zulässig** (eine historische Feststellung ist kein Fehler), in `capture_lead` nicht (sonst hätte `is_active` für den einzigen Pfad, der im Betrieb Zuordnungen erzeugt, keine Wirkung).
  - **[ZUSAMMENFÜHRUNG: DIE ERSTE NENNUNG GILT]** Beide neuen `capture_lead`-Parameter folgen `coalesce(Bestand, neu)` wie `company`/Name — NICHT der B3-1-Segmentierungsregel. Kommt derselbe Kontakt später über den Link eines ANDEREN Fachbetriebs, bleibt die erste Zuordnung stehen; sonst entschiede die zufällige Reihenfolge zweier Formularabsendungen darüber, wer das Montageprojekt bekommt. Beide Parameter hängen **hinten an** (Muster `p_locale`/B3-1) — ein bestehender POSITIONALER Aufruf bleibt gültig und wird im Gate genau so ausgeführt.
  - **[DER EXPORT BEKOMMT DEN PARTNER-FILTER MIT, obwohl die Aufgabe nur die Liste nannte]** `leads_matching`/`lead_filter_summary` sind die EINE Filterdefinition für `admin_list_leads` UND `admin_export_leads` (B2-1). Nur die Liste zu erweitern hiesse: ein Admin filtert auf einen Fachbetrieb, löst den Export aus — und bekommt eine Datei mit dem GESAMTBESTAND. Beide Zahlen wären plausibel, die Abweichung fiele erst an der Datei auf, wenn sie das System bereits verlassen hat. **Gefiltert wird über `partner_slug`, nicht über den Freitext:** gefragt ist „wem zugeschrieben", nicht „wer wurde erwähnt".
  - **[FK-AKTION AUSGESCHRIEBEN, obwohl sie gegenstandslos ist]** `on delete restrict`. Partner werden nie gelöscht (kein Grant); eine dennoch versuchte Löschung soll LAUT scheitern statt die Zuordnung still zu verlieren. **`on delete set null` ist ausgeschlossen** — es löschte genau die Aussage, die überleben soll, UND es ist selbst ein UPDATE und träfe damit auf die Asymmetrie, die hier schon dreimal aufgeschlagen ist (`leads.last_edited_by`, `email_events.lead_id`, `analyses.lead_id`).
  - **[GEMESSEN, nicht angenommen — vier Wächter-Proben, jede bringt Rot]** Lokal je EINE Invariante entschärft und die exakt erwarteten Tests beobachtet: (1) `referred_by_text` aus `guard_anonymized_lead` entfernt → die Unveränderlichkeits-Prüfung wird rot. (2) `anonymize_lead` nullt zusätzlich `partner_slug` → ZWEI Tests rot (die Zuordnung überlebt nicht mehr, und `lead_count` schrumpft). (3) `capture_lead`-Zusammenführung auf „neu gewinnt" gedreht → der Erste-Nennung-Test wird rot. (4) `admin_export_leads` reicht den Partner-Filter nicht durch → „Liste und Ausfuhr liefern dieselbe Menge" wird rot mit **`expected 3 to be 2`** (der Lead des anderen Partners wäre in der Datei gelandet). Danach `supabase db reset`, Gate wieder 366/366.
  - **[NEBENBEFUND, im Gate festgehalten statt geglättet]** `service_role` scheitert beim Umbenennen eines Slugs schon am fehlenden `update`-Grant, nicht am Trigger (sie hat auf `platform.partners` nur `select`). Der Test hält beide Schichten GETRENNT fest — `postgres` hat alle Rechte und scheitert am Trigger, und das ist die Schicht, die auch dann noch hält, wenn jemand später versehentlich ein `update`-Grant vergibt.
  - **[EIN BESTEHENDER TEST WURDE BEWUSST NACHGEZOGEN]** `lead-editing-export.test.ts` pinnte `admin_update_lead` auf 11 Parameter; jetzt 12. Die Zahl ist die Absicherung dagegen, dass ein Feld unbemerkt bearbeitbar wird — sie wird nachgezogen, nicht aufgeweicht, und die Gegenprobe (`p_referred_by_text` darf gerade NICHT dazukommen) steht daneben.
  - **[VERIFIKATION]** DB-Gate **366/366** gegen den lokalen Stack nach vollem `supabase db reset`; `pnpm lint`/`typecheck`/`test`/`build` root-weit grün (apps/web 162, engine 148, shared 56, tariff-monitor 62). Jeder der vier neuen Partner-Wrapper wird ECHT aufgerufen (Arbeitsregel 2), die Rechtefläche nach den vier DROPs nachgemessen (Arbeitsregel 1/B3-1). Migration zusätzlich auf die Cloud-DB gepusht und dort introspektiv gegengeprüft (kein Funktionsaufruf — Segfault-Vermeidung). **Keine Vercel-Verifikation und kein HTTP-Fetch:** in diesem Schritt entsteht weder Route noch UI.

- **[GEBAUT: B10-5 — die Registrierung erfasst Firma + Ansprechperson und schreibt einen Lead]** (22.07.2026). Bis hierher hinterliess eine Registrierung nur `auth.users` + `platform.profiles`: Wer sich anmeldete und die Bestätigungsmail nie öffnete, hinterliess eine Adresse ohne jeden Kontext — für ein Produkt, dessen Zielgruppe der Elektrobetrieb ist, der Teil des Trichters, der am ehesten verlorengeht. Neue Migration `supabase/migrations/20260724170000_create_registration_lead_sources.sql` (ZWEI `lead_sources`-Zeilen, sonst nichts) + `lib/leads/{registration-source,capture-registration}.ts` + Erweiterungen an `lib/auth/{schema,actions}.ts`, `components/auth/{register,login}-form.tsx`, `app/(site)/[locale]/registrieren/page.tsx`, `app/auth/callback/route.ts`. **KEINE Änderung an `public.capture_lead`, an den Zusammenführungsregeln oder an einem anderen Einstiegspunkt.** 20 neue anwendungsseitige Tests (142 → **162**), 3 neue im DB-Gate (325 → **328**).
  - **[DIE ENTSCHEIDUNG, DIE VOR DEM BAU GEMELDET WURDE: EIN produktübergreifendes Formular]** Es gibt genau EINEN Registrierungsweg (`/registrieren`), und er trägt beide Trichter — den Monitor-Gratis-Check (`components/monitor/gratis-check-result.tsx`) und den Kalkulator-Zugang. Die drei Pflichtfelder gelten deshalb **plattformweit gleich streng, ohne bedingte Validierung**: die Zielgruppe ist in beiden Produkten der Betrieb, und derselbe Screen mit zwei Strengegraden wäre ein Zustand, den niemand am Markup ablesen kann. Entschieden von Andreas, nicht im Bau.
  - **[HERKUNFT AUS DEM RÜCKSPRUNGZIEL, NICHT AUS EINEM ZWEITEN PARAMETER]** `?next=` existiert seit B10-2 und trägt bereits, wohin jemand wollte. Beginnt das SANIERTE Ziel mit `/peak-shaving/kalkulator`, entsteht der Lead unter `kalkulator-registrierung`, sonst unter `registrierung` (`lib/leads/registration-source.ts`, rein und ohne Datenbank). Ein eigener `?quelle=`-Parameter wäre eine zweite Angabe über denselben Sachverhalt — beide frei setzbar, und bei Widerspruch müsste jemand entscheiden, welche gilt.
  - **[⚠ GEMESSEN, NICHT VERMUTET: der Schlüssel heisst `kalkulator-registrierung`, mit BINDESTRICH]** Die Aufgabenstellung nannte `kalkulator_registrierung`. Das ist nicht einfügbar: `platform.lead_sources.key` trägt seit B1-1 den CHECK `^[a-z0-9-]+$` mit ausformulierter Begründung am Spaltenkommentar („Wandert in URLs/QR-Codes/Auswertungen"). Der erste Migrationslauf brach mit **SQLSTATE 23514**; alle elf bestehenden Schlüssel schreiben sich mit Bindestrich. Den CHECK aufzuweichen wäre der schlechtere Handel gewesen — eine Herkunft ist unveränderlich, ein Bestand mit zwei Schreibkonventionen liesse sich später nicht mehr vereinheitlichen.
  - **[REGISTRY: HERKUNFT ≠ FORMULAR — eine strukturelle Trennung, kein Beiwerk]** Bis B10-5 war jeder Einstiegspunkt zugleich ein Erfassungsformular mit vier Texten und einer Feldliste. Die Registrierung ist das nicht: ihre Felder stehen im Auth-Schema, ihre Texte im `Konto`-Namensraum. `LEAD_SOURCE_KEYS` (alle Herkünfte, gegen die das DB-Gate weiterhin in BEIDE Richtungen prüft) zerfällt deshalb in `LEAD_CAPTURE_FORM_KEYS` und `LEAD_SOURCE_KEYS_WITHOUT_FORM`; `LEAD_CAPTURE_REGISTRY` indiziert nur noch die ersten. **Sicherheitsgewinn nebenbei:** `findLeadCaptureEntry` prüft jetzt gegen `isLeadCaptureFormKey` — über den Formular-Endpunkt lässt sich damit kein Lead unter der Herkunft `registrierung` anlegen, also keine Kontoanlage behaupten, die nie stattgefunden hat.
  - **[RECHTSGRUNDLAGE: VERTRAGSANBAHNUNG, KEINE EINWILLIGUNG]** `capture_lead` wird ohne Zweck aufgerufen; es entsteht keine `consents`-Zeile und es gibt kein Ankreuzfeld. Eine Registrierung für einen zugangsbeschränkten B2B-Bereich IST die Anbahnung — dieselbe Linie wie beim Kontaktformular. `retention_basis` bleibt bewusst auf dem Vorgabewert (`marketing`, 24 Monate): Hochstufen geht später jederzeit, der umgekehrte Weg wäre eine rückwirkend zu lange Speicherung.
  - **[FEHLERTOLERANZ]** `captureRegistrationLead` wirft NIE. Schlägt der Schreibweg fehl, wird laut geloggt (ohne die Adresse — ein Fehlerlog ist kein zweiter Speicherort für Personenbezug) und die Registrierung schliesst normal ab: ein verlorenes Konto wiegt schwerer als ein verlorener Bestandseintrag. `await` statt „fire and forget", sonst schnitte Vercel den Insert mit der Antwort ab.
  - **[`next` DURCH DIE GANZE KETTE — der bis B10-4 offene Punkt ist damit geschlossen]** Anmeldeseite → „Noch kein Konto?" (neu) → Registrierungsseite (`searchParams`, `sanitizeNext` mit **leerem** Rückfallwert) → verstecktes Formularfeld → `signUpAction` (zweite Prüfung) → `emailRedirectTo` → Callback → Zielroute. Der Rückweg „Sie haben bereits ein Konto?" trägt es ebenfalls, sonst wäre der Umweg über die Registrierung eine Sackgasse. **Der Callback hängt das Ziel jetzt auch an den FEHLERFALL** (`/anmelden?error=callback&next=…`) — genau dort landet ein Bestätigungslink, der in einem anderen Browser geöffnet wurde (fehlender PKCE-Verifier), und ohne `next` wäre der Kontext ausgerechnet im häufigsten Störfall verloren.
  - **[WARUM DER LEERE RÜCKFALLWERT]** `sanitizeNext(…, '')` statt des üblichen `/konto`: Am Ziel hängen ZWEI Dinge — wohin der Bestätigungslink führt UND unter welcher Herkunft der Lead entsteht. Ein Ersatzwert verfälschte die zweite Aussage, ohne die erste zu verbessern. Dieselbe Regel wie bei der Gutscheineinlösung (B10-4).
  - **[GEMESSEN, nicht angenommen — zwei Mutationen, zwei gezielte Rotfärbungen]** Präfix-Grenzprüfung durch ein naives `startsWith` ersetzt → **genau 1 Test rot** (`/peak-shaving/kalkulator-fremd` zählte als Kalkulator-Registrierung). `sanitizeNext` in `signUpAction` entfernt → **genau 1 Test rot** (fremdes Ziel im Bestätigungslink UND erschlichene Kalkulator-Herkunft). Danach jeweils wiederhergestellt, 162/162 grün.
  - **[VERIFIKATION — voller Flow lokal gegen den PRODUCTION-Build (Port 3500, lokaler Supabase-Stack, Playwright, echte Mails über Mailpit)]** Kalkulator-Route anonym → `/anmelden?next=%2Fpeak-shaving%2Fkalkulator%2Frechner` → „Jetzt registrieren" trägt das Ziel in der URL **und** im versteckten Feld · Registrierung erzeugt Lead mit `first_source_key='kalkulator-registrierung'`, `retention_basis='marketing'`, **0 Einwilligungen**, Firma/Vor-/Nachname getrennt gespeichert · dieselbe Strecke ohne `next` → `registrierung` · manipuliertes `next` (fremde Domain) → kein verstecktes Feld, Herkunft `registrierung` · leere Firma → sichtbare Meldung, **0 Leads und 0 Konten**, getippte Namen bleiben stehen · bestehender `/kontakt`-Lead + spätere Registrierung → **dieselbe Lead-ID, kein zweiter Lead**, `first_source_key` und Firma unverändert beim Erstwert (COALESCE-Regel unangetastet) · Bestätigungsmail geöffnet → landet auf **`/peak-shaving/kalkulator/rechner`** (Anfrage-Seite, da ohne Grant korrekt kein iframe), ohne `next` weiterhin auf `/konto` · `/kontakt` unverändert 200 mit allen Feldern · **0 Konsolenfehler**. **Nebenbefund (Testumgebung, kein Defekt):** lokal wechselt der Host im Callback von `127.0.0.1` auf `localhost` (`origin` aus `request.url`), wodurch das Auth-Cookie nicht mitreist — in Produktion greift `HAS_CONFIGURED_SITE_URL` und beide Schritte laufen auf der kanonischen Domain. Der Nachweis wurde deshalb einhostig wiederholt.
  - **[VERIFIKATION — Gates]** DB-Gate **328/328** nach vollem `supabase db reset`; `pnpm lint`/`typecheck`/`test`/`build` root-weit grün (web 162, engine 148, shared 56, tariff-monitor 62).

- **[GEBAUT: B10 Schritt 3 von 3 — Abschluss: Doku nachgezogen, `apps/portal` gelöscht]** (22.07.2026, PR #35). **Kein Code in `apps/web`/`apps/website`, keine Migration, kein neuer Zugriffsweg** — reiner Doku- und Aufräum-Schritt. Geändert: `Fahrplan_2026.md` (B10 „offen" → „gebaut", mit beiden PR-Nummern), `Pflichtenheft_Kalkulator_MVP.md` (**§7a.3 neu** — die für B10 vorgesehene fachliche Tiefe, Muster wie §7a.2/B11), Root-`CLAUDE.md` (der Platzhaltersatz „Details zu B10 folgen…" ist durch den Verweis auf §7a.3 ersetzt), diese Datei, `.github/workflows/test.yml`, `pnpm-lock.yaml`.
  - **[`apps/portal` GELÖSCHT — vorher geprüft, nicht angenommen]** Der Ordner enthielt **10 Dateien / 114 Zeilen**, davon eine Seite mit dem Text „Grundgerüst — Auth & RLS folgen." Er hing **an keinem** `shared`/`engine`-Paket, hatte **kein** eigenes Vercel-Projekt und wurde von **keiner** Datei ausserhalb seiner selbst importiert. Gemessen: `pnpm-workspace.yaml` nutzt den Glob `apps/*` (keine Namensliste, nichts zu ändern), es gibt **kein `turbo.json`**, die Root-`package.json` fährt `pnpm -r` (ebenfalls glob-basiert). Die **einzige funktionale Referenz** stand in `.github/workflows/test.yml` (`pnpm --filter portal build`) und ist mitgezogen; `pnpm-lock.yaml` wurde neu erzeugt (0 Treffer für `apps/portal`).
  - **[VERCEL — gemessen statt aus `DEPLOYMENT.md` übernommen]** Die Projektliste des Teams über die API abgefragt: **5 Projekte, davon 2 auf diesem Repo** — `peak-shaving-web` (`rootDirectory: apps/web`) und `peak-shaving-website` (`rootDirectory: apps/website`). Die übrigen drei (`memorymark`, `handwerk`, `tauschboerse`) hängen an **anderen Repos**. **Kein Projekt mit `rootDirectory: apps/portal`** — die Angabe in `DEPLOYMENT.md` stimmt.
  - **[STALE REFERENZEN: die lebenden nachgezogen, die datierten NICHT]** Angefasst, weil sie den AKTUELLEN Aufbau behaupten: Root-`CLAUDE.md` (Monorepo-Block), `Pflichtenheft_Kalkulator_MVP.md` (Struktur + M4), diese Datei (Monorepo-Zeile), `packages/shared/src/demo-battery-catalog.ts` (Kommentar „später `apps/portal`"). **Bewusst unangetastet:** `STATUS_AUDIT_2026-07-03.md` und `ENGINE_CORE_AUDIT_2026-07-03.md` (datierte Momentaufnahmen — sie beschreiben korrekt, was am 03.07. galt), `Pflichtenheft_Monitor_MVP.md` und `packages/tariff-monitor/CLAUDE.md` (seit 20.07.2026 ruhend gestellt und ausdrücklich nur historisch zu lesen; ein Nachtrag dort machte genau diese Kennzeichnung falsch — dieselbe Begründung wie in der Root-`CLAUDE.md`), sowie `apps/web/Pflichtenheft_Website_Coolin.md` (Phase-2-Skizze, gehört einem anderen Pflichtenheft).
  - **[VERIFIKATION]** `pnpm lint`/`typecheck`/`test`/`build` root-weit grün **nach** der Löschung; die Löschung reisst nachweislich nichts mit. Kein Cloud-DB-Push (es gibt keine Migration).

- **[GEBAUT: B10 Schritt 1 von 3 — `calculator_pro` im Entitlement-System, NUR NACHWEIS]** (22.07.2026, PR #33, `48096fa`). **Kein Code in `apps/web`, keine Migration, keine Route, kein UI** — einzige Datei: `packages/db-tests/src/calculator-pro-entitlement.test.ts` (13 neue Tests, DB-Gate 312 → **325**). Hier festgehalten, weil er derselbe Bauabschnitt ist und die Voraussetzung für Schritt 2 schuf.
  - **[DER BEFUND: `calculator_pro` WAR SEIT T4-1 IM ENUM]** `platform.product_key` führt den Wert von Anfang an — `platform` wurde ausdrücklich für BEIDE Produkte gebaut. Das geplante `ALTER TYPE … ADD VALUE` wurde deshalb **nicht** geschrieben: eine Migration, die einen vorhandenen Enum-Wert hinzufügt, ist entweder ein Fehler oder ein Nichts, und beides stünde 2027 als Beleg für eine Änderung da, die nie stattfand. Ebenfalls schon vorhanden: `supabase/database.types.ts`, `KNOWN_PRODUCTS` im Stripe-Webhook, `PRODUCT_KEYS` + Label in `lib/admin/config.ts`.
  - **[AUCH DER WRAPPER WAR SCHON PARAMETRISIERT]** `public.get_my_entitlement(p_product)` delegiert an `platform.has_entitlement(p_user_id, p_product)` — das Produkt ist in beiden ein Parameter. Keine Codeänderung nötig; **was fehlte, war der AUFRUF**: bis dahin war jede Entitlement-Invariante ausschliesslich mit `monitor` geprüft (Arbeitsregel 2 — Introspektion beweist Existenz, nicht Lauffähigkeit).
  - **[DIE EIGENTLICHE NEUE GEFAHR: PRODUKT-ISOLATION]** Mit nur einem Produkt konnte eine vergessene `product`-Bedingung nirgends auffallen. Ab dem zweiten ist dieselbe Lücke ein **stilles Verschenken** — ein Monitor-Abonnent bekäme den Kalkulator gratis, und niemand bemerkte es, weil beide Zugänge „funktionieren". **Gemessen:** `platform.has_entitlement` lokal um die `and e.product = p_product`-Bedingung erleichtert → **genau 4 Tests rot**, die übrigen 9 blieben korrekt grün; danach wortgleich wiederhergestellt.
  - **[BEWUSST KEIN STRIPE-FALL]** `calculator_pro` bleibt `source='manual'` (Grant durch Admin, kein Selfservice) — Absicht, nicht Lücke; OP#1 blockiert diesen Schritt nicht.

- **[GEBAUT: B10 Schritt 2 von 3 — der Pro-Kalkulator hängt am Entitlement-System; das Soft-Gate ist GELÖSCHT]** (22.07.2026, PR #34, `779a8d7`). Der Zugang zu `/peak-shaving/kalkulator/rechner` verlangt ab jetzt eine Sitzung UND ein aktives `calculator_pro`-Entitlement. **KEINE Migration** (das Schema trägt seit T4-1 beide Produkte, s. B10-1) — reine Anwendungsarbeit in `apps/web`; **`apps/website` ist mit 0 Zeilen Diff unangetastet**, ebenso `packages/**`. Neu: `lib/kalkulator/access.ts` (+ Test) und `components/peak-shaving/calculator-access-request.tsx`. **Gelöscht: `lib/kalkulator-access.ts` und `components/peak-shaving/calculator-gate.tsx`.** 9 neue Tests (118 → **127**).
  - **[DER ALTE CODE WIRKT NICHT MEHR, WEIL ES DIE STELLE NICHT MEHR GIBT]** Der Zugangscode `coolin2026` stand im Client-Bundle und war ausdrücklich keine Sicherheit. Er wird jetzt nicht „ignoriert" — die Datei mit dem Vergleich, der Dialog und der Text-Schlüssel (`CalculatorFrame.gate.*`) sind entfernt; auf der Route gibt es **0 Eingabefelder** (gemessen). Der Legacy-localStorage-Aufräumer ist mit dem Dialog verschwunden: sein eigener Kommentar hatte das ausdrücklich für „die echte Auth in Phase 2" vorgesehen, und der Eintrag ist ohne Leser inert.
  - **[⚠ NAMENSKOLLISION, die beim Prüfen Verwirrung stiftet — nachgemessen]** In `platform.redemption_codes` existiert seit T4-3 ein GUTSCHEINCODE mit derselben Zeichenfolge `coolin2026` — für **`monitor`**. Der ist unberührt und wirkt weiter, aber woanders: eingelöst auf `/konto`, schaltet den Strom-Monitor frei. **Live gegengeprüft:** ein Konto, das `coolin2026` einlöst, hat danach ein aktives Monitor-Abo und steht auf der Kalkulator-Route **weiterhin vor dem Anfrage-Zustand** (0 iframes). Genau das ist die Produkt-Isolation aus B10-1, jetzt am sichtbaren Verhalten. Ein zweiter `coolin2026` für `calculator_pro` ist gar nicht anlegbar (`unique index on lower(code)`, produktübergreifend).
  - **[DREI ZUSTÄNDE, und der dritte ist der eigentliche Entwurf]** `anonymous` → Umleitung auf `/anmelden?next=…`. `no_entitlement` → **eigene Seite, KEINE Umleitung**: wer angemeldet ist und trotzdem weggeschickt würde, liefe im Kreis. Sie erklärt, dass der Zugang auf Anfrage vergeben wird, führt auf den bestehenden Deep-Link `/kontakt?thema=peakShaving` (kein neuer Parameter) und nennt die angemeldete Adresse — ein Grant hängt an genau EINEM Konto (B13 ist zurückgestellt), und wer mit dem falschen von zwei Konten angemeldet ist, sucht den Fehler sonst beim Zugang statt bei der Anmeldung. `granted` → der iframe, ohne jeden Dialog davor. **Im Anfrage-Zustand wird der iframe nicht gerendert** — er steht nicht hinter etwas, es gibt ihn nicht (der Unterschied zum abgelösten Gate, das eine Präsentationsschicht über einem ohnehin ausgelieferten Rechner war).
  - **[NEU IM AUTH-FUNDAMENT: `?next=` — es gab es noch nicht]** T4-2 hatte `sanitizeNext` gebaut, benutzt wurde es aber nur vom `/auth/callback`; `/anmelden` kannte kein Rücksprungziel und `signInAction` landete IMMER auf `/konto`. Jetzt: Parametername als EINE Konstante (`NEXT_PARAM`), die Seite prüft mit `sanitizeNext` (kein Open Redirect), reicht den Wert als verstecktes Feld weiter, und die Action **prüft ihn ein zweites Mal** — das Feld ist im Browser frei änderbar, und die Prüfung, die zählt, ist die auf dem Server. Die Prüfung steht VOR dem angemeldet-Zweig: sonst liefe genau die häufigste Reihenfolge ins Leere (Klick auf den Kalkulator → Login-Seite → „bin ja schon angemeldet" → `/konto` statt Rechner). `redirectToLocalized` nimmt dafür ein optionales `query`-Objekt und hängt es NICHT an den `href` (der ist ein Pfad-Schlüssel, den next-intl bei einer zweiten Sprache auflöst).
  - **[FAIL CLOSED, und das ist der wichtigste Test der Datei]** Ein Lesefehler bei `get_my_entitlement` gilt als „kein Zugang", nicht als Zugang. Die Abwägung ist asymmetrisch: ein zu Unrecht Ausgesperrter sieht einen Kontaktweg und meldet sich, ein zu Unrecht Freigeschalteter meldet sich nie. Ebenso `data === true` statt truthy — `'true'` und `1` sind truthy und öffneten eine lose Prüfung.
  - **[GEMESSEN, nicht angenommen — drei Mutationen, drei gezielte Rotfärbungen]** `data === true` → `data`: **2 Tests rot** (Zeichenkette `'true'`, Zahl `1`). Produkt `calculator_pro` → `monitor`: **1 Test rot** (genau der, der das vertauschte Argument fangen soll — der Fehler, der sonst NIRGENDS auffiele: der Zugang funktionierte, nur bekämen ihn alle Monitor-Abonnenten). Fail-open beim Lesefehler: **1 Test rot**. Danach jeweils wiederhergestellt, 127/127 grün.
  - **[TEIL 3 — Gutscheincode für `calculator_pro` reaktiviert]** `CODE_PRODUCT_KEYS` führt jetzt beide Produkte; die Begründung des Ausschlusses steht als Historie im Modul, weil sie erklärt, warum er bestand und was ihn auflöst. **Derselbe Mechanismus wie für `monitor`, kein neuer** — `public.redeem_code` liest das Produkt aus der Code-Zeile; **gemessen: die entstehende Entitlement-Zeile trägt `source='manual'`, `valid_until=null`.** Kein Stripe-Preis, kein Checkout (OP#1 offen; ein erfundener Platzhalterpreis wäre derselbe Fehler wie eine erfundene Tarifzahl in B11).
  - **[MITGEFUNDEN, vom Typecheck gemeldet] Der Einzelprodukt-Zweig im Admin-Codeformular war unerreichbar geworden.** `components/admin/forms.tsx` zeigte bei genau EINEM code-fähigen Produkt ein festes Feld statt einer Auswahl; mit zwei Produkten meldete TS die Bedingung als `2 === 1`. Der Zweig ist entfernt — und sein Begründungstext („Der Pro-Kalkulator prüft diesen Zugang noch nicht") war ohnehin zur Falschaussage geworden. **Live geprüft:** das Dropdown bietet „Strom-Monitor" und „Kalkulator Pro", ein darüber angelegter Code landet mit `product_key='calculator_pro'` in der Datenbank.
  - **[/konto UMGEBAUT — nötige Folge, kein Beiwerk]** (a) Ein **eigener Abschnitt „Peak-Shaving Kalkulator"** (derselbe Wrapper, anderes Produkt). Ohne ihn zeigte die Kontoseite jemandem, der gerade den Kalkulator freigeschaltet hat, weiterhin nur „Strom-Monitor — kein aktives Abo": je Zeile richtig, in der Summe „Sie haben nichts". Bewusst OHNE Kauf-Einstieg. (b) Das **Einlöseformular steht jetzt als eigener Abschnitt**, nicht mehr im „kein Monitor-Abo"-Zweig: dort verschwand es, sobald jemand ein Monitor-Abo hatte — **genau dieser Kunde hätte seinen Kalkulator-Code nirgends einlösen können.** Der Monitor-Zustand ist kein zulässiges Kriterium für ein Formular, das ein anderes Produkt freischaltet. Ausgeblendet nur, wenn BEIDE Produkte aktiv sind (jede Eingabe könnte dann nur noch `already_active` ergeben). (c) Die **Einlöse-Texte sind produktneutral** — „der Strom-Monitor ist jetzt freigeschaltet" wäre nach einem Kalkulator-Code schlicht falsch gewesen.
  - **[VERIFIKATION — voller angemeldeter Flow lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack, echte Konten über die Auth-Admin-API, Playwright)]** (1) Anonym → **307 auf `/anmelden?next=%2Fpeak-shaving%2Fkalkulator%2Frechner`**, verstecktes Feld korrekt belegt, 0 iframes; nach dem Anmelden landet der Nutzer auf **der Zielroute, nicht auf `/konto`**, mit geladenem iframe. (2) Konto MIT Grant → iframe direkt, **0 Formularfelder**, kein Anfrage-Zustand. (3) Konto OHNE Grant → URL bleibt stehen, **0 iframes**, Überschrift „Zugang zum Kalkulator", „Angemeldet als …", CTA `/kontakt?thema=peakShaving` → Kontaktformular mit **vorbelegtem Thema `peakShaving`**. (4) `coolin2026`: auf der Route **0 Eingabefelder**; über `/konto` eingelöst → Monitor aktiv, Kalkulator weiter „Nicht freigeschaltet", Route weiter 0 iframes. (5)+(7) Kalkulator-Code eingelöst → Bestätigung „Ihr Code wurde eingelöst…", Abschnitt springt auf „Freigeschaltet", Monitor bleibt unberührt, und die Route liefert **ohne Neuanmeldung** den iframe. Ungated-Gegenprobe: `/peak-shaving/kalkulator`, `/peak-shaving`, `/branchen/handwerk` je **200**. **0 Konsolenfehler** über alle Läufe. **J1 gehalten: 0 Treffer für Supabase-Client-Code in `.next/static`.** `pnpm lint`/`typecheck`/`test`/`build` root-weit grün.
  - **[ERLEDIGT MIT B10-5]** ~~Eine Registrierung führt weiterhin fest auf `/konto` zurück (der Bestätigungslink trägt kein `next`) — ein `next` durch den Mail-Flow zu tragen wäre eine eigene Entscheidung.~~ Gebaut am 22.07.2026: das Rücksprungziel reist jetzt durch die ganze Kette bis hinter die Bestätigung (s. B10-5-Eintrag).
  - **[OFFEN — Betrieb, unverändert]** **In Produktion muss mindestens ein Konto einen `calculator_pro`-Grant bekommen**, sonst steht auch Andreas vor dem Anfrage-Zustand — über einen im Admin-Bereich ausgestellten Gutscheincode oder eine manuelle `entitlements`-Zeile.

- **[GEBAUT: B11 — Analyse-Bündel Fassung 2; der Admin-Upload nimmt Fassung 1 UND 2]** (21.07.2026). B11 ist ein KALKULATOR-Bauabschnitt (`packages/shared/src/tariff-catalog.ts` + `apps/website`, Handover in der Root-`CLAUDE.md`, fachliche Tiefe in `Pflichtenheft_Kalkulator_MVP.md` §7a.2). In `apps/web` berührt er **nur den Analyse-Upload** — keine Migration, keine neue Route, keine Oberflächenänderung.
  - **Was neu ankommt:** `inputs` eines Bündels kann jetzt `tariffSetId`/`tariffSetLabel`/`tariffSetValidFrom`/`tariffProfileKey`/`tariffOverriddenFields` tragen (Herkunft der Tarifsätze). `prepareAnalysisUpload` reicht `inputs` unverändert durch — es war schon vorher wortgleich, deshalb genügte die Fassungs-Freigabe in `shared`.
  - **[EINE FALLE, die zugeschlagen hat]** `lib/admin/analysis-upload.test.ts` pinnte `bundleVersion: 2` als den „unbekannte Fassung"-Fall. Mit B11 ist 2 gültig, der Test wurde rot — nachgezogen auf 3. **Wer die Fassung erneut erhöht, sucht diesen Test.** Die gültigen Fassungen stehen an EINER Stelle: `SUPPORTED_ANALYSIS_BUNDLE_VERSIONS` in `packages/shared/src/analysis-bundle.ts`.
  - **[WARUM FASSUNG 1 GÜLTIG BLEIBT]** Ein Bündel kann exportiert und noch nicht hochgeladen worden sein; es unbrauchbar zu machen wäre der schlechtere Handel. Bei Fassung 1 fehlen die neuen Felder schlicht — und **fehlend ist eine andere Aussage als leer**: ein leeres `tariffOverriddenFields` heisst „Vorgabewerte unverändert übernommen", ein fehlendes „kein Netzbetreiber gewählt". Deshalb wird beim Einlesen NICHTS ergänzt oder migriert (das wäre aus dem Einfrieren ein Umschreiben, B14-1). Zwei neue Tests decken beide Fassungen ab (116 → 118).

- **[GEBAUT: B14-2 — Analyse-Archiv im Admin-Bereich (Fahrplan B14; Anwendungsseite zur B14-1-Ablage)]** Der Admin-Bereich bekommt einen dritten Abschnitt neben Übersicht und Leads: **Analysen**. Der Kalkulator (`apps/website`) exportiert ein Bündel, hier wird es zusammen mit der Ursprungsdatei archiviert und angesehen. Neue Routen `app/admin/analysen/{page,neu/page,[id]/page,[id]/datei/route}.tsx|ts` + `lib/admin/{analyses,analysis-upload,analyses-actions}.ts` + `components/admin/analysis-upload-form.tsx` + Navigationspunkt. **KEINE neue Migration** — B14-1 (`…_create_analysis_persistence.sql`) hat die Ablage samt vier Wrappern bereits angelegt. **NICHT dabei:** Kalkulator ans Entitlement-System (B10), Tarifsätze als Konfigurationsschicht (B11), Zeitreihen-Speicher (B12), `tenant_id` (B13), Kampagnenversand (B2-3), Wirkungsnachweis-Auswertung (2027).
  - **[DIE GRUNDENTSCHEIDUNG: EXPORT + UPLOAD, KEIN SPEICHERN AUS DEM RECHNER]** Kalkulator und Admin-Bereich sind getrennte Anwendungen mit getrennten Sitzungen; ein Speichern aus dem Rechner heraus verlangte Auth in dieser Anwendung (B10) oder eine zweite Rechner-Oberfläche hier. Zudem ist die Archivierung einer betreuten Analyse eine bewusste Handlung und soll kein Nebeneffekt jedes Rechenlaufs sein — der öffentliche Rechner läuft täglich mit Probedaten. Der Rechner behält damit unverändert seine Zusage, dass Verbrauchsdaten den Browser nicht verlassen: das Bündel entsteht dort lokal.
  - **[ERSTER DATEI-UPLOAD DES SYSTEMS]** Vor B14-2 gab es in `apps/web` nirgends ein `type="file"` — weder im Admin-Bereich noch in einem öffentlichen Formular. Folgen, die man beim nächsten Upload wiederverwenden kann: es gibt **kein Datei-Primitiv** in `components/ui/` (das Feld steht lokal in `analysis-upload-form.tsx`), und Next begrenzt den Rumpf einer Server Action standardmässig auf **1 MB** — `experimental.serverActions.bodySizeLimit: '24mb'` in `next.config.mjs` ist neu und bewusst ETWAS grösser als die fachliche Obergrenze von 20 MB, damit die Anwendung ablehnt und nicht die Plattform (deren Fehler wäre undurchsichtig).
  - **[KEIN service_role, Erlaubnisliste unverändert]** Alle vier B14-1-Wrapper sind `authenticated`-only — ausdrücklich AUCH der schreibende (`admin_create_analysis` setzt `created_by = auth.uid()`; über `service_role` wäre die Urheberschaft einer Geschäftsunterlage strukturell leer). Upload-Action und Download-Route benutzen den ANGEMELDETEN Client; die `no-restricted-imports`-Allowlist in der root-`eslint.config.mjs` wurde NICHT angefasst. Dasselbe Muster wie die Lead-Ausfuhr (B2-1).
  - **[DIE PRÜFKETTE IST REIN UND LIEGT VOR DER DATENBANK]** `lib/admin/analysis-upload.ts` ist ohne `server-only`, ohne `next/*` und ohne Supabase-Client: Grösse → JSON → Bündel-Prüfung → **Prüfsummen-Vergleich** → gzip. Scheitert etwas, wird **kein Client erzeugt und kein RPC abgesetzt** — die Datenbank wird nicht befragt, statt abzulehnen. Das ist die Eigenschaft, die sich nur anwendungsseitig prüfen lässt, und genau so ist sie getestet (der Client ist ersetzt und zählt mit, ob er überhaupt entstanden ist).
  - **[DER WICHTIGSTE FEHLERFALL DER SEITE]** Bündel und Ursprungsdatei werden GETRENNT hochgeladen; was sie aneinanderbindet, ist allein die SHA-256 im Bündel. Passt sie nicht, wird die Analyse mit einer FREMDEN Datei archiviert — die Zeile sähe vollständig aus, und der Fehler fiele frühestens 2027 auf, wenn niemand mehr rekonstruieren kann, welche Datei die richtige war. Die Meldung nennt deshalb BEIDE Prüfsummen im Klartext. Die Datenbank prüft dieselbe Sache ein zweites Mal (sie RECHNET sie über die übergebene Datei, B14-1) — keine Verdopplung: sie ist die harte Grenze, hier entsteht der Satz, den ein Mensch versteht, bevor 20 MB durch eine Server Action gehen.
  - **[GEMESSEN, nicht angenommen] `bytea` geht als Hex-Zeichenkette (`\x…`) durch PostgREST** — base64 ist kein `bytea`-Eingabeformat und bräuchte ein `decode()` IM Wrapper, der B14-1 ist und unangetastet bleibt. Gegen den lokalen Stack mit echtem Admin-Login geprüft: Anlegen läuft, die DB rechnet die Prüfsumme selbst, eine falsche wird mit 22023 abgewiesen, und der Rundlauf über `admin_get_analysis_source` liefert die Datei bit-identisch zurück.
  - **[SICHTBARER HINWEIS AN DER LEAD-ZUORDNUNG]** Sie ist nachträglich NICHT änderbar, weil `platform.analyses` append-only ist und es dafür keinen Wrapper gibt — ein passender Lead ist vorher anzulegen (mit Verweis auf die Lead-Liste). Der Hinweis steht VOR der Auswahl, nicht darunter. Die Auswahl selbst nutzt die bestehende Freitextsuche aus `admin_list_leads` als EIGENES GET-Formular (verschachtelte Formulare gibt es in HTML nicht) — mit dem ausdrücklichen Satz „zuerst suchen, dann die Dateien wählen", weil die Suche die Seite neu lädt und die Dateiauswahl dabei verlorengeht.
  - **[DIE ANSICHT BAUT NICHTS NACH]** Kein Chart, keine Neuberechnung, kein nachgebauter Report — `inputs` und `result` stehen als aufklappbarer Rohtext (`<details>` + `<pre>`, ohne Client-Zustand). Eine nachgebaute Report-Ansicht wäre eine zweite Darstellung derselben Zahlen, die mit jeder Rechner-Änderung auseinanderliefe; eine Neuberechnung wäre genau der Weg, auf dem eine eingefrorene Baseline mit einem Aufruf verschwindet (B14-1, „Was es hier bewusst nicht gibt"). Die Ursprungsdatei fliesst über eine EIGENE Route (`[id]/datei`), die entpackt UND gegen die gespeicherte Prüfsumme prüft — weicht sie ab, kommt ein Fehler und keine Datei.
  - **[OFFENGELEGTE GRENZE, nicht still] Die Kennzeichnung „ersetzt" prüft die neuesten 200 Analysen.** `admin_list_analyses` liefert je Zeile nur `supersedes_id` (die Vorgängerin); die Gegenrichtung hat in B14-1 bewusst keinen Wrapper, und 200 ist die Obergrenze, die der Wrapper zulässt. Eine Nachfolgerin entsteht immer später als ihre Vorgängerin, das Fenster trifft also zu — liegt der Bestand darüber, **sagt die Oberfläche das im Klartext** (eine stille Obergrenze liest sich wie Vollständigkeit).
  - **[VERIFIKATION — anwendungsseitig 116/116]** (91 bestehende + **25 neue**): unbekannte `bundleVersion`, Platzhalter-Commit, nicht zusammenpassende Prüfsummen und eine Datei über der Obergrenze werden je abgewiesen, **ohne dass ein Supabase-Client entsteht** · der Gutfall setzt GENAU EINEN RPC ab, dessen fünf Auszüge mit `result` übereinstimmen · ohne Sitzung legt der Upload nichts an und die Download-Route antwortet 307 auf `/anmelden` OHNE RPC · ohne Adminrolle 403 statt leerer Datei · **der vollständige Rundlauf Export → Upload → Download liefert die Datei byte-identisch** (Fixture mit Umlauten und CRLF — daran scheitert jede still dekodierende Zwischenstufe) · ein Archiv mit abweichender Prüfsumme liefert NICHTS und benennt den Befund.
  - **[MONITOR-SYNCHRONISATION AUSSER KRAFT]** Die in der Root-`CLAUDE.md` festgehaltene Pflicht, `platform`-Änderungen mit `Pflichtenheft_Monitor_MVP.md` §15 und `packages/tariff-monitor/CLAUDE.md` synchron zu halten, ist mit B14-2 **ausser Kraft gesetzt, nicht erfüllt**. Die Regel entstand, als der Monitor das aktive Produkt war; seit 20.07.2026 sind beide Dokumente ruhend gestellt und ausdrücklich nur historisch zu lesen. Neue Schema-Einträge dort nachzutragen machte genau diese Kennzeichnung falsch. Maßgeblich sind ab sofort die Root-`CLAUDE.md` und die Handover-Logs der aktiven Apps — also unter anderem diese Datei.

- **[GEBAUT: Kontaktname aufgetrennt — Vorname und Nachname statt `contact_name`]** Ein brechender Spaltenwechsel quer durch den gesamten Lead-Pfad, damit künftige Korrespondenz (u. a. der Rechnungs-Wächter, Fahrplan B9) eine korrekte Anrede bilden kann. Migration `supabase/migrations/20260724090000_split_contact_name.sql` (zwei Spalten + Backfill + DROP von `contact_name`, `guard_anonymized_lead`/`platform.anonymize_lead`/`capture_lead`/`admin_update_lead`/`admin_list_leads`/`admin_export_leads`/`admin_get_lead` nachgezogen) + Kontaktformular, Registry, CSV-Export und Admin-Bearbeitungsformular. **NICHT dabei:** die Anrede selbst (die entsteht erst mit dem Kanal, der sie braucht), Titel/Anredeform als eigene Felder, `tenant_id` (B13).
  - **[DIE ENTSCHEIDUNG: AN DER QUELLE TRENNEN, NICHT SPÄTER ABLEITEN]** Jede Zerlegung eines Freitextnamens ist eine Heuristik, und sie scheitert genau dort, wo es auffällt: bei Doppelnamen („Anna Maria Gruber"), Namenszusätzen („von der Leyen"), Titeln („Dr. Max Muster"), umgekehrter Schreibweise („Muster, Max"). Ein falsch geratener Nachname landet anschliessend in der ANREDE einer echten E-Mail — der eine Ort, an dem der Fehler garantiert bemerkt wird, und zwar von der betroffenen Person. Deshalb fragt das Kontaktformular zwei Felder, und beide Werte reisen getrennt bis in die Datenbank.
  - **[BACKFILL UND DROP IN EINEM SCHRITT — begründet, nicht abgekürzt]** Ein brechender Spaltenwechsel verlangt normalerweise einen zweistufigen Rollout. Der Grund entfällt hier: der reale Bestand enthält keine echten Leads mit diesem Feld (die Testfälle sind bereits anonymisiert, und `anonymize_lead` nullt `contact_name` seit B1-3). Der Backfill steht trotzdem in der Migration — sie darf nicht davon ausgehen, dass die Datenlage bei ihrem Lauf noch dieselbe ist wie beim Schreiben. **Zerlegungsregel:** bis zum ERSTEN Leerzeichen → Vorname, der Rest → Nachname; **ein Einzelwort wird zum NACHNAMEN**, nicht zum Vornamen (wer ein Wort einträgt, schreibt in aller Regel einen Nach- oder Firmennamen; als Vorname geführt fehlte er später in der Anrede).
  - **[ZWEI SIGNATUREN BRECHEND GEÄNDERT, DREI WEITERE WRAPPER MITGEZOGEN]** `capture_lead` und `admin_update_lead` per **DROP + CREATE** (Muster B3-1: `create or replace` kann die Parameterliste nicht umbauen, und ein blosses CREATE erzeugte eine zweite, mehrdeutige Überladung). Die zwei neuen Parameter stehen an **derselben Stelle** wie das frühere `p_contact_name` — ein Anhängen ans Ende wäre bequemer gewesen und hätte die Lesereihenfolge der Erfassung zerrissen. Aus den NEUN bearbeitbaren Feldern von B2-1 werden damit ZEHN. **`admin_list_leads`, `admin_export_leads` und `admin_get_lead` standen NICHT in der Aufgabenstellung** und sind trotzdem Teil der Migration: sie lasen `ld.contact_name` und wären nach dem DROP beim ERSTEN Aufruf gebrochen, nicht beim Anwenden der Migration (dieselbe plpgsql-Eigenschaft, die in B3-4 einen Fehler bis zum ersten Aufruf verborgen hat).
  - **[ZUSAMMENFÜHRUNGSSEMANTIK: IDENTITÄTSFELD, NICHT SEGMENTIERUNG]** `first_name`/`last_name` folgen `coalesce(Bestand, neu)` wie `company`/`phone` — **nicht** der B3-1-Regel für Branche/PLZ/Verbrauch/Messart/Versorger/Vertragsende (`coalesce(neu, Bestand)`). Ein Name ändert sich selten und dann bewusst; beim zweiten Mal wird weniger sorgfältig getippt. Die beiden Felder werden **einzeln** zusammengeführt: ein Einstiegspunkt, der nur den Nachnamen erhebt, kann ihn ergänzen, ohne dass ein fehlender Vorname etwas bewirkt. Leerstring-Normalisierung (`nullif(btrim(…),'')`) auf beide — sonst überlebte ein leer abgesendetes Feld als `''` jedes COALESCE und verdrängte eine echte Angabe (die Falle ist in B3-1 schon einmal beschrieben).
  - **[KONTAKTFORMULAR: BEIDE FELDER PFLICHT — bewusste Abweichung]** Als einziger Einstiegspunkt. Auf eine Kontaktanfrage folgt eine Antwort per E-Mail, und die beginnt mit einer Anrede; ein fehlender Nachname hiesse dort „Guten Tag," ohne Namen, obwohl die Person gerade um Kontakt gebeten hat. Überall sonst (Registry-Einträge `fachvortrag`/`direktkontakt`, Admin-Korrekturformular) bleiben beide optional — der Bestand enthält Leads aus Einstiegspunkten, die gar keinen Namen erheben, und ein Pflichtfeld machte jede andere Korrektur an so einem Lead unmöglich. `autoComplete` ist auf `given-name`/`family-name` gesetzt: der Browser füllt zwei Felder nur dann richtig vor, wenn er weiss, welcher Teil wohin gehört. `min(1)` statt des früheren `min(2)` — es gibt einbuchstabige Vornamen, und ein abgelehnter echter Name kostet mehr als ein zu kurz getippter.
  - **[DIE MAIL FÜGT ZUSAMMEN, DER BESTAND NICHT]** `lib/kontakt/deliver.ts` zeigt „Vorname Nachname" in EINER Zeile (`fullName`) und im Betreff — zwei Zeilen untereinander sind für den menschlichen Empfänger keine Information, sondern ein Umweg. Die Lead-Erfassung übergibt beide Werte GETRENNT an `capture_lead`; die Zusammenführung ist reine Darstellung und ändert nichts an den gespeicherten Werten.
  - **[CSV-EXPORT: ZWEI SPALTEN, NICHT EINE]** Aus „Ansprechperson" werden „Vorname" und „Nachname". Der Grund für die Auftrennung — korrekte Anrede, Wiederverwendbarkeit in einem Serienbrief — gilt für die ausgeführte Datei genauso wie für die Anzeige; sie beim Ausführen wieder zu verkleben, gäbe den Zweck genau dort auf, wo er am ehesten gebraucht wird.
  - **[GUARD ERWEITERT]** `guard_anonymized_lead` schützt jetzt **16 statt 15 Spalten** — aus einer geschützten wurden zwei, und beide stehen EINZELN in der Liste: ein Schutz nur auf den Nachnamen liesse den Vornamen frei änderbar, und ein Vorname ist genauso ein Identitätsmerkmal wie der Rest. Die B2-1-Asymmetrie bei `last_edited_by` (nur das Setzen gesperrt, das Nullen läuft durch, weil `ON DELETE SET NULL` selbst ein UPDATE ist) bleibt unverändert. `platform.anonymize_lead` nullt beide Felder statt `contact_name`.
  - **[GRANTS NACH DEM DROP ERNEUT GESETZT]** Ein DROP entfernt bestehende Grants (in B3-1 einmal ausdrücklich geprüft): `capture_lead` wieder **service_role-only**, `admin_update_lead` wieder **authenticated-only** — beides im DB-Gate nachgemessen, nicht vorausgesetzt.
  - **[VERIFIKATION — DB-Gate 291/291]** (274 bestehende + **17 neue** in `packages/db-tests/src/lead-name-split.test.ts`): `contact_name` existiert nicht mehr, `first_name`/`last_name` schon · `capture_lead` hat GENAU EINE Überladung, kein `p_contact_name`, und die zwei Parameter stehen nachweislich zwischen `p_company` und `p_phone` · Grant-Fläche exakt service_role bzw. authenticated · **ein zweiter Aufruf mit ANDEREM Namen überschreibt den zuerst erfassten NICHT** · die zwei Felder werden einzeln zusammengeführt (nur-Nachname zuerst, Vorname später → beide stehen, der Nachname unverändert) · ein Aufruf ohne Namen löscht nichts · Leerstrings und reine Leerzeichen werden zu null und überschreiben nichts · umgebende Leerzeichen fallen weg, ein Vorname DARF ein Leerzeichen enthalten („Anna Maria") · `anonymize_lead` nullt beide · ein anonymisierter Lead lässt beide Felder für **service_role UND postgres** nicht mehr setzen · `admin_update_lead` ändert beide und setzt `last_edited_by`, und „leer heisst löschen" gilt JE FELD EINZELN · die Backfill-Regel an fünf Beispielwerten mit und ohne Leerzeichen nachgerechnet (u. a. „Muster" → Vorname null / Nachname „Muster") · `admin_get_lead`, `admin_list_leads` und `admin_export_leads` werden ECHT aufgerufen und liefern beide Felder ohne `contact_name`. Die drei bestehenden Gates wurden auf die neue Signatur UMGESCHRIEBEN statt gelöscht — insbesondere der **positionale** `capture_lead`-Aufruf in `lead-capture-wrappers.test.ts`, der damit selbst der Test auf die Parameterreihenfolge ist (ein verrutschter Parameter schriebe sonst die Telefonnummer in den Nachnamen, und zwar ohne Fehler).
  - **[VERIFIKATION — anwendungsseitig 91/91, `pnpm lint`/`typecheck`/`build`/`test` root-weit grün]** `supabase/database.types.ts` neu generiert.
  - **[VERIFIKATION — voller Absendeweg lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack, Resend über einen Capture-Server via `RESEND_BASE_URL`)]** `/kontakt` liefert im Markup `name="vorname"` und `name="nachname"` mit `autocomplete="given-name"`/`"family-name"` und **beide mit `required`**, Labels „Vorname *"/„Nachname *"; ein `name="name"`-Feld gibt es nicht mehr. Absendung mit „Anna Maria" / „von der Gruber": HTTP 200, Lead in der Datenbank mit `first_name='Anna Maria'` und `last_name='von der Gruber'` **getrennt**, dazu Firma/Telefon/`first_source_key='kontaktformular'` und eine `pending`-Marketing-Einwilligung. Die interne Mail zeigt genau EINE Zeile `Name: Anna Maria von der Gruber` (keine getrennten Zeilen) und trägt denselben zusammengeführten Namen im Betreff. Leerer Nachname → **400 `{fieldErrors:{nachname:'nachnameRequired'}}`** und **0 Leads**.

- **[GEBAUT: B2-2 — Rückläufer und Beschwerden: Resend-Webhook auf die Sperrliste (Fahrplan B2)]** Der Zustellrand des Systems: erstmals wirkt etwas, das ein FREMDER Server meldet, unumkehrbar auf den Bestand. Migration `supabase/migrations/20260723120000_create_email_events_ledger.sql` (Tabelle `platform.email_events` + Append-only-Trigger, `platform.strip_emails`/`is_permanent_bounce`, `public.record_email_event` + `admin_list_email_events` + `admin_email_event_stats`, `admin_get_lead` um `suppression_reason` nachgezogen) + `apps/web/app/api/resend/webhook/route.ts` + Admin-Sichtbarkeit auf beiden Lead-Seiten + `DEPLOYMENT.md` §1h/§2-Resend. **NICHT dabei:** Kampagnenmodell, Empfängerliste, gestaffelter Versand, Zustellprotokoll je Kampagne (alles B2-3), Betroffenheits-Check (B3-3), `tenant_id` (B13). **Diese Aufgabe versendet nichts.**
  - **[TEIL 0 — DIE SCHNITTSTELLE WURDE ERMITTELT, NICHT ANGENOMMEN]** Gegen die offizielle Resend-Doku UND die Typdefinitionen des installierten SDK (`resend@6.17.2`) geprüft. **Ereignisarten:** `email.sent · email.scheduled · email.delivered · email.delivery_delayed · email.complained · email.bounced · email.opened · email.clicked · email.received · email.failed · email.suppressed` plus `contact.*`/`domain.*`. **Dauerhaft vs. vorübergehend:** ein harter Rückläufer kommt als `email.bounced` (Doku wörtlich: „permanently rejected"), ein weicher als `email.delivery_delayed` — und der trägt **gar kein `bounce`-Objekt**. Die Feinklassifikation steht in `data.bounce.type` ∈ `Permanent | Transient | Undetermined`, `data.bounce.subType` ∈ `General · NoEmail · Suppressed · MailboxFull · MessageTooLarge · ContentRejected · AttachmentRejected · Undetermined`. **Signatur:** Standard Webhooks (ehemals Svix) — Kopfzeilen `svix-id`/`svix-timestamp`/`svix-signature`, HMAC-SHA256 über `${id}.${timestamp}.${body}`, Geheimnis mit Präfix `whsec_`, 5-Minuten-Toleranz. **Die Ereigniskennung ist die `svix-id`-KOPFZEILE** — die Nutzlast trägt keine; `data.email_id` identifiziert die MAIL, und mehrere Ereignisse derselben Mail teilen sie sich (als Idempotenzschlüssel würde er die Zustellung einer Mail und ihren späteren Rückläufer als Duplikat verwerfen). **Verfolgung:** Öffnung/Klick sind je DOMAIN konfigurierbar (Dashboard → Domains → Configuration, oder `PATCH /domains/:id` mit `open_tracking`/`click_tracking`) und **standardmässig AUS**.
  - **[DIE EINE ENTSCHEIDUNG]** Eine **Beschwerde** ist eine Willenserklärung, ein **Rückläufer** ein technisches Ereignis. Beide sperren, aber nur die Beschwerde widerruft zusätzlich alle Einwilligungen — wer „Spam" drückt, hat die Erlaubnis zurückgenommen; wessen Postfach gelöscht wurde, hat gar nichts erklärt, und dessen Einwilligung als widerrufen zu führen wäre eine erfundene Handlung (dieselbe Fälschung, die `guard_consent_confirmation` in der Gegenrichtung verhindert). Praktische Folge: wird die Adresse später wieder erreichbar, ist das der Unterschied zwischen „muss neu einwilligen" und „war nie weg".
  - **[KEINE ROHNUTZLAST — der Grund ist eine Invariante, keine Sparsamkeit]** Naheliegend wäre `payload jsonb` wie bei `platform.stripe_events`. Falsch: die Resend-Nutzlast enthält die Empfängeradresse im Klartext (`data.to`) und wäre eine ZWEITE, vom Lead unabhängige Kopie personenbezogener Daten — bei einer Anonymisierung eigens mitzulöschen, und die dafür nötige Ausnahme würde genau den Append-only-Charakter aufweichen, der den Ledger zum Idempotenz-Anker macht. Benannte Spalten halten die Invariante ausnahmslos. Der Anbieter-Freitext (`bounce.message`) wird beim **Schreiben** von Adressen bereinigt (`platform.strip_emails`), nicht beim Anzeigen: was nie in der Tabelle steht, muss nie vergessen werden. **Bewusst breiter als „die bekannte Adresse":** jedes adressförmige Token fliegt raus — eine Bounce-Meldung kann eine abweichende Schreibweise, einen Alias oder die Postmaster-Adresse enthalten, und ein gezielter Abgleich liesse genau die stehen (im DB-Gate mit zwei Adressen in einem Satz gepinnt).
  - **[GEFUNDEN IM ERSTEN TESTLAUF — die eine begründete Ausnahme vom Append-only]** `lead_id` trägt `ON DELETE SET NULL`, und **diese referentielle Aktion IST ein UPDATE** auf die Ledger-Zeile. Ein ausnahmsloser Trigger machte damit jeden Lead, zu dem je ein Zustellereignis einging, UNLÖSCHBAR — ausgerechnet die Person, an die tatsächlich etwas versendet wurde, und ausgerechnet gegen ein Löschverlangen. Strukturell derselbe Fall wie `last_edited_by` in `guard_anonymized_lead` (B2-1), gleiche Antwort: erlaubt ist AUSSCHLIESSLICH das Nullen von `lead_id` bei sonst bit-identischer Zeile (`to_jsonb`-Vergleich ohne dieses Feld). Setzen und Umhängen bleiben gesperrt. **Die Zusage gilt dem INHALT des Ereignisses, nicht dem Verweis auf einen Lead** — drei DB-Tests pinnen beide Richtungen samt echtem Lead-Löschen.
  - **[EINE DEFINITION VON „DAUERHAFT"]** `platform.is_permanent_bounce(event_type, bounce_type)` — benutzt von der WIRKUNG (`record_email_event`) UND von der AUSWERTUNG (`admin_email_event_stats`) UND von der Anzeige (fährt in `admin_list_email_events` mit). Zwei eigene Bedingungen wären zwei Auslegungen desselben Begriffs, und die Abweichung fiele erst an einer Statistik auf, die andere Zahlen zeigt als die Wirkung, die sie beschreibt (Muster `leads_matching`/B2-1, `leads_due_for_contract_reminder`/B4-1). **Regel:** `email.bounced` UND (`bounce.type` fehlt ODER lautet `Permanent`). Fehlt der Typ, gilt dauerhaft — die Ereignisart selbst IST die Aussage „dauerhaft abgelehnt". Steht dort ausdrücklich `Transient` oder `Undetermined`, gilt sie NICHT: **die Abwägung ist asymmetrisch** — eine zu Unrecht gesperrte Adresse ist ohne Weg zurück verloren, eine zu spät gesperrte erzeugt beim nächsten harten Rückläufer die richtige Sperre.
  - **[REIHENFOLGE IST BEDEUTUNG] Bei der Beschwerde entsteht der Sperreintrag ZUERST mit `reason='complaint'`,** danach übernimmt `public.suppress_email_and_withdraw_all` (B1-2) den Widerruf — dessen eigener Sperr-Insert läuft dann in den Konflikt und tut nichts. Umgekehrt stünde in der Sperrliste `unsubscribed`, und der Grund für die schärfste Wirkung des Systems wäre falsch protokolliert. Der Widerruf läuft trotzdem über den BESTEHENDEN Pfad — „alle offenen und bestätigten Zeilen jedes Zwecks" ist dort definiert, und zwei Definitionen von „widerrufen" fielen erst beim ersten Massenversand auf. **Der Sperreintrag ist idempotent: der zuerst festgestellte Grund bleibt stehen** (ein DB-Test pinnt, dass ein späterer Rückläufer eine `unsubscribed`-Sperre nicht überschreibt — die Selbstabmeldung ist die stärkere Aussage).
  - **[DER WEBHOOK LEGT NIEMALS EINEN LEAD AN]** Ist die Adresse unbekannt, entstehen Ledger-Zeile und Sperreintrag über den HASHWERT, mehr nicht. Ein Lead ist ein Kontakt, den jemand hinterlassen hat; aus einem Zustellfehler einen zu erzeugen hiesse, einen Bestand aus Adressen aufzubauen, die uns nie jemand gegeben hat. **Genau für diesen Fall ist `platform.email_suppressions` seit B1-1 ohne Fremdschlüssel gebaut.**
  - **[`standardwebhooks` DIREKT STATT `resend.webhooks.verify` — begründete Abweichung]** Beides ist DASSELBE Verfahren: das SDK ruft intern `new Webhook(secret).verify(...)` aus genau dieser Bibliothek (in `node_modules/resend/dist/index.mjs` nachgesehen, Version 1.0.0 als SDK-Abhängigkeit; jetzt als direkte Abhängigkeit von `apps/web` in derselben Version gepinnt). Der Umweg über das SDK verlangt aber `new Resend(apiKey)`, und dessen Konstruktor **wirft ohne API-Key** (gemessen: „Missing API key"). Die Signaturprüfung hinge damit an `RESEND_API_KEY` — einem Geheimnis, das mit ihr nichts zu tun hat: fehlte nur der API-Key, antwortete der Endpunkt 500 statt der vorgesehenen glatten 400.
  - **[`resendWebhookSecretOrNull` STATT `requireResendWebhookSecret` — bewusst, mit Grund]** Die Aufgabenstellung nannte „require-on-use"; umgesetzt ist die Prüfung an der Verwendungsstelle (das Schema hält den Wert optional, damit ein Build ohne ihn durchläuft), aber mit der Form `string | null` wie `cronSecretOrNull` (B4-1). Ein geworfener Fehler käme beim Anbieter als **500** an und löste einen Wiederholungssturm für etwas aus, das keine Wiederholung behebt — gefordert ist eine glatte **400**.
  - **[FAIL-CLOSED, DREIMAL DIESELBE ANTWORT]** Fehlende Kopfzeile, ungültige Signatur **und fehlendes `RESEND_WEBHOOK_SECRET`** → je 400 ohne jeden Datenbankzugriff. Der dritte Fall ist der wichtige: „es ist keins konfiguriert, also nehme ich alles an" machte die dauerhafte Sperre beliebiger Adressen zu einer offenen Schnittstelle. Unbekannte Ereignisarten → 200 ignoriert (Resend wiederholt jede Nicht-2xx-Antwort; ein 500 für etwas, das sich nie ändert, wäre ein Wiederholungssturm), Verarbeitungsfehler → 500 (ein verlorener Rückläufer hiesse: eine tote Adresse bleibt im Verteiler), Duplikat → 200. **Die Adresse steht bewusst nicht im Log.**
  - **[ZWEITE VERTEIDIGUNGSLINIE GEGEN VERFOLGUNG]** `email.opened`/`email.clicked` stehen NICHT in der Aufnahmeliste und werden verworfen, **auch wenn sie ankämen** — sie sind weder abonniert noch aktiviert, aber falls beides einmal anders steht, landen sie trotzdem nicht in der Datenbank. Zwei anwendungsseitige Tests pinnen das. `email.failed`/`email.suppressed` sind bewusst ausgelassen: beides sind Aussagen über UNSEREN Versandpfad, nicht Rückmeldungen des Empfängers — sie gehören zum Zustellprotokoll je Kampagne (B2-3).
  - **[KEIN WEG, EINE SPERRE AUFZUHEBEN — und kein Wrapper dafür]** Entsperren ist der Sache nach Erteilen, und die Regel aus B1-3 lautet: der Admin kann widerrufen, nie erteilen. Eine Schaltfläche „doch wieder zustellen" wäre der Weg, auf dem eine Beschwerde — die schärfste Rückmeldung, die eine Person geben kann — mit einem Klick verschwindet. Ein begründeter Einzelfall bleibt ein bewusster Eingriff in der Datenbank. Steht als Satz auf der Detailseite, nicht nur im Code.
  - **[ADMIN: DIE FRÜHWARNUNG STEHT DORT, WO OHNEHIN JEDER HINSIEHT]** `/admin/leads` zeigt dauerhafte Rückläufer und Beschwerden der letzten 30 Tage und **hebt hervor, sobald ÜBERHAUPT eine Beschwerde auftritt** — die erste ist der Zeitpunkt zu handeln, nicht die zehnte. Eine steigende Beschwerdequote ist die einzige Frühwarnung vor einem Reputationsschaden der Absenderdomain, und niemand sucht von sich aus danach. `permanent_bounces` ist bewusst **nicht** die Zahl der `email.bounced`-Zeilen (darunter fallen auch vorübergehende) — beide Zahlen stehen nebeneinander, und der VERWENDETE Zeitraum fährt in der Antwort mit, damit die Oberfläche keinen anderen behaupten kann (Muster `stale_after_hours`, B4-2). `/admin/leads/[id]` zeigt die Ereignisse des Leads mit Art, Zeitpunkt, Anbieter-Einstufung und Grund; bei Sperre wird der Grund benannt (abgemeldet · Rückläufer · Beschwerde · manuell gesperrt) — mit B2-2 gibt es drei Wege auf die Liste, und sie bedeuten Verschiedenes.
  - **[GEFUNDEN IM UI-LAUF, BEHOBEN]** Der Hinweis „vorübergehend — sperrt bewusst nicht" hing zunächst nur an `email.bounced` mit nicht-dauerhafter Einstufung. Bei Resend ist der REGELFALL für einen weichen Rückläufer aber `email.delivery_delayed` — dort stand nur „verzögert", und die Frage, die man sich beim Lesen stellt (*hat das jetzt gesperrt?*), blieb unbeantwortet. Jetzt tragen beide Formen den Hinweis.
  - **[VERIFIKATION — DB-Gate 274/274]** (254 bestehende + **20 neue** in `packages/db-tests/src/email-events.test.ts`): Beschwerde sperrt mit `complaint` und widerruft **beide** Zwecke (auch den offenen) · dauerhafter Rückläufer sperrt mit `bounced` und lässt `confirmed` stehen · `email.bounced` ohne `bounce.type` gilt als dauerhaft · `Transient`/`MailboxFull` und `delivery_delayed` sperren NICHT, werden aber protokolliert · Zustellung/Versand sperren nicht · dieselbe Kennung zweimal → `duplicate`, **und eine zwischenzeitlich neu erteilte Einwilligung bleibt unangetastet** (die Wirkung läuft nachweislich nicht ein zweites Mal) · unbekannte Adresse legt keinen Lead an, erzeugt aber Ledger- und Sperreintrag mit `lead_id = null` · eine `unsubscribed`-Sperre wird nicht überschrieben · der Ledger ist in ZWEI unabhängigen Schichten dicht (`service_role` scheitert am fehlenden Tabellenrecht, `postgres` am Trigger) · die eine Ausnahme ist eng (Umhängen und „nullen plus etwas anderes ändern" scheitern, echtes Lead-Löschen läuft durch und der Inhalt überlebt) · beide Adressen im Freitext sind entfernt · Grant-Fläche exakt `service_role` bzw. `authenticated`, `anon` nirgends, Tabelle ohne jedes Grant + RLS, die `platform`-Helfer von aussen gar nicht aufrufbar · Nicht-Admin scheitert an BEIDEN Lese-Wrappern mit **42501** · beide Lese-Wrapper und das ersetzte `admin_get_lead` werden ECHT aufgerufen (Delta-Messung, weil der Ledger append-only ist) · **die Sperre wirkt nachweislich auf die bestehenden Pfade**: ein dauerhaft zurückgelaufener Lead fällt aus `leads_due_for_contract_reminder` (B4-2) heraus, OBWOHL seine Einwilligung `confirmed` bleibt, und aus `admin_export_leads` (B2-1).
  - **[VERIFIKATION — anwendungsseitig 91/91]** (80 bestehende + **11 neue** in `app/api/resend/webhook/route.test.ts`, Nutzlasten mit derselben Bibliothek signiert, die der Handler prüft — kein nachgebautes HMAC): ungültige Signatur / fehlende Kopfzeile / fehlendes Geheimnis / **nach dem Signieren manipulierte Nutzlast** je 400, und der service_role-Client wird nachweislich NIE erzeugt · unbekannte Art, `email.opened`, `email.clicked` je 200 ohne einen einzigen RPC-Aufruf · Ereignis ohne Empfänger → 200 (eine Wiederholung änderte nichts) · die Abbildung auf die sieben Wrapper-Parameter ist exakt gepinnt, **inklusive: die Kennung stammt aus der KOPFZEILE** · fehlende Optionalfelder kommen als `undefined` (⇒ SQL-Default NULL) · Duplikat → 200 · RPC-Fehler → 500.
  - **[VERIFIKATION — voller Weg lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack, selbst signierte Nutzlasten)]** Rückläufer: `200 {effect:"suppressed"}`, Sperre `bounced`, Einwilligung bleibt `confirmed`, gespeicherter Freitext `no mailbox for [Adresse entfernt]` · Beschwerde: `suppressed_and_withdrawn`, Sperre `complaint`, Einwilligung `withdrawn` · `delivery_delayed`: `effect:"none"`, keine Sperre, aber Ledger-Zeile · Duplikat: zweiter Aufruf `duplicate`, **1** Ledger-Zeile · ungültige Signatur 400 bei **unverändertem** Ledger · fehlende Kopfzeile 400 · manipulierte Nutzlast 400 · `domain.updated`/`opened`/`clicked` je 200 ignoriert · unbekannte Adresse: **0 neue Leads**, Sperre über den Hash · GET auf den Endpunkt **405**, erfundener Pfad unter `/api/resend` **404** · zweiter Server OHNE gesetztes Geheimnis: **400 „Webhook secret not configured"**.
  - **[VERIFIKATION — Admin-Oberfläche per Playwright, echter Login, Production-Build]** Anonym `/admin/leads` → Umleitung auf `/anmelden`. Angemeldet: Panel „Rückläufer und Beschwerden — letzte 30 Tage" mit **16 / 9**, deckungsgleich mit einer direkten SQL-Gegenrechnung; Hervorhebung aktiv, Warnsatz sichtbar; Zeile „Alle Ereignisse im Zeitraum: Rückläufer 20 · Beschwerde 9 · zugestellt 9 · verzögert 3 · versendet 2" (20 Rückläufer, davon 16 dauerhaft — genau die Trennung, die die Zahl trägt). Detailseite Rückläufer-Lead: „gesperrt (Rückläufer)", Ereigniszeile mit „dauerhaft — hat die Adresse gesperrt", Anbieter-Einstufung „Permanent / General", Freitext **ohne `@`**; Beschwerde-Lead: „gesperrt (Beschwerde)" + „hat gesperrt und alle Einwilligungen widerrufen"; nur-verzögert-Lead: „nicht gesperrt" + „vorübergehend — sperrt bewusst nicht". **0 Entsperren-Schaltflächen, 0 Konsolenfehler.** `pnpm lint`/`typecheck`/`test`/`build` root-weit grün, `supabase/database.types.ts` neu generiert, Migration auf die Cloud-DB gepusht und dort introspektiv gegengeprüft.
  - **[OFFEN — betrifft den Betrieb, nicht den Code] Der Endpunkt ist in Produktion NICHT scharf geschaltet.** Nötig: Webhook bei Resend registrieren (`https://coolin.at/api/resend/webhook`, genau die fünf Ereignisarten `email.sent · email.delivered · email.delivery_delayed · email.bounced · email.complained`), das dort erzeugte Signing Secret als `RESEND_WEBHOOK_SECRET` in Vercel setzen, Redeploy. Vollständig in `DEPLOYMENT.md` §1h.
  - **[OFFEN — NICHT PRÜFBAR VON HIER AUS, ausdrücklich gemeldet] Der Ist-Zustand der Öffnungs-/Klick-Verfolgung liess sich nicht messen.** `RESEND_API_KEY` ist in Vercel als **„sensitive"** hinterlegt und damit weder über die API noch über `vercel env pull` auslesbar (das ist der Zweck dieses Variablentyps), und lokal liegt kein Schlüssel. Belegt ist aus der Anbieter-Doku, dass beides **standardmässig deaktiviert** ist und aktiv eingeschaltet werden muss — der erwartete Zustand ist also „aus". `DEPLOYMENT.md` §2-Resend-a trägt den Prüf- und den Abschaltbefehl (`GET`/`PATCH /domains`) samt ausgeschriebener Begründung, damit der Weg bei einem Kontowechsel nicht verlorengeht. **Zu tun: den `curl` einmal mit dem echten Schlüssel laufen lassen und das Ergebnis festhalten.**

- **[GEBAUT: B2-1 — Bestand bearbeitbar machen: Segmentierungsfilter, Korrekturweg, Export (Fahrplan B2)]** Der erste Weg, auf dem personenbezogene Daten dieses Systems seinen Wirkungsbereich DAUERHAFT verlassen — und der erste, auf dem ein Mensch die Angaben einer anderen Person von Hand überschreibt. Migration `supabase/migrations/20260723090000_create_lead_editing_filters_export.sql` (Spalte `leads.last_edited_by`, Guard nachgezogen, `public.admin_update_lead`, `platform.leads_matching`/`lead_filter_summary`/`marketing_consent_state`, `admin_list_leads` um sieben Filter erweitert, Tabelle `platform.admin_exports` + `public.admin_export_leads`/`admin_list_exports`, `admin_get_lead` nachgezogen) + `apps/web/app/admin/leads/export/route.ts` + `lib/admin/{lead-filters,csv,format}.ts` + `components/admin/lead-edit-form.tsx` + neue Seite `/admin/leads/exporte`. **NICHT dabei:** Kampagnenversand, Zustellprotokoll je Kampagne, Rückläufer-/Beschwerdeverarbeitung (alles B2-2), Betroffenheits-Check (B3-3, blockiert auf die Branchenkennzahlen), `tenant_id` (B13). **Diese Aufgabe versendet nichts.**
  - **[NEUE ARBEITSREGEL, ab hier verbindlich] Jeder neue oder geänderte Wrapper wird im DB-Gate tatsächlich AUFGERUFEN.** Introspektion beweist nur, dass eine Funktion existiert; plpgsql prüft Funktionsrümpfe nicht beim Anlegen — in B3-4 lief eine Migration sauber durch und wäre erst beim ersten Aufruf an einem fehlenden Join gescheitert. Zusätzlich wurden alle vier Wrapper direkt nach dem `db reset` einmal per psql smoke-getestet, bevor überhaupt ein Test geschrieben war.
  - **[NEUN FELDER BEARBEITBAR, VIER AUSDRÜCKLICH NICHT]** `company · contact_name · phone · industry · postal_code · annual_consumption_kwh · metering_type · supplier · contract_end_date`. **`email` hat GAR KEINEN Parameter** (ein DB-Test prüft `proargnames`): sie ist die Adresse, VON DER die Einwilligung erteilt und AN DIE die Bestätigung gesendet wurde — eine Änderung übertrüge eine bestätigte Einwilligung auf eine Adresse, die nie zugestimmt hat, also die B1-3-Regel „der Admin kann widerrufen, nie erteilen" durch die Hintertür. Der Verzicht kostet nichts: eine falsch eingegebene Adresse bestätigt nie, die Einwilligung bleibt `pending` und fällt aus jeder Aussendung heraus. **Ein unerreichbarer Lead wird gekennzeichnet, nicht repariert.** Ebenfalls ohne Parameter: `status`/`retention_basis` (dafür gibt es `admin_set_lead_status` samt Einbahnstrassen-Trigger), `first_source_key` (seit B1-1 unveränderlich), `deletion_due_at` (immer abgeleitet).
  - **[NULL HEISST HIER LÖSCHEN — bewusst umgekehrt zu `capture_lead`]** B3-1 hat für den Erfassungspfad `coalesce(neu, BESTAND)` festgelegt („null lässt unberührt"), weil dort ein fehlender Wert „weiss ich nicht" heisst. Ein BEARBEITUNGSFORMULAR schickt dagegen immer alle neun Felder, und ein geleertes Feld ist eine Aussage: „das war falsch, soll weg". Mit COALESCE-Semantik liesse sich kein einziges Feld je bereinigen — genau das, was ein Korrekturweg können muss. Beide Regeln stehen in der Migration nebeneinander begründet, damit sie niemand „vereinheitlicht".
  - **[ZWECKBINDUNG WIRD DURCHGESETZT, NICHT VORAUSGESETZT]** Der Wrapper **WIRFT** (SQLSTATE 22023), wenn `supplier` oder `contract_end_date` auf einen Wert ungleich null gesetzt werden sollen und für den Lead keine Einwilligung zu `contract_expiry_reminder` im Zustand `pending` ODER `confirmed` besteht. Der B3-1-Trigger LÖSCHT diese Felder beim Widerruf, weil sie ohne ihren Zweck keine Grundlage haben; ein händischer Eintrag umginge genau das — und zwar unsichtbar, denn die Felder sähen danach aus wie erhoben. `pending` ist zugelassen, weil `capture_lead` sie bereits vor der Bestätigung schreibt (sonst wäre eine Korrektur in genau dem Zeitfenster unmöglich, in dem Tippfehler auffallen). **Auf null setzen ist immer erlaubt** — das ist die Richtung, die Daten entfernt. Die Oberfläche zeigt dafür einen deutschen Satz, keine Datenbankfehlermeldung.
  - **[GUARD-ERWEITERUNG MIT EINER BEGRÜNDETEN ASYMMETRIE — der einzige Punkt, an dem die Vorgabe nicht wörtlich umsetzbar war]** `guard_anonymized_lead` schützt jetzt **15 statt 14 Spalten**. Bei `last_edited_by` ist aber nur das **SETZEN** gesperrt, nicht das Nullen: die Spalte trägt `ON DELETE SET NULL`, und diese referentielle Aktion IST ein UPDATE auf die Lead-Zeile — ein vollständiger `is distinct from`-Schutz hätte das Löschen eines Admin-Kontos blockiert, sobald irgendein ANONYMISIERTER Lead darauf zeigt. Genau diesen Fall hat B1-3 bei `anonymized_by` mit dem völligen Verzicht auf Schutz gelöst; hier geht mehr: niemand kann einem anonymisierten Lead nachträglich einen Bearbeiter anheften, und das Konto bleibt löschbar. **Zwei DB-Tests pinnen beide Richtungen** (Setzen scheitert für `service_role` UND `postgres`; das echte Konto-Löschen nullt die Zuschreibung, statt zu blockieren).
  - **[EINE FILTERBEDINGUNG, ZWEI KONSUMENTEN]** `platform.leads_matching(...)` (13 Parameter, `setof platform.leads`) ist die einzige Stelle, an der der Filter steht — `admin_list_leads` und `admin_export_leads` benutzen sie beide. Zwei eigene WHERE-Klauseln wären zwei Auslegungen desselben Filters, und die Abweichung fiele erst an einer ausgeführten Datei auf, die andere Zeilen enthält als die Sicht, aus der sie entstand. Dieselbe Begründung wie `leads_due_for_anonymization` (B4-1: eine Definition von „fällig"). Neue Filter: Branche, Messart, **PLZ-PRÄFIX**, Jahresverbrauch von/bis, Vertragsende von/bis. Der Präfix statt Gleichheit, weil die führenden Ziffern das Netzgebiet sind („11" trifft die Wiener Innenbezirke) — ein Gleichheitsfilter zwänge dazu, ein Gebiet als Aufzählung zu treffen, und eine vergessene PLZ wäre nicht sichtbar, sondern nur eine etwas kleinere Menge. Ein unbekannter Wert wird als `invalid_filter` abgelehnt (Messart, PLZ-Präfix, Status), eine erfundene Branche fängt der Anwendungscode ab, BEVOR sie am Postgres-Enum als Datenbankfehler scheitert.
  - **[`export_total` NEBEN `total` — die Zahl, die wirklich in der Datei landet]** Der Export schliesst gesperrte und anonymisierte Zeilen strukturell aus, die Trefferzahl der Sicht tut das nicht. Eine Oberfläche, die `total` als Export-Zeilenzahl anbietet, verspricht eine Datei, die es so nicht gibt — und die Differenz fiele niemandem auf, weil beide Zahlen plausibel sind. Der Knopf trägt deshalb `export_total`, und daneben steht, wie viele Treffer dadurch herausfallen. **Live gemessen:** Filter `plz=10` → „3 Treffer" und „2 Zeilen ausführen (CSV)" mit dem Satz „Von den 3 Treffern fallen dadurch 1 heraus".
  - **[DIE ZWEI AUSSCHLÜSSE STEHEN IN DER ABFRAGE, NICHT IM FILTER]** Anonymisierte Leads und Adressen auf der Sperrliste fallen in `admin_export_leads` selbst heraus. Eine ausgeführte Datei kann in ein beliebiges fremdes Werkzeug eingespielt werden, das die Sperrliste nicht kennt — und sie mangels Klartext (B1-1: nur SHA-256) auch nicht nachträglich anwenden könnte. Der Ausschluss muss deshalb in der Quelle liegen und nicht in einer Einstellung, die jemand versehentlich weglässt. **Ein DB-Test stellt den Filter absichtlich so, dass er alle drei Zeilen einschlösse** — und zwar über den JAHRESVERBRAUCH als Marker, nicht über den Firmennamen: der wird bei der Anonymisierung genullt, ein Freitext-Marker verlöre die Zeile schon im Filter und der Test bewiese nichts (beim ersten Lauf genau so aufgefallen).
  - **[EINE ZEILE OHNE EINWILLIGUNGSSTAND IST DIE GEFÄHRLICHSTE ZEILE DER DATEI]** Je Zeile fährt `platform.marketing_consent_state` mit: **bestätigt · offen · widerrufen · keine**. „bestätigt" kommt aus `platform.has_confirmed_consent` (die EINE Sendefrage, B1-1), nicht aus einer zweiten Abfrage. **Ein abgelaufener Bestätigungslink zählt als „keine", nicht als „offen"** — bestätigt werden kann er nicht mehr, und „offen" behauptete, da käme noch etwas (dieselbe Überlegung wie `consent_effective_status` in B1-3, hier in der vorsichtigeren Richtung entschieden).
  - **[EXPORTPROTOKOLL: VIER SPALTEN, KEINE KOPIE DER DATEN]** `platform.admin_exports` (`exported_by` mit ON DELETE SET NULL, `exported_at` mit **`clock_timestamp()`** — `now()` ist in einer Transaktion konstant und mehrere Ausfuhren wären nicht ordenbar, Befund aus B4-1; `row_count`, `filter_summary`). RLS an, keine Policy, für KEINE Rolle ein Grant (Muster `platform.job_runs`). Der Protokolleintrag entsteht im SELBEN Aufruf wie die Zeilen — ein getrenntes „protokolliere jetzt" könnte ausbleiben, und dann gäbe es eine Kopie ohne Spur. Der Filtertext kommt aus `platform.lead_filter_summary` und nicht aus dem Frontend: das Protokoll soll beschreiben, was die DATENBANK angewandt hat. **Ein leerer Filter wird ausdrücklich als „alle (kein Filter gesetzt)" protokolliert — es gibt keinen ungefilterten Export, nur den Filter „alles".** Neue Seite `/admin/leads/exporte` macht das Protokoll lesbar; Einträge lassen sich dort nicht entfernen, und es gibt dafür auch keinen Wrapper.
  - **[EXPORTWEG OHNE service_role]** `app/admin/leads/export/route.ts` (Route Handler, `nodejs`, `force-dynamic`) nutzt den ANGEMELDETEN Supabase-Client; `admin_export_leads` ist SECURITY DEFINER mit `is_admin()`-Prüfung und authenticated-only. **Die Erlaubnisliste in der root-`eslint.config.mjs` wurde NICHT erweitert.** Zweiter Grund gegen service_role: `exported_by` wäre dort immer null und das Protokoll strukturell aussagelos. Die CSV entsteht SERVERSEITIG und wird gestreamt (`ReadableStream`) — im Browser gebaut müsste der gesamte Bestand unpaginiert an den Client, und genau das soll der Export nicht nebenbei tun. Ohne Sitzung: **307 auf `/anmelden`, KEIN RPC** (anwendungsseitig gepinnt); angemeldet ohne Adminrolle: 403; abgelehnter Filter: 400 — nie eine leere Datei, denn die liest sich wie „es gibt keine Leads".
  - **[SEMIKOLON UND BOM SIND KEINE KOSMETIK]** Mit Komma landet unter deutscher Excel-Ländereinstellung die ganze Zeile in EINER Spalte; ohne UTF-8-BOM wird aus „Bäckerei" „BÃ¤ckerei". Eine Datei, die beim ersten Öffnen zerfällt, wird von Hand repariert — dabei entstehen die Fehler. RFC-4180-Quoting, weil Firmennamen Semikolons und Zeilenumbrüche enthalten: EINE solche Firma verschöbe sonst alle folgenden Spalten ihrer Zeile, und „eine Spalte verschoben" heisst in einer Adressdatei irgendwann „falscher Empfänger". **Der Rücklese-Test bringt einen eigenen RFC-4180-Parser mit** (im Test, nicht im Produktionsmodul: dort wäre er toter Code, und ein Parser, der die Annahmen des Schreibers teilt, bewiese nichts). **Leere Angaben bleiben LEER und werden nicht zu „—"** — ein Gedankenstrich ist am Bildschirm eine Lesehilfe, in einer Datei ein Wert.
  - **[REFAKTOR, verhaltensgleich]** `formatDate`/`formatDateTime`/`formatKwh` sind aus `components/admin/ui.tsx` nach `lib/admin/format.ts` gewandert (rein, ohne React) und werden dort re-exportiert — kein bestehender Import musste angefasst werden. Grund: die Export-Route braucht dieselben Formate für die CSV; eine zweite Kopie hätte bedeutet, dass die ausgeführte Datei ein anderes Datumsformat trägt als die Sicht, aus der sie entstand.
  - **[DETAILSEITE UMGEBAUT]** Die neun Felder stehen jetzt in GENAU EINER Komponente (`lead-edit-form.tsx`) — bei einem anonymisierten Lead rendert dieselbe Komponente dieselben neun als reine Anzeige. Zwei getrennte Darstellungen hätten bedeutet, dass jede künftige Änderung an der Feldmenge an zwei Stellen nachzuziehen ist, und die vergessene Stelle wäre ausgerechnet die des anonymisierten Leads. Neu: „Zuletzt bearbeitet von" (bei `null` bewusst **„nicht von Hand bearbeitet"** statt einer Vermutung — anders als `exported_by` im Protokoll ist der Wert hier ZWEIDEUTIG, und die Oberfläche rät nicht) sowie ein sichtbarer Hinweis, sobald das Vertragsende geändert wird: ein anderes Datum erzeugt nach B4-2 eine neue Fälligkeit, die Person bekommt also eine weitere Erinnerung — beabsichtigt, aber nicht überraschend.
  - **[VERIFIKATION — DB-Gate 254/254]** (226 bestehende + **28 neue** in `packages/db-tests/src/lead-editing-export.test.ts`): Grant-Fläche exakt `authenticated` für alle vier Wrapper (anon und service_role dürfen nicht) · die drei `platform`-Helfer sind von aussen gar nicht aufrufbar · `platform.admin_exports` hat RLS und für keine Rolle irgendein Tabellenrecht · ein eingeloggter Nicht-Admin scheitert an jedem Wrapper mit **42501** und hinterlässt weder Änderung noch Protokollzeile · alle neun Felder ändern sich und `last_edited_by` trägt den handelnden Admin · ein leeres Feld löscht · `admin_update_lead` hat nachweislich **keinen** `p_email`-Parameter (10 Argumente, `proargnames` geprüft) und die Adresse bleibt nach einem vollständigen Aufruf unverändert · Versorger ohne Einwilligung wirft **22023** und lässt AUCH die erlaubte Firmenänderung weg (die Ausnahme kommt vor jeder Wirkung) · eine widerrufene und eine fremde Einwilligung genügen nicht · mit `pending` geht es · auf null setzen geht immer · ein anonymisierter Lead liefert `anonymized` und bleibt unverändert · das SETZEN von `last_edited_by` scheitert für `service_role` und `postgres`, das Nullen und das Konto-Löschen laufen durch · die neuen Filter zählen einzeln und kombiniert korrekt, der PLZ-Präfix „10" trifft zwei Bezirke und „1" drei, Vertragsende-Grenzen gehören dazu · unbekannte Filterwerte werden abgelehnt statt still ignoriert · `export_total` ist 1 wo `total` 3 ist · der Export schliesst gesperrte und anonymisierte Zeilen aus, auch wenn der Filter sie einschliesst · der Einwilligungsstand unterscheidet alle vier Fälle und wird von einer Einwilligung zu einem ANDEREN Zweck nicht gefärbt · jede Ausfuhr hinterlässt genau EINEN Eintrag mit passender Zeilenzahl, handelndem Konto und Filtertext · `admin_list_exports` und `admin_get_lead` (mit `last_edited_by_email`) werden echt aufgerufen.
  - **[VERIFIKATION — anwendungsseitig 80/80]** (74 bestehende + **6 neue** in `app/admin/leads/export/route.test.ts`, dazu **11** in `lib/admin/csv.test.ts`): ohne Sitzung 307 auf `/anmelden` mit **0 RPC-Aufrufen** und leerem Rumpf · ohne Adminrolle 403 · abgelehnter Filter 400 mit Nennung des Feldes · die Filter der Anfrage gehen UNVERÄNDERT an den Wrapper · eine erfundene Branche wird nicht durchgereicht · null Treffer liefern eine Datei MIT Kopfzeile · die ersten drei BYTES sind `EF BB BF` (auf den Bytes geprüft, nicht auf `res.text()`: `TextDecoder` entfernt ein führendes BOM beim Dekodieren — die Zeichenkette hätte es nie, auch wenn die Datei es trägt) · Felder mit Semikolon, Anführungszeichen UND Zeilenumbruch kommen über einen unabhängigen Parser unverändert zurück, bei unveränderter Spaltenzahl. `vitest.config.ts` nimmt dafür `app/admin/**/*.test.ts` dazu — dieselbe Begründung wie B4-1 für `app/api/**`: ein Route Handler ist eine gewöhnliche Funktion `Request → Response`; die Admin-SEITEN bleiben ausgeschlossen.
  - **[VERIFIKATION — voller angemeldeter Flow lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack, Playwright)]** Anonym: `/admin/leads/exporte`, `/admin/leads/export` und `/admin/leads/export?branche=handel` je **307 auf `/anmelden`**, erfundener Pfad unter `/admin` **404**. Angemeldet als Admin, fünf Fixtures: Filter Branche → 2, PLZ-Präfix „10" → 3, „1" → 4, Messart → 2, Verbrauch 100 000–300 000 → 2, kombiniert → 2, ungültige PLZ „11a" → sichtbar abgelehnt. Download über die ECHTE Schaltfläche: Dateiname `coolin-leads-2026-07-21-15-22-07.csv` (Zeitpunkt AUS DER DATENBANK, derselbe wie im Protokoll), erste Bytes `ef bb bf`, Trennzeichen `;`, die Firma `Kühlhaus "Nord"; Söhne KG` steht korrekt gequotet als `"Kühlhaus ""Nord""; Söhne KG"`, **die gesperrte Adresse fehlt** (4 von 5 Zeilen), Einwilligungsspalte `bestätigt` bzw. `keine`. Bearbeiten über das echte Formular: „Änderungen gespeichert.", „Zuletzt bearbeitet von" wechselt von „nicht von Hand bearbeitet" auf `b21-admin@test.local`. Versorger ohne Einwilligung → deutscher Satz, keine Datenbankfehlermeldung, nichts gespeichert. Vertragsende geändert → Hinweis auf die neue Fälligkeit erscheint VOR dem Absenden. Anonymisierter Lead: **0 Eingabefelder**, Sperrhinweis und Nur-Anzeige der neun Felder. `/admin/leads/exporte` zeigt Zeitpunkt, Konto, Zeilenzahl und Filtertext. **0 Konsolenfehler.** `pnpm build`/`test`/`typecheck`/`lint` root-weit grün, `supabase/database.types.ts` neu generiert.
  - **[BEFUND, für künftige Live-Prüfungen wichtig] `/admin/leads/exporte` lieferte schon VOR dieser Aufgabe 307** — nicht weil die Seite existierte, sondern weil `/admin/leads/[id]` das Segment als Lead-ID verschluckt und das Layout anonym umleitet. Dasselbe gilt für `/admin/leads/export`. Der Statuscode taugt damit NICHT als Vorher/Nachher-Unterscheidung für diese zwei Pfade; die Gegenprobe auf einen erfundenen Pfad unter `/admin` (404) bleibt aussagekräftig, der eigentliche Nachweis läuft über die Introspektion der Cloud-DB und den angemeldeten Zustand.

- **[GEBAUT: B3-4 — Warteliste-Landingpage und Postaktions-QR (Fahrplan B3)]** Der erste Einstiegspunkt, dessen Adresse auf PAPIER steht. Migration `supabase/migrations/20260722180000_create_warteliste_source_and_lead_source_stats.sql` (eine `lead_sources`-Zeile + `public.admin_lead_source_stats`) + `apps/web/lib/leads/{registry,warteliste}.ts` + `components/leads/warteliste-page.tsx` + zwei Routen + Herkunftszählung auf `/admin/leads` + `DEPLOYMENT.md` §5. **NICHT dabei:** Betroffenheits-Check (B3-3, blockiert auf die Branchenkennzahlen), gefilterte Sicht/Export/Massenaussendung/Zustellprotokoll je Kampagne (B2), Bearbeitbarkeit der Stammdaten (B2), `tenant_id` (B13).
  - **[KEINE NEUE EINWILLIGUNGSART — der Kern der Aufgabe]** Die Warteliste ist fachlich `marketing_email` und wird über den `source_key` der EINWILLIGUNG unterschieden; B1-1 hält den Herkunftskontext je Einwilligung genau dafür vor. Eine eigene Art verhielte sich in jeder Hinsicht identisch (gleiches Double-Opt-in, gleiche Sperrprüfung, gleiche Aktivierung) und zerlegte den Bestand vor der 48-Stunden-Aktivierung in getrennte Listen, deren Vereinigung jede Aussendung selbst wieder herstellen müsste. Begründung im Kopf der Migration.
  - **[ZWEI ROUTEN, EIN FORMULAR]** `/warteliste` (organisch, indexierbar, in der sitemap, im Leistungen-Menü als dritter Quereinstieg) und `/warteliste/[quelle]` (erlaubt ist ausschliesslich `wko` → `wko-postaktion-qr`, `noindex`, **nirgends intern verlinkt**). Beide rendern dieselbe Komponente und erheben dieselben Felder; sie unterscheiden sich in der ANSPRACHE („Sie haben von uns Post erhalten") und darin, unter welcher Herkunft die Eintragung landet. Zwei nahezu gleiche indexierbare Seiten wären ein Duplikat; `noindex` ist keine Sperre.
  - **[⚠ DAUERHAFTE ZUSAGE: `/warteliste/wko` STEHT AUF PAPIER]** Nie umbenennen, nie entfernen, nie auf eine andere Quelle umhängen — auch nicht bei einer Umstrukturierung der Seitenstruktur; wird die Seite inhaltlich ersetzt, muss der Pfad bestehen bleiben und weiterleiten. Steht an drei Stellen: `DEPLOYMENT.md` §5, der Erlaubnisliste (`lib/leads/warteliste.ts`) und der Route selbst.
  - **[EIN UNBEKANNTES SEGMENT IST 404, KEIN RÜCKFALL]** Die Zuordnung ist eine Erlaubnisliste, keine Ableitung aus dem Segment. Ein Rückfallwert stempelte eine falsche Herkunft auf eine echte Einwilligung — und die Herkunft ist seit B1-1 Pflichtfeld (`first_source_key`, unveränderlich) und die Grundlage jeder Segmentierung. Ein Vertipper in der gedruckten Adresse fiele damit nie auf: die Seite funktionierte, die Leads kämen an, und die Rücklauf-Auswertung wäre still falsch. **Eine tote Route ist ein sichtbarer Fehler, eine falsch zugeordnete Einwilligung ein unsichtbarer.**
  - **[BRANCHE IST PFLICHT — bewusste Abweichung von der Wertleiter, am Registry-Eintrag begründet]** Der Fahrplan sieht vor, die Wartenden zum Erscheinen der Tarifverordnung mit bereits bekannter Betriebsgrösse anzusprechen; ohne Branche wäre das eine Rundmail, die Liste hätte ihren Zweck verfehlt. Der Preis ist an der EINGABEART bemessen: Branche ist ein Auswahlfeld ohne Rechercheaufwand, der Jahresverbrauch verlangt, eine Rechnung herauszusuchen — er bleibt optional, ebenso die PLZ. **`wko-postaktion-qr` wurde dabei von „E-Mail + Firma" auf dieselben Felder umgestellt**: unterschiedliche Felder je Route ergäben eine Warteliste, deren Segmentierbarkeit vom Zugangsweg abhinge.
  - **[SEITENINHALT: KEINE ZAHL, und das IST der Inhalt]** Kein Betrag, kein Prozentsatz, keine Ersparnisangabe (im Markup gemessen: 0× `€`, 0× `%`). Die Seite sagt, was sich zum 1.1.2027 ändert (Leistungstarif erreicht Netzebene 7), dass die Beträge noch nicht feststehen, weil die Tarifverordnung nicht veröffentlicht ist, was die Eintragung bewirkt — und in drei Karten, was sie NICHT bedeutet (keine Kosten/Verpflichtung, Abmeldung in jeder Nachricht, erst nach Bestätigung). Am Schluss des Flaggschiff-Artikels („die Beträge stehen noch nicht fest") orientiert.
  - **[HERKUNFTSZÄHLUNG, TEIL 5 — die zwei Spalten zählen VERSCHIEDENE Dinge, mit Absicht]** `public.admin_lead_source_stats` (authenticated-only, `platform.is_admin()`, wirft 42501; Zählung in SQL) liefert je Quelle die Leads über `leads.first_source_key` UND die bestätigten Marketing-Einwilligungen über `consents.source_key`. Beides über die Lead-Herkunft zu zählen wäre die naheliegende, aber falsche Vereinfachung: Wer den Betrieb über einen Artikel kennengelernt hat und Monate später den Brief scannt, behält `first_source_key = 'artikel-inline'` — die Reaktion auf den Brief würde dem älteren Kanal gutgeschrieben und der Brief systematisch zu niedrig bewertet. Folge, die auch in der Oberfläche steht: die zweite Zahl ist **kein „davon"**. Anonymisierte Leads bleiben enthalten (sie waren echter Rücklauf, die Herkunft überlebt die Anonymisierung bewusst). **Kein Export, keine gefilterte Sicht — die Abgrenzung zu B2 steht im Code und sichtbar unter der Tabelle.**
  - **[VERIFIKATION — DB-Gate 226/226]** (218 bestehende + **8 neue** in `packages/db-tests/src/lead-source-stats.test.ts`, gemessen in DELTAS gegen den Bestand, jeder Test in einer Transaktion mit `rollback`): die `warteliste`-Zeile existiert und ist AKTIV · `wko-postaktion-qr` unverändert vorhanden · ein eingeloggter Nicht-Admin bekommt **42501** statt leerer Zahlen · jede Quelle erscheint, auch mit 0 · Leads landen bei ihrer Herkunft, bestätigte Einwilligungen getrennt von offenen (`pending` zählt NICHT) · **eine Einwilligung zählt bei IHRER Herkunft, nicht bei der des Leads** (der Kreuzfall explizit gestellt) · ein anonymisierter Lead bleibt in der Zählung · Grant-Fläche exakt `authenticated`, `anon` UND `service_role` dürfen nicht. Der B3-2-Gleichheitstest Registry ↔ aktive `lead_sources` bleibt grün (beidseitig nachgezogen). **Nebenbefund:** der erste Testlauf hat einen echten Fehler der Migration gefangen — `consents` hat keine eigene `purpose`-Spalte (der Zweck hängt am Text), der Join fehlte; plpgsql prüft Funktionsrümpfe nicht beim Anlegen, der Fehler wäre erst beim ersten Aufruf sichtbar geworden.
  - **[VERIFIKATION — anwendungsseitig 65/65]** (52 bestehende + **13 neue** in `apps/web/lib/leads/warteliste.test.ts`): `wko` löst auf den QR-Einstiegspunkt auf · `erfunden`/`warteliste`/`WKO`/`wko-postaktion-qr`/leer/Nicht-String liefern **null**, insbesondere NICHT die organische Quelle · jedes vorgerenderte Segment zeigt auf einen Registry-Eintrag · ein unbekannter Schlüssel fasst die Datenbank nicht einmal an · beide Routen schreiben ihre eigene Herkunft bei identischem Zweck · beide Einträge tragen dieselben Felder · fehlende Branche → `fieldRequired` ohne Lead · eine erfundene Branche wird abgewiesen statt still verworfen · leere PLZ/Verbrauch kommen als `undefined` (⇒ SQL-`null`) an, nicht als Leerstring (sonst überschriebe die B3-1-COALESCE-Zusammenführung Bestehendes) · „84.000" mit Tausenderpunkt wird zur Zahl 84000 · die Einwilligung entsteht mit Token, es geht NUR die Bestätigungsmail raus.
  - **[VERIFIKATION — voller Absendeweg lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack)]** Statuscodes: `/warteliste` **200**, `/warteliste/wko` **200**, `/warteliste/erfunden` **404**, `/warteliste/WKO` **404**. `/warteliste` ohne `noindex`, mit Canonical, **in der sitemap** (22 → 23 URLs); `/warteliste/wko` mit `noindex, follow`, **0 Canonicals**, nicht in der sitemap. Einwilligungswortlaut aus `platform.consent_texts` in BEIDEN Seiten im Markup, Branchen-Auswahlfeld mit allen zehn Enum-Werten, `company` nirgends. Absendung über die ECHTE Seite (Playwright): `/warteliste` → Lead mit `first_source_key='warteliste'`, `industry='kuehlhaus'`, PLZ 1100, 84000 kWh, Einwilligung `pending` MIT Token, `consents.source_key='warteliste'`; `/warteliste/wko` mit leer gelassenen Optionalfeldern → `first_source_key='wko-postaktion-qr'`, `postal_code` und `annual_consumption_kwh` **null** (nicht Leerstring), Branche gesetzt. Ohne Branche: Feld `aria-invalid`, „Bitte ausfüllen.", **0 Leads**. Interne Verlinkung gemessen: `/warteliste` je 1× aus dem Flaggschiff-Artikel und `/branchen/handwerk`, **0 interne Links auf `/warteliste/wko`** über alle geprüften Seiten. Nav: „Warteliste Leistungstarif 2027" als LETZTER Quereinstieg des Leistungen-Menüs (nach Strom-Monitor und Vertragsende-Erinnerung). Admin mit echtem Login: „Rücklauf je Herkunft" zeigt alle Quellen, die Zahlen decken sich mit einer direkten SQL-Gegenrechnung; anonym ist der Abschnitt nicht erreichbar. @375px **0 px** horizontaler Überlauf und genau 1 H1 auf beiden Seiten, **0 Konsolenfehler** (der einzige Eintrag ist der absichtlich provozierte 404). `pnpm lint`/`typecheck`/`test`/`build` root-weit grün, `supabase/database.types.ts` neu generiert.

- **[GEBAUT: B4-2 — Vertragsablauf-Erinnerung und Landingpage (Fahrplan B4)]** Der ERSTE automatisierte E-Mail-Versand des Systems an reale Personen. Migration `supabase/migrations/20260722150000_create_contract_reminders.sql` (Tabelle `platform.contract_reminders`, `contract_reminder_lead_days`, `leads_due_for_contract_reminder`, vier `public`-Versand-Wrapper + `admin_contract_reminder_health`, `job_key`-CHECK/`clear_contract_data_on_withdrawal`/`anonymize_lead`/`admin_get_lead` nachgezogen) + `apps/web/app/api/cron/contract-reminders/route.ts` + `lib/leads/mail.ts` + Landingpage `/vertragsende-erinnerung` + Admin-Sichtbarkeit. **NICHT dabei:** Betroffenheits-Check (B3-3), gefilterte Sicht/Export/Massenaussendung/Zustellprotokoll je Kampagne (B2), Bearbeitbarkeit der Stammdaten (B2), `tenant_id` (B13).
  - **[DER PRIMÄRSCHLÜSSEL IST DIE DOPPELVERSAND-SPERRE, nicht eine Abfrage]** `platform.contract_reminders (lead_id, contract_end_date)`. Eine Prüfung im Anwendungscode hätte zwischen „nachsehen" und „einfügen" ein Zeitfenster — und genau darin entstünde der Doppelversand, den sie verhindern soll. Die KÖRNUNG ist die eigentliche Entscheidung: korrigiert jemand sein Vertragsende, entsteht ein ANDERER Schlüssel und eine erneute Erinnerung ist richtig, kein Duplikat. Ein Schlüssel allein auf `lead_id` machte die Erinnerung zu einem einmaligen Ereignis je Person und liesse jede Korrektur still ins Leere laufen. **Ausdrücklich NICHT das Zustellprotokoll aus B2** (Kampagnen, Rückläufer, Beschwerden) — hier geht es allein um „wurde für DIESES Vertragsende schon erinnert".
  - **[ERST BEANSPRUCHEN, DANN SENDEN, DANN ERGEBNIS FESTHALTEN]** Bricht der Vorgang zwischen Beanspruchung und Versand ab, bleibt eine Zeile ohne `delivered_at` zurück: sichtbar, prüfbar, KEINE zweite Mail. Die umgekehrte Reihenfolge erzeugte im selben Fall einen stillen, sich täglich wiederholenden Doppelversand. **Zeilen ohne `delivered_at` werden NICHT automatisch wiederholt** — automatische Wiederholung von E-Mail-Versand erzeugt Schleifen; sie sind ein Admin-Befund (`admin_contract_reminder_health`, Schwelle 24 h, und die Schwelle FÄHRT IN DER ANTWORT MIT, damit die Oberfläche keine andere Zahl behaupten kann als die verwendete).
  - **[DIE BEIDEN VERSANDPRÜFUNGEN STEHEN IN DER AUSWAHL, NICHT IM CODE]** `leads_due_for_contract_reminder` verlangt bestätigte Einwilligung (`has_confirmed_consent` — bei `pending` ausdrücklich false) UND `not is_suppressed`. Eine Prüfung im Anwendungscode kann übersprungen werden (beim Umbau, durch einen zweiten Aufrufer, durch ein vergessenes `if`); eine in der Auswahl nicht: was sie nicht liefert, kann nicht angeschrieben werden. Dieselbe Funktion beantwortet „wie viele sind fällig", „welche bearbeite ich jetzt" UND (über `claim_contract_reminder`) „darf ich diesen einen jetzt anschreiben" — EINE Definition von fällig.
  - **[„KLEINER ODER GLEICH" STATT STICHTAG]** Vorlaufzeit als EINE benannte Konstante `platform.contract_reminder_lead_days()` = 56 (Muster wie `retention_months`, B1-1). Wer ein Vertragsende einträgt, das nur noch drei Wochen entfernt ist, bekäme mit einer Stichtagsprüfung NIE eine Erinnerung — der Tag, an dem es acht Wochen entfernt war, liegt in der Vergangenheit. Mit „≤" bekommt er sie sofort; dass daraus keine tägliche Wiederholung wird, verhindert der Primärschlüssel und nicht ein zweiter Datumsvergleich.
  - **[MENGENOBERGRENZE AUSNAHMSWEISE IM ENDPUNKT (200 / 500)]** B4-1 begründet, warum ein Schwellwert nicht in einen HTTP-Handler gehört. Der Grund ist hier gegenstandslos: der wirksame Schritt liegt ohnehin ausserhalb der Datenbank, eine reine DB-Funktion könnte ihn gar nicht bremsen — **`platform.run_contract_reminders` gibt es deshalb bewusst NICHT** (sie wäre eine Hülle, die Vollständigkeit vortäuscht). Die Werte sind Modulkonstanten und werden NICHT aus der Anfrage gelesen. Oberhalb der Grenze wird KEINE einzige Mail versendet, nicht die erste Teilmenge: ein Fehler in der Datumslogik schriebe sonst den gesamten Bestand in einem Lauf an, eine versendete Mail ist nicht zurückholbar, und an der Zustellreputation hängt die 48-Stunden-Aktivierung im November.
  - **[DIE MAIL ENTHÄLT KEIN ANGEBOT — auch nicht bei zusätzlicher Marketing-Einwilligung]** Drei Gründe, im Code ausformuliert: (1) die Einwilligung lautet auf eine Erinnerung, nicht auf Werbung — genau dafür sind die Zwecke seit B1-1 getrennt; (2) hinge der INHALT vom Vorliegen einer zweiten Einwilligung ab, hinge der rechtliche Charakter der Mail an einem Zustand und wäre im Nachhinein nicht mehr prüfbar; (3) die Zurückhaltung IST hier das Vertrauensargument. Der einzige Link zeigt auf den kostenlosen, unabhängigen **E-Control-Tarifkalkulator**. **Erster Konsument von `unsubscribeHeaders`** (B1-2 gebaut, bis heute unbenutzt): `List-Unsubscribe` + `List-Unsubscribe-Post` plus Abmeldelink im Fuss. `deliver()` in `lib/leads/mail.ts` nimmt dafür optionale `headers` — optional, weil die Bestätigungsmail sie ausdrücklich NICHT bekommen darf (abgemeldet werden kann nur, was besteht).
  - **[VIER WRAPPER STATT EINEM — mit Grund]** `start_contract_reminder_run` (legt `job_runs` an UND liefert Zahl + Stapel aus DERSELBEN Momentaufnahme — sonst prüfte der Endpunkt seine Obergrenze gegen eine andere Menge als er abarbeitet; `items_considered` steht SOFORT im Protokoll, nicht erst beim Abschluss), `claim_contract_reminder`, `record_contract_reminder_result`, `finish_contract_reminder_run` (fasst AUSSCHLIESSLICH `job_key='contract_reminder'` an — eine verwechselte run_id soll nicht das Protokoll des Fristenlaufs überschreiben). Alle vier service_role-only, `admin_contract_reminder_health` authenticated-only, `platform.contract_reminders` hat RLS und für KEINE Rolle ein Grant. Die eslint-Allowlist deckt `apps/web/app/api/cron/**` bereits ab (B4-1) — **keine Änderung nötig, kein zweiter Client**.
  - **[ZWECKBINDUNG AN BEIDEN STELLEN NACHGEZOGEN]** `clear_contract_data_on_withdrawal` (B3-1) löscht jetzt zusätzlich die `contract_reminders`-Zeilen, `anonymize_lead` ebenso. Das Vertragsende steht dort im PRIMÄRSCHLÜSSEL und liesse sich nicht nullen — ohne diese Regel überlebte eine Kopie den Wegfall ihres Zwecks. Nebeneffekt und erwünscht: eine erneute Einwilligung später erzeugt zu Recht eine neue Erinnerung, statt durch eine Zeile aus einem widerrufenen Vorgang dauerhaft gesperrt zu sein.
  - **[LANDINGPAGE `/vertragsende-erinnerung`, INDEXIERBAR]** Nutzt die B3-2-Komponente mit `source_key='vertragsablauf-landing'`; der Registry-Eintrag ist jetzt `placed: true`, und der B3-2-Kommentar („ein Vertragsende zu erfassen und die versprochene Erinnerung nicht senden zu können, ist ein gebrochenes Versprechen") ist durch die Begründung ersetzt, warum der Grund weggefallen ist. Erhoben werden E-Mail + Versorger + Vertragsende (Pflicht) und die nie vorausgewählte Marketing-Ankreuzmöglichkeit; `contract_expiry_reminder` ist bestätigungspflichtig, der B1-2-Double-Opt-in greift unverändert. **Der Löschsatz beschreibt den TRIGGER, er verspricht ihn nicht** („unsere Datenbank löscht … automatisch" statt „wir löschen") — der Unterschied ist der zwischen einer Zusage und einer Invariante. `revalidate = 3600` wie `/kontakt`, fail-closed ohne Wortlaut. **Navigation:** letzter Punkt des Leistungen-Menüs (Header + Mobile), neben dem Strom-Monitor. Dafür wurde `NavItem.trailingLeaf` zu `trailingLeaves: NavLeaf[]` verallgemeinert — alles, was in `LEISTUNGEN_FLAT` landet, wird von `lib/leistungen.ts` zu einer LEISTUNG erklärt (Icon, Übersichtskachel, Cross-Links, eigener Textblock) und hätte beim Bauen hart gebrochen; der Abschluss-Slot ist genau für kostenlose Quereinstiege da. Die Seite steht deshalb explizit in `lib/routes.ts` (wie der Monitor), bleibt aber im Gegensatz zu ihm **indexierbar**.
  - **[ADMIN: ZWEI JOB-ZEILEN, NICHT EINE]** `/admin/leads` zeigt beide Läufe mit EIGENEM Stand (48-Stunden-Hervorhebung für beide) — ein gemeinsamer „die Crons laufen"-Indikator verschwiege den Fall, in dem der eine läuft und der andere nicht, und die Folgen sind verschieden (Rechtspflicht gegen gebrochenes Versprechen). Zusätzlich der Befund „beansprucht, aber nie zugestellt, älter als 24 h". Die Detailseite nennt für das AKTUELLE Vertragsende, ob und wann erinnert wurde, bei Fehlschlag mit Grund; frühere Vertragsenden stehen darunter als sichtbare Spur einer Korrektur. Weiterhin **kein Auslöseknopf**.
  - **[VERIFIKATION — DB-Gate 218/218]** (199 bestehende + **19 neue** in `packages/db-tests/src/contract-reminders.test.ts`): Lead in der Vorlaufzeit mit bestätigter Einwilligung wird erfasst · ohne Bestätigung nicht, auch `pending` nicht · gesperrte Adresse nicht · Vertragsende in der Vergangenheit UND „heute" nicht · 57 Tage nicht, 56 schon (Grenze gehört dazu) · nach vorhandener Zeile nicht mehr · 21 Tage werden SOFORT erfasst · doppeltes Beanspruchen liefert `already_claimed` und lässt `attempted_at` unverändert · Ergebnis ist entweder `delivered_at` oder `error`, nie beides · nicht (mehr) Fälliges wird abgelehnt OHNE Zeile anzulegen · geändertes Vertragsende erzeugt eine neue Fälligkeit (beide Zeilen bleiben) · Widerruf löscht Zeilen + nullt Versorger/Vertragsende · `anonymize_lead` löscht die Zeilen · anonymisierter Lead wird nie erfasst · `start`-Wrapper zählt 3 und liefert 2, älteste zuerst, `items_considered` sofort protokolliert · `finish` fasst einen `lead_retention`-Lauf NICHT an · `job_key`-CHECK kennt genau zwei Werte · Grant-Fläche exakt service_role bzw. authenticated, `anon` nirgends, Tabelle ohne jedes Grant + RLS.
  - **[VERIFIKATION — anwendungsseitig 52/52]** (38 bestehende + **14 neue**): ohne Kopfzeile / falsches Geheimnis / ohne `Bearer`-Präfix / **ohne gesetztes `CRON_SECRET`** jeweils 401, und weder der service_role-Client noch die Mail-Funktion werden angefasst · oberhalb des Schwellwerts wird nicht einmal BEANSPRUCHT (`claim` 0×, `send` 0×), Lauf steht auf `refused`, Detailtext nennt „KEINE einzige Mail" · bei 500 (= Grenze) läuft der Versand normal · ein fehlgeschlagener Einzelversand bricht den Lauf nicht ab: 3 Empfänger → 2 zugestellt, 1 mit `error` (und der Fehlertext enthält nachweislich KEIN „@") , Lauf endet auf `success` mit gesondert ausgewiesenem Fehlschlag · ein zwischenzeitlich hinfälliger Fall (`not_eligible`) löst KEINEN Versand aus · ein Empfänger je Aufruf, `to` ist ein String, kein `bcc`/`cc` · die Mail trägt beide List-Unsubscribe-Kopfzeilen (signiert auf Lead UND Zweck) und den Abmeldelink in Text- UND HTML-Fassung, enthält weder `/leistungen` noch `/peak-shaving`, nennt Versorger und Vertragsende als deutsches Datum, und ohne Versorger fehlt die Zeile statt leer dazustehen.
  - **[VERIFIKATION — voller Versandweg lokal gegen den PRODUCTION-Build (Port 3400, lokaler Supabase-Stack, Resend über einen Capture-Server)]** Landingpage 200, Formular mit `email`/`supplier`/`contractEndDate`/`marketing` im ausgelieferten Markup, Einwilligungswortlaut aus der DB, **kein `noindex`**, Canonical gesetzt, in der sitemap enthalten (22 URLs). Absendung über die ECHTE Seite (Playwright): Marketing-Häkchen nachweislich NICHT vorausgewählt, danach zwei `pending`-Einwilligungen + zwei Bestätigungsmails **ohne** List-Unsubscribe-Kopfzeilen. Solange unbestätigt: `leads_due_for_contract_reminder` = **0**, Lauf `itemsConsidered=0`. Nach Bestätigung über die echte Bestätigungsseite (beide Zwecke `confirmed`): Lauf `itemsConsidered=1, itemsProcessed=1`, Protokollzeile mit `delivered_at`, Mail mit Betreff „Erinnerung: Ihr Stromvertrag läuft am 20.08.2026 aus", beiden Kopfzeilen, Abmeldelink, E-Control-Link — und **ohne jeden Verweis auf eigene Leistungen, obwohl der Lead zusätzlich eine bestätigte Marketing-Einwilligung trug**. Zweiter Lauf: `itemsConsidered=0`, **0 Mails** (Doppelversand-Sperre). One-Click-Abmeldung (`POST /api/abmelden`, echter signierter Link aus der Mail) → 200, Versorger und Vertragsende genullt, **0 Erinnerungszeilen**, Marketing-Einwilligung unberührt. Zugangsgrenze: anonym **401**, falsches Geheimnis 401, erfundener Pfad unter `/api/cron` **404**. Admin (echter Login als Admin): beide Job-Zeilen sichtbar, 06:40-UTC-Zeile korrekt, Befund „1 Erinnerung(en) … beansprucht, aber nie zugestellt … älter als 24 Stunden", Detailseite zeigt den Fehlschlag samt Grund und das frühere Vertragsende. Landing @375px **0 px** horizontaler Überlauf, genau 1 H1, **0 Konsolenfehler** über alle Läufe. `pnpm lint`/`typecheck`/`test`/`build` root-weit grün, `supabase/database.types.ts` neu generiert.
  - **[OFFEN — betrifft den Betrieb, nicht den Code]** Zusätzlich zu `CRON_SECRET` müssen `RESEND_API_KEY` und `RESEND_FROM` in Vercel gesetzt sein (sind sie seit dem Domain-Umzug). Fehlen sie, protokolliert der Lauf jeden fälligen Fall als Fehlschlag — und wiederholt ihn NICHT. Sichtbar wird das über den 24-Stunden-Befund auf `/admin/leads`.

- **[GEBAUT: B4-1 — Scheduling-Infrastruktur und automatische Fristdurchsetzung (Fahrplan B4)]** Der ERSTE zeitgesteuerte Job im System. Migration `supabase/migrations/20260722120000_create_job_runs_and_lead_retention.sql` (Tabelle `platform.job_runs`, Spalte `leads.anonymized_by_system` + CHECK, `leads_due_for_anonymization`, `run_lead_retention`, 2 neue `public`-Wrapper, `anonymize_lead`/`guard_anonymized_lead`/`admin_get_lead` nachgezogen) + `apps/web/app/api/cron/lead-retention/route.ts` + `apps/web/vercel.json` + Admin-Sichtbarkeit auf `/admin/leads`. **NICHT dabei:** die Vertragsablauf-Erinnerung selbst und JEGLICHER E-Mail-Versand samt Landingpage (B4-2), der Betroffenheits-Check (B3-3), gefilterte Sicht/Export/Aussendung (B2), `tenant_id` (B13).
  - **[DER JOB VERSENDET KEINE EINZIGE E-MAIL — das ist der Grund, warum er der erste ist]** Der erste Cron im System soll eine Aufgabe haben, die nachweislich keinen realen Menschen erreichen kann. Fällige Löschfristen gibt es zudem vor 2028 keine (24 Monate ab letzter Interaktion, der Bestand beginnt 2026): der Lauf findet null Fälle, beweist die Kette von der Plattform-Zeitsteuerung über die neue Privilegiengrenze bis in die Datenbank — und verändert nichts.
  - **[DIE GESAMTE ENTSCHEIDUNGSLOGIK LIEGT IN DER DATENBANK, nicht im Endpunkt]** `platform.run_lead_retention(p_max_batch, p_refuse_above)` legt den Laufdatensatz an, zählt, verweigert oder anonymisiert und schliesst ab. Der Endpunkt kennt weder Schwellwert noch Auswahl und ruft den Wrapper OHNE Argumente. Grund ist nicht Geschmack, sondern Reichweite: ein HTTP-Handler ist von aussen erreichbar, eine Datenbankfunktion nicht — läge die Mengenbegrenzung im Endpunkt, entschiede ein Query-Parameter über die Grösse eines unumkehrbaren Vorgangs.
  - **[OBERHALB DER OBERGRENZE PASSIERT GAR NICHTS — nicht „die erste Teilmenge"]** Vorgabewerte 500 / 1000. Anonymisierung ist seit B1-3 endgültig, auch für `service_role` und `postgres`. Ein Fehler in der Fristableitung (falsches `last_interaction_at`, geänderte `retention_months`, Massenimport mit altem Datum) macht schlagartig den GESAMTEN Bestand fällig; ein ungebremster Job zerstörte ihn in einem einzigen nächtlichen Lauf. Eine Teilmenge zu löschen wäre die schlechteste Möglichkeit — derselbe irreversible Charakter, nur portionsweise, und morgen liefe es weiter. Die Abwägung ist asymmetrisch: **ein zu später Lauf ist reparabel, ein zu grosser nicht.**
  - **[EINE DEFINITION VON „FÄLLIG"]** `leads_due_for_anonymization(p_limit)` beantwortet „wie viele sind fällig" (`p_limit => null` = unbegrenzt, SQL-Semantik von `LIMIT NULL`) UND „welche bearbeite ich jetzt" (Stapelgrösse). Zwei getrennte Abfragen wären zwei Definitionen — und die Abweichung fiele erst auf, wenn der Schwellwert gegen eine andere Menge prüft als die, die anschliessend gelöscht wird.
  - **[URHEBERSCHAFT INS DATENMODELL, nicht in eine Vermutung der Oberfläche]** Neue Spalte `platform.leads.anonymized_by_system` + CHECK („System UND Konto" ist unmöglich). B1-3 stellte `anonymized_by = null` als „durch ein inzwischen gelöschtes Konto" dar — richtig, solange nur Menschen anonymisieren konnten (die Spalte trägt `ON DELETE SET NULL`). Mit dem Systemlauf wäre dieselbe Aussage still falsch geworden, ausgerechnet an der Stelle, an der ein Mensch rekonstruieren muss, wer einen unumkehrbaren Vorgang ausgelöst hat. `anonymize_lead` bekam `p_by_system boolean default false` (DROP+CREATE wie `capture_lead` in B3-1 — `create or replace` kann die Parameterliste nicht erweitern; der bestehende Zwei-Argument-Aufruf aus `admin_anonymize_lead` läuft unverändert) und **WIRFT**, wenn zugleich ein `p_actor` mitkommt. `guard_anonymized_lead` schützt jetzt **14 statt 13 Spalten** — derselbe Grund wie in B3-1: ein Schutz, der seine eigene Erweiterung nicht abdeckt, läuft an ihr vorbei. Die Detailseite zeigt bei Systemlauf **„automatisch (Fristablauf)"**.
  - **[PROTOKOLL: `platform.job_runs`]** Der wahrscheinlichste Fehler eines Cron-Jobs ist nicht das Scheitern, sondern das AUSBLEIBEN — und ein ausgebliebener Lauf ist von „nichts zu tun" nicht unterscheidbar, dem planmässigen Zustand dieses Jobs bis 2028. Die Zeile entsteht beim START (ausserhalb des Ausnahmeblocks) und wird am Ende genau einmal vervollständigt; ein abgestürzter Lauf hinterlässt `finished_at is null`. **Kein Append-only-Schutz wie `stripe_events`** (dort wird eine fremde Aussage archiviert, hier entsteht EINE Aussage in zwei Schritten). **`started_at`/`finished_at` nutzen `clock_timestamp()`, nicht `now()`** — `now()` ist die Transaktionszeit und in einer Transaktion konstant: die Dauer jedes Laufs wäre strukturell null und zwei Läufe derselben Transaktion nicht ordenbar (im DB-Gate der Normalfall).
  - **[FAIL-CLOSED AN DER NEUEN AUSSENGRENZE]** `app/api/cron/lead-retention` (Node, force-dynamic, unter `/api` — der Middleware-Matcher schliesst das aus, in T4-3 verifiziert). Fehlende Kopfzeile, falsches Geheimnis **und fehlendes `CRON_SECRET`** ergeben alle 401 ohne jeden Datenbankzugriff. Der dritte Fall ist der wichtige: „nichts konfiguriert" darf nie „für alle offen" heissen — heute wäre das ein fremdgesteuerter Massen-Anonymisierungslauf, ab B4-2 ein fremdgesteuerter Massenversand. Vergleich über **SHA-256 + `timingSafeEqual`** statt Längenprüfung + Vergleich (anders als `verifyUnsubscribe`: dort ist die erwartete Länge eine öffentliche Eigenschaft des Verfahrens, hier eine des Geheimnisses).
  - **[EINE VERWEIGERUNG IST HTTP 200, EIN ABBRUCH 500]** `refused` ist das vorgesehene Verhalten und darf keinen Wiederholungsversuch auslösen, der genauso ausginge; die Kennzeichnung steht im Rumpf (`refused: true`), im Log und im Klartext im Admin-Bereich. `error` (der Lauf wurde per Sicherungspunkt vollständig zurückgenommen — nie ein halber Stapel) antwortet 500, damit er in Vercel als fehlgeschlagener Lauf erscheint.
  - **[ADMIN-SICHTBARKEIT ERSETZT DEN B1-3-HINWEIS]** „Löschfristen werden derzeit manuell durchgesetzt" ist weg; an derselben Stelle steht jetzt der Stand des Laufs (zuletzt erfolgreich, wie viele Fälle gesehen/bearbeitet). **Nach 48 Stunden ohne erfolgreichen Lauf wird die Zeile hervorgehoben** — das ist der eigentliche Zweck des Protokolls: ein ausbleibender Job soll auffallen, ohne dass jemand danach sucht. `admin_list_job_runs` ermittelt `last_success` GETRENNT von der Liste, sonst hinge „zuletzt erfolgreich am …" an der Fenstergrösse und behauptete nach genug misslungenen Läufen „noch nie erfolgreich". **Kein Auslöseknopf:** ein Mensch, der versehentlich einen unumkehrbaren Massenvorgang startet, ist ein Risiko ohne Gegenwert.
  - **[RECHTE: DER JOB BEKOMMT KEIN EINZIGES NEUES TABELLEN-GRANT]** B1-1 hatte `service_role` bewusst nirgends `delete` gegeben, mit dem Satz „der Löschjob … bekommt sein Recht, wenn er gebaut wird". Antwort: er bekommt gar keins — er arbeitet vollständig in SECURITY-DEFINER-Funktionen. `service_role` darf genau eine neue Sache: `public.run_lead_retention_job` AUFRUFEN. `platform.job_runs` hat RLS, keine Policy und für keine Rolle ein Grant. Die eslint-Allowlist (`no-restricted-imports`) wurde um `apps/web/app/api/cron/**` **erweitert, nicht umgangen** — strukturell derselbe Fall wie der Stripe-Webhook.
  - **[VERCEL-PLAN GEPRÜFT, nicht geraten]** Team liegt auf **Pro**: 100 Cron-Jobs je Projekt, Mindestintervall eine Minute, **minutengenaue** Auslösung — `15 3 * * *` läuft tatsächlich um 03:15 (auf Hobby wären es ±59 min). `apps/web/vercel.json` behält seinen `ignoreCommand` und bekommt den Cron-Eintrag ergänzt. **Die Registrierung hängt am Production-Deployment, nicht an der Datei** — per API gegengeprüft, nicht angenommen.
  - **[VERIFIKATION]** DB-Gate **199/199** (185 bestehende + **14 neue** in `packages/db-tests/src/job-runs-lead-retention.test.ts`): Fälligkeit trennt Vergangenheit/Zukunft/bereits anonymisiert · `p_limit` begrenzt, älteste Frist zuerst · oberhalb der Obergrenze bleiben alle 3 Leads unangetastet und der Lauf steht auf `refused` · die Stapelgrenze schneidet ab UND sagt es im Detailfeld · nach dem Lauf `anonymized_by_system = true`, `anonymized_by = null`, E-Mail auf `anonymized+<id>@invalid` · der CHECK weist „System UND Konto" ab · `p_by_system` mit `p_actor` wirft VOR jeder Wirkung · die Kennzeichnung ist für `service_role` UND `postgres` unveränderlich · drei Läufe = drei Zeilen (leer/verweigert/erfolgreich), keiner offen · `admin_list_job_runs` liefert den jüngsten und den zuletzt erfolgreichen getrennt · Nicht-Admin scheitert mit **42501** statt mit leerer Antwort · Grant-Fläche exakt `service_role` bzw. `authenticated` · die drei `platform`-Funktionen sind von aussen gar nicht aufrufbar. Anwendungsseitig `pnpm --filter web test` **38/38** (31 bestehende + **7 neue**): ohne Kopfzeile / mit falschem Geheimnis / ohne `Bearer`-Präfix / **ohne gesetztes `CRON_SECRET`** jeweils 401, und der service_role-Client wird nachweislich NIE angefasst (ohne Client kein RPC, ohne RPC kein Laufdatensatz). `pnpm lint`/`typecheck`/`build`/`test` root-weit grün, `supabase/database.types.ts` neu generiert.
  - **[VERIFIKATION — echte HTTP-Läufe gegen den Production-Build, Port 3400]** Ohne Kopfzeile `401 {"error":"unauthorized"}` · falsches Geheimnis `401` · richtiges Geheimnis `200` mit echtem Lauf (`outcome=success, itemsConsidered=0, itemsProcessed=0`, `startedAt` ≠ `finishedAt` — die `clock_timestamp()`-Umstellung wirkt) · erfundener Pfad unterhalb `/api/cron` → **404**. Der Unterschied 401 vs. 404 ist der Beweis, dass die Route existiert und greift.
  - **[OFFEN — betrifft den Betrieb, nicht den Code]** `CRON_SECRET` muss in Vercel (Production) gesetzt werden, sonst antwortet der Endpunkt 401 und der Fristenlauf findet nicht statt. Sichtbar wird das im Admin-Bereich (48-Stunden-Hervorhebung). **✔ Der Wert ist gefahrlos rotierbar** — im Gegensatz zu `LEAD_TOKEN_SECRET`: er ist zustandsbehaftet nur zwischen Vercel und dem Endpunkt, es hängen keine bereits versendeten Links daran. Beschrieben in `DEPLOYMENT.md` §1g und `apps/web/.env.example`.

- **[GEBAUT: B3-2 — Erfassungskomponente, Registry und erste Einsatzorte (Fahrplan B3)]** Der erste ÖFFENTLICH SICHTBARE Teil des Lead-Fundaments: eine einbettbare Erfassungskomponente, EIN getypter Katalog aller Einstiegspunkte, EINE Server Action für alle — und drei echte Platzierungen. Migration `supabase/migrations/20260722090000_lead_capture_single_opt_in.sql` (`capture_lead` neu gefasst + Backfill) + `apps/web/lib/leads/{registry,capture-request,capture-flow,capture-action,capture-texts}.ts` + `lib/schnellrechner.ts` + `components/leads/lead-capture-form.tsx` + drei Einsatzorte. **NICHT dabei:** der Betroffenheits-Check (B3-3, blockiert auf die Branchenkennzahlen), gefilterte Sicht/Export/Versand/Bearbeitbarkeit (B2), zeitgesteuerte Jobs und die Vertragsablauf-Landingpage (B4), `tenant_id` (B13).
  - **[FEHLER DER B1-2-SPEZIFIKATION, KORRIGIERT — der eigentliche Anlass der Migration]** `capture_lead` legte JEDE Einwilligung mit `status='pending'` an, auch bei Zwecken OHNE Bestätigungspflicht. Für `result_delivery` blieb sie damit DAUERHAFT pending (es gibt keinen Schritt, der sie hebt — `confirm_consent` braucht einen Token, den es dort nicht gibt), `has_confirmed_consent` lieferte false, und die Versandprüfung blockierte genau die Zusendung, um die die Person gebeten hatte. **'pending' heisst „wartet auf Bestätigung" — bei einem Zweck ohne Bestätigungspflicht wartet nichts, und der Zustand wäre lediglich eine Sperre gegen die eigene Erfüllung** (so als Kommentar in der Migration). Folgenlos geblieben, weil bis dahin nur das Kontaktformular schrieb, und das schreibt ausschliesslich `marketing_email`.
  - **[NEUER AUSGANG `consent_confirmed` — damit der Anwendungscode nicht falsch abzweigen KANN]** Nicht bestätigungspflichtiger Zweck → Einwilligung sofort `confirmed` mit `confirmed_at=now()`, Rückgabe `consent_confirmed`; bestätigungspflichtiger Zweck unverändert `pending` + Token, Rückgabe `consent_created`. Ohne den zweiten Wert müsste der Anwendungscode aus dem ZWECK zurückrechnen, ob Bestätigungsmail oder Lieferung fällig ist — also `purpose_requires_double_opt_in` ein zweites Mal in TypeScript auslegen. **Ein übergebener Token wird bei nicht bestätigungspflichtigen Zwecken NICHT gespeichert** (weder Hash noch Ablauf): er wäre ein einlösbares Geheimnis ohne Einlösestelle. Die `consent_already_pending`-Prüfung greift jetzt NUR noch bei bestätigungspflichtigen Zwecken — sie schützt gegen Bestätigungsmail-Fluten und hätte sonst ausgerechnet die sofortige Lieferung verweigert. `create or replace` bei unveränderter Signatur → die B3-1-Grants bleiben unangetastet (kein DROP+CREATE nötig).
  - **[BACKFILL, idempotent] `confirmed_at = granted_at`, NICHT `now()`.** Die Person hat damals eingewilligt, und für diese Zwecke war die Erteilung immer schon der vollständige Vorgang. Ein Zeitstempel von heute behauptete eine Handlung, die heute niemand vorgenommen hat — dieselbe Fälschung, die `guard_consent_confirmation` bei den bestätigungspflichtigen Zwecken hart verhindert. `withdrawn`/`expired` bleiben unberührt: ein Widerruf ist eine Handlung der Person und wird nicht durch eine Migration zurückgenommen.
  - **[REGISTRY: EIN Modul für Darstellung UND Prüfung]** `lib/leads/registry.ts` legt je Einstiegspunkt Zweck, Felder (mit Pflichtkennzeichen) und die Existenz von vier kontextspezifischen Texten fest. Dieselbe Definition speist das Formular im Client UND das zod-Schema auf dem Server (`capture-request.ts` leitet es ab) — zwei Quellen liefen auseinander: das Formular zeigte ein Feld, das der Server verwirft, oder umgekehrt. **Die Datei ist bewusst ABHÄNGIGKEITSFREI** (kein `server-only`, kein `next/*`, kein `@/`-Alias, kein zod), damit das DB-Gate sie über einen RELATIVEN Pfad importieren kann. `lib/leads/config.ts` beweist beim Typecheck, dass die dort literal geführte Zweck-Union deckungsgleich mit dem generierten DB-Enum ist.
  - **[DER SYNC-TEST IST DER KERN DER REGISTRY]** `packages/db-tests/src/lead-source-registry.test.ts` prüft in BEIDE Richtungen, dass die Schlüssel-Union genau den AKTIVEN Zeilen in `platform.lead_sources` entspricht. `lead_sources` ist eine Tabelle und kein Enum (B1-1) — die Datenbank verhindert die Drift also NICHT. Fehlt ein Schlüssel im Code, fällt das auf; fehlt er in der DB, scheitert erst der erste echte Aufruf am FK; und trägt ein Eintrag den Schlüssel eines ANDEREN Einstiegspunkts, schreibt er still unter falscher Herkunft in den Bestand — der Lead ist da, aber die Kanal-Auswertung ist falsch, und das merkt niemand.
  - **[ZEHN EINTRÄGE, DAVON DREI PLATZIERT]** Alle zehn `lead_sources` haben einen Eintrag. Platziert: `artikel-inline` (im Flaggschiff-Artikel `content/wissen/leistungstarif-2027.de.mdx`, am Ende nach „Was bleibt offen" — der Artikel schliesst mit „die Beträge stehen noch nicht fest", und genau dort passt „wir schreiben Ihnen, sobald sich das ändert"), `branchenseite` (auf **`/branchen/handwerk`** — der Fall, den der Artikel wörtlich durchrechnet, und die einzige Branchenseite, auf die er verlinkt), `rechnerergebnis` (unter dem Schnellrechner auf **`/peak-shaving`** — von dessen vier Einbettungen die einzige, bei der der Rechner der Zweck des Abschnitts ist und nicht Beiwerk). Die anderen vier Einbettungen des Schnellrechners und die vier übrigen Branchenseiten bleiben unverändert.
  - **[BEWUSST NICHT PLATZIERT, mit Begründung am Registry-Eintrag]** `betroffenheits-check`: die Branchenkennzahlen (Vollbenutzungsstunden) liegen nicht vor — ein Formular, das Branche, PLZ und Verbrauch erhebt, aber keine belastbare Betroffenheit zurückgeben kann, sammelt Daten für eine Auskunft, die es nicht gibt. `vertragsablauf-landing`: die Erinnerung selbst entsteht erst mit B4 — **ein Vertragsende zu erfassen und die versprochene Erinnerung nicht senden zu können, ist ein gebrochenes Versprechen an eine reale Person, kein Terminproblem.** Beide Einträge rendern trotzdem korrekt; die Platzierung ist eine getrennte Entscheidung.
  - **[ZWECKBINDUNG STRUKTURELL, NICHT PER DISZIPLIN]** Der Contract (`LeadCaptureSubmission`) hat **gar kein `purpose`-Feld** — der Zweck kommt ausschliesslich über den `source_key` aus der Registry. Begründung im Code: andernfalls erzeugte ein manipulierter Aufruf eine Marketing-Einwilligung von einem Einstiegspunkt, der den Marketing-Text nie angezeigt hat; der Nachweis wäre wertlos, und zwar rückwirkend auch für die echten. Ein unbekannter Schlüssel endet in einer neutralen Meldung **ohne Ersatzwert** (ein Fallback schriebe unter falscher Herkunft). Ein angekreuztes Marketing-Häkchen wirkt nur dort, wo der Eintrag es anbietet — es wird sonst still verworfen, nicht gemeldet: ein manipulierter Aufruf soll nichts bewirken, nicht auffallen.
  - **[EIN ABLAUF, DREI SCHICHTEN]** `capture-flow.ts` trifft die Entscheidungen und ist REIN (Datenbank, Mail und Token kommen als `LeadCaptureEffects` herein) — dadurch sind die anwendungsseitigen Pflichttests ohne laufenden Stack möglich. `capture-action.ts` ('use server') ist nur Verdrahtung. **Verzweigt wird strikt am `outcome`**, nie am Zweck: `consent_created` → Bestätigungsmail, `consent_confirmed` → sofortige Lieferung, alles andere → kein Versand. **Zwei Einwilligungen sind zwei `capture_lead`-Aufrufe**, nicht ein Aufruf mit zwei Zwecken — jede trägt ihre eigene Textfassung, ihren eigenen Zeitpunkt und ggf. ihren eigenen Token (B1-1: die Historie IST der Nachweis).
  - **[RÜCKMELDUNG IST IN ALLEN FÄLLEN IDENTISCH]** Gesperrte Adresse, bereits laufende Bestätigung, Datenbankausfall, glatter Erfolg — dieselbe Antwort (`ACCEPTED`, als Konstante, damit kein Pfad eine zweite Form erfindet). Unterschieden wird ausschliesslich, was der Absender selbst sieht und ändern kann: seine Feldeingaben. Sonst wäre jedes eingebettete Formular ein Auskunftsdienst über fremde Kontakte.
  - **[MISSBRAUCHSSCHUTZ: DERSELBE WEG, KEIN ZWEITER]** Honeypot (immer aktiv, per Position ausgeblendet statt `display:none`) + `verifyTurnstile` aus `lib/kontakt/turnstile.ts` — dieselbe env-gated Funktion wie das Kontaktformular, nicht nachgebaut. Honeypot lehnt AB statt still 200 zu liefern (dieselbe begründete Abweichung von der Lehrbuch-Empfehlung wie in `app/api/kontakt/route.ts`).
  - **[TEIL 5 — Zusendung des Ergebnisses]** `sendCalculatorResultMail` (`lib/leads/mail.ts`, gemeinsamer `deliver`-Versandweg mit der Bestätigungsmail). **KEIN PDF, keine Anlage** — das Versprechen lautet „Ergebnis per E-Mail" und wird damit vollständig eingelöst. **Der Server RECHNET SELBST NACH** (`lib/schnellrechner.ts`, aus `components/quick-calculator.tsx` herausgezogen: Formel + die zwei Intl-Formate) und übernimmt keine fertige Zahl vom Client — sonst wäre die Zahl in unserer eigenen Mail frei wählbar. Beschriftungen, Formel und Beträge stammen aus denselben `QuickCalculator.*`-Keys wie der Bildschirm; wurde die Zielreduktion auf die Spitze geklemmt, steht der Hinweis MIT in der Mail (sonst folgte die Zahl nicht aus den daneben gezeigten Eingaben). **Ein Fehlversand wird laut geloggt und bricht nichts ab**, der Nutzer sieht dieselbe Erfolgsmeldung: ein verlorenes Ergebnis wiegt leichter als ein verlorener Lead.
  - **[ISR STATT STATISCH auf den drei Seiten]** `/peak-shaving`, `/branchen/handwerk` und `/wissen/[slug]` bekommen `revalidate = 3600` — dasselbe Muster und dieselbe Stunde wie `/kontakt` (B1-2). Der angezeigte Einwilligungswortlaut kommt aus `platform.consent_texts` (append-only); eine juristisch geprüfte `version 2` soll ohne Deploy durchschlagen, weil genau dieser Satz archiviert wird. **Fail-closed:** fällt das Lesen aus (keine Env im CI-Build, DB nicht erreichbar), rendert die Komponente NICHTS und die Seite läuft unverändert weiter. **Die drei Seiten bleiben indexierbar — kein `noindex`**, es sind öffentliche Marketingseiten.
  - **[TEIL 7 — brüchiger Test geradegezogen, mit begründeter Erweiterung der Aufgabenstellung]** `packages/db-tests/src/admin-rpc-wrappers.test.ts` verglich an mehreren Stellen eine BESTANDSWEITE Admin-Anzahl. Die Umstellung auf eigene Fixtures (`hasAdminRole`) allein hätte **nicht gereicht**: der Lockout-Guard in `admin_revoke_role` zählt die ganze Tabelle, das erwartete VERHALTEN (`last_admin`) hängt also selbst an der Alleinstellung — bei einem fremden Admin liefert der Wrapper `ok`, und der Test wäre trotz umgestellter Assertions rot. Deshalb zusätzlich `inIsolatedAdminWorld()`: die zwei Lockout-Tests laufen in EINER Transaktion, in der fremde admin-Zeilen entfernt sind, und ein `rollback` stellt sie vollständig wieder her (der Bestand bleibt unangetastet). **Gegenprobe gemessen:** mit einem absichtlich hinterlassenen Admin schlägt die ALTE Fassung an genau den drei Zähl-Stellen fehl (3 von 23 Tests), die neue ist grün, und der fremde Admin steht danach unverändert in der Tabelle.
  - **[VERIFIKATION — DB-Gate]** **185/185** (172 bestehende + **8 neue** in `lead-single-opt-in.test.ts` + **5 neue** in `lead-source-registry.test.ts`): `result_delivery` entsteht sofort `confirmed` mit `confirmed_at` und `outcome='consent_confirmed'` · `has_confirmed_consent` liefert dafür true ohne Bestätigungsschritt · ein übergebener Token wird NICHT gespeichert und steht bestandsweit nirgends · `marketing_email` bleibt `pending` mit Token, `has_confirmed_consent` false · ein bestätigungspflichtiger Zweck ohne Token wird weiter hart abgelehnt · der Backfill (WÖRTLICH dieselbe Anweisung, gegen privilegiert hergestellte Alt-Zeilen) stellt nur nicht bestätigungspflichtige pending-Zeilen um, setzt `confirmed_at = granted_at`, lässt `marketing_email` und `withdrawn` unberührt und ist idempotent · bestandsweit ist keine solche Alt-Zeile übrig · Registry ↔ `lead_sources` in beide Richtungen deckungsgleich.
  - **[VERIFIKATION — anwendungsseitig]** `pnpm --filter web test` **31/31** (13 bestehende + 14 in `capture-flow.test.ts` + 4 in `registry.test.ts`): unbekannter `source_key` erzeugt keinen Lead (die Datenbank wird nicht einmal angefasst) · ein mitgeschickter Zweck ändert den aus der Registry abgeleiteten nicht · die Rückmeldung ist bei gesperrter, bereits bekannter und unbekannter Adresse identisch — auch bei komplettem Datenbankausfall · gesperrte Adresse löst keinen Versand aus · `consent_created` schickt NUR die Bestätigungsmail, nie die Leistung · zu jedem Registry-Eintrag existieren alle vier Texte, und es gibt keinen verwaisten Textblock.
  - **[VERIFIKATION — Playwright gegen den Production-Build, voller Absende-Flow]** `/peak-shaving`: Absenden → DB `rechnerergebnis|result_delivery|confirmed|<Zeitstempel>|kein Token|has_confirmed_consent=true`, Erfolgsmeldung „Ihr Ergebnis ist unterwegs". Mit zusätzlich angekreuzter Marketing-Einwilligung (**Häkchen nachweislich NICHT vorausgewählt**): zwei Zeilen — `marketing_email:pending:mit-token` UND `result_delivery:confirmed:ohne-token`. `/wissen/leistungstarif-2027`: `artikel-inline|marketing_email|pending|mit-token|false`, `consents.source_key='artikel-inline'`, Hinweis auf die Bestätigungsmail. `/branchen/handwerk`: `branchenseite|marketing_email|pending`. **Gesperrte Adresse: 1 Lead, 0 Einwilligungen, identischer Erfolgstext.** Ungültige Adresse → feldgenaue Meldung. 0 Konsolenfehler.
  - **[OFFEN, rechtlich — unverändert aus B1-1]** Der Wortlaut der Einwilligungstexte ist Arbeitsstand; die juristische Prüfung steht aus (Owner Martin). Mit den drei jetzt platzierten Einstiegspunkten werden diese Sätze erstmals öffentlich gezeigt und archiviert — eine geprüfte Fassung kommt als NEUE Zeile mit `version 2`, Anzeige UND Archivierung ziehen automatisch nach.

- **[GEBAUT: B3-1 — Segmentierungsspalten und Erweiterung des Erfassungspfads (Fahrplan B3)]** Die Dimensionen, auf denen B2 später segmentiert. B1 hatte sie ausdrücklich ausgeklammert („die kommen GETYPT mit B3, wenn der Betroffenheits-Check definiert, was genau erhoben wird" — Kommentar in der B1-1-Migration). Migration `supabase/migrations/20260721210000_create_lead_segmentation_columns.sql` (1 Enum, 6 Spalten, 3 Teilindizes, 1 neuer Trigger, 2 nachgezogene Funktionen, `capture_lead` + `admin_get_lead` erweitert, 5 neue `lead_sources`) + `apps/web/lib/admin/leads.ts` + `components/admin/ui.tsx` + die Detailseite. **NICHT dabei:** die Erfassungskomponente und ihre Einsatzorte (B3-2), der Betroffenheits-Check (B3-3, blockiert auf fachlichen Input), gefilterte Sicht/Export/Versand (B2), zeitgesteuerte Jobs (B4), `tenant_id` (B13).
  - **[ENUM vs. TABELLE — die Regel aus B1-1, andersherum angewandt]** `platform.industry` ist ein **Enum** (baeckerei · gastronomie · handel · hotellerie · tischlerei · landwirtschaft · kuehlhaus · metallverarbeitung · buero_dienstleistung · sonstige), weil der Anwendungscode je Branche eine **Vollbenutzungsstunden-Kennzahl** braucht (B3-3). Eine neue Branche ist dadurch zwangsläufig ein gemeinsames Code- UND Migrationsereignis — genau das verhindert, dass eine Branche im Auswahlfeld erscheint, für die keine Kennzahl hinterlegt ist (der Fall, der niemandem auffiele, weil die Rechnung trotzdem eine Zahl ausspuckt). `lead_sources` bleibt das Gegenbeispiel: Tabelle, weil der Code die Einstiegspunkte NICHT kennen muss.
  - **[GETYPTE SPALTEN, ausdrücklich KEIN jsonb]** Alle sechs nullable (die Einstiegspunkte sind kontextspezifisch und erheben unterschiedliche Felder — eine NOT-NULL-Spalte zwänge jeden Einstiegspunkt, ein Feld zu erfragen, das er fachlich nicht braucht). CHECKs: PLZ auf **genau vier Ziffern**, Jahresverbrauch **> 0**, `metering_type in ('leistungsgemessen','netzebene_7','unknown')`. Ein `attributes jsonb` wäre schneller geschrieben und hier falsch: B2 muss darauf FILTERN und Trefferzahlen in SQL bilden — über jsonb gäbe es weder Typ noch CHECK noch brauchbaren Index, „Verbrauch grösser als" wäre ein Textvergleich, und ein Tippfehler im Schlüssel erzeugte still eine leere Menge statt eines Fehlers.
  - **[`metering_type` wird ABGELEITET UND GESPEICHERT, nicht bei jedem Lesen neu gerechnet]** B2 segmentiert in SQL darauf (leistungsgemessen vs. Netzebene 7 ist die zentrale Zielgruppen-Trennung des Marktstarts 2027). Eine Ableitung zur Lesezeit wäre nicht indizierbar und änderte rückwirkend die Zuordnung eines Leads, sobald die Regel justiert wird — der Bestand bewegte sich unter der laufenden Aussendung weg. **`'unknown'` ist ein ECHTES Ergebnis** („geprüft, nicht bestimmbar") und deshalb ein eigener Wert; `null` heisst „nie geprüft". Die Oberfläche hält beides auseinander.
  - **[NEUER TRIGGER `clear_contract_data_on_withdrawal` — Zweckbindung wird DURCHGESETZT, nicht behauptet]** AFTER UPDATE auf `consents`: wechselt eine Einwilligung mit `purpose='contract_expiry_reminder'` auf `withdrawn`, nullt die DB `supplier` und `contract_end_date` des Leads. Versorger und Vertragsende werden für genau diesen Zweck erhoben (der B1-1-Einwilligungstext sagt es wörtlich) — fällt der Zweck weg, fällt die Grundlage für die Daten weg, nicht nur die Erlaubnis, sie zu benutzen. **Bewusst NICHT bei `status='expired'`:** ein verfallener Double-Opt-in-Token ist ein technischer Zustand (B1-2 räumt lazy ab), kein Widerruf — die Person hat nichts zurückgenommen und kann die Bestätigung erneut anfordern; dann wären die Daten weg, die sie gerade angegeben hat. Andere Zwecke lassen die Felder unberührt. Der vollständige Rückzug (`suppress_email_and_withdraw_all`) trifft alle Zwecke und räumt sie dadurch von selbst mit ab — **kein zweiter Mechanismus**.
  - **[DIE ZUSAMMENFÜHRUNGSSEMANTIK IST DER KERN — und der wahrscheinlichste stille Datenverlust]** `capture_lead` bekam sechs Parameter mit Vorgabewert `null`, **angehängt** nach `p_locale` (kein bestehender Aufruf bricht). Bei wiederholter Erfassung gilt `coalesce(NEU, BESTAND)`: ein übergebener Wert überschreibt, **ein null-Wert lässt den bestehenden UNBERÜHRT**. Ohne diese Regel löschte der zweite Kontakt still, was der erste erbracht hat — dieselbe Person wird über mehrere Einstiegspunkte erfasst, die unterschiedliche Felder erheben (Betroffenheits-Check: Branche + Verbrauch; Vertragsablauf-Seite: Versorger + Ablaufdatum). Kein Fehler, kein Log, sichtbar erst beim ersten Segmentierungslauf an einer unerklärlich kleinen Menge. Zwei db-tests pinnen es.
  - **[BEWUSSTER UNTERSCHIED zu company/contact_name/phone]** Die B1-2-Identitätsfelder benutzen die UMGEKEHRTE Vorrangregel (`coalesce(BESTAND, neu)` — Bestand gewinnt). Beide schützen gegen Verlust durch eine knappere zweite Absendung; sie unterscheiden sich, was bei ZWEI Angaben gilt. Bei Firma und Telefon ist die frühere Angabe die verlässlichere (beim zweiten Mal wird weniger sorgfältig getippt, eine Firmierung ändert sich selten); bei Verbrauch, Versorger und Vertragsende ist die JÜNGERE die richtige — sie ist genau das, was sich ändert, und eine Erinnerung an ein längst ersetztes Vertragsende wäre wertlos. In der Migration ausformuliert, damit es niemand „vereinheitlicht".
  - **[DROP + CREATE statt `create or replace`]** `create or replace` kann die Parameterliste nicht erweitern, ein blosses CREATE erzeugte eine ZWEITE Überladung — ein Aufruf mit den bisherigen elf Argumenten wäre dann mehrdeutig („function is not unique") und der Erfassungspfad läge lahm. Folge: Supabases ALTER-DEFAULT-PRIVILEGES greifen wieder, deshalb erneut `revoke all … from public, anon, authenticated, service_role` + `grant … to service_role`.
  - **[BESTEHENDE INVARIANTEN NACHGEZOGEN]** `guard_anonymized_lead` schützt jetzt **13 statt 7 Spalten** — ohne die sechs neuen liefe der Guard an seiner eigenen Erweiterung vorbei: ausgerechnet PLZ und Versorger, deren Entfernung die Anonymisierung ausmacht, liessen sich nachträglich wieder füllen. `platform.anonymize_lead` nullt zusätzlich `postal_code`/`supplier`/`contract_end_date`, **erhält aber `industry`/`annual_consumption_kwh`/`metering_type`**. Die Trennlinie verläuft entlang **„lokalisierend" gegen „grob einordnend"**, nicht entlang „geschäftlich nützlich": PLZ + Branche + Versorger zusammen erkennen einen Betrieb wieder (in einem 4-Ziffern-Gebiet gibt es selten zwei Kühlhäuser mit 180 MWh), Branche + Verbrauchsgrösse + Messart allein nicht — die bleiben als statistische Merkmale nutzbar.
  - **[TEILINDIZES auf `anonymized_at is null`]** B-Tree auf industry/metering_type/postal_code, partiell — ein anonymisierter Lead taucht in keiner Segmentierung auf. Der Index bleibt dadurch so gross wie der ANSCHREIBBARE Bestand, nicht wie der Gesamtbestand (der wächst, weil die Anonymisierung Zeilen nicht löscht).
  - **[SEED: fünf neue Einstiegspunkte]** `betroffenheits-check` · `rechnerergebnis` · `artikel-inline` · `branchenseite` · `vertragsablauf-landing`, idempotent; die fünf B1-1-Zeilen unverändert. Stehen hier und nicht in B3-2, damit die Erfassungskomponente **ohne eigene Migration** auskommt: `first_source_key` ist ein FK, ein Einstiegspunkt ohne Zeile könnte gar keinen Lead anlegen.
  - **[ADMIN-ANZEIGE, bewusst minimal]** `admin_get_lead` liefert die sechs Felder mit, `/admin/leads/[id]` zeigt sie als eigenen Abschnitt „Betriebsdaten" — **nur Darstellung, keine Filter, keine Bearbeitbarkeit** (die gefilterte Sicht ist B2; ein editierbares Feld bräuchte einen eigenen Schreibwrapper samt Begründung, warum ein Admin eine Angabe überschreiben darf, die die Person selbst gemacht hat). Grund für die Aufnahme jetzt: **ohne Anzeige lässt sich am ersten echten Lead nicht prüfen, ob die Felder ankommen** — ein Schreibpfad, den niemand nachsehen kann, ist ein unbewiesener Schreibpfad. `admin_list_leads` bleibt unangetastet. Texte im Code, dem T4-4-/B1-3-Muster folgend (nicht `messages/de.json`).
  - **[VERIFIKATION]** DB-Gate **172/172** (156 bestehende + **15 neue** in `packages/db-tests/src/lead-segmentation.test.ts` + **1 neuer** in `lead-admin-wrappers.test.ts`; die bestehende „unveränderlich"-Prüfung dort um die sechs Spalten ERWEITERT, nicht ersetzt): Enum trägt genau die zehn Werte in der vereinbarten Reihenfolge · die fünf neuen `lead_sources` sind da und die fünf aus B1-1 unberührt · PLZ „110"/„11000"/„1100 Wien"/„A-1100"/„1a00" werden abgewiesen, ein leer abgesendetes Feld wird zu `null` (und überschreibt dadurch nichts) · Verbrauch 0/−1 abgewiesen · unbekannte Messart abgewiesen statt still bereinigt · `capture_lead` schreibt alle sechs Felder · **zweiter Aufruf mit null-Werten löscht NICHTS** · zwei Einstiegspunkte ERGÄNZEN sich zum vollständigen Lead · übergebene Werte überschreiben · die B1-2-Parameter verhalten sich unverändert (Bestand gewinnt bei Identitätsfeldern, `consent_already_pending` weiter korrekt) · Widerruf von `contract_expiry_reminder` nullt beide Felder und lässt die anderen vier stehen · Widerruf von `marketing_email` lässt beide unberührt · **ein abgelaufener Token räumt NICHT ab** · der vollständige Rückzug räumt ab · anonymisierter Lead ist in allen **13** Feldern für `service_role` unveränderlich · `anonymize_lead` nullt die drei lokalisierenden und erhält die drei einordnenden. `pnpm lint`/`typecheck`/`build`/`test` root-weit grün, `supabase/database.types.ts` neu generiert.
  - **[VERIFIKATION — Playwright gegen den echten Dev-Server, voller angemeldeter Flow]** Anonym `/admin/leads` und `/admin/leads/<id>` → **307 auf `/anmelden`, kein 404**. Angemeldet zeigt die Detailseite alle sechs Felder korrekt: Branche „Kühlhaus" · PLZ 1100 · Jahresverbrauch „184 500 kWh" · Messart „Netzebene 7" · Versorger „Wien Energie" · Vertragsende 31.03.2027. Danach über die ECHTE Schaltfläche „Einwilligung widerrufen" der Vertragsablauf-Erinnerung: Versorger und Vertragsende stehen sofort auf „—", **die anderen vier unverändert** — der Trigger wirkt bis in die sichtbare Oberfläche. Keine Konsolenfehler.

- **[GEBAUT: B1-3 — Admin-Abschnitt „Leads", Anonymisierung und Statuspflege (Fahrplan B1) — B1 DAMIT ABGESCHLOSSEN]** Die dritte und letzte Zugriffsrichtung auf den Lead-Bestand: der ANGEMELDETE Admin. **Erstmals schreibt ein eingeloggter Nutzer in `platform`-Lead-Daten** — bis B1-2 konnte `authenticated` dort ausschliesslich lesen. Migration `supabase/migrations/20260721180000_create_lead_admin_wrappers.sql` (2 Trigger, 1 Spalte, 1 Ableitungsfunktion, `platform.anonymize_lead`, 6 neue `public`-Wrapper **alle authenticated-only**, `admin_list_leads` um Filter erweitert) + `apps/web/lib/admin/leads*.ts` + `components/admin/{nav,lead-actions,suppression-lookup}.tsx` + drei Routen. **NICHT dabei:** Export, Versand, Zustellprotokoll, Segmentierung nach Branche/Netzebene/PLZ (alles B2 — die Spalten dafür entstehen erst mit B3), kein zeitgesteuerter Job (B4), kein `tenant_id` (B13).
  - **[DIE NEUE PRIVILEGIENGRENZE IN EINEM SATZ]** Der Admin darf WIDERRUFEN, SPERREN, den Lebenszyklus pflegen und ANONYMISIEREN — er darf **keine Einwilligung erteilen und keine bestätigen**. **ES GIBT DAFÜR BEWUSST KEINEN WRAPPER** (als Kommentar in der Migration und als Satz auf der Detailseite): eine Oberfläche, in der sich „bestätigt" ankreuzen lässt, entwertet den gesamten Nachweis rückwirkend — auch die echten Einwilligungen, weil ab dann keine mehr von einer gesetzten unterscheidbar ist. Der einzige Weg zu `confirmed` bleibt `public.confirm_consent` (B1-2), also der Klick der Person in ihrer eigenen Mailbox. Aus demselben Grund gibt es auch keinen Wrapper, der einen Lead ANLEGT.
  - **[ZWEI NEUE TRIGGER — Invarianten gehören in die DB, nicht in die Admin-UI]** `sync_retention_basis_on_customer` (Status → 'customer' setzt `retention_basis='commercial'`; **der Rückweg commercial → marketing WIRFT** — eine einmal entstandene kaufmännische Aufbewahrungspflicht endet nicht dadurch, dass ein Kunde abspringt) und `guard_anonymized_lead` (ist `anonymized_at` gesetzt, sind email/company/contact_name/phone/status/retention_basis unveränderlich — auch für service_role und für `postgres`). **`anonymized_at` steht ZUSÄTZLICH zur Aufgabenstellung mit unter Schutz:** ohne das liesse sich der Guard mit seiner eigenen Bedingung abschalten (`set anonymized_at = null` wäre durchgelaufen, danach wäre die Zeile wieder frei änderbar). `anonymized_by` bewusst NICHT — die Spalte trägt `ON DELETE SET NULL`, ein Schutz blockierte genau diesen Pfad.
  - **[TRIGGER-REIHENFOLGE IST ABLAUFSTEUERUNG, KEIN GESCHMACK]** Postgres feuert BEFORE-ROW-Trigger **alphabetisch nach Triggernamen**. `leads_customer_retention_basis` muss VOR `leads_sync_retention` laufen (sonst rechnete die Frist noch mit der alten Rechtsgrundlage), `leads_protect_anonymized` NACH `leads_normalize_email` (sonst läse der Guard „Max@x.at" gegen „max@x.at" als Änderung). Der naheliegende Name `leads_sync_retention_basis` wäre **falsch** gewesen — er sortiert HINTER `leads_sync_retention` (gleiches Präfix, länger). Steht als Block-Kommentar in der Migration.
  - **[ANONYMISIERUNG — was geht, was bleibt]** `platform.anonymize_lead(lead, actor)`: E-Mail → `anonymized+<lead_id>@invalid` (RFC 2606, nie zustellbar; **ersetzt statt geleert**, weil die Spalte NOT NULL ist und der UNIQUE über die normalisierte Adresse halten muss — ein fester Platzhalter für alle scheiterte beim ZWEITEN Lead mitten in der Löschung), company/contact_name/phone → null, `source_ip`/`user_agent` ALLER Einwilligungen → null. **BLEIBEN:** die Einwilligungszeilen selbst (Zweck, Textfassung, Zeitpunkte — ohne Identitätsmerkmale kein Personenbezug mehr, aber weiterhin der Beleg, dass korrekt gearbeitet wurde) und **`platform.email_suppressions` unangetastet** (Kern des B1-1-Entwurfs: die Sperre überlebt die Löschung). Idempotent, `anonymized_at` bleibt der ERSTE Zeitpunkt. Reihenfolge im Code: erst consents, dann lead — sonst liefe der B1-1-Trigger `touch_lead_on_consent` gegen den frisch gesetzten Guard.
  - **[NEUE SPALTE `platform.leads.anonymized_by` mit `ON DELETE SET NULL`, begründete Abweichung von der wörtlichen Vorgabe]** Eine Spalte, keine Audit-Tabelle (der Vorgang ist je Lead einmalig und endgültig — es gibt keine Historie zu entfalten). Die drei Verhalten beim Löschen des handelnden Kontos: `cascade` löschte den LEAD, weil jemand sein Konto schliesst (absurd); die Voreinstellung `no action` BLOCKIERTE das Löschen des Kontos an fremden Lead-Zeilen; `set null` lässt das Konto gehen und behält `anonymized_at` — der VORGANG bleibt belegt, nur die Zuschreibung entfällt. Die Oberfläche sagt dann „durch ein inzwischen gelöschtes Konto".
  - **[SECHS WRAPPER, alle authenticated-only, alle werfen 42501]** `admin_set_lead_status` (nur `new`/`contacted`/`customer` — **`anonymized` ist bewusst kein setzbarer Zielwert**: der Status ist die FOLGE der Anonymisierung, nicht ihr Auslöser; sonst entstünde ein Lead, der „anonymisiert" HEISST und alle Identitätsmerkmale noch trägt) · `admin_withdraw_consent` · `admin_suppress_lead` · `admin_anonymize_lead` (`auth.uid()` als Handelnder — funktioniert auch in SECURITY DEFINER, es liest die JWT-Claims, nicht die DB-Rolle) · `admin_is_email_suppressed` · `admin_suppression_count`. **Zwei davon DELEGIEREN an die B1-2-Wrapper** (`withdraw_consent`, `suppress_email_and_withdraw_all`) statt SQL zu wiederholen: ein Widerruf muss ALLE offenen und bestätigten Zeilen des Zwecks treffen, und zwei Definitionen von „widerrufen" fielen erst beim ersten Massenversand auf. Als SECURITY-DEFINER-Funktionen laufen sie unter ihrem Eigentümer und dürfen die service_role-only-Wrapper aufrufen.
  - **[`admin_suppress_lead` LEHNT anonymisierte Leads AB, und zwar nicht aus Formalismus]** Deren gespeicherte Adresse ist bereits `anonymized+…@invalid`. Eine Sperre darauf sperrte nichts (die echte Adresse ist weg) und schriebe einen dauerhaften Müll-Hash in eine Liste, die per Konstruktion nie aufgeräumt wird. Ein eigener DB-Test prüft, dass genau dieser Hash NICHT entsteht.
  - **[`consent_effective_status` — EINE Definition von „abgelaufen"]** B1-2 räumt abgelaufene Bestätigungen **lazy** ab (vor B4 gibt es bewusst keine Zeitsteuerung): eine `pending`-Zeile mit vor Monaten verfallenem Token steht weiter als `pending` in der Tabelle. Gespeichert ist das richtig, ANGEZEIGT wäre es falsch — „offen" liest sich als „da kommt noch was", und die Einwilligungsspalte ist die operativ wichtigste (nur BESTÄTIGTE sind im November aktivierbar). Neue STABLE-Funktion in `platform`, benutzt von Liste, Detailsicht UND Filter. Die Detailsicht zeigt den gespeicherten Wert daneben an, wo er abweicht — verschwiegen wäre er eine zweite Wahrheit.
  - **[FILTER IN SQL, NICHT IN DER ANWENDUNG]** `admin_list_leads` bekam Status, Herkunftsquelle, Einwilligungszweck + -zustand (inkl. `none` = keine passende Einwilligung), Freitext über E-Mail/Firma und „zur Anonymisierung fällig" (`deletion_due_at <= now() and anonymized_at is null`). **`total` ist jetzt die Zahl der TREFFER, nicht die Bestandsgröße.** Nachgelagertes Filtern bräche die Seitenaufteilung (DB liefert 50, App wirft 40 weg, zeigt 10 — Trefferzahl falsch, „Seite 2" überspränge Treffer) und holte mehr personenbezogene Daten, als angezeigt werden. **Ein unbekannter Filterwert wird als `{status:'invalid_filter'}` ABGELEHNT, nicht still ignoriert** — sonst hielte man ein ungefiltertes Ergebnis für gefiltert. LIKE-Sonderzeichen im Suchbegriff werden maskiert (ein getipptes `%` trifft nichts, nicht alles).
  - **[KEIN SIEBTER WRAPPER FÜR DIE HERKUNFTSLISTE]** `admin_list_leads` liefert `sources` (key+label) mit, `admin_get_lead` zusätzlich `first_source_label`/`source_label`. Grund: `lead_sources` ist eine TABELLE, kein Enum (B1-1/B3 — laufend kommen Einstiegspunkte dazu). Eine Konstante im Anwendungscode ließe jede neue Quelle im Filter fehlen, ohne dass es auffiele; und ohne die Labels zeigte die Detailsicht rohe Schlüssel, während die Liste Klartext zeigt.
  - **[ADMIN-NAVIGATION — neu, T4-4 kam ohne aus]** `components/admin/nav.tsx` (Übersicht · Leads) im Admin-Layout, **nur im Zugangs-Zweig** (eine Leiste im Ablehnungs-Zweig verriete, was es zu holen gäbe — derselbe Grund wie der neutrale Seitentitel). Benutzt **`next/link`, NICHT das locale-bewusste `components/ui/link.tsx`**: `/admin` liegt ausserhalb der Sprach-Struktur, und bei einer zweiten Sprache erzeugte der locale-bewusste Link `/en/admin/...` — eine Route, die es nicht gibt.
  - **[DREI ROUTEN]** `/admin/leads` (Liste; **Filterformular ist ein echtes GET-Formular** — die Filter SIND die URL, teilbar, funktioniert ohne JavaScript, kein zweiter Ort für den Filterzustand), `/admin/leads/[id]` (Detail mit **vollständigem Wortlaut jeder Einwilligung** — der Wortlaut IST der Nachweis, die Person hat einen Satz gelesen, keinen Zweckschlüssel), `/admin/leads/sperrliste` (Anzahl + Einzelabfrage). **Kein Export, keine Sammelaktionen** (B2), sichtbar so vermerkt.
  - **[ANONYMISIEREN IST DIE EINZIGE AKTION MIT RÜCKFRAGE]** Eigener Bestätigungsschritt als `<details>`-Aufklappen (kein `window.confirm`): er muss BENENNEN, was gelöscht wird und was bleibt — dafür braucht es Fließtext, keinen Systemdialog; nebenbei funktioniert er ohne JavaScript. Die anderen Aktionen sind zurücknehmbar bzw. neu erteilbar; eine Rückfrage auf jedem Knopf würde genau diesen Unterschied einebnen.
  - **[GEFUNDEN UND BEHOBEN — Rückmeldung verschwand durch ihren eigenen Erfolg]** Beim Playwright-Lauf: nach einem erfolgreichen Widerruf rendert `revalidatePath` die Seite neu, dabei verschwand der Widerruf-Knopf (nichts mehr zu widerrufen) — und mit ihm die Komponente, die die Meldung „Widerrufen (1 Eintrag)" hält. Die Bestätigung war also nie zu sehen. **Fix:** der Knopf bleibt stehen und wird DEAKTIVIERT statt entfernt; die Erklärzeile darunter wechselt auf „Hier ist nichts mehr zu widerrufen". Beim Anonymisieren ist es umgekehrt gewollt (der ganze Block wird durch den dauerhaften „Dieser Lead ist anonymisiert"-Hinweis ersetzt — die stärkere Bestätigung, weil sie bleibt); als Kommentar festgehalten, damit es niemand „repariert".
  - **[GEFUNDEN UND BEHOBEN — „nicht gesperrt" war eine falsche Auskunft]** Bei einem anonymisierten Lead wird `is_suppressed` über die PLATZHALTER-Adresse berechnet und sagt nichts über die echte. Die Detailsicht zeigte deshalb „nicht gesperrt" und las sich wie „die Sperre wurde mitgelöscht" — das Gegenteil des B1-1-Entwurfs. Jetzt: **„nicht mehr ermittelbar"** plus ein Satz, dass ein etwaiger Eintrag weiter wirkt, nur diesem Lead nicht mehr zuzuordnen ist. In der LISTE fehlt bei anonymisierten Leads folglich die „gesperrt"-Markierung — kein Bug, sondern die absichtlich durchtrennte Verbindung.
  - **[B4-HINWEIS PROMINENT AUF `/admin/leads`]** „Löschfristen werden derzeit manuell durchgesetzt. Das System hat vor B4 bewusst keinen zeitgesteuerten Job." Steht oben, nicht im Kleingedruckten: es ist keine Einschränkung der Oberfläche, sondern eine Betriebspflicht.
  - **[BEWUSSTE ABWEICHUNG: Texte NICHT in `messages/de.json`]** Der gesamte `/admin`-Bereich hält seine deutschen Sätze im Code — so gebaut in T4-4 und dort begründet (`lib/admin/schema.ts`: „der Admin-Bereich liegt außerhalb der next-intl-Struktur … ein Key-Umweg ohne Wörterbuch wäre eine Indirektion ohne Nutzen"). B1-3 folgt dem Muster, statt den Bereich zur Hälfte auf einen Nachrichtenkatalog umzustellen. Ein Wechsel wäre eine eigene, ganzheitliche Aufgabe.
  - **[VERIFIKATION]** DB-Gate **156/156** (129 bestehende + **27 neue** in `packages/db-tests/src/lead-admin-wrappers.test.ts`): Nicht-Admin scheitert an allen sechs Wrappern mit **SQLSTATE 42501** und ohne Wirkung · Grant-Fläche exakt `authenticated` (anon UND service_role dürfen nicht) · `platform.anonymize_lead` ist von aussen gar nicht aufrufbar · Wechsel auf 'customer' → `commercial` + Frist exakt 84 Monate (in SQL geprüft, kein Float-Vergleich) · Rückweg abgelehnt, auch für service_role · Statuswechsel ZURÜCK lässt die kaufmännische Frist stehen · Anonymisierung nullt Identitätsfelder und `source_ip`/`user_agent`, **lässt 2/2 Einwilligungszeilen samt Wortlaut und den Sperrlisteneintrag stehen**, ist idempotent (`anonymized_at` unverändert) · ein anonymisierter Lead ist für **service_role UND postgres** in allen sieben geschützten Feldern unveränderlich · `last_interaction_at` bleibt änderbar (sonst bräche `touch_lead_on_consent`) · zwei anonymisierte Leads kollidieren nicht am E-Mail-UNIQUE · Löschen des handelnden Kontos nullt nur die Zuschreibung · Widerruf trifft beide Marketing-Zeilen und lässt den anderen Zweck unberührt · Filter liefern konsistente Trefferzahlen über zwei Seiten ohne Doppelung/Auslassung · verfallenes Token zählt als `expired` und NICHT zusätzlich als `pending`. `pnpm lint`/`typecheck`/`build` root-weit grün.
  - **[VERIFIKATION — Playwright gegen den echten Dev-Server, voller angemeldeter Flow]** Anonym `/admin/leads` → **307 auf `/anmelden`, KEIN 404**, und der sichtbare Text der Antwort enthält weder „Verwaltung" noch „Löschfrist" noch „Sperrliste" (`<script>` gestrippt — der next-intl-Flight-Payload erzeugt sonst falsche Treffer). Angemeldet: 3 Testleads korrekt dargestellt (bestätigt · abgelaufen · fällig) · `faellig=1` → 1 Treffer (nur der überfällige) · `einwilligung=expired` → 1 Treffer · Freitextsuche „Kühlhaus" → 1 Treffer · `status=quatsch` → sichtbar abgelehnt · Detailseite zeigt den vollen Wortlaut · **Hinweis „7 Jahre" erscheint VOR dem Absenden** beim Umschalten auf „Kunde", danach steht „kaufmännisch (7 Jahre)" · Widerruf meldet „Widerrufen (1 Eintrag)" und der Knopf bleibt deaktiviert stehen · Sperre → Pille „Adresse gesperrt" · Anonymisieren: Rückfrage benennt Löschung, Unumkehrbarkeit und was bleibt; danach Banner **„Anonymisiert am … durch b13-admin@test.local"**, 3 deaktivierte Schaltflächen, IP genullt, **Wortlaut weiterhin sichtbar** · Sperrliste: Anzahl 1, Abfrage mit `  UI-B13-ALPHA@Test.local ` → „steht auf der Sperrliste", unbekannte Adresse → „steht NICHT". Keine Konsolenfehler.

- **[GEBAUT: B1-2 — Erfassungspfad, Double-Opt-in und Abmeldung (Fahrplan B1)]** Der Schreibpfad auf das B1-1-Schema. Migration `supabase/migrations/20260721150000_create_lead_capture_wrappers.sql` (6 `public`-Wrapper, **alle service_role-only**) + `apps/web/lib/leads/**` + zwei Seiten + eine API-Route + die Verdrahtung des Kontaktformulars. **Genau EIN Einstiegspunkt** ist verdrahtet (das bestehende Kontaktformular) — Admin-Bereich (B1-3), Aussendung/Segmentierung (B2), weitere Erfassungsstellen (B3) und zeitgesteuerte Jobs (B4) sind ausdrücklich NICHT Teil davon.
  - **[WRAPPER]** `capture_lead` (EIN atomarer Aufruf: Lead + optionale Einwilligung in einer Transaktion; `outcome` ∈ `lead_only`/`consent_created`/`consent_already_pending`/`suppressed` — **nur bei `consent_created` versendet der Anwendungscode eine Mail**) · `get_pending_consent_by_token` (STABLE, der GET-Pfad) · `confirm_consent` (idempotent, lazy-`expired`) · `withdraw_consent` (ein Zweck) · `suppress_email_and_withdraw_all` (alles + Sperrliste). **`consent_already_pending` ist kein Komfort, sondern der Schutz davor, dass jemand über wiederholtes Absenden fremde Adressen mit Bestätigungsmails zudeckt.**
  - **[ZWEI BEWUSSTE, ADDITIVE ABWEICHUNGEN VON DER AUFGABENSTELLUNG, beide in der Migration begründet]** (1) `capture_lead` trägt einen zusätzlichen, defaultenden LETZTEN Parameter `p_locale` (default `'de'`) — ohne ihn müsste die Auswahl der „jüngsten passenden" Textfassung die Sprache raten, und B1-1 führt `consent_texts.locale` ausdrücklich als Teil des Nachweises. (2) Es gibt einen SECHSTEN Wrapper `get_active_consent_text`: das Formular MUSS den Wortlaut anzeigen, den `capture_lead` anschliessend archiviert — eine zweite Kopie in `messages/de.json` wäre genau der Zustand, gegen den B1-1s append-only-Design gebaut ist. Ein DB-Test pinnt, dass **archiviert wird, was angezeigt wurde** (gleiche `consent_text_id`).
  - **[ZWEI TOKEN-MECHANISMEN, bewusst nicht derselbe]** `lib/leads/token-crypto.ts` (REIN, Geheimnis als Parameter → ohne Env testbar) + `tokens.ts` (`server-only`, env-gebunden). **Bestätigung:** 32 Zufallsbytes base64url, in der DB nur der SHA-256 — der Klartext existiert ausschliesslich in der Mail; 7 Tage gültig, **lazy** geprüft (vor B4 gibt es im System bewusst keine Zeitsteuerung). **Abmeldung:** HMAC-SHA256 über `${leadId}:${purpose}` mit dem neuen `LEAD_TOKEN_SECRET`, Vergleich per `timingSafeEqual`, **zustandslos und dauerhaft gültig** — ein Abmeldelink muss auch in einer zwei Jahre alten Mail funktionieren.
  - **[⚠ `LEAD_TOKEN_SECRET` NICHT ROTIEREN]** Genau weil die Abmeldelinks zustandslos sind, ist die Signatur der einzige Beweis ihrer Echtheit. Ein neues Geheimnis entwertet JEDEN je versendeten Abmeldelink auf einen Schlag; Empfänger griffen dann zur Spam-Schaltfläche = dauerhafter Zustellbarkeitsschaden für alle. Steht als Warnung in `DEPLOYMENT.md` §1f, `.env.example` und am `requireLeadTokenSecret`.
  - **[SERVICE-ROLE-GRENZE ERWEITERT, NICHT UMGANGEN]** Die root-`eslint.config.mjs`-Allowlist trägt jetzt zusätzlich `apps/web/lib/leads/**/*.ts`. Es gibt weiterhin **einen** service_role-Client, und innerhalb von `lib/leads` importiert ihn nur `store.ts`. Ein Import in einer Server-Component/Page bleibt ein Lint-Fehler — die Regel bleibt die Bremse, die sie ist.
  - **[GET ZEIGT NUR AN, WIRKUNG NUR PER POST]** `/einwilligung-bestaetigen?token=…` bestätigt **nie** beim Laden: Mailscanner in Unternehmen rufen Links vorab ab; ein bestätigender GET erzeugte Einwilligungen, die niemand erteilt hat, und entwertete den Nachweis. Vier Zustände (gültig · abgelaufen **ohne** automatischen Neuversand · unbekannt · bereits bestätigt) plus ein eigener technischer `error`-Zustand — „wir konnten nicht nachsehen" darf sich nicht als „diesen Link gibt es nicht" lesen.
  - **[ABMELDUNG: ZWEI WEGE + RFC 8058]** `/abmelden?l=…&p=…&s=…` zeigt „Von diesen E-Mails abmelden" (nur dieser Zweck) und, deutlich abgesetzt, „Keine E-Mails mehr von COOLiN" (alles + dauerhafte Sperre). Ungültige Signatur → **neutrale** Seite, kein Hinweis, ob die Adresse bekannt ist, und die Adresse wird nirgends angezeigt. `app/api/abmelden/route.ts` nimmt den **POST** für One-Click-Unsubscribe (bei Gmail/Yahoo für Massenversender Pflicht) und antwortet **immer 200** — unterschiedliche Statuscodes wären ein Orakel für gültige Lead-IDs. GET auf dieselbe Route → 303 auf die Menschenseite. Liegt unter `/api`, weil der Middleware-Matcher das ausschliesst (ein Locale-Redirect zerstörte den POST — in T4-3 verifiziert). Der Helfer `unsubscribeHeaders` (List-Unsubscribe + List-Unsubscribe-Post) ist **nur bereitgestellt**, verbraucht wird er von B2.
  - **[KONTAKTFORMULAR — ZWEI RECHTSGRUNDLAGEN IN EINEM FORMULAR]** Jede Absendung schreibt einen Lead (`first_source_key='kontaktformular'`) auf Grundlage **Vertragsanbahnung** — daraus entsteht bewusst KEINE Einwilligungszeile. Nur die zusätzliche, **nicht vorausgewählte** Ankreuzmöglichkeit erzeugt eine `pending`-Einwilligung + Bestätigungsmail. **Der Nachrichtentext wird NICHT gespeichert** (nur Identitätsfelder; `platform.leads` hat dafür auch keine Spalte). Der Schreibvorgang läuft **NACH** dem erfolgreichen Versand in `app/api/kontakt/route.ts` und wirft nie — eine verlorene Kundenanfrage wiegt schwerer als ein verlorener Lead. **Die Rückmeldung an den Nutzer ist in ALLEN Fällen identisch**, auch bei gesperrter Adresse.
  - **[deliver.ts-KOMMENTAR ERLEDIGT]** Die „Phase 2 — Supabase `contacts`"-Einhängestelle ist aufgelöst: die Tabelle wird es nicht geben, die Persistenz ist `platform.leads`/`platform.consents`, und der Aufruf sitzt in der Route, nicht in `deliver.ts` (das Modul bleibt der eine Ort, der weiss, WOHIN eine Anfrage geht — es kennt keine Datenbank). `.env.example` nachgezogen.
  - **[EINWILLIGUNGSTEXT AUS DER DB, ISR statt force-dynamic]** `/kontakt` liest den Wortlaut serverseitig über `get_active_consent_text` und reicht ihn ins Formular (`revalidate = 3600` — append-only Texte ändern sich Mal im Jahr; eine Marketing-Seite pro Request zu rendern wäre die falsche Rechnung). **Fällt das Lesen aus** (fehlende Env im CI-Build, DB nicht erreichbar), rendert das Formular OHNE die Ankreuzmöglichkeit — laut geloggt, fail-closed: ohne Wortlaut darf keine Einwilligung eingesammelt werden. Praktische Folge: **ohne `SUPABASE_SERVICE_ROLE_KEY` in Vercel gibt es keine Checkbox und keine Leads** (die Anfrage wird trotzdem zugestellt).
  - **[BEWUSST NICHT PARAMETRISIERT] `retention_basis` bleibt auf dem B1-1-Default `marketing` (24 Monate)** — obwohl das Kontaktformular geschäftlich veranlasst ist. Eine längere Frist ist eine ESKALATION der Speicherdauer und hängt am noch offenen Löschkonzept (Fahrplan, offene Entscheidung 1). Heraufsetzen geht später je Lead (der Trigger zieht `deletion_due_at` nach); der umgekehrte Weg wäre eine rückwirkend zu lange Speicherung.
  - **[NEU: Unit-Tests in `apps/web`]** Erstmals `vitest` in dieser App (`vitest.config.ts`, `include` eng auf `lib/**/*.test.ts` — kein jsdom/Renderer-Setup, das soll auch nicht beiläufig entstehen). 13 Tests zur Token-Kryptografie. Neuer CI-Schritt „Test web" in `.github/workflows/test.yml`.
  - **[VERIFIKATION]** DB-Gate **129/129** (89 bestehende + **40 neue** in `packages/db-tests/src/lead-capture-wrappers.test.ts`): anon UND authenticated dürfen keinen der sechs Wrapper aufrufen (Katalog-Introspektion, Segfault-Vermeidung) · zweite Absendung legt weder zweiten Lead noch zweite offene Bestätigung an · `MAX@…`/Leerzeichen treffen denselben Lead · gesperrte Adresse → `suppressed` ohne Einwilligung · bestätigungspflichtiger Zweck ohne Token wird hart abgelehnt · nach Ablauf ist eine neue Erfassung wieder möglich · **der Bestätigungs-GET verändert die Zeile nachweislich nicht** (voller Zeilen-Snapshot vor/nach zwei Aufrufen identisch, `pending` bleibt `pending`) · abgelaufener Token → `expired` statt Bestätigung, `has_confirmed_consent` bleibt false · Bestätigen idempotent, `confirmed_at` wird **nicht** umgeschrieben · Widerruf trifft nur den eigenen Zweck · vollständige Sperre widerruft 2/2 Zwecke und **überlebt die Lead-Löschung** · unbekannter Lead → neutrale Rückgabe. Zusätzlich `pnpm --filter web test` 13/13, `build`/`typecheck`/`lint` root-weit grün.
  - **[VERIFIKATION — echte HTTP-Läufe gegen den Production-Build, Port 3200]** `/kontakt` liefert `name="marketing"` **und den DB-Wortlaut** im server-gerenderten Markup · Bestätigungsseite: gültig → „Bitte bestätigen Sie Ihre Einwilligung" bei **unverändertem** `status='pending'` in der DB, unbekannt/ohne Token → „Dieser Link ist uns nicht bekannt" · Abmeldeseite: gültige Signatur → zwei Möglichkeiten, **manipulierte Signatur / fremde Lead-ID / keine Parameter → identische neutrale Seite** · `GET /api/abmelden` → 303 auf `/abmelden` · **`POST /api/abmelden` (One-Click) → 200 und `pending`→`withdrawn`**, mit falscher Signatur → ebenfalls 200, Status **unverändert**. Kontaktformular-Kette (Versand für den Lauf lokal überbrückt, danach zurückgebaut): ohne Häkchen → Lead ohne Einwilligung · mit Häkchen → Lead + `pending` mit `source_ip`/`user_agent` · zweite Absendung → **kein** zweiter Lead, **keine** zweite Einwilligung · gesperrte Adresse → identische `200 {ok:true}`-Antwort, 0 Einwilligungen · Nachrichtentext im Bestand: **0 Treffer** · und der Mailversuch wurde über alle vier Läufe **genau einmal** angestossen (nur bei `consent_created`).
  - **[OFFEN, rechtlich — unverändert aus B1-1]** Der Wortlaut der Einwilligungstexte ist Arbeitsstand; die juristische Prüfung steht aus (Owner Martin). Eine geprüfte Fassung kommt als NEUE Zeile mit `version 2` — Anzeige UND Archivierung ziehen dann automatisch nach, weil beide dieselbe „jüngste Fassung"-Regel benutzen.

- **[GEBAUT: B1-1 — Lead- und Einwilligungsfundament (Fahrplan B1), NUR DATENBANK]** Migration `supabase/migrations/20260721100000_create_lead_consent_foundation.sql` + DB-Gate `packages/db-tests/src/lead-consent-foundation.test.ts` (18 neue Tests, 89 im Gate gesamt). **Kein UI, keine Server Action, kein Resend, kein Admin-Bereich** — das sind B1-2/B1-3, die gegen genau dieses Schema bauen. Konventionen exakt T4-1: alles im `platform`-Schema (NICHT über REST exponiert), Zugriff nur über SECURITY-DEFINER-`public`-Wrapper, alle Funktionen `SET search_path = ''`, RLS auf allen fünf Tabellen, `anon` nirgends Grant/Policy/Schema-Usage.
  - **[TABELLEN]** `lead_sources` (Referenztabelle statt Enum — Einstiegspunkte kommen laufend dazu, B3; 5 Seed-Zeilen) · `leads` (EIN Bestand; `first_source_key` PFLICHT und unveränderlich; `status` ist REINER Lebenszyklus — eine Abmeldung ist bewusst KEIN Lead-Status, weil man abgemeldet UND zahlender Kunde sein kann; KEINE Segmentierungsspalten, die kommen getypt mit B3) · `consent_texts` (versioniert + append-only) · `consents` (MEHRERE zweckgebundene je Lead, kein UNIQUE auf (lead_id, purpose) — erteilen/widerrufen/erneut erteilen ist der Normalfall) · `email_suppressions` (nur SHA-256, KEIN FK auf leads).
  - **[ENUM vs. TABELLE]** `platform.consent_purpose` (marketing_email · contract_expiry_reminder · result_delivery) ist ein **Enum**, weil der Anwendungscode jeden Zweck kennen MUSS; `lead_sources` ist eine **Tabelle**, weil er die Einstiegspunkte NICHT kennen muss. Vertragsablauf-Erinnerung ist ausdrücklich NICHT dieselbe Einwilligung wie Marketing.
  - **[INVARIANTEN ALS TRIGGER, nicht als Anwendungscode]** `sync_lead_retention` (deletion_due_at = last_interaction_at + `retention_months`; ein mitgegebener Wert wird überschrieben — die Spalte ist Ableitung, keine Eingabe) · `guard_lead_first_source` (Herkunft einmalig) · `guard_consent_confirmation` (**ein bestätigungspflichtiger Zweck erreicht NIE status=confirmed ohne confirmed_at** — auch service_role kann keine Bestätigung fälschen) · `reject_consent_text_mutation` (append-only, Muster `reject_stripe_event_mutation`; ein änderbarer Text wäre kein Nachweis) · `touch_lead_on_consent` (jede Einwilligung schiebt die Löschfrist nach).
  - **[FRISTEN AN EINER STELLE]** `platform.retention_months`: marketing 24, commercial 84 Monate — eine spätere Justierung bleibt Konfiguration statt Umbau. `purpose_requires_double_opt_in` hält die Zuordnung Zweck→Bestätigungspflicht (Regel: Bestätigung nötig, sobald die Erfüllung eine KÜNFTIGE Mail ist — nicht erst bei Werbung; `result_delivery` deshalb false).
  - **[VOR JEDEM VERSAND ZWEI FRAGEN]** `platform.has_confirmed_consent(lead, purpose)` (ausdrücklich false bei `pending` — unbestätigt ist rechtlich wertlos) **UND** `platform.is_suppressed(email)`. Beide SECURITY DEFINER und NUR an `service_role` gegrantet (für `authenticated` wären sie ein Auskunfts-/Enumerations-Orakel). Die Sperrliste überlebt die Lead-Löschung bewusst (kein FK, kein Cascade) und trägt nur den Hash — sie darf nicht selbst als Verteilerliste taugen.
  - **[WRAPPER + BEWUSSTE ABWEICHUNG VON T4-4]** `public.admin_list_leads(limit, offset)` (ohne Filter — die gefilterte Sicht ist B2) und `public.admin_get_lead(id)` (Einwilligungen **inkl. Textkörper + Version** — ohne Wortlaut wäre der Nachweis keiner). Beide **WERFEN** bei fehlender Adminrolle (SQLSTATE 42501) statt `{status:'forbidden'}` zurückzugeben wie die T4-4-Wrapper: „kein Zugriff" darf sich nie als „keine Leads" lesen lassen. Fachliche Zustände (`not_found`) bleiben Status. Grant nur `authenticated`.
  - **[SCHREIBPFAD]** `authenticated` hat auf leads/consents/suppressions KEIN Grant (ein Lead ist Betriebs-, kein Nutzerdatum — die Person hat meist gar keinen Account). Die anonyme Erfassung läuft in B1-2 über eine Server Action mit `service_role`, konsistent mit dem Stripe-Webhook. `service_role`: select/insert/update auf leads+consents, select/insert auf suppressions, nirgends `delete` (der Löschjob der Fristen gehört nicht zu B1-1 — keine Rechte auf Vorrat).
  - **[B13 vorbereitet, nicht gebaut]** Kein `tenant_id`. Der additive Weg steht als Kommentar in der Migration: Spalte in `leads`, E-Mail-UNIQUE auf `(tenant_id, normalize_email(email))` umstellen, eine RLS-Policy — die Beziehungen zwischen den Tabellen ändern sich dabei nicht.
  - **[OFFEN, rechtlich]** Der Wortlaut der drei Seed-Einwilligungstexte ist **Arbeitsstand**; juristische Prüfung vor breiter Aussendung steht aus (Fahrplan_2026.md §7 „Fachliche Abhängigkeiten", Owner Martin). Eine geprüfte Fassung kommt als NEUE Zeile mit `version 2` — die bestehende wird nicht editiert (append-only).
  - **[VERIFIKATION]** `supabase db reset` lokal grün; DB-Gate 89/89 (u. a.: anon scheitert auf allen fünf Tabellen · Nicht-Admin bekommt aus `admin_list_leads` einen Fehler statt einer leeren Liste · UPDATE/DELETE auf `consent_texts` sind auch für `postgres` gesperrt · `marketing_email` lässt sich ohne `confirmed_at` weder einfügen noch hochstufen, mit Zeitstempel schon · Frist rückt bei neuer Einwilligung um exakt die verstrichenen Tage nach und springt beim Wechsel auf `commercial` auf 84 Monate · Sperrliste überlebt das Löschen des Leads, dessen Einwilligungen mit ihm verschwinden). `pnpm lint`/`pnpm typecheck` root-weit grün. Migration zusätzlich auf die Cloud-DB gepusht und dort per Katalog-Introspektion gegengeprüft.

- **[VERCEL-DEPLOY — WICHTIG, verbindlicher Standard: „abgeschlossen" = live-verifiziert]** Vercel überspringt Production-Builds fälschlich: die Default-Funktion **„Skipping unaffected projects"** (Dashboard, pro Projekt: Settings → Build and Deployment → Root Directory → Toggle **„Skip deployment"**) vergleicht einen neuen `main`-Commit gegen das ZULETZT DEPLOYTE Commit. Bei einem PR-Merge ist das der bereits als Preview gebaute PR-Head → der Merge-Commit ist gegenüber diesem „Not affected" → **Production-Build übersprungen, obwohl der Fix real neu auf `main` liegt** (passiert bei PR #7 / `17d5b0e`; gemergt, aber tagelang nicht live).
  - **[EMPIRISCH BEWIESEN, 2026-07-19]** Ein `apps/web/vercel.json` mit `ignoreCommand` (`exit 1` für Production) behebt das **NICHT** — „Skipping unaffected projects" entscheidet VOR dem Build-Step (belegt: leerer Test-Commit trotz vorhandenem `ignoreCommand` als „Skipped - Not affected" übersprungen). Es gibt **KEINEN Repo-only-Fix**, um diese Funktion abzuschalten.
  - **[EINZIGER FIX — Dashboard, braucht Andreas]** Toggle **„Skip deployment" → Disabled** pro Projekt (peak-shaving-web = coolin.at, + peak-shaving-website). Danach greift `apps/web/vercel.json` als Ignored-Build-Step: Production IMMER bauen (`VERCEL_ENV != preview` → `exit 1`), Preview affected-only (`git diff HEAD^ HEAD .`).
  - **[NOTFALL-EINZELDEPLOY]** Bis der Toggle aus ist: Commit mit ECHTER `apps/web`-Änderung pushen (ein **leerer** Commit reicht NICHT — ebenfalls „Not affected"). So wurde der PR-#7-Fix live gebracht (`apps/web/vercel.json` selbst war die echte Änderung).
  - **[VERBINDLICHER STANDARD (Nutzer)]** „abgeschlossen" heißt ab jetzt gemergt UND per `curl -sL https://www.coolin.at` live bestätigt (konkreter server-gerenderter Marker, z. B. Footer-/Nav-Text; bis 5 Min auf Propagation warten) — NICHT nur „PR gemergt"/„Vercel Ready".

- **[GEFIXT: Auth-/Stripe-Redirect-Basis-URL — EINE zentrale Quelle statt Request-Host]** Live-Test-Bug: der Registrierungs-Bestätigungslink landete auf `https://www.coolin.at/?code=…` (Startseite, `code` unverarbeitet) statt `/auth/callback`. **Root Cause (App-Seite):** `emailRedirectTo`/`redirectTo` (signup/resend/reset) UND Stripes `success_url`/`cancel_url`/`return_url` wurden aus dem **Request-`origin`-Header** (`getOrigin()`) gebaut — einer zweiten, volatilen Basis-URL-Quelle neben `SITE_URL` (Apex vs. www vs. Preview). Supabase akzeptiert `emailRedirectTo` NUR bei EXAKTEM Allowlist-Treffer und fällt sonst STILL auf die Site-URL-Wurzel zurück → genau das `/?code`-Symptom. **Fix:** neue zentrale `redirectBaseUrl()` (`lib/auth/server-helpers.ts`) — in Produktion die konfigurierte `SITE_URL` (deterministisch → allowlistbar, `HAS_CONFIGURED_SITE_URL` in `lib/site.ts`), lokal/Preview weiter der Request-Origin. Auth (`callbackUrl`) UND Stripe (`kontoUrl`) nutzen sie; `getOrigin` ist zu einem privaten Fallback (`getRequestOrigin`) degradiert. Damit gibt es wieder genau EINE Domain-Quelle (das erklärte `lib/site.ts`-Prinzip).
  - **[NÖTIGE ABSTIMMUNG — Config, nicht Code]** Die App emittiert jetzt `${NEXT_PUBLIC_SITE_URL}/auth/callback` = derzeit `https://coolin.at/auth/callback` (**Apex**). Diese EXAKTE Apex-URL muss in Supabases **Redirect-Allowlist** stehen (der Apex→www-308 erhält Pfad+Query, der Nutzer landet trotzdem auf www/auth/callback). Am robustesten: Apex UND www-Callback allowlisten. **Wenn stattdessen alles www werden soll:** `NEXT_PUBLIC_SITE_URL=https://www.coolin.at` **und gleichzeitig** `PRODUCTION_ORIGIN` in `lib/site.ts` auf www ziehen — sonst kippt `IS_PRODUCTION_SITE`/`robots.txt` auf `Disallow: /` (de-indexiert die Live-Seite).
  - **[Stripe 5a]** Die vier T4-3-Env-Variablen (`SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_MONITOR_PRICE_ID`) müssen in Vercel gesetzt sein (require-on-use → sonst Checkout-Start-Fehler „etwas schiefgelaufen"). Nach Nachtrag: Redeploy.
  - **[Stripe 5a — LIVE-DIAGNOSE, behoben]** Genau dieser „etwas schiefgelaufen"-Fehler trat live auf. Diagnose: der Text ist der generische Catch von `startCheckoutAction` (→ `?checkout=error`), NICHT die Ursache. Isolationstest der Stripe-Kette gegen den Test-Account (customer + subscription checkout session mit der Price-ID) lief grün → **kein Code-Bug**, Produktions-Konfig. Da der Nutzer die eingeloggte Kontoseite erreicht (Auth ok → `SUPABASE_URL`/`ANON_KEY` gesetzt), war der einzige checkout-exklusive Erst-Zugriff `SUPABASE_SERVICE_ROLE_KEY` (`createServiceRoleClient`, erste env-Zeile im Pfad); `STRIPE_WEBHOOK_SECRET` liegt NICHT auf dem Checkout-Pfad (nur Webhook). **Fix:** `SUPABASE_SERVICE_ROLE_KEY` in Vercel Production gesetzt/korrigiert + Redeploy. Zusätzlich: der Catch loggt jetzt strukturiert `{name,type,code,param,statusCode,message}` (`describeError` in `lib/stripe/actions.ts`), damit ein künftiger Fehlschlag im Vercel-Log eindeutig ist (kein Secret, Nutzer-Text unverändert).
  - **[VERIFIKATION, lokal gemessen]** Reset-Formular von `http://127.0.0.1:3000` abgeschickt → App ruft Supabase `/recover` mit `redirect_to=http://localhost:3000/auth/callback?next=/passwort-neu` (die KONFIGURIERTE Basis, NICHT der 127.0.0.1-Request-Origin — Stub-Supabase-Capture). Echter Stripe-Test-Checkout (cs_test-Session per API abgerufen): `success_url=http://localhost:3000/konto?checkout=success`, `cancel_url=…?checkout=cancel` — ebenfalls die konfigurierte Basis. `build`/`typecheck`/`lint` grün, `pnpm -r test` unverändert (engine 144 + shared 18 + tariff-monitor 62).
- **[GEBAUT: Nav — „Strom-Check" → „Strom-Monitor", vom Footer ins Leistungen-Menü]** Label zentral in `messages/de.json` (`Nav.stromCheck` = „Strom-Monitor"; `Footer.stromCheck` entfernt). Der Monitor-Einstieg wandert aus der Footer-„Unternehmen"-Spalte UND der Mobile-Aktionsleiste in das **Leistungen-Menü** als letzter Punkt (neues optionales `NavItem.trailingLeaf` in `lib/nav.ts` → Header-Mega-Menü + Mobile-Accordion; fließt bewusst NICHT in `LEISTUNGEN_FLAT`, erscheint also NICHT im Footer). Playwright-verifiziert: „Strom-Monitor" ist der letzte Eintrag im Leistungen-Dropdown, 0 Vorkommen im Footer.

- **[GESCHAFFT, außerhalb von CC-Prompts — Domain-Umzug] coolin.at ist live.** Nameserver von Netlify auf world4you umgestellt (`ns1.world4you.at`/`ns2.world4you.at`), alle bestehenden DNS-Records 1:1 migriert — insbesondere **Mail unverändert mitgenommen** (MX `mail.coolin.at`, SPF, DKIM, DMARC), kein Provider-Wechsel für `@coolin.at`-Postfächer. Zwei neue Records für Vercel ergänzt: `coolin.at` A → `216.150.1.1`, `www.coolin.at` CNAME → `b2155869eae4c165.vercel-dns-016.com.`. Domain in Vercel verifiziert (SSL ausgestellt).
  - **[CUTOVER VOLLZOGEN]** `NEXT_PUBLIC_SITE_URL` = `https://coolin.at` in Vercel gesetzt (Scope Production) + Redeploy. Damit sind Canonicals, hreflang, OG-Bild UND Indexierung gemeinsam scharf (der eine Variablenwechsel, den 13a/13b vorgesehen hatten). `robots.txt` zeigt jetzt `Allow: /` + `Sitemap: https://coolin.at/sitemap.xml` — vorher bewusst `Disallow: /`, solange nur die vercel.app-URL lief.
  - **[RESEND LIVE]** `RESEND_API_KEY`/`RESEND_TO`/`RESEND_FROM` in Vercel gesetzt. Ein Fehlversuch dabei: `RESEND_FROM` zunächst falsch formatiert (Resend verlangt strikt `email@domain` oder `Name <email@domain>`) → 422 `validation_error`, im Log sichtbar, Fehlerzustand im Formular griff korrekt (kein stiller Fehlschlag, Prompt-12-Verhalten bestätigt). Korrigiert auf `COOLiN ENERGY <energy@coolin.at>`. Testmail über das Kontaktformular zugestellt, `reply-to` korrekt auf den Absender gesetzt.
  - **[OFFEN, unabhängig]** Turnstile-Keys weiterhin nicht gesetzt (Formular läuft auf Honeypot-Fallback) — vor breiter öffentlicher Bewerbung nachziehen. `RESEND_FROM` sendet aktuell von derselben Adresse, an die auch zugestellt wird (`energy@coolin.at` → `energy@coolin.at`); funktioniert, eine getrennte Absenderadresse (z. B. `kontakt@coolin.at`) wäre langfristig sauberer, kein Blocker.

- **[GEBAUT: Prompt 28] Kalkulator-Gate verlangt den Code bei JEDEM Seitenaufruf — localStorage-Persistierung entfernt.** Verhaltensänderung gegenüber 25/26: Es gibt keinen gespeicherten „entsperrt"-Zustand mehr. Nur `lib/kalkulator-access.ts` + `calculator-gate.tsx` + ein Text-Key; `apps/website` unangetastet, kein Rate-Limit/Lockout (bleibt Soft-Gate).
  - **[PERSISTENZ RAUS]** Kein `setItem`, kein `getItem`. `unlocked` lebt nur im React-State → Reload/Navigation wirft ihn weg, wer auf der Seite bleibt, bleibt entsperrt. Der Grund steht am Code (`lib/kalkulator-access.ts`): Ein einmal auf einem fremden Gerät eingegebener Code soll dieses Gerät nicht dauerhaft freischalten, und in Demos soll sichtbar bleiben, dass der Rechner hinter einem Code liegt.
  - **[DER DREIWERTIGE STATE AUS PROMPT 25 IST ERSATZLOS ENTFALLEN]** — nicht übersehen, sondern gegenstandslos geworden: `unlocked: boolean | null` existierte AUSSCHLIESSLICH, damit das Modal bei einem freigeschalteten Nutzer nicht einen Frame lang aufblitzt, bevor der Effekt den gespeicherten Zustand nachreicht. Ohne gespeicherten Zustand gibt es nichts nachzureichen — „gesperrt" ist auf dem Server und im Browser dieselbe Wahrheit, `useState(false)` ist ab dem ersten Render richtig. Kein Hydration-Mismatch: Radix' Portal rendert seinen Inhalt ohnehin erst nach dem Mount. Ein beibehaltenes `null` hätte jetzt das Gegenteil bewirkt (ein Frame OHNE Modal).
  - **[ALTLAST WIRD AKTIV GELÖSCHT, nicht nur ignoriert]** `KALKULATOR_ACCESS_STORAGE_KEY` → **`KALKULATOR_ACCESS_LEGACY_STORAGE_KEY`** umbenannt (Wert `coolin.kalkulator.access` **unverändert** — sonst liefe die Bereinigung ins Leere). Der Name macht am Fundort klar, dass der Key nur noch zum Aufräumen existiert; ein Mount-Effekt ruft `removeItem` (in `try`, Safari privat wirft). Wer den Code vor Prompt 28 eingegeben hat, trägt das Flag sonst weiter im Browser — ein toter Zustand, den ein späteres Feature versehentlich wieder ernst nimmt.
  - **[GEFUNDEN UND MITGEFIXT — der Text war zur Lüge geworden]** Der Modal-Lead (`CalculatorFrame.gate.lead`) versprach wörtlich „**die Eingabe merken wir uns in diesem Browser**". Genau das tut der Code seit diesem Prompt nicht mehr. Am Screenshot des gerenderten Modals aufgefallen, nicht im Diff — der Key liegt in `messages/de.json` und wäre von einer reinen Logik-Änderung nie berührt worden. Neu: „… — er wird bei jedem Besuch neu abgefragt." Das setzt zugleich die Erwartung, dass der wiederkehrende Dialog Absicht ist und kein Bug. Fehlermeldung/Escape-/Outside-Click-Verhalten (Prompt 26) bleiben wörtlich unverändert.
  - **[VERIFIKATION — echter Chrome (`channel: 'chrome'`, sichtbares Fenster), Production-Build, Port 3100]** Erstbesuch `modal=1 · iframe=0 · localStorage=null` → falscher Code: Modal bleibt, `aria-invalid=true`, „Dieser Zugangscode stimmt nicht…" → Escape: `modal=1` → Outside-Click: `modal=1` → richtiger Code: `modal=0 · iframe=1 · **localStorage=null**` (Beweis, dass NICHTS geschrieben wird), iframe lädt den echten Rechner (Screenshot: Schritt-1-Upload sichtbar) → 2,5 s auf der Seite bleiben inkl. Scrollen/Klicken: `modal=0` (bleibt entsperrt) → **Reload: `modal=1 · iframe=0` — die Kernänderung, gegengeprüft, nicht behauptet** → weg-navigieren und zurück: `modal=1`. **Altlast-Test:** Key `coolin.kalkulator.access='true'` auf demselben Origin gesetzt (wie ihn 25/26 hinterlassen haben), dann Gate-Seite geladen → `modal=1 · iframe=0`, `localStorage` nach dem Mount `null`, `Object.keys(localStorage)` = `[]` — kein automatisches Entsperren, Alt-Eintrag weg. **@375px 0 px horizontaler Überlauf** (gesperrt UND entsperrt), **0 Konsolenfehler** über alle Läufe, **11 Routen weiter 200**. `build`/`test` (162/162: 18 shared + 144 engine)/`typecheck`/`lint` grün.
  - **[ÜBERHOLT DAMIT]** Die Persistenz-Aussagen in den Prompt-25/26-Einträgen unten („localStorage=`true` → Reload: iframe direkt, kein erneutes Prompt", „dreiwertig") beschreiben ab jetzt das **Vor-28-Verhalten** und stehen nur noch als Historie.

- **[GEBAUT: Prompt 26] Vier unabhängige Header-Korrekturen.**
  - **[TEIL 1 — Nav-Zentrierung, Root Cause geklärt]** `site-header.tsx`: Header-Grid von `1fr auto 1fr` auf **`auto 1fr auto`** umgestellt. Mit gleich breiten Außenspalten (`1fr auto 1fr`) blieb der sichtbare Abstand trotzdem asymmetrisch (**gemessen: 124px links vs. 48px rechts** bei 1280/1440px), weil Logo (schmal, 164px) und Aktions-Block (breiter, 240px: Login+Kontakt+Kalkulator) ihre GLEICH breiten Spalten unterschiedlich stark ausfüllten — der Leerraum blieb dadurch ungleich verteilt, obwohl die Spalten selbst gleich breit waren. Mit `auto 1fr auto` sind Logo-/Aktions-Spalte exakt content-breit (kein Leerraum innerhalb), die Mittelspalte ist dadurch exakt der Raum zwischen Logos rechter und Aktions-Blocks linker Kante — `justify-self-center` zentriert die Nav darin tatsächlich mittig (**gemessen: 86px beidseitig, 0px Differenz bei 1024/1280/1440px, Playwright, Dev UND Production-Build**). **Bewusst NICHT** `flex-1` + `justify-center` direkt auf der NavigationMenu-Primitive (die von Haus aus `flex-1` trägt) versucht: deren Mega-Menü-Viewport dockt intern an der LINKEN Kante der Nav-eigenen Box an (`navigation-menu.tsx`, `absolute left-0`) — eine gestreckte, nur inhaltlich zentrierte Box hätte das Dropdown-Panel vom zentrierten Trigger wegrücken lassen. Mit `auto 1fr auto` bleibt die Nav-Box selbst unangetastet (content-breit), nur ihre Position im Grid ändert sich — Mega-Menü-Alignment gegen den Trigger Playwright-verifiziert (Dropdown-Wrapper-x = Trigger-x, exakt deckungsgleich). Bei 1024–1280px geprüft: reichlich Luft, kein Umbruch, kein Fallback nötig.
  - **[TEIL 2 — Login ohne Funktion]** „bald"-Badge entfernt, Element von `<Link href="/login">` auf reinen `<span>` umgestellt (Desktop-Header UND Mobile-Drawer, beide vorher inkonsistent — Mobile hatte zusätzlich einen echten Link samt „· bald"-Text). Kein Klick-Handler, kein Href, nur dezenter Hover-Ton (`text-text-muted` → `text-ink`). Echtes Auth kommt erst in Phase 2 (Supabase). `LOGIN_HREF` bleibt exportiert (weiter genutzt von `/login`-Platzhalterseite + `lib/routes.ts`-noindex-Liste) — nur die Header-Verlinkung entfernt. Ungenutzter `loginSoon`-Key aus `messages/de.json` entfernt.
  - **[TEIL 3 — Button-Höhe: UNTERSUCHT, KEIN BUG]** Pixel-Messung (Playwright, `getBoundingClientRect` UND geschossene Pixel-Bounding-Box bei DPR 2) zeigt: Kontakt- und Kalkulator-Button sind bereits **exakt 32px hoch** (0px Differenz), identisches Padding (12px L/R, 0 T/B), `box-sizing: border-box` — der 1px-Rand des outline-Buttons (`secondary`-Variante) erzeugt KEINE reale Höhendifferenz (border-box zählt den Rand zur Box, nicht dazu). Die Breiten-Differenz (79px vs. 93px) ist rein textbedingt („Kontakt" vs. „Kalkulator", exakt wie im Prompt als akzeptabel benannt). Keine Code-Änderung — ein erzwungener Padding-Fix hätte hier eine Diskrepanz erst EINGEFÜHRT, wo keine bestand. Falls der wahrgenommene Unterschied real ist, ist er optischer Natur (gefüllte Akzentfläche liest sich „satter" als eine dünn umrandete Fläche gleicher Boxgröße) — kein Layout-Defekt.
  - **[TEIL 4 — Kalkulator-Gate als Popup]** `calculator-gate.tsx` von Vollseiten-Formular auf Modal umgestellt. Neues **`components/ui/dialog.tsx`** (Radix Dialog, zentriert, shadcn-Muster — Schwester von `sheet.tsx`, dieselbe Primitive, andere Platzierung). Bewusst OHNE eingebauten Schließen-Button (das Primitiv selbst ist „nackt"; ob ein Dialog schließbar ist, entscheidet der Aufrufer). `CalculatorGate` zeigt jetzt, solange `unlocked !== true`, eine leere `bg-surface-alt`-Fläche in exakt der Höhe des späteren iframes (neue geteilte Konstante `CALCULATOR_FRAME_STYLE` in `lib/config.ts`, von `rechner/page.tsx` UND dem Gate verwendet — verhindert einen Layout-Sprung beim Entsperren) mit dem Modal darüber. Modal ist NICHT schließbar ohne Code: `onEscapeKeyDown`/`onPointerDownOutside`/`onInteractOutside` auf `DialogContent` abgefangen, kein X-Button vorhanden. `lib/kalkulator-access.ts` (Code, localStorage-Key, `isValidAccessCode`) **unverändert** — nur die Präsentationsebene geändert, wie gefordert.
  - **[VERIFIKATION, Playwright gegen Dev- UND Production-Build]** Nav-Zentrierung 0px Differenz (3 Breakpoints), Mega-Menü-Dropdown exakt unter seinem Trigger, 0 „bald"-Badges/0 Login-`<a>`-Elemente im DOM (Desktop + Mobile-Drawer), Button-Höhen 0px Differenz. Gate-Flow komplett durchgespielt: Erstbesuch → Modal (kein iframe, kein localStorage) → Escape/Outside-Click ändern nichts → falscher Code → Fehlermeldung (`aria-invalid`, `role="alert"`) → richtiger Code → Modal weg, iframe lädt (echter Rechner-Screenshot bestätigt), `localStorage=true` → Reload → iframe direkt, kein Modal. **Kein horizontaler Scroll @375px auf 9 Routen** (`/`, `/leistungen`, `/peak-shaving`, `/peak-shaving/kalkulator`, `/branchen`, `/wissen`, `/kontakt`, `/login`, `/peak-shaving/kalkulator/rechner`), alle 200, 0 Konsolenfehler über alle Läufe. `build`/`test` (162/162: 18 shared + 144 engine)/`typecheck`/`lint` grün.

- **[GEBAUT: Prompt 25] Fünf unabhängige Teile.**
  - **[TEIL 1 — Wortmarke: Revert + Neuanordnung]** Commit `140ab77` (Prompt 24) berührte NACHWEISLICH nur `wordmark.tsx` + `opengraph-image.tsx` (`git show --stat` vor dem Revert geprüft) → sauberer `git revert`, kein chirurgischer Eingriff nötig. Ausgehend vom Prompt-23-Stand: „COOLiN" **unangetastet** (Text/Größe/i-Knoten/Farbe, exakt dieselben Koordinaten), „ENERGY" jetzt `energySize=62` statt 90 (Cap-Height-Verhältnis **1,61×**, an der Versalhöhe bemessen — beide Zeilen sind Versalsatz, also liest das Auge genau dieses Verhältnis). **`textLength`/`lengthAdjust` ERSATZLOS entfernt** (der Grund steht am Code): Zwei gleich breite Blöcke wirken gleichrangig — genau die Unterordnung, die ENERGY tragen soll, nahm der Stretch wieder zurück. ENERGY steht linksbündig (`x=0` wie COOLiN) in natürlicher Breite und endet vor COOLiNs rechtem Rand. **Vertikale Zentrierung:** Header/Footer nutzen NICHT das `Lockup`-Component, sondern eigene `items-center`-Flexboxen — die zentrieren die SVG-BOX, nicht den sichtbaren Block. Deshalb schließt der viewBox jetzt oben/unten mit demselben Polster ab (`pad=8`, vorher 10 oben / 8 unten bei einer Knoten-Oberkante von 23). **Gemessen (Playwright, DSF 3):** Header Emblem-Mitte **32,0** = Wortmarken-Mitte **32,0**; Footer **490,1** = **490,1**; ENERGY links **142** = COOL links **142**, ENERGY rechts **222,4** < COOLiN rechts **251,6**. `opengraph-image.tsx` (eigener Satori-Nachbau, kein gemeinsames Component) mit denselben Em-Werten nachgezogen, `ENERGY_STRETCH`/`COOL_N_WIDTH_EM`/`ENERGY_NATURAL_WIDTH_EM` gelöscht. **Am gerenderten PNG vermessen** (1200×630, HTTP 200): COOLiN x 92..477, ENERGY x 93..371 (endet **106 px** vor COOLiN), ENERGY-Cap **49 px** ≈ Soll 104×0,62×0,727=46,9. Die 1 px Differenz links ist die Seitenlage der Glyphen (Ink „C" vs. „E"), keine Fehlausrichtung — die Textboxen starten identisch (im Browser gemessen: beide 142).
  - **[TEIL 2 — Hero-Text]** `Home.Hero.lead` wörtlich ersetzt, H1/CTAs/Struktur unverändert (keine Layout-Anpassung nötig — im DOM verifiziert).
  - **[TEIL 3 — Branchen 4 → 5]** Hotellerie+Gastronomie **verschmolzen**, Bäckerei und Handel **ersatzlos** entfallen, Bau & Baunebengewerbe / Handwerk / Industrie & Verarbeitendes Gewerbe / Land- und Forstwirtschaft **neu**. **Kein Template-Umbau**: `lib/nav.ts` (5 Leafs) + `lib/branchen.ts` (Icons/HOURLY/CAP_HINT/HEBEL/ARTICLE_LINK) + `messages/de.json` + 5 Seitendateien mit je nur dem Schlüssel. Die Propagierung über `BRANCHEN_FLAT` funktionierte wie im Prompt-10-Bericht behauptet **automatisch** — Nav-Dropdown, Footer, Startseiten-Teaser, Übersicht und `lib/routes.ts`/sitemap zogen ohne Zutun nach (gemessen: alle 5 überall).
    - **[EHRLICHKEIT — die fachliche Kernentscheidung] Land- und Forstwirtschaft bekommt KEINEN `capHint`** — und zwar aus einem ANDEREN Grund als früher der Handel: nicht weil das Profil flach wäre (es ist im Gegenteil sehr bewegt), sondern weil es **saisonal/wetterabhängig** ist. Eine Linie würde eine Wiederholbarkeit behaupten, die es nicht gibt. Der Unterschied ist im Code UND im Seitentext sauber ausformuliert, nicht kopiert. **Gemessen:** Legende „Mögliche Kappungsschwelle" = 1 bei Bau/Hotel-Gastro/Handwerk/Industrie, **0** bei Land/Forst. Beim Bau ist das Peak-Shaving-Framing zusätzlich explizit auf den **festen Standort** (Bauhof/Halle) begrenzt — Baustrom-Anschlüsse bekommen ausdrücklich keine Zusage.
    - **[301-REDIRECTS, `statusCode: 301` wie 13b]** ZWEI Ziele, inhaltlich begründet: Hotellerie/Gastronomie → `/branchen/hotellerie-gastronomie` (Inhalt lebt dort weiter, Ranking soll mit); Bäckerei/Handel → `/branchen` (**kein Nachfolger** — ein Redirect auf eine der neuen Branchen behauptete „das hier ist jetzt die Adresse dafür" und wäre ein soft 404). **Gemessen:** alle vier 301, Ziele korrekt.
    - **[GRID 4 → 3 SPALTEN]** `lg:grid-cols-4` → `lg:grid-cols-3` in Teaser UND Übersicht (dieselben Kacheln, dürfen nicht unterschiedlich brechen). Fünf Spalten hätten bei `max-w-container` (72rem) je ~200 px ergeben — „Industrie & Verarbeitendes Gewerbe" wäre 3–4-zeilig gebrochen. 3+2 lässt eine Lücke, hält aber alle Titel einzeilig (am Screenshot geprüft).
    - **[ARTIKEL-CROSS-LINKS ummappt]** `ARTICLE_LINK_BRANCHEN`: `baeckerei`/`gastronomie` → **`handwerk`/`hotellerieGastronomie`**. Das Handwerk IST der „Tischlerei mit drei Kompressoren"-Fall des 2027-Artikels in Reinform. Industrie bewusst NICHT (der Artikel richtet sich ausdrücklich an die bisher NICHT gemessenen KMU — „dort ist der Leistungspreis nichts Neues" steht wörtlich im Artikel), Bau/Land-Forst ebenfalls nicht. **Gemessen:** Artikel verlinkt exakt `/branchen/handwerk` + `/branchen/hotellerie-gastronomie`; Artikel-Karte erscheint auf genau diesen zwei Branchenseiten, auf den anderen drei 0×.
    - **[GREP-SWEEP, vollständig]** Über `apps/web` gefahren. Neben den offensichtlichen Stellen gefunden und korrigiert: `content/wissen/leistungstarif-2027.de.mdx` (Fließtext + `ChartFigure`-Caption), `Wissen.Charts.Netzebenen.level7` („Bäckerei" → „Handwerk" in der Netzebenen-Grafik), `UeberUns.prinzipien` („Ob Bäckerei, Hotel oder Handel" → „Ob Werkstatt, Hotel oder Produktionsbetrieb"), `Branchen.Overview.metaDescription`/`lead`/`intro`/`cta`, `Home.Branchen.lead`, sowie stale Code-Kommentare in `branche-page.tsx`/`mdx-components.tsx`/`branchen-teaser.tsx`/`branchen-overview.tsx`. **Verbleibende Treffer sind ausschließlich bewusst historisch/kontextuell:** die Redirect-Begründung in `next.config.mjs`, der Erklärabsatz in `lib/branchen.ts`, die Handover-Historie in dieser Datei, `report-gallery.tsx` (meint die **Demo-Bäckerei des Kalkulators** in `packages/engine` — ein echtes Testfixture, keine Branchenseite) und der Styleguide-Platzhalter „Bäckerei Muster GmbH" (ein Firmenname-Beispiel in einem Formularfeld, keine Branchenreferenz).
    - **[NICHT ANGEFASST, bewusst] `Pflichtenheft_Website_Coolin.md`** nennt weiterhin Bäckerei/Handel als Start-Branchen und „Nachtspeicheröfen" (§5.3/§6.2/§4.1). Das ist das SPEZIFIKATIONSDOKUMENT, kein Seiteninhalt — es zu überschreiben hieße, die Entscheidungsgrundlage rückwirkend umzuschreiben. **Andreas' Entscheidung**, ob das Pflichtenheft nachgezogen wird; bis dahin weicht der Bau an genau diesen zwei Stellen bewusst und dokumentiert ab.
  - **[TEIL 4 — „Nachtspeicheröfen" → „Speicheröfen"]** 8 Ersetzungen, alle in `messages/de.json` (Singular/Plural passend). **Nicht angefasst und vorher geprüft, nicht geraten:** der Slug `/leistungen/smart-heating` und der Nav-Label „Smart Heating" (enthalten das Wort nachweislich nicht). „Nachtfenster" (in „starres Nachtfenster") bleibt — anderes Wort, andere Bedeutung. **Gemessen:** 0 verbleibende Treffer in `messages`/`components`/`app`/`lib`/`content`.
  - **[TEIL 5 — Kalkulator-Zugangscode, Soft-Gate]** `lib/kalkulator-access.ts` (Code `coolin2026` + localStorage-Key + `isValidAccessCode`, mit dem geforderten „keine echte Sicherheit"-Kommentar) + `components/peak-shaving/calculator-gate.tsx` (Client). Das Gate sitzt **auf der Zielroute** `/peak-shaving/kalkulator/rechner`, nicht an den Links dorthin — sonst per Direkt-URL umgehbar und an jeder neuen Verlinkung mitzudenken. **`apps/website` unangetastet.** `unlocked` ist **dreiwertig** (`null` = „noch nicht gelesen"): localStorage gibt es beim Vorrendern nicht, ein Formular als Startzustand blitzte bei jedem freigeschalteten Nutzer einen Frame lang auf. localStorage-Zugriffe in `try` (Safari privat wirft) — nicht persistierbar heißt „gilt für diese Sitzung", nicht „ausgesperrt". **Gemessen (Playwright, Production-Build):** Erstbesuch Formular=1/iframe=0/localStorage=null → falscher Code: Formular bleibt, iframe=0, `aria-invalid=true` + sichtbare Meldung „Dieser Zugangscode stimmt nicht…" → richtiger Code: Formular=0, iframe=1, localStorage=`true` → Reload: iframe direkt, kein erneutes Prompt. **Ungated belegt:** `/peak-shaving/kalkulator` (Produktseite) 0 Gate-Formulare; Schnellrechner auf `/branchen/handwerk` in einem **frischen Browser ohne localStorage** 3 Felder bedienbar (erster Wert 500), 0 Gate-Formulare.
  - **[VERIFIKATION gesamt, gemessen gegen den Production-Build]** `build`/`test` (162/162: 18 shared + 144 engine)/`typecheck`/`lint` grün, **0 Build-Warnungen**. Alle 5 neuen Branchen **SSG (●)**, alle 21 sitemap-Routen + die 7 noindex-Routen **200**, genau 1 H1 je Branchenseite, sitemap zeigt 5 statt 4 Branchen. **Kein horizontaler Überlauf @375px (0 px auf 10 Seiten), 0 Konsolenfehler** über alle Läufe.
  - **[TEST-ARTEFAKT, kein Bug — für Folge-Prompts]** Ein `p.locator('[role=alert]').textContent()` auf der Gate-Seite wirft (Strict-Mode: **2** `role=alert`-Knoten im Dokument). Mit einem `.catch(() => null)` daneben sieht das aus wie „keine Fehlermeldung". Die Meldung IST da — gezielt gegen `#kalkulator-code-error` + `aria-invalid` gegengeprüft.

- **[GEBAUT: Prompt 18] Grafik-Platzierung auf `/leistungen/pv-speicher` korrigiert + neue Grafik auf `/leistungen/energiemanagement` von Anfang an richtig platziert.** Prompt 16 hatte die PV-Grafik fälschlich neben die Hero-Sektion selbst gehängt (Eyebrow/H1/Intro) — dort sitzen auf allen 6 Leistungsseiten ausdrücklich nur diese drei Elemente, einspaltig (§5.1). Korrigiert: der Grafik-Slot sitzt jetzt neben dem ERSTEN Inhaltsblock DANACH (`AusgangslageSection` — eigene Zwischenüberschrift + Fließtext, „Ausgangslage/Problem").
  - **[SLOT VERSCHOBEN, EIN MECHANISMUS für beide Seiten]** `components/leistung/leistung-page.tsx`: der optionale Grafik-Slot heißt jetzt `firstSectionGraphic` (vorher `heroGraphic`) und wird in `AusgangslageSection` gerendert (zweispaltig ab `lg`, Text links/Grafik rechts, `grid-cols-[1fr_22rem]` — dieselbe Grid-Form wie zuvor am Hero, nur eine Sektion tiefer). `LeistungHero` ist wieder auf allen 6 Seiten bit-identisch einspaltig. Bewusst KEIN zweites Slot-Muster für die neue energiemanagement-Grafik — beide Seiten befüllen denselben Slot.
  - **[UMBENANNT]** `pv-verbrauch-hero-graphic.tsx` → `pv-verbrauch-graphic.tsx` (`PvVerbrauchHeroGraphic` → `PvVerbrauchGraphic`, „Hero" war nach dem Umzug irreführend). `pv-verbrauch-chart.tsx`/`lib/pv-verbrauch-curve.ts` selbst unverändert (Ehrlichkeitsregeln/Caption/Legende bleiben) — nur die Einbindungsstelle hat sich verschoben.
  - **[NEU] `EnergiemanagementAggregationGraphic`** (`components/leistung/energiemanagement-aggregation-graphic.tsx`): reine Topologie, kein Recharts. Fünf optisch gleichrangige Quellblöcke (Druckluft, Kälte, Wärme, Lüftung, Produktion — exakt gleich groß, keine Größen-/Flächenhierarchie) laufen als SVG-Bezierkurven in EINEN Zählpunkt-Knoten zusammen (einziger Teal-Akzent der Grafik), der in EINEN neutral gehaltenen, undifferenzierten Endpunkt „Ihre Rechnung" mündet. SVG statt HTML (anders als `netzebenen-grafik.tsx`): alle Labels stehen als echte `<text>`-Knoten im DOM (kein `aria-hidden`, keine ausgelagerte Caption nötig — anders als bei den Recharts-Charts, die illustrative MESSWERTE zeigen, die hier gar nicht existieren). Eigene Texte unter `Leistungen.Pages.energiemanagement.chart` (`messages/de.json`) — eigenständige Caption, bewusst NICHT die PV-Curve-Caption „keine Messdaten" kopiert (andere Aussage: hier wird gar keine Messung suggeriert, sondern erklärt, dass die Rechnung unaufgeschlüsselt ist).
  - **[VERIFIKATION, gemessen via Playwright gegen den Production-Build]** Hero auf beiden Seiten strukturell einspaltig (kein `lg:grid-cols-*`-Ancestor am `<h1>`); erste `<section>` auf beiden Seiten zweispaltig MIT `<svg>`; die anderen 4 Leistungsseiten unverändert (0 `<svg>`/kein Grid in der ersten Sektion). Zahlen-Audit energiemanagement-Grafik: `textContent` der SVG enthält 0 Ziffern, 0 „%". Signature-Motiv-Zählung (`SignatureRule`/`SignatureField`-Pfadsignatur) auf beiden Seiten weiterhin genau **1×** (Footer). Kein horizontaler Scroll @375px (`scrollWidth === clientWidth`, beide Seiten). 0 Konsolenfehler. Alle 18 stichprobenartig geprüften Routen weiter 200. `pnpm build`/`test`/`lint`/`typecheck` (Root) grün.

- **[GEBAUT: Prompt 13c] Zwei kleine, unabhängige Korrekturen aus dem 13b-Bericht.**
  - **[Cross-Links zum Artikel]** `/branchen/baeckerei` + `/branchen/gastronomie` verlinken jetzt konkret auf „Leistungstarif 2027" (`lib/branchen.ts`: neues `relatedArticleSlug` an genau diesen beiden — ihre Tageskurve ist eine kurze Anlaufspitze, exakt der Mechanismus, den der Artikel durchrechnet; Hotellerie/Handel bewusst NICHT, gleiche Zurückhaltung wie bei `CAP_HINT`). Karte in `HebelSection` (`branche-page.tsx`) — **dieselbe Kartenoptik wie die Hebel-Karten daneben** (kein neues Muster), `BookOpen`-Icon statt Leistungs-Icon, Titel/Teaser 1:1 aus dem Artikel-Frontmatter (`findArticle`, nicht zweitgetippt).
    **Alle 6 Leistungsseiten** bekommen zusätzlich eine universelle „Weiterlesen"-Karte in der bestehenden `CrossLinkSection` (gleiche `<li><TextLink>`-Optik wie die übrigen Cross-Links) — bewusst NICHT über `CROSS_LINKS` (das bleibt inhaltlich kuratiert, `esg` bleibt dort leer): Der Leistungstarif 2027 ist Kostenkontext für JEDE Leistung, keine Passung zwischen zwei Leistungen. `/leistungen/esg` zeigt die Karte deshalb trotzdem.
    **Bewusst KEIN generisches Artikel-Vokabular:** ein literaler Slug (`RELATED_ARTICLE_SLUG`) an beiden Stellen, keine `relatedArticles: string[]`-Infrastruktur für einen einzigen Artikel.
    **Gemessen:** `/branchen/baeckerei` + `/branchen/gastronomie` → 1 Link auf den Artikel; `/branchen/hotellerie` + `/branchen/handel` → 0 (korrekt kein Zwangslink); alle 6 Leistungsseiten inkl. `/leistungen/esg` → je 1 Link.
  - **[Platzhalter aus dem Index]** `/produkte`, `/referenzen`, `/ueber-uns`, `/impressum`, `/login` sind reine `PagePlaceholder` ohne Inhalt — jetzt `indexable: false` in `lib/routes.ts` (`PLACEHOLDER_HREFS`). Die 5 Seiten holen ihr `robots`-Tag jetzt über `robotsFor(href)` ab (vorher gar kein `robots`-Feld) — **kein neuer Mechanismus**, derselbe wie bei der rechner-Hülle.
    **Zurückstellen, sobald eine Seite Inhalt bekommt:** den Href aus `PLACEHOLDER_HREFS` (`lib/routes.ts`) streichen — ein Einzeiler, die Seite fällt auf `indexable: true` zurück, `robots` verschwindet automatisch, die sitemap nimmt sie automatisch wieder auf.
    **BEOBACHTUNG, nicht behoben:** Die 5 Seiten behalten ihren Canonical (`alternates`), obwohl sie jetzt `noindex` sind — dieselbe Kombination, die 13a bei der rechner-Hülle ausdrücklich als „widersprüchliches Signal" vermieden hat. Bewusst NICHT angefasst: Der Prompt hat explizit nur „Robots-Meta + sitemap-Ausschluss" verlangt, und die Seiten sind ohnehin bald weg (bekommen echten Inhalt). Falls das vor dem Befüllen stört: `alternates` in denselben 5 Dateien entfernen, analog zur rechner-Hülle.
    **Gemessen:** sitemap.xml vorher/nachher-Diff zeigt exakt diese 5 URLs verschwunden (24 → 19 Einträge); alle 5 Seiten `noindex, follow` im HTML; unveränderte Seiten weiterhin ohne `robots`-Tag.
  - **[VERIFIKATION, gemessen]** `build`/`test` (162/162)/`typecheck`/`lint`/`prettier` grün. Alle 26 Routen weiter 200, genau 1 H1 pro Seite. 13b-JSON-LD-Regression: alle 17 FAQ-Paare weiter wörtlich identisch, FAQPage weiter nur wo erwartet. CLS 0,0000 + 0 Konsolenfehler auf allen 4 geänderten Seiten (`/branchen/baeckerei`, `/branchen/gastronomie`, `/leistungen/esg`, `/leistungen/pv-speicher`).

- **[GEBAUT: Prompt 13b] JSON-LD + sitemap.xml + robots.txt + 301-Redirects (§6.4).** Baut auf 13a auf: alles leitet sich aus `SITE_URL`/`absoluteUrl` (`lib/site.ts`) ab, es gibt weiterhin KEINE zweite Domain-Quelle.
  - **[NEUE ENV-WIRKUNG — betrifft Andreas' Cutover] `NEXT_PUBLIC_SITE_URL` steuert jetzt AUCH die Indexierung.** Solange der Wert ≠ `https://coolin.at` ist, liefert `/robots.txt` **`Disallow: /`** — die Preview darf nicht ranken und der späteren coolin.at kein inhaltsgleiches Duplikat entgegenstellen. Erst mit `https://coolin.at` schaltet robots auf `Allow: /` + Sitemap-Zeile. **Der Cutover bleibt EIN Handgriff** (dieselbe Variable schaltet Canonicals, hreflang, OG-Bild UND robots gemeinsam). **Bewusst NICHT an `VERCEL_ENV`**: Das Vercel-Projekt hat JETZT SCHON ein Production-Deployment — es liegt nur auf der vercel.app; `VERCEL_ENV=production` gäbe die Preview frei. Fail-closed: unbekannter Origin → kein Index. Gemessen: localhost → `Disallow: /` (ohne Sitemap-Zeile, die wäre daneben ein Widerspruch); Build mit `NEXT_PUBLIC_SITE_URL=https://coolin.at` → `Allow: /` + `Sitemap: https://coolin.at/sitemap.xml`.
  - **[EIN FUNDORT FÜR INDEXIERBARKEIT] `lib/routes.ts`** ist neu die Wahrheit über „welche Routen gibt es, welche sollen in den Index". Die sitemap liest `indexable`, und **die rechner-Hülle holt ihr `noindex` jetzt VON DORT** (`robotsFor(CALCULATOR_RUN_HREF)` statt eines eigenen Literals) — vorher hatte die Aussage zwei Fundorte und die sitemap hätte eine Seite anbieten können, die sich selbst auf `noindex` stellt. Verhalten unverändert (gemessen: weiter `noindex, follow`, weiter 0 Canonicals).
  - **[GUARD, bewiesen statt behauptet] `assertRoutesMatchDisk()`** vergleicht beim BAUEN die Routenliste mit den echten `page.tsx` unter `app/(site)/[locale]/` und bricht in BEIDE Richtungen (Seite ohne Eintrag → fehlte still in der sitemap; Eintrag ohne Seite → sitemap schickt Google auf 404). Gleiche Haltung wie `lib/site.ts`/`lib/wissen.ts`. **Test gefahren:** Wegwerf-Seite angelegt → Build bricht mit `„… fehlten damit still in der sitemap: /__guard-test"`; Seite entfernt → Build grün. Die meisten Routen werden aus `lib/nav.ts` ABGELEITET, nur menülose Seiten (`/`, Footer-/Rechtstexte) stehen explizit.
  - **[JSON-LD] `lib/json-ld.ts` (Formen) + `components/json-ld.tsx` (`<JsonLd>`)** — serverseitig gerendert, ein wiederverwendbares Muster statt Copy-Paste je Seite. **Organization+LocalBusiness als EIN Knoten mit ZWEI Typen** (`["Organization","LocalBusiness"]`, ein `@id`, im Root-Layout → auf jeder Seite genau 1×): `LocalBusiness` IST eine `Organization`, zwei Knoten behaupteten zwei Firmen. Artikel/`Kalkulator` verweisen nur per `{"@id": …#organization}`, statt die Firma neu zu beschreiben. **Gemessen über 8 Seiten: genau 1 Organization-Definition je Seite, 0 relative URLs**; der Artikel trägt 2 `@id`-Verweise darauf (`author` + `publisher`).
  - **[FAQPage SITZT IN `faq-section.tsx`]** — genau dafür wurde die Komponente in Prompt 11 herausgezogen. Sie liest DIESELBE `items`-Liste im DERSELBEN Render wie die sichtbaren Q&A, damit Googles Bedingung („markup must match visible content", sonst Richtlinienverstoß) nicht eingehalten, sondern **strukturell unverletzbar** ist. **Gemessen: alle 17 Q&A (5 Artikel + 4×3 Branchen) WÖRTLICH identisch mit dem sichtbaren Text**; Gegenprobe: kein FAQPage auf `/`, `/wissen`, `/kontakt`, `/leistungen/esg`, `/peak-shaving/kalkulator`.
  - **[Article] Vollständig aus dem Frontmatter**, kein Feld neu erfasst (Prompt 11 hatte es dafür vorbereitet). `headline` = reiner Titel (nicht der `<title>` mit Marken-Suffix). `author: COOLiN ENERGY` → **Verweis auf den Organization-Knoten, nicht `Person`** — mit Fallunterscheidung, weil ein künftiger menschlicher Autor sonst zur Firma erklärt würde (und „COOLiN ENERGY" als `Person` einen Menschen erfände). `dateModified` fällt auf `date` zurück, wenn `updated` fehlt — das ist die Wahrheit, nicht eine Notlösung.
  - **[BEFUND: `Article.image` bewusst WEGGELASSEN]** Die einzige Bildquelle ist die OG-Karte, und ihre URL ist eine Next-INTERNE Route mit generiertem Hash (**gemessen**: `/de/opengraph-image-1yhjss?5d6238ec81b4ed34` — Pfad-Hash UND Cache-Buster stammen aus dem Dateiinhalt, es gibt keine API dafür). Nachbauen hieße Next-Interna kopieren, die beim nächsten Karten-Wechsel STILL auf ein 404 zeigen. `image` ist bei Google für `Article` **empfohlen, nicht erforderlich** — ein fehlendes Feld ist ehrlicher als ein totes. **Sauberer Weg später:** ein `image`-Frontmatter-Feld, sobald echte Artikelbilder existieren (OP#7); `lib/wissen.ts` validiert es dann wie die übrigen Pflichtfelder.
  - **[Kalkulator: `SoftwareApplication`, kein `Product`]** Ein `Product` ohne `offers` ist für Google unvollständig, und die einzige Art es zu füllen wäre ein Preis — den gibt es nicht (OP#1 offen; §3.3 verbietet der Seite schon in der Copy jede endgültige Preis-Aussage). `SoftwareApplication` beschreibt dieselbe Sache ehrlicher und braucht kein Preisfeld. **Gemessen: kein `offers`/`price`/`priceCurrency`/`aggregateRating`/`review`/`ratingValue`/`reviewCount`.** Preis der Ehrlichkeit: kein Rich Result — richtig bezahlt. Neuer Message-Key `PeakShavingCalculator.appName` (ein Produktname ist eine eigene Rolle, nicht der Nav-Label „Der Kalkulator" oder der Hero-Eyebrow).
  - **[LocalBusiness schlank und WAHR]** Nur `name`, `url`, `email`, `address` (Karl-Popper-Straße 22 / 1100 / Wien / AT). Rechtsform, UID, Firmenbuchnr., Geschäftsführung sind **OP#13 und im Bestands-Impressum selbst „[ergänzen]"** — also unbekannt, nicht bloß unerfasst; Telefon/Social-Profile trägt der Bestand **nachweislich nicht** (geprüft: 0 Treffer). Weggelassen sind außerdem `logo` (OP#7), `geo`, `openingHours`, `priceRange`. **`COMPANY` (`lib/nav.ts`) trägt die Adresse jetzt in EINZELTEILEN** (`PostalAddress` braucht sie getrennt); die sichtbare Zeile „1100 Wien, Österreich" wird daraus ZUSAMMENGESETZT statt danebengeschrieben — sonst stünde dieselbe Adresse zweimal im Repo.
  - **[301 STATT 308 — gemessener Unterschied, nicht Kosmetik]** Next macht aus `permanent: true` ein **308** (belegt). Für Google gleichwertig, aber: 301 ist der universell verstandene Dauer-Redirect (308 ist von 2015), und **308 erhält die HTTP-Methode** — für `/danke.html` relevant, das POST-Ziel des alten Netlify-Formulars: Ein POST aus einer alten, noch im Browser liegenden Seite liefe per 308 als POST auf `/kontakt` → 405. Deshalb `statusCode: 301` (schließt `permanent` aus). Gemessen: `/impressum.html`→`/impressum`, `/datenschutz.html`→`/datenschutz`, `/danke.html`→`/kontakt`, alle **301**; Ziele alle 200.
  - **[BEFUND: `/coolin.html` GIBT ES NICHT]** §6.4 nennt den Pfad, aber die alte Startseite lief unter „/" — belegt durch ihr eigenes `<meta property="og:url" content="https://coolin.at/">`. „/" liefert bereits die neue Startseite. **Kein Redirect gebaut** (nur real existierende Alt-Pfade, sonst erfände man eine URL, um sie umzuleiten). Gemessen: `/coolin.html` → 404. Ebenfalls nicht umgeleitet: die Anker der alten Scroll-Seite (`#leistungen`, `#peak-shaving`, `#vorgehen`, `#ergebnisse`, `#kontakt`) — **Fragmente erreichen den Server nie und sind serverseitig nicht umleitbar**; und `/favicon.png` / `/logo-coolin-energy.png` (Bilder, keine Seiten — §6.4 will Ranking/Backlinks retten, die hängen an Dokumenten).
  - **[sitemap]** 24 URLs, valides XML (`xmllint`), `application/xml`. **URLs entstehen über `getPathname`** — dieselbe Funktion wie Canonical/`Link`, damit sitemap-URL und Canonical nicht auseinanderlaufen können (ein handgebautes `${SITE_URL}/de${href}` wäre eine zweite Auslegung von `localePrefix: 'as-needed'`). **noindex-Seiten fehlen (belegt): rechner-Hülle und `/styleguide` nicht drin, `/peak-shaving/kalkulator` (der indexierbare Elternpfad) drin.** `lastModified` NUR beim Artikel (echtes Frontmatter-Datum, `2026-07-16`) — für die übrigen Seiten gibt es kein ehrliches Datum, und die Bauzeit behauptete bei jedem Deploy, jede Seite habe sich geändert (Google lernt daraus, dem Feld nicht zu glauben). Kein `changeFrequency`/`priority`: Google ignoriert beide erklärtermaßen.
  - **[robots widerspricht `noindex` NICHT — und zwar andersherum als man denkt]** Es stehen bewusst KEINE `Disallow`-Einträge für die noindex-Seiten drin: `Disallow` verbietet das CRAWLEN, nicht das Indexieren — eine gesperrte Seite sieht ihr eigenes `noindex` NIE und kann trotzdem (ohne Inhalt, über Links) im Index landen. `noindex` wirkt nur, wenn der Crawler lesen darf.
  - **[VERIFIKATION, gemessen gegen den Production-Build]** `build`/`test` (162/162)/`typecheck`/`lint`/`prettier` grün. **Alle 26 Routen weiter 200**, **genau 1 H1 pro Seite** (0 Abweichungen über alle 26). 13a-Metadaten unverändert: jede indexierbare Seite weiter genau 1 Canonical + 1 og:image, rechner-Hülle weiter 0 Canonicals + `noindex, follow`, `/styleguide` weiter `noindex, nofollow`. Alle JSON-LD-Blöcke als **valides JSON geparst** (kein Syntaxfehler), alle URLs absolut. **CLS 0,0000 und 0 Konsolenfehler** auf Startseite/Artikel/Branche/Kalkulator; JSON-LD-Scripts haben nachweislich Höhe 0 (erzeugen nichts Sichtbares).
  - **[BERICHTET, NICHT GEBAUT — Verlinkungslücken §6.4]** Audit über `<main>` (Header/Footer ausgeklammert, die verlinken ohnehin alles und würden jede Lücke zudecken): **Die Richtung „→ Wissen" fehlt fast vollständig.** Der 2027-Artikel verlinkt vorbildlich hinaus (Leistungen ✓2, Flaggschiff ✓2, Branchen ✓2), aber **keine einzige Leistungs- oder Branchenseite verlinkt zurück in den Wissen-Bereich** — der Flaggschiff-Artikel hängt allein am Startseiten-Teaser + Nav/Footer. Das ist **strukturell, nicht per Seite vergessen**: `CROSS_LINKS` (`lib/leistungen.ts`) und `HEBEL` (`lib/branchen.ts`) kennen nur Leistungs-Keys + `peakShaving`/`kalkulator` — es gibt **kein Vokabular für einen Artikel-Link**. Ein Fix braucht eine Datenmodell-Entscheidung, keinen Textedit. Naheliegendster Nutzen: `/branchen/baeckerei` → 2027-Artikel (wer über seine Morgenspitze liest, ist genau dessen Publikum). Weitere Beobachtung: **`/leistungen/esg` ist die einzige Leistung ohne jeden Cross-Link nach außen** (`esg: ['energiemanagement','ppa']` — beides Leistungen); wirkt wie eine bewusste Datenwahl (ESG hat sachlich wenig mit Peak Shaving zu tun), daher als Beobachtung gemeldet, nicht als Defekt.
  - **[MERKER: FAQ-Rich-Results sind seit 8/2023 stark eingeschränkt]** Google zeigt FAQ-Rich-Results nur noch für „well-known, authoritative government and health websites" — eine kommerzielle Seite bekommt dafür **kein Sternchen-Snippet mehr**. Das FAQPage-Markup ist trotzdem gebaut (§6.4 verlangt es, es ist valide, Bing wertet es weiter aus, und es hilft bei der maschinellen Erfassung/LLM-Nutzung) — aber die Erwartung „damit kommen FAQ-Boxen in Google" wäre falsch.
  - **[BEOBACHTET, Andreas' Entscheidung] 5 Platzhalter-Seiten stehen in der sitemap** (`/produkte`, `/referenzen`, `/ueber-uns`, `/impressum`, `/login`) — sie tragen nur `PagePlaceholder` („Inhalt folgt"). Die Regel dieses Prompts war „noindex raus", und noindex sind sie nicht; sie fliegen also korrekt NICHT raus. Trotzdem: Dünne Seiten aktiv zum Indexieren anzubieten, ist nicht gratis. Sobald klar ist, welche davon in Phase 1 keinen Inhalt bekommen, wäre `indexable: false` in `lib/routes.ts` ein Einzeiler je Seite (und die Seite bekäme ihr `noindex` automatisch mit).

- **[HANDLUNGSBEDARF ANDREAS — sonst zeigen die Canonicals ins Leere] `NEXT_PUBLIC_SITE_URL` in Vercel setzen (Scope Production).** Wert **jetzt**: `https://peak-shaving-web.vercel.app`; beim DNS-Umzug (§12) auf `https://coolin.at` umstellen — **dieser eine Variablenwechsel IST der Cutover**, kein Code-Deploy. **Ohne die Variable** fällt die Seite auf `NEXT_PUBLIC_VERCEL_URL` zurück — und das ist die URL des EINZELNEN Deployments (`…-a1b2c3-team.vercel.app`): jeder Push schriebe dann sämtliche Canonicals auf eine neue Adresse um. Für Previews/lokal ist genau dieser Rückfall richtig (sie kanonisieren auf sich selbst) und die Variable bleibt dort bewusst leer. Erklärung: `apps/web/.env.example` + Kopf von `lib/site.ts`. Ein ungültiger Wert (z. B. `coolin.at` ohne Schema) **bricht den Build absichtlich** — verifiziert.
  **SEIT 13b HÄNGT AUCH DIE INDEXIERUNG DARAN (§6.4):** Solange der Wert ≠ `https://coolin.at` ist, sagt `/robots.txt` **`Disallow: /`** — gewollt, damit die Preview nicht rankt und der späteren coolin.at kein inhaltsgleiches Duplikat entgegenstellt. **Konsequenz für den Cutover:** Dieselbe eine Variable auf `https://coolin.at` zu setzen schaltet Canonicals, hreflang, OG-Bild **und** die Indexierung gemeinsam scharf. **Danach prüfen:** `https://coolin.at/robots.txt` muss `Allow: /` + die `Sitemap:`-Zeile zeigen (nicht `Disallow: /`) — sonst bleibt die Seite unsichtbar.

- **[GEBAUT: Prompt 13a] Metadaten-/Sharing-Schicht: Basis-URL, metadataBase, Canonical + hreflang, OG-Bild (§6.3/§6.4).** KEIN JSON-LD, keine sitemap/robots, keine Redirects (= 13b).
  - **[BEFUND, vorab geprüft: die URL-Form stimmte bereits] Deutsch läuft unter „/", nicht unter „/de/".** `localePrefix: 'as-needed'` (`i18n/routing.ts`) war schon korrekt gesetzt — **kein Routing-Refactor nötig**, nichts umgestellt. Gemessen am Production-Build: `/leistungen` → **200**, `/de/leistungen` → **307** auf `/leistungen`. Die Canonicals bilden damit exakt die ausgelieferten URLs ab.
  - **[EINE BASIS-URL] `lib/site.ts`** (`SITE_URL` + `absoluteUrl()`): `NEXT_PUBLIC_SITE_URL` → sonst `NEXT_PUBLIC_VERCEL_URL` (Preview kanonisiert auf sich selbst — eine Preview, die auf coolin.at kanonisiert, behauptete, Inhalte einer fremden Domain zu sein) → sonst `http://localhost:3000`. Validiert LAUT (Schema/Origin-only, kein Pfad/Query — ein Pfad in der Basis würde `new URL(pfad, basis)` still verfälschen). `metadataBase` im Root-Layout leitet sich daraus ab; es gibt KEINE zweite Stelle mit einer Domain.
  - **[CANONICAL + hreflang OHNE DRIFT] `lib/seo.ts` → `pageAlternates(locale, href)`** baut die URL über **`getPathname` aus `@/i18n/navigation`** — exakt die Funktion, die auch `Link` benutzt. Der Canonical entsteht damit aus DERSELBEN Quelle wie die Links, die auf die Seite zeigen, und kann keine andere URL behaupten als die ausgelieferte; ein zusammengebautes `${SITE_URL}/de${href}` wäre eine zweite, stille Auslegung von `as-needed`. hreflang läuft über `routing.locales` → **eine zweite Sprache erweitert alle Seiten von selbst**. **`de` + `x-default`, bewusst NICHT `de-AT`**: hreflang sagt, für WEN eine Fassung gedacht ist — es gibt nur diese eine deutsche, sie ist auch für München richtig; `de` ist die weitere, sichere Angabe und deckt sich mit `<html lang="de">`. Die drei Templates holen ihren Pfad aus der bestehenden Datenquelle (`findLeistung(key).href` / `findBranche(key).href` / `articleHref(article.slug)` + `article.locale`), nicht aus getippten Strings (§4.1).
  - **[ENTSCHEIDUNG] Die rechner-Shell bekommt als EINZIGE Seite KEIN `alternates`** — Absicht, kein Vergessen: Canonical/hreflang sind Aussagen über eine Seite, die in den Index soll; auf `noindex` widersprechen sie sich (Google wertet die Kombination als widersprüchliches Signal). `robots: {index:false, follow:true}` ist unverändert. Verifiziert: rechner-Shell weiter `noindex, follow` **und 0 canonical/alternate**; `/styleguide` weiter `noindex, nofollow`.
  - **[ROOT CAUSE, am Build gemessen — nicht geraten] Das OG-Bild liegt NEBEN dem Root-Layout (`app/(site)/[locale]/opengraph-image.tsx`), NICHT in `app/`.** Erster Anlauf lag im App-Root (wie im Prompt skizziert) und lieferte **still gar kein `og:image`**: Next führt Metadaten SEGMENTWEISE zusammen und **ersetzt verschachtelte Objekte, statt sie zu mischen** — der `openGraph`-Block des Root-Layouts (`type`/`siteName`/`locale`) überschrieb das aus der Datei erzeugte `images` vollständig. Bewiesen durch Gegenprobe: Layout-`openGraph` entfernt → `og:image` war wieder da; der `(dev)`-Zweig (Layout ohne `openGraph`) bekam es die ganze Zeit. Im selben Segment greift die Zusammenführung, beides steht nebeneinander. **Nebeneffekt, der zur i18n-Struktur passt: die Karte kennt ihre Locale** (`params.locale` → `messages/<locale>.json`), eine zweite Sprache bekommt ihre eigene ohne Umbau.
  - **[MIDDLEWARE, nötige Folge]** `matcher` um `.*opengraph-image` erweitert. Grund: Die Bild-Route trägt keine Dateiendung und fiele nicht unter den Asset-Ausschluss `.*\..*`; ohne den Eintrag leitete die Middleware die fertige Bild-URL (`/de/opengraph-image-…`) per **307** auf die präfixlose Fassung um (`as-needed` streicht das „/de") — **und verstümmelte dabei den Cache-Buster zu `?hash=`**. Nicht am Anfang verankert wie die anderen Einträge, da der Pfad mit „/de/" beginnt. Ergebnis gemessen: **HTTP 200, image/png, 1200×630, 0 Redirects** unter exakt der URL, die im `og:image` steht.
  - **[OG-KARTE] Gebrandet, ohne OP#7-Asset:** Navy-Fläche (`--color-navy`), Wortmarke **Variante A** (dieselbe wie Header/Footer) in Weiß, der i-Punkt als **Teal-Knoten** (`--color-node` — laut `globals.css` genau für Emblem/Signature auf Navy), kurzer Teal-Akzentstrich, Claim aus `messages/de.json` (`Brand.claim` — derselbe Satz, den der Footer neben derselben Wortmarke zeigt, §8.7: kein String im JSX). Kein Foto, kein Logo-Asset, kein Gradient. **Die Wortmarke ist nachgebaut, nicht importiert:** `components/brand/wordmark.tsx` ist ein SVG mit `<text>` + `var(--color-*)` — Satori rendert beides nicht. Übernommen sind die dort an Inters Glyphen vermessenen METRIKEN (als Em-Anteile), nicht der Code; die Hex-Literale in der Datei sind die einzige erlaubte Ausnahme (Satori kennt keine CSS-Variablen) und sind als „bei Token-Änderung nachziehen" markiert.
  - **[WORTMARKE PIXELGENAU VERIFIZIERT]** Nicht nur „sieht gut aus": PNG dekodiert und ausgemessen — i-Stamm endet auf **177**, „N" auf **177** (Grundlinie exakt getroffen); Stamm-Oberkante **121** = Grundlinie − 57 = die x-Höhe (0,546 em) auf den Pixel; Teal-Knoten **18×18 px** bei cy **106,5** (Soll 106,8). Die Konstanten aus `wordmark.tsx` (Versalhöhe 27,3 / x-Höhe 45,4) wurden zusätzlich gegen die echten Font-Tabellen geprüft: cap 1490/2048 = 0,7275 em, x 1118/2048 = 0,5459 em — **sie stimmen**.
  - **[FONTS IM REPO] `apps/web/assets/fonts/Inter-{Regular,Bold}.ttf`** (~640 KB, SIL OFL, mit README). Satori braucht Glyphen-Bytes und kann `next/font` nicht anzapfen (hash-benannte Pfade ohne API, und **woff2 versteht Satori nicht**). Bewusst im Repo statt Build-Zeit-`fetch`: sonst hinge jeder Vercel-Build an Google (§7.4 „selbst gehostet"). **Landen nie im Browser** — nur beim Vorrendern gelesen. Voller Latin-Satz, bewusst NICHT per `text=` subsettet: ein Subset würde beim nächsten Claim-Wechsel still leere Kästchen rendern statt laut zu brechen.
  - **[og:title/description ERBEN, wie gefordert]** Der `openGraph`-Block im Root-Layout (`type`/`siteName`/`locale: de_AT`) + `twitter: {card: 'summary_large_image'}` genügen: Next füllt `og:title`/`og:description`/`twitter:*` automatisch aus dem `title`/`description` **jeder** Seite, sobald ein `openGraph`-Objekt existiert. **Keine einzige `generateMetadata` musste dafür angefasst werden** (nur um `alternates` ergänzt).
  - **[VERIFIKATION, gemessen gegen den Production-Build]** `build` (**0 Warnungen**, insb. keine `metadataBase`-Warnung) / `test` **162/162** / `typecheck` / `lint` grün. **Alle 26 Routen weiter 200**; **jede indexierbare Seite genau 1 Canonical** (0 Abweichungen, Sweep über alle Routen). View-Source Startseite: title, canonical, hreflang `de` + `x-default`, `og:title`/`description`/`site_name`/`locale`/`type`, **absolute** `og:image` + `:width` 1200 / `:height` 630 / `:alt` / `:type`, `twitter:card=summary_large_image` + `twitter:image`. **Cutover-Probe:** Build mit `NEXT_PUBLIC_SITE_URL=https://coolin.at` → Canonicals `https://coolin.at/leistungen/pv-speicher`, og:image absolut auf coolin.at; Build mit `coolin.at` (ohne Schema) → **bricht** mit deutscher Meldung. **CLS 0,0000 und 0 Konsolenfehler** auf Startseite/Leistung/Artikel/rechner-Shell (Playwright) — die Änderungen fassen nur den `<head>` an.
  - **[HINWEIS]** `hrefLang` steht im HTML in genau dieser Schreibweise (React-Attributname) — ein `grep hreflang` findet es nur case-insensitiv. Kein Fehler: HTML-Attribute sind case-insensitiv, Google liest es korrekt.
  - **[NICHT GEBAUT, bewusst]** Titelgetriebene OG-Variante für den 2027-Artikel (Scope gehalten — die Karte ist markengetrieben, `og:title` trägt bereits den Artikeltitel). Ein `de-AT`-hreflang (s. o.). JSON-LD/sitemap/robots/Redirects = 13b.
- **[HANDLUNGSBEDARF ANDREAS — sonst geht jede Kontaktanfrage verloren] Resend-Env in Vercel setzen.** Das Kontaktformular ist gebaut und live-fähig, aber **ohne `RESEND_API_KEY` + `RESEND_FROM` stellt es NICHTS zu** — es meldet dem Nutzer ehrlich „nicht eingerichtet" (kein falscher Erfolg) und loggt serverseitig. Nötig: (1) Domain in Resend verifizieren (DNS: SPF+DKIM), (2) `RESEND_API_KEY` + `RESEND_FROM` (verifizierte Domain, z. B. `COOLiN ENERGY <noreply@coolin.at>`) in Vercel eintragen, (3) einmal echt absenden. `RESEND_TO` ist optional (Default `energy@coolin.at`). Namen + Erklärung: `apps/web/.env.example` und der Kopf von `lib/kontakt/deliver.ts`. **Der echte Sendetest gehört zu Andreas** — CC kann ohne Key nur den Sendepfad beweisen, nicht die Zustellung.
- **Fertig gebaut:** Scaffold · Design-System (Tokens, Inter-only, Signature-Motiv, neutrales Off-White) · Layout/Nav/i18n-Grundgerüst · Startseite (7 Sektionen) · Mega-Menü-Feinschliff · Schnellrechner (funktionsfähig, live nachrechnend, `components/quick-calculator.tsx`) · Peak-Shaving-Flaggschiff (`/peak-shaving` Erklärseite mit Diagramm + `/peak-shaving/kalkulator` Produktseite) · Sticky Header (Root-Cause-Fix: `overflow-x: clip` statt `hidden`, s. u.) · Kalkulator-iframe-Einbettung (`/peak-shaving/kalkulator/rechner`, embed-Modus in `apps/website`) · 2 native Grafik-Sektionen auf der Produktseite · 6 Leistungsseiten + Übersicht über EIN Template (`lib/leistungen.ts` + `components/leistung/leistung-page.tsx`) · **Report-Galerie mit 4 echten Kalkulator-Screenshots** (`components/peak-shaving/report-gallery.tsx`, ersetzt die Platzhalter — `screenshot-placeholder.tsx` ist gelöscht) · 4 Branchenseiten + Übersicht über EIN Template (`lib/branchen.ts` + `components/branche/`) · **Wissen-Bereich + Flaggschiff-Artikel „Leistungstarif 2027"** (s. u.).

- **[GEBAUT: Prompt 12] Kontaktformular `/kontakt` mit Live-Resend (§5.5/§8.6).** Der `PagePlaceholder` ist ersetzt: eigene Seite (Intro → Formular → Kontaktblock mit `<address>` Karl-Popper-Straße 22, Local-SEO), Formular als eigene Client-Komponente. **Netlify Forms und reCAPTCHA sind damit endgültig raus** (der Bestand postete an `/danke.html` — auf Vercel ein 404 und ein still verlorener Lead).
  - **[THEMEN DATENGETRIEBEN, nicht getippt] `lib/kontakt/themen.ts`** leitet die 8 Optionen aus `LEISTUNGEN` (→ `lib/nav.ts`) ab + `peakShaving` + `sonstiges`. Gemessen im DOM: `pvSpeicher · energiemanagement · smartHeating · ppa · finanzierung · esg · peakShaving · sonstiges`. Labels der 6 Leistungen kommen aus `Nav.<key>` (ein zweiter Name für dieselbe Leistung wäre für den Absender verwirrend), die zwei Zusätze aus `Kontakt.themen.*`. **§5.5 zählt „Smart Heating" separat auf — das ist eine Ungenauigkeit des Pflichtenhefts**: es IST eine der 6 und steht deshalb nicht doppelt. Der Bestand führte 7 handgetippte Optionen inkl. „Gewerbespeicher" (keine eigene Leistung) — genau die Drift, die dieses Modul verhindert.
  - **[EINE SCHEMA-WAHRHEIT] `lib/kontakt/schema.ts`** (zod, rein — kein `server-only`, kein `next/*`) läuft im Formular UND in `app/api/kontakt/route.ts`. Fehlertexte sind **Keys** (`nameRequired`), keine Sätze: §8.7 verlangt die Wortwahl in `messages/de.json`, und serverseitig gibt es keinen Locale-Kontext. **Der Contract (`KontaktResponse`) steht in der Schema-Datei, nicht in der Route** — sonst hinge das Client-Formular an einem Modul, das `server-only` + das Resend-SDK zieht.
  - **[ZUSTELLUNG ISOLIERT] `lib/kontakt/deliver.ts`** mit `import 'server-only'` + **dynamischem** `await import('resend')` hinter dem Env-Guard. Env: `RESEND_API_KEY`/`RESEND_FROM` (Pflicht) + `RESEND_TO` (Default `COMPANY.email` aus `lib/nav.ts` — kein zweiter getippter Adress-Fundort). **Gemessen (lokaler Capture-Server via `RESEND_BASE_URL`, echter SDK-Aufruf):** `to=energy@coolin.at · reply_to=<Absender-Adresse> · subject="Kontaktanfrage: ESG / CSRD — Martin Muster"`, text+html mit Name/E-Mail/Unternehmen/Telefon/Thema/Nachricht/Zeitstempel (Europe/Vienna, de-AT). **Thema-Label wird SERVERSEITIG aufgelöst** (Client schickt nur den Key — sonst beschriftet der Absender unsere eigene Betreffzeile). HTML-Escaping bewiesen: `<img src=x onerror=…>` landet als `&lt;img …&gt;`, kein rohes `<script>`.
  - **[KEIN FALSCHER ERFOLG — gemessen]** Ohne Key → HTTP **503 `not_configured`** + Server-Warnung „Resend nicht konfiguriert — RESEND_API_KEY, RESEND_FROM fehlt", **kein Crash** (Seite danach weiter 200). Mit ungültigem Key → echter Resend-Aufruf, dessen **401** → HTTP **502 `send_failed`**, im Log `{statusCode: 401, message: 'API key is invalid'}`. Beide Fehler zeigen dem Nutzer die **Fallback-Adresse** `mailto:energy@coolin.at` + „Erneut senden"; Eingaben bleiben erhalten.
  - **[ENTSCHEIDUNG, bewusst gegen die Lehrbuch-Empfehlung] Honeypot lehnt AB (400 `spam`), statt still 200 zu liefern.** Das Lehrbuch sagt „nie verraten, dass die Falle zuschnappte" — aber ein stiller Erfolg zeigt dem Absender „Danke, wir melden uns", und niemand meldet sich je. Träfe die Falle einen echten Menschen (Autofill), wäre der Lead unwiederbringlich weg, ohne dass eine Seite davon weiß. Falscher Erfolg = verlorener Lead wiegt hier schwerer als das bisschen, was ein Bot aus einem 400 lernt. Feld `website`, off-screen + `tabIndex={-1}` + `aria-hidden` (per Position ausgeblendet, NICHT `display:none` — das überspringen auch schlichte Bots). Playwright: für Menschen unsichtbar.
  - **[TURNSTILE ENV-GATED, beide Richtungen]** Ohne `NEXT_PUBLIC_TURNSTILE_SITE_KEY` rendert das Widget nicht und **lädt kein Script** (gemessen: 0 Cloudflare-Scripts im DOM, 0 Requests) — Honeypot ist dann der Schutz. Ohne `TURNSTILE_SECRET_KEY` wird serverseitig **übersprungen**; Secret gesetzt + kein Token → **abgelehnt** (sonst umginge ein Bot den Schutz durch Weglassen des Feldes). Netzfehler gegen Cloudflare → ablehnen, nicht durchwinken.
  - **[SUPABASE: NUR EINHÄNGESTELLE]** Kommentierter Block in `deliver.ts` (nach erfolgreichem Versand). Kein Client, keine Dependency, kein Env — `grep supabase apps/web/package.json` = 0. **Vorgabe für später:** Der Insert darf die Zustellung nicht umwerfen — ein DB-Fehler nach erfolgreicher Mail ist KEIN `send_failed` (die Anfrage IST beim Menschen; eine Fehlermeldung provozierte eine zweite, identische Anfrage).
  - **[DEEP-LINK, bounded] `?thema=<key>`** nur von den **6 Leistungsseiten** (`kontaktHrefFor(leistung.key)` — der Key IST ein Thema-Key, geschenkt statt geraten; wirft bei unbekanntem Key). Startseite/Branchen/Übersichten behalten den nackten `/kontakt`: eine geratene Vorauswahl müsste der Nutzer wegklicken. Gemessen: Klick auf den CTA von `/leistungen/esg` → `/kontakt?thema=esg` → Dropdown steht auf „ESG / CSRD"; `?thema=quatsch` wird ignoriert (kein Crash).
  - **[ENTSCHEIDUNG] `window.location.search` statt `useSearchParams()`** — der Hook zwänge die Seite in dynamisches Rendering oder den Baum hinter eine `<Suspense>`-Grenze; dann stünde das Formular **nicht mehr im vorgerenderten HTML** (ohne JS und für Crawler unsichtbar). Die Vorauswahl ist Bequemlichkeit und darf nach der Hydration passieren, das Formular nicht. Belegt: `/kontakt` bleibt im Build **● (SSG)**, alle 7 Felder stehen im statischen HTML.
  - **[A11Y §9.4, gemessen]** `<form noValidate>` + `required` bleibt (Screenreader sagen „erforderlich"; die native Blase spräche in Browser-Sprache und zeigte nur EINEN Fehler). Jedes Feld: verknüpftes `<label for>`, `aria-invalid`, `aria-describedby` → deutsche Meldung. Fehler-Summary `role="alert"`, Erfolg `role="status"` + Fokus (das Formular ist dann weg — der Fokus läge sonst im Nirgendwo). Fokus springt ins **erste** fehlerhafte Feld (gemessen: `name`). Der `/datenschutz`-Link steht **im** Label — die HTML-Spec schließt interaktive Nachfahren von der Label-Aktivierung aus, der Klick navigiert also, ohne die Checkbox zu schalten.
  - **[NEUES PRIMITIV] `Checkbox`** in `components/ui/input.tsx` (nativ + `accent-color` via `accent-accent`). Kein Radix: Der einzige historische Grund, eine Checkbox nachzubauen, war ihre Unstylebarkeit — `accent-color` löst genau das. DESIGN.md nachgezogen.
  - **[REFAKTOR]** Die Unterlagen-Liste (`doc1..doc5`) ist von `Home.Kontakt` nach `Kontakt.docs` gewandert — `/kontakt` und der Startseiten-CTA zeigen dieselbe Liste; zwei Kopien liefen beim ersten „ach ja, und die Zählpunktnummer" auseinander.
  - **[VERIFIKATION, gemessen gegen den Production-Build]** `build`/`test` (144/144)/`typecheck`/`lint` grün. **Alle 26 Routen weiter 200.** Zustände idle/submitting (Spinner + `disabled` + SR-Ansage „Wird gesendet …")/success/error alle im Browser gesehen. **Kein Secret im Client-Bundle** (`grep RESEND_API_KEY|api.resend.com .next/static/` = leer; das SDK liegt nur in `.next/server/`). Kein Gradient, kein reCAPTCHA (0 Treffer), **kein horizontaler Scroll @375px (0 px)**, keine Konsolenfehler bei sauberer Ladung. Signature-Motiv weiterhin nur im Footer (1×/Ansicht).
  - **[HINWEIS FÜR FOLGE-PROMPTS]** Der `frontend-design`-Skill war in dieser Session **nicht installiert** (nicht in der Skill-Liste, kein `.claude/skills/`). Gebaut wurde deshalb strikt nach `DESIGN.md` (das sich selbst als Wahrheit erklärt) + den Mustern der bestehenden Seiten. Falls der Skill existieren soll, muss er eingerichtet werden — er lässt sich nicht „konsultieren", wenn er fehlt.

- **[GEBAUT: Prompt 11] Wissen-Bereich (MDX) + Flaggschiff-Artikel „Leistungstarif 2027" (§6.1/§6.5/§10.1).** `/wissen` (datengetriebene Collection) + `/wissen/[slug]` (dynamische Route — hier korrekt, weil Collection; Leistungen/Branchen bleiben statische Ordner, weil kuratierte IA). Artikel als MDX im Repo: `content/wissen/<slug>.<locale>.mdx`. Der Platzhalter (`PagePlaceholder`) auf `/wissen` ist ersetzt.
  - **[PIPELINE] `next-mdx-remote/rsc` (`compileMDX`), nicht `@next/mdx`.** Grund: `@next/mdx` ist für MDX-ALS-ROUTE gedacht; für eine Collection mit `[slug]` bräuchte es dynamische Imports + ein Remark-Plugin fürs Frontmatter. `compileMDX` liest die Datei mit `fs`, kann Frontmatter nativ und braucht KEINE `next.config.mjs`-Änderung. Kompiliert dank `generateStaticParams` + `dynamicParams = false` zur BUILD-Zeit → beide Routen sind prerendert (● SSG), kein Compiler im Request-Pfad, kein MDX-Compiler im Client-Bundle. Neue Deps: `next-mdx-remote`, `vfile`, `vfile-matter`. **`vfile-matter` bewusst als DIREKTE Abhängigkeit** (nicht `gray-matter`): `next-mdx-remote` nutzt intern exakt dieses Paket — eine zweite YAML-Engine wäre eine zweite Auslegung derselben Datei, und Übersicht und Artikelseite könnten unterschiedliche Titel zeigen. Der Typ der Komponenten-Map kommt aus `MDXRemoteProps['components']` statt aus `mdx/types` — spart die Dep `@types/mdx` und bindet die Map an das, was `compileMDX` tatsächlich nimmt.
  - **[I18N] Locale im Dateinamen**, nicht Fließtext in `messages/de.json`. Ein EN-Pendant ist eine Geschwisterdatei (`leistungstarif-2027.en.mdx`) — kein Rearchitecting. **Bewusste, begründete §8.7-Ausnahme:** UI-Chrome des Bereichs (Übersicht, Labels, FAQ-Titel, Chart-Beschriftungen) steht weiter in `messages/de.json` (Namespace `Wissen`); nur der ARTIKEL-FLIESSTEXT nicht — ein 9-Minuten-Fachtext ist in flachem JSON weder les- noch redigierbar, und das Autoren-UI (§10.1, Phase 2 Keystatic) schreibt genau diese MDX-Dateien.
  - **[FRONTMATTER, JSON-LD-ready] `lib/wissen.ts`** liest `content/wissen/` und validiert LAUT (wirft beim Build statt eine Seite ohne `description` auszuliefern — bei §6.1 der Fehler, der niemandem auffällt, bis das Ranking fehlt). Felder: `title`/`description` (§6.3) · `slug` (gegen den Dateinamen geprüft — driften sie, zeigt die Seite unter anderer Adresse als der Artikel behauptet) · `tag`/`teaser` (Index-Karte + Startseiten-Teaser; `teaser` ≠ `description`, zwei Rollen) · `date`/`updated` (→ späteres `datePublished`/`dateModified`, im DOM als `<time dateTime>`) · `author` · `readingMinutes` · `featured` (genau EINER je Locale, sonst wirft es) · `faq: [{q,a}]` · `sources: [{label,url}]`. **JSON-LD selbst ist NICHT gebaut** (eigener Prompt) — nur nicht verbaut.
  - **[FAQ WIEDERVERWENDET, nicht neu gebaut] `components/faq-section.tsx`** ist aus `branche-page.tsx` HERAUSGEZOGEN und wird jetzt von Branchen UND Artikeln benutzt. Grund ist nicht Zeilenersparnis, sondern §6.4: ein späterer `FAQPage`-JSON-LD soll GENAU EINE Markup-Wahrheit haben. Weiterhin kein Radix-Accordion (Antworten müssen im HTML stehen). Verifiziert: alle 4 Branchenseiten rendern unverändert 3 Q&A-Paare, der Artikel 5 — je mit Antworttext im DOM, `<button>`/`data-state` im FAQ-Block: keine.
  - **[RICH-MDX-BIBLIOTHEK] `components/wissen/`:** `Callout` (info/accent/warning — **keine Icons**, §7.3; nur drei Varianten, weil semantische Farben nur Bedeutung tragen, kein `success`/`tip`), `Figure`/`ChartFigure` (`<figure>`/`<figcaption>` — die Caption trägt die §9.5-Kennzeichnung, muss also maschinell zugeordnet sein), `mdx-components.tsx` (Typo-Map). **`Figure` ist gebaut, aber im 2027-Artikel NICHT exerziert** — er trägt bewusst nur generierte Charts, kein Stockfoto (§9.5).
  - **[NEUER TOKEN] `--color-warning-border` (#fde68a, Amber 200)** — Gegenstück zu `--color-accent-border`; `/alpha` scheidet auf `var()`-Hex-Tokens aus (DESIGN.md). In `globals.css` + `tailwind.config.ts` + DESIGN.md dokumentiert.
  - **[3 CHARTS, §7.5]** `arbeit-leistung-chart.tsx` (Prinzip-Schema: DIESELBE Kurve zweimal — links Fläche = Arbeitspreis, rechts ein Punkt = Leistungspreis; keine Y-Achse/kein Tooltip/kein Grid/keine Einheit, Ehrlichkeitsregeln aus `tagesverlauf-chart.tsx`) · `netzebenen-grafik.tsx` (**bewusst HTML statt SVG/Recharts** — vorlesbar, indexierbar („Netzebene 7" soll ranken, §6.2), ohne Client-JS; einzige Server-Komponente der drei; NE 7 trägt zusätzlich ein TEXT-Label „Neu ab 2027", Farbe ist nicht das einzige Merkmal, WCAG 1.4.1) · `entzerrung-chart.tsx` (illustratives Beispiel MIT kW-Achse, wie `load-curve-chart.tsx`; **`assertSameEnergy()` wirft beim Modul-Laden**, wenn beide Kurven nicht dieselbe Energie tragen — ein „weniger Spitze, weil weniger verbraucht" wäre unehrlich und am Bild unsichtbar).
  - **[FAKTEN-KORREKTUR, WICHTIG — der Prompt lag falsch] Der Zielsplit ist 50/50, NICHT 40/60.** Der Prompt gab „Zielsplit E-Control 40 % Leistung / 60 % Arbeit" als verifizierte Faktenbasis vor. Gegen das Primärdokument geprüft (E-Control SNE-G-V Begutachtungsentwurf, PDF-Text extrahiert): dort steht wörtlich „Innerhalb der nächsten drei Jahre soll ein **Zielwert von 50 % Arbeits- und 50 % Leistungsanteil** über alle Netzebenen hinweg umgesetzt werden." **„40 %" kommt im ganzen Dokument 2×, „60 %" 0× vor** — der 40/60-Wert ist dort nirgends belegt. Der Artikel nennt daher **50/50**. Falls Andreas/Martin eine Quelle für 40/60 haben (evtl. ein älteres Konsultationsdokument), gehört sie geprüft — der aktuelle Entwurf trägt sie nicht.
  - **[ALLE ÜBRIGEN KENNZAHLEN am Primärdokument verifiziert]** (nicht aus dem Gedächtnis): 10-kW-Stufe NE7 (§6 Abs. 2 + Erläuterungen: „stützt sich auf eine empirische Auswertung von Smart-Meter-Daten") · Mindestbemessung „zumindest 20% … und hat jedenfalls 2 kW zu betragen" (§6 Abs. 3) · „höchster monatlicher Viertelstundenleistungswert" (§3 Abs. 1) · Inkrafttreten 1.1.2027 (§32) · Übergang: bestehende NE7-Anschlüsse gelten mit 10 kW als vereinbart (§34) · flexible Entnahme „vier Stunden auf den Netzebenen 5 bis 7", bis zu zwei Zeiträume täglich, bis 100 % (§10 Abs. 2) · SNE-G-V soll „bis Oktober 2026" erlassen werden, Tarife erst in der SNE-T-V. **RLM-Schwelle vor dem Schreiben geprüft** (Prompt-Auftrag): energie.gv.at — Lastprofilzähler „gemäß ElWOG im Strom für Kunden mit über 50 kW **und** 100.000 kWh Jahresverbrauch" — eine **UND**-Bedingung, nicht ODER; im Artikel und in der FAQ so formuliert und sauber vom neuen NE7-Smart-Meter-Monatspeak abgegrenzt.
  - **[ZAHLEN-AUDIT, gemessen]** Keine erfundenen €-Tarife im Artikel: die einzigen €-Werte im DOM stammen aus dem Schnellrechner selbst (dessen eigene, als Rechenbeispiel ausgewiesene Vorbelegung). Ein eigener Callout („Warum hier keine Euro-Beträge stehen") begründet das offen. „Entwurf" 11×/„Begutachtungsentwurf" 4× im Text; eigener `warning`-Callout „Entwurf, nicht geltendes Recht"; Gesetz (ElWG, beschlossen 11.12.2025, BGBl. I 91/2025) und Verordnung (Entwurf) durchgehend getrennt. 4 Quellen am Seitenende, offizielle zuerst.
  - **[BUG AM BILD GEFUNDEN UND BEHOBEN — Text vs. Chart widersprachen sich]** Der erste Wurf schrieb „Nur der Leistungspreis unterscheidet sich — **um den Faktor drei**", während der Chart als Kennzahl **22 kW vs. 10 kW** auswies (= Faktor 2,2). Faktor 3 gilt nur für den GERÄTE-Anteil (18 → 6 kW), nicht für die Spitze inkl. der 4 kW Grundlast, die in beiden Fällen durchläuft. Der Text nennt jetzt 22/10 und erklärt die Differenz ausdrücklich („entzerren senkt den Teil der Spitze, der aus Gleichzeitigkeit entsteht, nicht den Rest"). Genau der Widerspruch, der einem Artikel die Glaubwürdigkeit nimmt — nur am gerenderten Bild sichtbar, nicht im Code.
  - **[STARTSEITEN-TEASER: Root Cause statt Kaschieren]** `wissen-teaser.tsx` war 3 hartkodierte Karten mit „in Vorbereitung", alle auf `/wissen` (der Artikel existierte nicht). Jetzt datengetrieben aus `lib/wissen.ts`; die Flaggschiff-Karte zeigt auf den Artikel. **Die zwei anderen Stubs sind ERSATZLOS entfernt** — sie hießen „Arbeitspreis und Leistungspreis" und „RLM-Messung": beides sind inzwischen H2-ABSCHNITTE INNERHALB des 2027-Artikels (§6.5). Als eigene Artikel anzukündigen wäre die Keyword-Dopplung, die §6.2 verbietet. Die Karte nimmt ohne Nachbarn die volle Breite (kein leeres Drittel); ein zweiter Artikel erscheint ohne Code-Änderung daneben. Die zugehörigen `Home.Wissen.a2*/a3*/soon`-Keys sind aus `messages/de.json` entfernt.
  - **[VERIFIKATION, gemessen gegen den Production-Build]** `pnpm build`/`lint`/`typecheck` grün, `pnpm test` 144/144. Alle **25 Routen HTTP 200**, unbekannter Slug → **404** (`dynamicParams=false`). Unique title/description auf `/wissen` und `/wissen/leistungstarif-2027` belegt. Nav „Wissen" → `/wissen`; Startseite verlinkt `/wissen` UND `/wissen/leistungstarif-2027`. Alle 6 geforderten internen Ziele im Artikel-DOM (§6.4). Alle Rich-Bausteine exerziert (3 Callout-Varianten, 3 `<figure>`/`<figcaption>`, Prose-Map, FAQ). **Kein horizontaler Scroll @375px (0 px gemessen), keine Konsolenfehler.**
  - **[FALLE FÜR FOLGE-PROMPTS]** Ein noch laufender `next start` serviert nach einem Rebuild die ALTE HTML gegen ein neues `.next` → Chunks liefern **400**, die Client-Charts hydratisieren nicht und **jedes Recharts-SVG fehlt** (die SSR-Teile stehen weiter da, es sieht also nach „Chart kaputt" aus). Das ist ein Test-Artefakt, kein Code-Bug — Server vor dem Nachmessen wirklich beenden (`lsof -ti:PORT | xargs kill -9`).

- **[GEBAUT: Prompt 9] Report-Galerie auf `/peak-shaving/kalkulator`.** Die 2 „Screenshot folgt"-Rahmen sind weg, an ihrer Stelle stehen 4 echte Report-Ansichten (`public/images/kalkulator-report/`, Dateien auf kebab-case umbenannt — sie kamen mit Leerzeichen im Namen). Alle 4 Bilder vor dem Bau angesehen; die vorgeschlagene Zuordnung stimmte in allen 4 Fällen (Lastgang · Kostenvergleich · Tages-Energiefluss · Empfehlung). Gemessen: CLS **0** auf Desktop UND Mobile, kein horizontaler Scroll @375px, keine Konsolenfehler.
  - **[POSITION] Galerie ans Seitenende gezogen**, direkt vor den CTA (vorher gleich nach dem Schnellrechner-Vergleich). Erklärung → Ergebnis → Start: Der Beweis, dass das Werkzeug liefert, wirkt unmittelbar vor der Handlungsaufforderung. Der Datenschutz-Callout („Ihre Verbrauchsdaten bleiben bei Ihnen") ist als Teil der Sektion mitgewandert — steht damit als letzter Einwand-Abbau vor dem Klick, was besser passt als vorher. **HowItWorks und EnergyFlow haben ihre `Section`-Töne getauscht** (jetzt alt/default statt default/alt), sonst stünden nach dem Umzug zwei gleichfarbige Sektionen aneinander; der Grund-Wechsel alt/default/…/navy ist unverändert intakt.
  - **[LAYOUT — bewusste Abweichung von „2×2", gemessen begründet]** Gebaut ist **1 + 3**: der Jahres-Lastgang über die volle Zeile, darunter die drei Hochformate nebeneinander (Desktop); alles einspaltig ab < 1024px. Grund: Drei Bilder sind hochformatig (0,56/0,66/0,66), eines querformatig (1224×664 = 1,84). Im zuerst gebauten 2×2 lief der Lastgang auf **40 % seiner nativen Breite** (486 px von 1224) und stand mit **356 px totem Weißraum** in seiner Zelle — die Zelle wirkte leer, die Achsenbeschriftung war unlesbar. In der jetzigen Fassung ist er **1054 px breit (86 % nativ)** und gestochen. Die drei Hochformate sitzen in Fenstern im Seitenverhältnis des höchsten Bildes → gleiche Kartenhöhen, Bildunterschriften auf EINER Baseline (gemessen: alle drei bei y=4387). Ein 2×2 ohne Loch ist mit 3 Hoch- + 1 Querformat geometrisch nicht möglich.
  - **[§9.5] Sichtbarer Einordnungssatz VOR den Bildern:** „Beispielhafter Report, gerechnet mit einem synthetischen Demo-Lastgang — kein realer Kundenfall." Die Bilder zeigen nachweislich einen Demo-Lauf (Demo-Bäckerei + `DEMO_BATTERY_CATALOG`), keine Kundendaten. **Keine identifizierenden Daten sichtbar** — kein Name, keine Adresse, keine Zählpunkt-ID (alle 4 Bilder geprüft).
  - **[TECHNIK]** `next/image` mit echten Pixelmaßen (kein Roh-`<img>`), `sizes` auf die tatsächliche Anzeigebreite, `quality={90}` (Default 75 verwischt die Achsenziffern sichtbar), lazy. Alle 4 PNGs haben **transparente** Hintergründe (Eckpixel RGBA 0,0,0,0) — deshalb sitzen sie auf `bg-surface` (weiß) und die Pillarbox der Hochformate ist unsichtbar.

- **[OFFEN, kosmetisch, Andreas' Entscheidung] „[MARTIN: Katalog]" steht sichtbar im Empfehlungs-Screenshot.** Der Demo-Katalog des Kalkulators führt den Hersteller als Platzhalter (`manufacturer: '[MARTIN: Katalog]'`), und dieser Marker ist im Bild unter „PeakStore C60" lesbar — auf einer öffentlichen Produktseite liest er sich wie ein vergessenes internes TODO. Nicht blockierend (die Sektion ist als Demo gekennzeichnet), aber **ein neuer Screenshot dieser einen Karte, sobald Martins echter Katalog steht (OP#2), räumt es auf.** Kein Fix im Bild selbst — Screenshots werden nicht retuschiert.
- **[TECHNISCHE NOTIZ, wichtig für künftige Prompts]** `overflow-x: hidden` macht `<body>` zum Scroll-Container und bricht `position: sticky` (Web-Plattform-Verhalten, kein Bug im engeren Sinn) → **immer `overflow-x: clip` verwenden**, `hidden` nur als Safari-<16-Fallback davor. Der gleiche Fehler steckt **ungefixt in `apps/website/app/globals.css`** (dessen Kommentar behauptet fälschlich, sticky bleibe unberührt) — Fix ansteht, sobald der Kalkulator in Phase 2 nach `apps/web` migriert wird, nicht vorher (Scope).
- **[ENTSCHEIDUNG, gesetzt] Kalkulator-Einbindung Phase 1 = iframe, nicht Migration.** `/peak-shaving/kalkulator/rechner` bettet `apps/website` per iframe ein (`?embed=1` blendet die App-eigene Headline aus — einzige bewusste Änderung an `apps/website`, eng gekapselt, Standalone-Verhalten ohne Parameter unverändert verifiziert). Route ist `noindex, follow` (leere Hülle soll nicht gegen die Produktseite ranken). **Echte Engine-Migration bleibt Phase 2** (§8.1) — hängt auch an OP#1.
- **[ENTSCHEIDUNG, gesetzt] Header/Footer neutral, kein Grünton.** Prompt 7 hatte testweise einen zarten Teal-Tint eingeführt (Kontraste waren einwandfrei), Andreas fand es am Bild zu unruhig → zurückgenommen. Aktueller Zustand: Header weiß, Footer Off-White (`#fafafa`) — bewusst nicht zwingend identisch, siehe DESIGN.md.
- **[OFFEN, klein] Weißer „Kontakt"-Button auf weißem Header** ist nur an seinem Rand erkennbar (Nebenbefund aus Prompt 7, seit Grünton-Rückbau wieder relevant). Kein Blocker, in DESIGN.md notiert — bei Gelegenheit lösen (z. B. dezenter Rand/Fläche).
- **[DRINGEND, unabhängig vom Bau] OP#13:** Das aktuell LIVE geschaltete Impressum auf coolin.at ist selbst unvollständig (`[ergänzen]`-Platzhalter für Rechtsform/UID/Firmenbuchnr./Gewerbebehörde/Kammer — Pflicht nach §5 ECG). Andreas/Martin müssen diese Angaben real zuliefern, bevor die Impressum-Seite gebaut wird. Bis dahin bleibt `/impressum` Platzhalter. Siehe Pflichtenheft §9.1.
- **[OFFEN, einzige Geschäfts-Weiche] OP#1:** Kalkulator in Phase 1 kostenlos (Lead-Magnet) vs. verkauft. Andreas tendiert „frei"; pending Martin. Blockiert den Bau weiterhin nicht.
- **[MERKER] OP#6:** Vor Live-Gang Top ~20 Keywords mit Tool (Ahrefs/Semrush/Keyword Planner) gegenchecken. Nicht vergessen.
- **[ASSETS] OP#7:** Kalkulator-Report-Screenshots sind geliefert **und in Prompt 9 eingebaut** (Galerie auf `/peak-shaving/kalkulator`). Offen bleiben: **Logo hochauflösend** (Emblem bleibt bis dahin eine saubere Favicon-Nachzeichnung) und ein **Screenshot der Kalkulator-OBERFLÄCHE** — die Galerie zeigt bewusst nur den Ergebnis-Report, den 4-Schritt-Flow erklärt stattdessen die native „So funktioniert's"-Sektion.
- **[ENTSCHEIDUNG, bestätigt]** Repo bleibt `peak-shaving` (kein Umbenennen).
- **CTA-Wording überall „Kalkulator"** (nicht „Kostenlos testen") — phasenfest, macht keine Preis-Aussage, Entscheidung aus Prompt 5, seither konsistent durchgezogen.
