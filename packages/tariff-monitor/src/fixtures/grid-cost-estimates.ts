/**
 * Erfundene, aber plausible AT-Netzkosten-Schätzwerte für die Stufe-4-Rechnungs-Rückrechnung
 * (§5.3 Punkt 4, `checkPlausibility`). [MARTIN: §12 #5 echte Netzkosten je Netzgebiet] —
 * Platzhalter, kein echter Netzbetreiber-Tarif (analog zu den Tarif-Fixtures in `./tariffs.ts`).
 */
export const SAMPLE_GRID_COST_ESTIMATE = {
  baseFeeEur: 60,
  taxesCtPerKwh: 8,
}
