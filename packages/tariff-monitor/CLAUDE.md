# CLAUDE.md — Haushalts-Energiemonitor

> Wird im Ordner `packages/tariff-monitor` automatisch geladen. Bewusst kurz.
> **Maßgeblich: `../../Pflichtenheft_Monitor_MVP.md`** — bei Widerspruch gilt das Pflichtenheft.
> **Achtung:** Die `CLAUDE.md` im Repo-Root gehört dem **Kalkulator**, nicht dem Monitor — hier nicht anwenden. Die Monitor-UI in `apps/web` fällt unter `apps/web/CLAUDE.md` (Website) plus diese Spec.

## Was wir bauen
Ein Monitor, der Haushalten/Kleinbetrieben ehrlich zeigt, ob sie beim Strom zu viel zahlen, konkrete günstigere Alternativen nennt und das im Abo laufend überwacht. Erster Bauabschnitt: die Tarif-Engine (`/packages/tariff-monitor`) — rein & getestet, vor jeder UI-Verdrahtung.

## Nicht verhandelbare Prinzipien
1. **Monetarisierungs-agnostische Engine (§3).** Kein Abo-Wissen im Rechenkern. Genau **zwei** kommerzielle Flags: `hasActiveMonitor` und Affiliate — beide als Flag, keine Code-Verzweigungen. „Monitor gratis machen" = ein Flag umlegen.
2. **Ehrlich grob schlägt bewusst ungenau (§1.1).** Der Gratis-Check ist grob, **weil** er wenige Felder hat — nie künstlich verschlechtert. Genauigkeit kommt aus **Feld-Tiefe**, nicht aus einem Abo-Flag: dieselbe Vergleichsfunktion läuft für Gratis und Abo; fehlen Detailfelder, markiert sich das **Ergebnis** als grob (`confidence`-Flag, Analogon zu `taxEffectsIncluded` des Kalkulators).
3. **Dauerpreis ist die Headline, Bonus strikt separat (§1.3/§5.4).** Headline-Ersparnis = Folgejahres-/Dauerpreis-Differenz. Wechselbonus (Erstjahr) getrennt ausgewiesen, **nie** in die Ersparnis gemischt.
4. **Nur der Lieferanten-Anteil ist vergleichbar (§1.4).** Verglichen wird **Arbeitspreis + Grundgebühr** (beide lieferantengesteuert). Netz, Steuern, Abgaben sind netzgebietsabhängig und für alle Lieferanten gleich → **nie** in den Vergleich. Ein als Energiepreis eingetragener Gesamtpreis muss aktiv abgefangen werden (§5.3).
5. **Dauerpreis-Modellannahme (dokumentiert).** Nach Ablauf der Preisgarantie ist der echte Folgepreis unbekannt → Dauerkosten = aktueller Arbeitspreis flach fortgeschrieben; Garantie-Ende als **Metadatum** ausgewiesen, nicht in die Headline gefaltet.
6. **Tarif-Tabelle historisierend (§7).** Zeitreihe, nicht überschreibend (betrifft T2; die Engine rechnet gegen einen Tabellen-Snapshot als Eingabe).
7. **Self-Service (§1.7).** Einzige laufende Team-Pflicht: Tarif-Tabelle aktuell halten.
8. **KI an den Rändern, Determinismus im Kern (§1.8).** KI ist zulässig für Scraping-Extraktion (§7), Rechnungsscan (§5.2/T6), spätere Ergebnis-Erklärung — **ausgeschlossen** im Vergleichs-Rechenkern (`packages/tariff-monitor`, §3): gleiche Eingabe muss gleiche Zahl liefern.

**Explizit verworfen — nicht wieder einführen (§1):** IP-basierte Sperre des Gratis-Checks; Detailfelder künstlich beschneiden; Formular zur Zermürbung leeren; Bonus in die Ersparnis-Headline mischen.

## Engine-Regeln (`/packages/tariff-monitor`)
- **Rein & isomorph:** framework-frei, deterministisch, **kein I/O im Vergleichskern**. Scraper (T2) und Cron (T7) sind separate, unreine Ränder — **kein Teil von T1**.
- **100 % unit-testbar. Fixtures vor UI** (Muster wie Kalkulator §3.11). Erst Logik + Tests, dann Verdrahtung.
- **Normalisierung auf Jahreskosten** je Tarif als Kosten-Objekt (§5.4): Arbeitspreis, Grundgebühr, Bonus (Betrag+Bedingung+Erstjahr), Preisgarantie-Dauer, Bindung/Kündigungsfrist, Abrechnung, Ökostrom. Ausgabe: Erstjahres- **und** Dauerkosten getrennt.
- **Plausibilität (§5.3) gestuft, mit Dependency Injection:** Stufe 1 (Range-Check) + Stufe 2 (Gesamtpreis-Verdacht) sind reine T1-Logik. Stufe 3 (Abgleich Tabellenpreis) + Stufe 4 (Rechnungs-Rückrechnung inkl. Netzkosten-Schätzung) als **reine Funktionen mit injizierten Argumenten** bauen (Tabellenpreis/Netzkosten als Parameter) — heute gegen Fixtures testbar, später an T2/T6 verdrahtet, ohne Umbau.
- **Präferenz-Filter (§9) als reine Eingabe:** Ökostrom-only, keine Vorauskasse/Kaution, max. Bindung — bestimmen, welche Alternativen als „besser" gelten.
- **Fachliche Invarianten als Tests:** Headline = Dauerpreis; Bonus nie in Headline; Grundgebühr im Vergleich enthalten; Plausi fängt Gesamtpreis-Eingabe ab; Σ-/Vorzeichen-Konsistenz der Kostenposten.

## Tech-Stack & Struktur (§4)
- **TypeScript.** `/packages/tariff-monitor` (Engine, Herzstück) · `/packages/shared` (geteilte Typen/Konstanten) · `/apps/web` (UI: Gratis-Check + Dashboard, spätere Tracks) · `/supabase` (Schema/RLS/Auth/Entitlements, ab T2/T4).
- **Auth & Zahlung:** Supabase Auth + **Stripe** (Checkout + Subscription; `hasActiveMonitor` aus Stripe-Webhook in Supabase gespiegelt) — ab T4, erstes reales Portal-Fundament.
- **Supabase-Struktur (§4.4):** EIN Projekt für die ganze Plattform (EU/Frankfurt), Produkt-Trennung über Postgres-Schemas (`monitor`) statt separate Projekte; server-only Zugriff über nicht-präfixte `SUPABASE_URL`/`SUPABASE_ANON_KEY` (`NEXT_PUBLIC_SUPABASE_*` bleibt für T4 reserviert).
- **UI erst spätere Tracks, gegen die coolin.at-Design-Grundlagen** — es gelten `apps/web/CLAUDE.md` + Website-Pflichtenheft/Design. **Keine eigenen Design-Tokens hier.**
- **Kein iframe, keine native App (§4.1).** Responsive Web-App.

## Bauabschnitte (§13)
T1 Tarif-Engine (jetzt) → T2 Scraper + historisierende Tabelle → T3 Gratis-Check-UI → T4 Auth+Stripe+Entitlements → T5 Dashboard+Self-Service → T6 KI-Rechnungsscan → T7 Cron+Benachrichtigung. Auth/Stripe/Scraper/Scan sind **spätere** Abschnitte, nicht T1.

## Offene Abhängigkeiten (blockieren Livegang, nicht den Bau)
§12: Abo-Modell final (#1, kommerziell offen — dank §3 kein Umbau) · Datenpartner/Affiliate (#2) · Scraping-ToS (#3) · Plausi-Grenzwerte [MARTIN] (#4) · Netzkosten-Schätzung je Netzgebiet (#5) · **Scraper-Targets Top 15–20 (#6) — betrifft erst T2, T1 braucht sie nicht; künftig übers Admin-UI (T4) gepflegt** · DSGVO-Paket (#7) · Regulatorik Affiliate (#8) · Bonus-Zeile an echten Tarifdaten verifizieren (#9, reine Verifikation) · `billingCycle`-Effekt + Plausi-Stufe-4-Bezugsgröße (#10, technische Detailfragen, betrifft T6). Details: `../../Pflichtenheft_Monitor_MVP.md` §12.

## Stand & offene Entscheidungen
> Lebendiger Handover-Anker. Erledigtes wandert raus.
- **[GEBAUT: T1-Teil 1] Package-Scaffold + Output-Contract als reine Typen.** `packages/tariff-monitor` (Name `tariff-monitor`, unscoped wie `engine`/`shared`) als Workspace-Paket angelegt: `package.json`/`tsconfig.json` 1:1 nach dem Muster von `packages/shared` (Source-Exporte, kein `dist`-Build-Zwang für andere Consumer, `vitest` als Runner), `src/index.ts` als Barrel, `src/types.ts` mit dem vollständigen Contract (`UserTariffInput`, `TariffCostObject`, `NormalizedYearlyCost`, `ComparisonPreferences`, `PlausibilityWarning`, `TariffComparisonResult`) + den drei Kernfunktions-**Stubs** (`normalizeTariffCost`/`compareTariffs`/`checkPlausibility`, werfen bewusst `not implemented`). `.github/workflows/test.yml` um einen `Test tariff-monitor`-Schritt ergänzt (analog `Test engine`/`Test shared`); `lint`/`typecheck`/`build` liefen bereits vorher root-weit (`pnpm -r`) und erfassen das Paket automatisch. Ein Smoke-Test (`src/types.test.ts`) beweist nur Importierbarkeit/Build, keine Rechenlogik. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün (inkl. `engine`/`shared`/`apps/*` unverändert).
- **[GEBAUT: T1-Teil 2] `normalizeTariffCost` implementiert + erste Fixture-Suite (§5.4, §14-DoD).** Logik liegt in `src/normalize/normalize.ts` (+ `src/normalize/index.ts`-Barrel, Muster wie `engine/src/roi/`), aus `src/types.ts` ausgelagert — der Stub dort ist ersatzlos entfernt, `compareTariffs`/`checkPlausibility` unverändert Stubs. Root-`src/index.ts` reexportiert `./normalize` zusätzlich zu `./types`. Reine Formel: `ongoingYearlyCostEur = energyPriceCtPerKwh/100 × annualConsumptionKwh + baseFeeEurPerYear` (nie mit Bonus vermischt); `firstYearCostEur = max(0, ongoing − bonusEur)` (nur hier, gegen negatives Clamping abgesichert); `bonusEur`/`priceGuaranteeMonths` unverändert durchgereicht. `billingCycle` fließt bewusst NICHT in die Jahressumme ein — als offene Frage im Code markiert (`// [OFFEN: billingCycle-Effekt?]` in `normalize.ts`), nicht spekulativ implementiert; beim Bau ist kein Grund aufgetaucht, der das nahelegen würde (kein unterjähriger Abschlags-Mechanismus im Contract).
  - **[TESTS]** `src/normalize/normalize.test.ts` (9 Tests) sichert die 5 verbindlichen Invarianten (Bonus nie in ongoing, Clamping bei Bonus > ongoing, `bonusEur=0` → `firstYear==ongoing` exakt, Determinismus, `priceGuaranteeMonths` wirkungslos auf beide Kostenzahlen) + Skalierung mit dem Verbrauch, mit kleinen Ad-hoc-Tarif-Literalen (Muster wie `savings/attribute.test.ts` in `engine` — inline statt über die Fixtures unten).
  - **[FIXTURES, §14-DoD]** `src/fixtures/tariffs.ts` (Muster wie `engine/src/fixtures/profiles.ts`, NICHT über den Root-Barrel exportiert): zwei erfundene, aber plausible AT-Tarife — `bonusGuaranteeTariff` (22,5 ct/kWh, 96 €/Jahr Grundgebühr, €150 Bonus, 12 Monate Preisgarantie) und `noBonusNoGuaranteeTariff` (24,9 ct/kWh, 110 €/Jahr, kein Bonus, keine Garantie, 12 Monate Bindung) + zwei Referenzverbräuche `HOUSEHOLD_CONSUMPTION_KWH=3500`/`SMALL_BUSINESS_CONSUMPTION_KWH=8000`. `src/fixtures/normalize-gate.test.ts` (5 Tests) pinnt alle vier Kombinationen von Hand vorgerechnet: `bonusGuaranteeTariff`@3500 → ongoing €883,50/firstYear €733,50; @8000 → €1896/€1746; `noBonusNoGuaranteeTariff`@3500 → €981,50/€981,50 (kein Bonus, identisch); @8000 → €2102/€2102.
  - **Testanzahl:** 15 Tests in `tariff-monitor` (1 Scaffold-Smoke-Test + 9 Normalisierungs-Invarianten + 5 Fixture-Gate), `engine` unverändert 144/144. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün.
- **[GEBAUT: T1-Teil 3] `compareTariffs` implementiert (§1.3/§3/§9) — Vergleich, Präferenz-Filter, `confidence`.** Logik liegt in `src/compare/compare.ts` (+ `src/compare/index.ts`-Barrel, Muster wie `src/normalize/`), Stub in `src/types.ts` ersatzlos entfernt (nur `checkPlausibility` bleibt Stub). Root-`src/index.ts` reexportiert zusätzlich `./compare`.
  - **[CONTRACT-ERGÄNZUNG]** `TariffCostObject` bekam `requiresPrepayment?: boolean` (Vorauskasse/Kaution, fehlt/false = nein) — sonst wäre `excludePrepayment` totes, stilles Verhalten gewesen. Die zwei Teil-2-Fixtures (`src/fixtures/tariffs.ts`) wurden ergänzt: `bonusGuaranteeTariff` lässt das Feld bewusst weg (deckt den "fehlt=nein"-Default), `noBonusNoGuaranteeTariff` trägt `requiresPrepayment: true` (macht den Filter testbar).
  - **[LOGIK]** `current` = `UserTariffInput` über einen internen Adapter (`userInputAsCostObject`) durch das UNVERÄNDERTE `normalizeTariffCost` (§5.4) — fehlende Stufe-2-Felder auf neutrale Defaults (0/`'monthly'`/false), kein Fehler (§5.1). Je `candidate`: `normalizeTariffCost` am selben Verbrauch, `savingOngoingEurPerYear = current.ongoing − candidate.ongoing` (Headline, §1.3) strikt getrennt von `savingFirstYearEur` (inkl. Bonus). `alternatives` kommt VOLLSTÄNDIG zurück (Länge == candidates.length, KEINE Vorfilterung — Muster wie `engine/src/recommendation/rank.ts`), sortiert nach `savingOngoing` absteigend, Tie-Break `savingFirstYear` absteigend; teurere Alternativen bleiben drin und sinken nach unten. `passesPreferenceFilter` (§9) UND-verknüpft `greenEnergyOnly`/`maxContractCommitmentMonths`/`excludePrepayment`, fehlende Präferenzen → alle `true`. `recommendation` = oberster Kandidat mit `passesPreferenceFilter && savingOngoing > 0`, sonst `undefined` (nie ein Wechsel, der beim Dauerpreis nichts spart) — deterministischer Rationale-Satz (Dauerpreis-Ersparnis + Bonus-Zusatz falls `bonusEur>0`, Muster wie `buildRationale` in `engine/src/recommendation/rank.ts`). `confidence`: `'detailed'` NUR wenn sowohl `bonusEur` ALS AUCH `contractCommitmentMonths` im `UserTariffInput` explizit gesetzt sind (auch `0` zählt als gesetzt), sonst `'rough'` — reines Label, ändert keinen Rechenzweig (§3-Kernbeweis unten). `plausibility.warnings` bleibt bewusst `[]` (`checkPlausibility` unverdrahteter Stub, T1-Teil 4/5).
  - **[TESTS]** `src/compare/compare.test.ts` (17 Tests, Ad-hoc-Fixtures) sichert alle 7 verbindlichen Invarianten: Headline=ongoing (nie firstYear); `alternatives.length==candidates.length` + Monotonie; die drei Präferenzfilter einzeln + UND-kombiniert (bestehend + ein Kandidat, der alle drei erfüllt); Empfehlung überspringt einen besser sparenden, aber filterfehlgeschlagenen Kandidaten zugunsten des nächsten passierenden; `recommendation=undefined` sowohl wenn alle Alternativen teurer sind als auch wenn der einzig sparende Kandidat durchfällt; **§3-Kernbeweis** (invariant 5): derselbe Kandidatensatz, rough vs. detailed `UserTariffInput` (Bonus wirkt nur auf `firstYear`) → `savingOngoingEurPerYear` je Kandidat IDENTISCH, nur `confidence` unterscheidet sich (`savingFirstYearEur` wechselt bewusst als Gegenprobe); `confidence`-Boundary (4 Fälle: nur Stufe 1 / nur Bonus / nur Bindung / beide); Determinismus.
  - **[FIXTURE-GATE]** `src/fixtures/compare-gate.test.ts` (3 Tests, §14-DoD-Analogon zu `normalize-gate.test.ts`): Nutzer-Ist-Tarif spiegelt `noBonusNoGuaranteeTariff` 1:1 als `UserTariffInput`, `candidates=[bonusGuaranteeTariff, noBonusNoGuaranteeTariff]`. Gepinnt @3500 kWh: `bonusGuaranteeTariff` → Ersparnis €98 (ongoing)/€248 (firstYear), `noBonusNoGuaranteeTariff` (Tarif vs. sich selbst) → €0/€0; @8000 kWh: €206/€356 bzw. €0/€0 — beide aus den bereits gepinnten `normalizeTariffCost`-Zahlen aus T1-Teil 2 hergeleitet (Subtraktion, keine neue Handrechnung). `confidence='detailed'` (gespiegelte Stufe-2-Felder). Ein dritter Test zeigt `excludePrepayment` gegen die echten Fixtures (`noBonusNoGuaranteeTariff.requiresPrepayment=true` fällt durch, `bonusGuaranteeTariff` ohne das Feld passiert).
  - **Testanzahl:** 35 Tests in `tariff-monitor` (1 Scaffold + 9 Normalisierung + 5 Normalize-Gate + 17 Compare + 3 Compare-Gate), `engine` unverändert 144/144. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün.
- **[GEBAUT: T1-Teil 4] `checkPlausibility` implementiert (§5.3) — vier gestufte Checks mit Dependency Injection.** Logik liegt in `src/plausibility/plausibility.ts` (+ `src/plausibility/constants.ts` für die Grenzwerte, + `src/plausibility/index.ts`-Barrel, Muster wie `src/normalize/`/`src/compare/`), Stub in `src/types.ts` ersatzlos entfernt — **alle drei Kernfunktionen sind damit implementiert, kein Stub mehr offen.** Root-`src/index.ts` reexportiert zusätzlich `./plausibility`.
  - **[GRENZWERTE]** `src/plausibility/constants.ts`, alle vier als benannte Konstanten `[MARTIN: §12 #4/#5 marktrealistisch bestätigen]` markiert (Muster wie `TOP_PEAKS_N` in `engine`): `ENERGY_PRICE_MIN_CT=5`/`ENERGY_PRICE_MAX_CT=40` (Stufe 1), `TOTAL_PRICE_SUSPECT_MIN_CT=25`/`MAX_CT=35` (Stufe 2), `TABLE_PRICE_TOLERANCE_CT=2` (Stufe 3), `INVOICE_RECONCILE_TOLERANCE_RATIO=0,10` (Stufe 4).
  - **[LOGIK, DI-Schaltung über Argumente]** Stufe 1 (Range) und Stufe 2 (Gesamtpreis-Verdacht) prüfen ihre Schwelle unabhängig voneinander, immer aktiv, kein gegenseitiger Ausschluss. Stufe 3 (Tabellen-Abgleich) rechnet NUR bei übergebenem `matchedTariff`, Stufe 4 (Rechnungs-Rückrechnung) NUR bei BEIDEN `invoiceTotalEur` UND `gridCostEstimate` — fehlt eines, schweigt die Stufe komplett (kein Platzhalter-Warning). Dieselbe Funktion deckt damit unverändert T1 (nur 1+2 aktiv), T2 (+ `matchedTariff`) und T6 (+ `invoiceTotalEur`/`gridCostEstimate`) ab. Stufe 4 rekonstruiert `expected = energyPriceCtPerKwh/100×kWh + baseFeeEurPerYear + gridCostEstimate.baseFeeEur + gridCostEstimate.taxesCtPerKwh/100×kWh` und vergleicht relativ (bezogen auf `invoiceTotalEur`, den beobachteten Wert) gegen die Toleranz. **[ENTSCHEIDUNG]** Stufe-4-Warning trägt `field:'energyPriceCtPerKwh'` (nicht `'invoiceTotal'`) — `invoiceTotalEur` ist kein Formularfeld (§5.3 Punkt 4, nur zur Selbstvalidierung), eine UI kann dort nichts hervorheben; `energyPriceCtPerKwh` ist zudem der beim §1.4-Kernfehler typischerweise falsch sitzende Wert.
  - **[TESTS]** `src/plausibility/plausibility.test.ts` (13 Tests, Ad-hoc-Helper wie `compare.test.ts`) sichert alle 7 verbindlichen Invarianten: sauberer Fall → leeres Array; §14-DoD 28 ct → gezielte Stufe-2-Warnung; Korridor-/Band-Grenzen inklusive getestet; Stufe 1/2-Überlappung unabhängig geprüft (33 ct → nur 2, 45 ct → nur 1) **plus** ein gepinnter Beweis, dass „beide feuern" mit den AKTUELLEN Default-Grenzwerten mathematisch nicht konstruierbar ist (Verdachtsband ist echte Teilmenge des Korridors) — Eigenschaft der Konstanten, nicht der (bewusst unabhängigen, nicht-deduplizierenden) Prüf-Logik; Stufe 3/4 bleiben bei fehlenden Argumenten still, auch bei einem Mismatch, der bei übergebenen Argumenten nachweislich sofort beide feuert (Gegenprobe im selben Test); Stufe 3 isoliert an der exakten 2-ct-Grenze (>2 ct feuert, ==2 ct nicht); Stufe 4 isoliert (konsistent vs. inkonsistent, Zahlen von Hand vorgerechnet) + Nachweis, dass BEIDE Stufe-4-Argumente nötig sind; Warnungs-Reihenfolge aufsteigend nach `stage`; Determinismus.
  - **[FIXTURES]** `src/fixtures/grid-cost-estimates.ts` (neu, `SAMPLE_GRID_COST_ESTIMATE`, `[MARTIN: §12 #5 echte Netzkosten je Netzgebiet]` markiert) + Wiederverwendung von `bonusGuaranteeTariff`/`noBonusNoGuaranteeTariff`/`HOUSEHOLD_CONSUMPTION_KWH` aus Teil 2 als `matchedTariff`. `src/fixtures/plausibility-gate.test.ts` (3 Tests, Muster wie `normalize-gate.test.ts`/`compare-gate.test.ts`) pinnt die zwei §14-DoD-Kriterien gegen die SHARED Fixtures: ~28 ct → Stufe 2; Stufe 3 gegen `bonusGuaranteeTariff` (19,5 ct eingetragen vs. 22,5 ct hinterlegt, Abweichung 3 ct); Stufe 4 an `noBonusNoGuaranteeTariff` gespiegelt (24,9 ct, für sich genommen plausibel, weder Stufe 1 noch 2 würde greifen) — Handrechnung `expected = 0,249×3500 + 110 + 60 + 0,08×3500 = 1321,5 €`, konsistent bei `invoiceTotalEur=1321,50` (keine Warnung), widersprüchlich bei `invoiceTotalEur=1000` (Abweichung 32 % > 10 % → Stufe-4-Warnung) — genau das §14-DoD-Kriterium „Rechnungs-Rückrechnung erkennt widersprüchliche Extraktion", demonstriert an einem Energiepreis, der isoliert betrachtet unauffällig gewesen wäre.
  - **[BEWUSST NICHT GETAN]** Keine Verdrahtung in `compareTariffs`/`TariffComparisonResult.plausibility.warnings` (bleibt `[]`, wie in Teil 3 gebaut) — kein Contract-Feld angefasst, keine neuen Typen. Kein Gate-Zusammenzug aller drei Kernfunktionen (das ist T1-Teil 5).
  - **Testanzahl:** 51 Tests in `tariff-monitor` (1 Scaffold + 9 Normalisierung + 5 Normalize-Gate + 17 Compare + 3 Compare-Gate + 13 Plausibilität + 3 Plausibilität-Gate), `engine` unverändert 144/144. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün.
- **[GEBAUT: T1-Teil 5, T1 DAMIT ABGESCHLOSSEN] Integriertes T1-Gate (§14-DoD final) — `compareTariffs` + `checkPlausibility` gemeinsam über zwei End-to-End-Szenarien.** Muster wie `engine/src/fixtures/m1-gate.test.ts`: die Einzel-Gates (`normalize-gate`/`compare-gate`/`plausibility-gate.test.ts`) testen je EINE Funktion isoliert und BLEIBEN bestehen — `src/fixtures/t1-gate.test.ts` ist der integrierte Überbau, kein Ersatz. Keine neue Rechenlogik, keine Contract-Änderung — nur Tests + Fixtures + Doku. `checkPlausibility` bleibt bewusst UNVERDRAHTET in `compareTariffs` (`plausibility.warnings` weiter `[]`) — "gemeinsam" heißt: beide öffentlichen Funktionen laufen im selben Szenario, keine Verkettung.
  - **[FIXTURES]** `src/fixtures/scenarios.ts` (NICHT über den Root-Barrel exportiert): zwei benannte Szenarien, je ein `UserTariffInput` (Stufe-1-„rough"- UND Stufe-2-„detailed"-Variante) + 4 `TariffCostObject`-Kandidaten. **HAUSHALT** (3500 kWh, current ongoing €981,50): SparStrom Bonus (Bonus €120, kein Vorauskasse, Ökostrom, saving ongoing ≈€125), Komfort Fix (teurer, saving ≈−€101), Blitz Prepaid (billigster, Vorauskasse, saving ≈€210), Öko Vorkasse (Ökostrom + Vorauskasse, saving ≈€165). **KLEINBETRIEB** (8000 kWh, current ongoing €2092): Gewerbe Spar Bonus (Bonus €300, saving ≈€270), Gewerbe Komfort (teurer, saving ≈−€228), Blitz Gewerbe Prepaid (billigster, Vorauskasse, saving ≈€460), Öko Gewerbe Vorkasse (Ökostrom + Vorauskasse, saving ≈€370). Kandidaten bewusst so konstruiert, dass Präferenzen die Empfehlung nachweisbar verschieben, nicht nur filtern.
  - **[GATE, `src/fixtures/t1-gate.test.ts`, 11 Tests]** DoD-1 (Normalisierung+Bonus-Trennung): HAUSHALT — `current.ongoing=€981,50`, SparStrom-Bonus-Kandidat `ongoing=€856,50`/`firstYear=€736,50`, `savingOngoing=€125` vs. `savingFirstYear=€245` (Differenz exakt €120 = Bonus), bonusfreier Kandidat hat `savingOngoing==savingFirstYear` exakt. DoD-2 (Plausibilität): HAUSHALT mit `energyPriceCtPerKwh=28` → genau 1 Stufe-2-Warnung („Gesamtpreis"); mit korrektem `24,9` → `[]`. DoD-3 (§3, kein Abo-Wissen): je Szenario rough- vs. detailed-`UserTariffInput` → identische `savingOngoing`-Rangfolge (Array-weise verglichen) UND identische Empfehlung (`Blitz Prepaid` bzw. `Blitz Gewerbe Prepaid`), nur `confidence` kippt `'rough'→'detailed'`. DoD-4 (ehrliche Empfehlung): **HAUSHALT ohne Präferenz → `Blitz Prepaid`; mit `excludePrepayment:true` → `SparStrom Bonus`; mit `greenEnergyOnly:true` → `Öko Vorkasse`. KLEINBETRIEB ohne Präferenz → `Blitz Gewerbe Prepaid`; mit `excludePrepayment:true` → `Gewerbe Spar Bonus`; mit `greenEnergyOnly:true` → `Öko Gewerbe Vorkasse`.** Plus ein Invarianten-Test über alle 2×3 Szenario/Präferenz-Kombinationen: jede erzeugte Empfehlung hat `savingOngoingEurPerYear > 0` (6 geprüfte Empfehlungen). DoD-5 (Determinismus+Vollständigkeit): `alternatives.length == candidates.length` (4) in beiden Szenarien; zweifacher Aufruf von `compareTariffs`/`checkPlausibility` mit identischen Argumenten → `toEqual`.
  - **Testanzahl:** 62 Tests in `tariff-monitor` (1 Scaffold + 9 Normalisierung + 5 Normalize-Gate + 17 Compare + 3 Compare-Gate + 13 Plausibilität + 3 Plausibilität-Gate + 11 T1-Gate), `engine` unverändert 144/144. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün.
  - **T1 (Tarif-Engine, §13) ist damit fachlich abgeschlossen:** alle drei Kernfunktionen (`normalizeTariffCost`/`compareTariffs`/`checkPlausibility`) implementiert, je ein Einzel-Gate + das integrierte T1-Gate, alle §14-DoD-Kriterien, die T1 betreffen, bewiesen.
- **[GEBAUT: T2 — Schema-Hälfte] Supabase-Fundament + historisierende Tarif-Tabelle (§7).** `/supabase` real angelegt (`supabase init`): `config.toml` (Schema `monitor` in `[api].schemas`/`extra_search_path` freigeschaltet — EIN Supabase-Projekt für die ganze CoolIn-Plattform, Produkt-Trennung über Postgres-Schemas statt separate Projekte), `migrations/20260717174454_create_monitor_schema.sql`, `seed.sql`. Root-`.env.example` von auskommentierten Platzhaltern (aus einer früheren Kalkulator-Session) auf aktive `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY`-Zeilen umgestellt (`.gitignore` deckte `.env*.local` bereits ab, keine Änderung nötig). **KEIN Auth, KEINE User-Tabellen, KEINE Nutzerdaten-RLS** — bleibt T4.
  - **[SCHEMA] `monitor.tariff_snapshots`** — append-only Zeitreihe (§7): Spalten 1:1 semantisch zum T1-Contract `TariffCostObject` (snake_case) + Provenienz (`captured_at`, `source`). Dedup-/Aktualitäts-Key vorerst `(provider_name, tariff_name)` — ein stabiler externer Tarif-Key ist als Kommentar in der Migration vermerkt, kommt evtl. mit dem Scraper. **`monitor.current_tariffs`**-View (`DISTINCT ON (provider_name, tariff_name) … ORDER BY captured_at DESC`, `security_invoker=true`) liefert den jeweils neuesten Snapshot je Tarif — das liest später der Gratis-Check (T3); Analysen lesen die volle Snapshot-Tabelle.
  - **[RLS + Grants]** RLS aktiv, EINE Read-Policy für `anon`+`authenticated` (`using (true)` — öffentliche Tarifdaten, keine Nutzerdaten). KEIN insert/update/delete-Grant für `anon`/`authenticated`. `service_role` bekommt bewusst NUR `select, insert` (KEIN update/delete) — Append-only ist Architektur (§7), nicht nur Konvention.
  - **[ÜBER DIE AUFGABE HINAUS] Harte Append-only-Absicherung per Trigger:** `service_role`/`postgres` haben `BYPASSRLS`, RLS allein hätte das „nie überschreiben"-Prinzip also nicht für JEDE Rolle erzwungen. Ein `BEFORE UPDATE/DELETE`-Trigger (`monitor.reject_tariff_snapshot_mutation`) blockt beide Operationen unabhängig von Grants/RLS — verifiziert, s. u.
  - **[SEED, `seed.sql`]** Die erfundenen AT-Tarif-Fixtures aus T1 (`src/fixtures/tariffs.ts` + `scenarios.ts`, 10 distinkte Tarife), `source='seed_placeholder'`, klar als Platzhalter markiert — echte Tarife (`source='scrape'`) ersetzen ihn, sobald §12 #6 vorliegt. Für den Historisierungs-Beweis trägt EIN Tarif (Sonnenstrom Direkt/Klick Strom Online) zusätzlich einen zweiten, älteren Snapshot mit einem frei erfundenen Vor-Preis (23,9 statt 22,5 ct, `captured_at = now() − 30 Tage`).
  - **[VERIFIKATION — Docker/psql in dieser Session nicht installiert; das `supabase`-CLI-Binary konnte keine einzige TCP-Verbindung aufbauen, auch nicht zu einem trivialen lokalen Test-Server (Timeout selbst ohne Sandbox — Netzwerk-Restriktion des Binaries, nicht der Migration).]** Statt reiner Syntaxprüfung: Migration + Seed real gegen `@electric-sql/pglite` (WASM-Postgres, npm) ausgeführt (Rollen `anon`/`authenticated`/`service_role` manuell angelegt — eine echte Supabase-Instanz provisioniert die bereits vor). Ergebnis: Migration + Seed fehlerfrei; `tariff_snapshots`=11 Zeilen/`current_tariffs`=10 Zeilen; **Historisierungs-Beweis bestanden** (nur der neuere 22,5-ct-Snapshot erscheint in `current_tariffs`, der ältere 23,9-ct-Snapshot bleibt nur in der vollen Tabelle sichtbar); `UPDATE`/`DELETE` auf `tariff_snapshots` beide vom Trigger blockiert; `anon` kann lesen, `INSERT` als `anon` scheitert am fehlenden Grant; `service_role` kann `INSERT`, `UPDATE` scheitert am fehlenden Grant. Ein zusätzlicher Versuch, `supabase gen types typescript --db-url` gegen dieselbe PGlite-Instanz laufen zu lassen (via `@electric-sql/pglite-socket`, exponiert PGlite über das echte Postgres-Wire-Protokoll) scheiterte an genau der oben genannten Netzwerk-Restriktion, NICHT an der Migration — ein `pg`-npm-Client verband sich über denselben Socket-Server problemlos, das `supabase`-Go-Binary nie. **Offen für den Nutzer:** `supabase start && supabase db reset` (wendet Migration+Seed an — sollte laut PGlite-Probe unverändert durchlaufen) + Typgenerierung (`supabase gen types typescript --local --schema public,monitor > supabase/database.types.ts` — Zielpfad noch nicht angelegt, da real nicht generierbar) müssen mit funktionierendem Docker manuell nachgeholt werden.
  - **[REINHEITSGRENZE EINGEHALTEN]** `packages/tariff-monitor` unangetastet — kein Supabase-Client, kein DB-Zugriff, keine neue Dependency dort. Scraper (T2-Rest) und ein DB-Lesecode (T3) sind die künftigen, separaten unreinen Ränder.
  - **[FORWARD-NOTES für die T2-Scraper-Hälfte + T3]** `scrape_targets`/`scrape_runs`-SCHEMA steht jetzt (s. Zwischenschritt-Bullet unten) — was noch MIT dem eigentlichen Scraper-Code kommt: das Befüllen von `scrape_targets` (blockiert auf §12 #6, echte Targets) und das Schreiben der `scrape_runs`-Zeilen samt Robustheits-Alert-Auslösung (§7 „Team-Alert bei 0/unplausiblen Ergebnissen"). Ein Runtime-DB-Client (Supabase JS SDK) kommt mit T3 (Gratis-Check-UI), nicht hier — Consumer-Ort (`apps/web` vs. neues `packages/*`) noch offen. `TariffCostObject` (aktuell nur `packages/tariff-monitor/src/types.ts`) wandert voraussichtlich nach `packages/shared`, sobald der DB-Lesecode (T3) einen zweiten Consumer für exakt diesen Shape braucht — die generierten DB-Typen (snake_case) sind NICHT derselbe Typ; das Mapping auf das camelCase-Contract passiert am Lese-Rand (T3), nicht in der Engine. Eine Netzkosten-je-Netzgebiet-Tabelle (§12 #5, Plausi-Stufe 4) ist bewusst NICHT Teil von `tariff_snapshots` — eigene, spätere Tabelle.
- **[GEBAUT: T2 — Zwischenschritt] `scrape_targets` + `scrape_runs`-Schema (§7 Robustheits-Alert-Basis).** Neue Migration `supabase/migrations/20260717184124_create_monitor_scrape_tables.sql` (Migrationshistorie append-only gewahrt — bestehende Migration unangetastet außer einem nachgezogenen `ALTER FUNCTION`, s. u.). **REINHEITSGRENZE gewahrt:** `packages/tariff-monitor` unangetastet — reine SQL-Struktur, kein Scraper-Code.
  - **[SCHEMA] `monitor.scrape_targets`** — editierbare, kuratierte Anbieterliste (§7 „Top 15–20"), bewusst NICHT historisierend (anders als `tariff_snapshots`): `is_active` deaktiviert einen Anbieter ohne Löschen, `updated_at` läuft über einen neuen generischen Trigger (`monitor.set_updated_at`, `TG_TABLE_NAME`-unabhängig, für künftige editierbare Tabellen im Schema wiederverwendbar). `provider_slug` (unique) ist der stabile Referenz-Key, `extraction_config jsonb` (null = noch nicht eingerichtet) der spätere CSS-Selektor-Andockpunkt, `last_scrape_status`/`last_scrape_at`/`last_scrape_error` der Statuscache. **Kein Seed** (echte Top-15–20-Liste = §12 #6, noch nicht von Andreas/Martin geliefert) — nur die leere Struktur, wie beauftragt.
  - **[SCHEMA] `monitor.scrape_runs`** — Lauf-Protokoll je Scraper-Durchlauf, append-only wie `tariff_snapshots` (gleicher Trigger-Mechanismus: `BEFORE UPDATE/DELETE` → `RAISE EXCEPTION`, aber eine NEUE, tabellen-agnostische Funktion `monitor.reject_append_only_mutation` statt Wiederverwendung von `reject_tariff_snapshot_mutation` — jene nennt „tariff_snapshots" fest im Fehlertext, das wäre für `scrape_runs` irreführend gewesen; die neue Funktion interpoliert `TG_TABLE_SCHEMA`/`TG_TABLE_NAME` dynamisch und ist für künftige weitere Append-only-Tabellen wiederverwendbar). `target_id` FK auf `scrape_targets` mit `on delete cascade` **und `not null`** — über den wörtlichen Spaltenkatalog der Aufgabe hinaus ergänzt, weil `on delete cascade` ohne verpflichtende FK keine saubere Semantik hätte (ein „loser" Lauf-Log-Eintrag ist fachlich nicht vorgesehen); **falls anders gewollt, bitte melden.** `status`/`last_scrape_status` (beide Tabellen) tragen `CHECK`-Constraints auf die im Prompt benannten Enum-Werte (Muster wie `billing_cycle` in der ersten Migration) — `tariffs_found` bewusst OHNE zusätzliche `>= 0`-Constraint (kein Nutzereingabe-Rand, interner Scraper-Wert, keine im Prompt genannte Invariante).
  - **[RLS + GRANTS]** Beide Tabellen: RLS aktiv, **kein** `anon`/`authenticated`-Zugriff (anders als `tariff_snapshots` — hier Betriebs-/Admin-Datum, keine öffentliche Tarifinfo). Kein Grant + keine Policy = doppelt abgesichert; `service_role` (RLS-Bypass) bekommt `select+insert+update` auf `scrape_targets` (bewusst KEIN `delete` — Deaktivierung läuft über `is_active`) und `select+insert` auf `scrape_runs` (kein `update`/`delete`, zusätzlich Trigger-geblockt). Eine Admin-Rolle kommt erst mit T4.
  - **[AUFGABE 4 — search_path-Advisory]** Alle drei Funktionen im `monitor`-Schema tragen jetzt `SET search_path = ''` (Supabase-Advisory-Empfehlung, per Web-Recherche verifiziert statt geraten: `CREATE … SET search_path=''` für neue, `ALTER FUNCTION … SET search_path=''` für bestehende Funktionen). Die bestehende `monitor.reject_tariff_snapshot_mutation` (erste Migration) wurde NICHT editiert, sondern per `ALTER FUNCTION` in der NEUEN Migration nachgezogen (Migrationshistorie bleibt append-only) — ihr Verhalten ändert sich dadurch nicht (die Funktion griff nie auf eine Tabelle zu, nur `RAISE EXCEPTION`).
  - **[VERIFIKATION — Docker lief diesmal, echter `supabase db reset`, kein PGlite-Ersatz nötig]** `supabase db reset` wendet beide Migrationen + `seed.sql` fehlerfrei an. Gegen die echte lokale Instanz (`docker exec … psql`) bewiesen: (a) `pg_proc.proconfig` zeigt `search_path=""` für alle drei `monitor`-Funktionen; (b) `UPDATE` auf `scrape_targets` bewegt `updated_at` sichtbar vor (`created_at ≠ updated_at`, neuer Wert > alter Wert); (c) `UPDATE` UND `DELETE` auf `scrape_runs` schlagen beide mit der erwarteten, tabellenspezifisch interpolierten Fehlermeldung fehl, Zeile bleibt unverändert; (d) `anon`/`authenticated` (`SET ROLE`) bekommen `permission denied` auf SELECT/INSERT bei beiden Tabellen. DB danach per zweitem `db reset` wieder auf sauberen Migrations+Seed-Stand zurückgesetzt (keine Test-Zeilen hinterlassen). **`supabase/database.types.ts` real regeneriert** (`supabase gen types typescript --local --schema public,monitor`, diesmal nicht nur als offener Punkt für den Nutzer) — enthält jetzt `scrape_targets`/`scrape_runs` inkl. der FK-Relation.
  - **`pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün** (`engine` 144/144, `shared` 18/18, `tariff-monitor` 62/62 unverändert; `website`/`portal`/`web` bauen durch).
- **[GEBAUT: T3 — Teil 1 von ~4] Datenpipe-Beweis in `apps/web` — Server-Fetch (`monitor.current_tariffs`) → Mapping am Leserand → Client-Engine (`compareTariffs`) → Ergebnis.** Neue Route `/strom-check` (`apps/web/app/(site)/[locale]/strom-check/page.tsx`), `noindex` (WIP, `lib/routes.ts`), keine Nav-Verlinkung (bleibt Website-Session, §4.2). **REINHEITSGRENZE gewahrt:** `packages/tariff-monitor` unangetastet — kein DB-/UI-Code hier, nur als Workspace-Dependency (`workspace:*`) + `transpilePackages` in `apps/web` eingehängt.
  - **[ARCHITEKTUR]** `apps/web/lib/monitor/supabase.ts` (Server-Component-only, `import 'server-only'`): Supabase-Client fürs `monitor`-Schema (`db.schema:'monitor'`), liest `monitor.current_tariffs` (T2, bereits seed-befüllt). Global-`fetch`-Wrapper injiziert `next.revalidate=86400` (§7: die Tarif-Tabelle ändert sich nur 1×/Tag), zusätzlich `export const revalidate = 86400` auf dem Route-Segment. `apps/web/lib/monitor/mapping.ts`: reine Mapping-Funktion snake_case→camelCase (`TariffCostObject`, T1-Contract) — **am Leserand in `apps/web`, wie vom Prompt gefordert, NICHT in der Engine.** `current_tariffs` ist eine View (T2, `DISTINCT ON`) — der Typgenerator markiert alle Spalten nullable; eine Zeile mit fehlendem Pflichtfeld wird defensiv verworfen statt gecastet (kann laut DB-Constraints nicht vorkommen, aber ein Cast wäre eine unbewiesene Behauptung).
  - **[ENV, bewusst NICHT NEXT_PUBLIC_]** `SUPABASE_URL`/`SUPABASE_ANON_KEY` (apps/web/.env.example) — non-prefixte Server-Env, obwohl dieselben Werte am Root bereits unter `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` stehen (dort für einen künftigen Client-seitigen Auth-SDK-Einsatz reserviert, T4). Begründung: Next.js ersetzt `NEXT_PUBLIC_*` beim Build TEXTUELL an jeder Fundstelle im Bundle; ein non-prefixter Name kann strukturell NIE ins Client-Bundle wandern (im Browser schlicht `undefined`) — selbst wenn `lib/monitor/supabase.ts` versehentlich aus einer Client-Komponente importiert würde. `import 'server-only'` erzwingt zusätzlich einen Build-Fehler in genau diesem Fall.
  - **[CLIENT]** `apps/web/components/monitor/gratis-check-client.tsx` (`'use client'`) bekommt das fertige `TariffCostObject[]` als Prop — KEIN Supabase-Import, KEIN Key. Ruft `compareTariffs` (T1) direkt im Browser mit einem hart hinterlegten `UserTariffInput` auf (NUR die 4 Stufe-1-Pflichtfelder — 3500 kWh, 27,9 ct/kWh, 118 €/Jahr, PLZ 1100 — bewusst kein Formular, kein localStorage, das ist T3-2). Render minimal: aktuelle Jahreskosten (Dauerpreis), `confidence`-Label, `plausibility.warnings` (aktuell immer leer — `checkPlausibility` ist weiterhin NICHT in `compareTariffs` verdrahtet, unverändert seit T1-Teil 5), Empfehlung, alle Alternativen mit `savingOngoingEurPerYear`, Mehrheitsabdeckungs-Disclaimer (§7, Platzhaltertext).
  - **[VERIFIKATION]** Lokaler Supabase-Stack (`supabase start`/`db reset`, Docker lief) mit den echten T2-Seed-Tarifen (10 Zeilen) gegen den Dev-Server UND den Production-Build gefahren (Playwright): Route lädt, „10 Tarife aus der Datenbank geladen", Empfehlung „Wechsel zu Blitz Energie Blitz Prepaid spart ca. €323/Jahr beim Dauerpreis." (rechnerisch korrekt: 3500×0,279+118=€1.095 aktuell vs. 3500×0,199+75=€771,50), 10 sortierte Alternativen, 0 Konsolenfehler, **0 Browser-Requests an Supabase** (der Client ruft nie direkt die DB, nur die Server-Component tut das). **Anon-Key-Bündel-Check:** `.next/static` (gebauter Client-Output) enthält 0 Treffer für den Key, die URL, `SUPABASE_*`-Variablennamen oder `createClient`/`supabase` — der gebaute `strom-check`-Client-Chunk enthält dagegen nachweislich `tariff-monitor`s Rationale-Strings („Wechsel zu", „spart ca.", „Wechselbonus"), beweist also, dass die Engine (nicht die DB-Anbindung) im Bundle landet. `next build` prerendert `/strom-check` als SSG mit `Revalidate 1d` (ISR) — der Server-Fetch lief schon zur Build-Zeit erfolgreich gegen die lokale Instanz.
  - **[KEIN Node-only-Import-Problem]** `tariff-monitor` ist rein/isomorph (kein I/O) wie erwartet — `transpilePackages` genügte, kein Sonderfall wie bei `engine`s Parser nötig.
  - **[OFFEN, nächste T3-Teile]** Echtes Eingabeformular (nur Stufe-1-Felder) statt Hardcoded-Sample; localStorage-Merken (§6 Schritt 3); Design-Feinschliff gegen `apps/web/DESIGN.md`; Nav-Verlinkung/Produktseite (Website-Session).
  - **`pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün** (`engine` 144/144, `shared` 18/18, `tariff-monitor` 62/62 unverändert; `apps/web`/`apps/website`/`apps/portal` bauen durch).
- **[GEBAUT: T3 — Teil 2 von ~4] Echtes Eingabeformular (4 Stufe-1-Pflichtfelder) statt Hardcode + Plausi-Wiring + localStorage-Merken.** Neu `apps/web/lib/monitor/schema.ts` (zod-Schema `gratisCheckSchema`, de-AT-Komma-Zahlenparsing wie `components/quick-calculator.tsx`, Grundgebühr-Normalisierung €/Monat→€/Jahr) + `apps/web/lib/monitor/storage.ts` (localStorage read/write/clear, `try`/`catch` wie `lib/kalkulator-access.ts`) + neu `apps/web/components/monitor/gratis-check-form.tsx` (reine Formular-Komponente: `values`/`onChange` von außen kontrolliert — der Elternknoten hält den Zustand für die localStorage-Vorbelegung —, Validierung/Fehlertexte/Fokusführung bleiben INNERHALB der Komponente, reines Formularverhalten). `gratis-check-client.tsx` ersetzt den hartkodierten `SAMPLE_INPUT` aus T3-1 vollständig. Ergebnis-Render bleibt bewusst MINIMAL wie in T3-1 (Design ist T3-3), NICHT angefasst: `packages/tariff-monitor` selbst.
  - **[ENGINE-WIRING]** Beim Absenden laufen `compareTariffs(userInput, tariffs)` UND `checkPlausibility(userInput)` (OHNE `matchedTariff`/`invoiceTotalEur`/`gridCostEstimate` — es feuern also nur Stufe 1+2, §5.3) NEBENEINANDER im Browser. `checkPlausibility` bleibt bewusst UNVERDRAHTET in `compareTariffs` (seit T1-Teil 4/5 Architektur, `result.plausibility.warnings` bleibt der leere Stub) — die UI ruft beide Funktionen direkt auf und rendert die `checkPlausibility`-Warnungen in einer eigenen Karte, nicht über den Stub.
  - **[FORMULAR-UX]** Grundgebühr: EIN Zahlenfeld + Select (€/Monat · €/Jahr, Default Monat), intern immer auf €/Jahr normalisiert (`toUserTariffInput`). Das Energiepreis-Feld trägt den §1.4-Pflicht-Hinweis IMMER, nicht erst nach einem Fehler — Prominenz über `<strong>`-Textgewicht (`t.rich`), NICHT über Warnfarbe (DESIGN.md reserviert Bernstein für echte Warnungen, nicht für Dekor). Zahlenfelder: `type="text"` + `inputMode="decimal"` statt `type="number"` — dieselbe, bereits in `quick-calculator.tsx` getroffene und dort begründete Entscheidung (Komma-Eingabe liefert bei `type="number"` browserabhängig einen leeren Wert). PLZ-Grundform `^\d{4}$`. Je Feld EIN Hinweis-Slot, der bei einem Fehler den Hilfetext ERSETZT (Muster wie `quick-calculator.tsx`s `fieldHint()`) — nie zwei Meldungen nebeneinander. Fokus springt nach einem fehlgeschlagenen Absenden ins erste fehlerhafte Feld (Muster wie `kontakt-form.tsx`).
  - **[localStorage, §6/§10]** Gespeichert werden die ROHEN Text-Werte, nicht die geparsten Zahlen — ein Rückkehrer sieht exakt seine eigene Schreibweise wieder (z. B. „9,90"). Geschrieben wird NUR nach einem erfolgreich geparsten Absenden (kein Zwischenstand). Vorbelegung + „Willkommen zurück"-Hinweis laufen erst NACH DEM MOUNT (`useEffect`, Muster wie `calculator-gate.tsx` — `window.localStorage` existiert während SSR/erstem Paint nicht). Restore befüllt NUR das Formular, löst KEINE automatische Neuberechnung aus (§6: der Nutzer sieht seine Eingabe wieder und vergleicht bewusst erneut, kein Auto-Submit). „Neu eingeben" leert Formular + localStorage und erzwingt per `key`-Wechsel einen Remount von `GratisCheckForm` — räumt dessen internen Fehler-Zustand mit auf, kein zweiter Reset-Kanal nötig.
  - **[VERIFIKATION, Playwright gegen den echten Dev-Server + lokalen Supabase-Stack]** Leeres Absenden → 4 korrekt formulierte Feldmeldungen, Fokus im ersten fehlerhaften Feld, kein Crash. Echte Werte (4.200 kWh / 21,5 ct/kWh / 9,90 €-Monat / PLZ 1010) → Ergebnis zeigt **€ 1.022/Jahr** (rechnerisch exakt: 4200×0,215 + 9,90×12 = 1.021,80 €) und 10 sortierte Alternativen — nachweislich NICHT mehr der alte Hardcode-Wert (T3-1: 3500 kWh/27,9 ct/€1.095). **0 Netzwerk-Requests während des Absendens** (mitgeloggt) — belegt, dass `compareTariffs`/`checkPlausibility` rein im Browser laufen, keine Daten den Client verlassen (§10). Energiepreis auf **28 ct/kWh** geändert → Stufe-2-Warnung erscheint wortgleich „28 ct/kWh sieht nach dem Gesamtpreis inkl. Netz und Steuern aus…" in einer eigenen, sichtbaren Karte. Reload → Formular exakt aus localStorage vorbelegt (inkl. „28"), „Willkommen zurück"-Hinweis sichtbar, KEIN automatisch berechnetes Ergebnis. „Neu eingeben" → Formular leer, `localStorage`-Eintrag entfernt, Hinweis weg. 0 Konsolenfehler über den gesamten Ablauf. `pnpm lint`/`typecheck`/`test`/`build` monorepo-weit grün (`engine` 144/144, `tariff-monitor` 62/62, `shared` 18/18 unverändert).
  - **[OFFEN, nächste T3-Teile]** Design-Feinschliff des Ergebnis-Renderings (T3-3, bleibt bewusst roh wie T3-1); Nav-Verlinkung/Produktseite (Website-Session). PLZ wird gesammelt+geprüft, fachlich aber (wie schon im T1-Contract) noch NICHT verwendet (§12 #5, Netzgebiets-Zuordnung erst mit echten Netzkosten-Daten).
- **[GEBAUT: T3 — Teil 3 von ~4] Ergebnis-Darstellung im apps/web-Design — die Ehrlichkeits-Prinzipien werden SICHTBAR.** Neu `apps/web/components/monitor/gratis-check-result.tsx`, aus `gratis-check-client.tsx` extrahiert (jener bleibt reine Orchestrierung: Formular-Zustand halten, Engine aufrufen, Ergebnis reichen). NICHT angefasst: `packages/tariff-monitor`, `gratis-check-form.tsx`.
  - **[HEADLINE, §1.3]** Dominante `text-h1 text-positive`-Zahl = `savingOngoingEurPerYear` der Empfehlung, NUR wenn `result.recommendation` existiert (das ist in `compare.ts` bereits an `savingOngoing > 0` gebunden, hier also keine zweite Prüfung nötig, nur konsumiert). Ohne Empfehlung: KEINE Zahl, stattdessen „Du zahlst bereits konkurrenzfähig — bei den größten Anbietern Österreichs findet sich aktuell nichts spürbar Günstigeres für deinen Tarif." (eigener Textblock, kein `text-positive`).
  - **[BONUS STRIKT SEPARAT, §1.3]** Eigene Zeile UNTER der Headline (`border-t`, eigenes Label „Zusätzlich: einmaliger Wechselbonus"), gerendert nur bei `recommendation.tariff.bonusEur > 0` — nie Teil der großen Zahl. Mit dem aktuellen Seed-Platzhalter-Datensatz (`seed.sql`) ist dieser Pfad NICHT live vorführbar: jeder bonustragende Tarif ist dort bei JEDEM Verbrauch strukturell teurer (höherer Preis UND höhere Grundgebühr) als „Blitz Prepaid"/„Blitz Gewerbe Prepaid" — die Empfehlung fällt deshalb nie auf einen Bonus-Tarif. Geprüft per Code-Lesung + Tabellenabgleich (`monitor.current_tariffs`), nicht nur behauptet. Sobald echte Tarife (§12 #6) den Platzhalter ersetzen, ist der Pfad live zu verifizieren.
  - **[ALTERNATIVEN, Punkt 4]** Die VOLLSTÄNDIGE `result.alternatives`-Liste (kein Slice) als Karten-Zeilen; die empfohlene Zeile trägt zusätzlich Akzent-Rand + „Empfehlung"-Badge (taucht bewusst doppelt auf — Headline UND Liste, Analogon zum Kalkulator-`perBattery`). Teurere Alternativen (`savingOngoing < 0`) zeigen EIGENEN Text „X/Jahr mehr" mit negativer (roter) Badge-Farbe, NIE das Wort „sparen"/„Ersparnis".
  - **[PLAUSI-WARNUNGEN, §5.3]** Eigene, warnfarbene Box (`border-warning-border bg-warning-subtle`, DESIGN.md-Token, `/alpha` vermieden) VOR der Ergebnis-Karte — bewusst OHNE Icon (dieselbe Zurückhaltung wie `components/wissen/callout.tsx`, ein ⚠️ wäre der von §7.3 verbotene verspielte Ton).
  - **[MEHRHEITSABDECKUNG, §7]** `coverageDisclaimer` auf den im Prompt vorgegebenen Wortlaut umgestellt: „Wir vergleichen die größten Anbieter Österreichs, nicht den kompletten Markt." (vorher eine ähnliche, aber nicht wortgleiche Fassung aus T3-1).
  - **[GROB-HINWEIS + ABO-BRÜCKE, §5.1]** Das Formular liefert STRUKTURELL nie mehr als die 4 Stufe-1-Felder (`toUserTariffInput`, `lib/monitor/schema.ts`) — `result.confidence` ist hier also IMMER `'rough'`. Die `isRough`-Bedingung bleibt trotzdem im Code (nicht als `true` hartkodiert), falls das Formular später Stufe-2-Felder bekommt. Zwei Textstellen: ein kurzer Hinweis direkt unter der Headline („grobe Dauerpreis-Schätzung ohne exakte Bonus- und Folgejahr-Rechnung") UND eine explizite Überleitung direkt über der Abo-Karte.
  - **[ABO-KARTE, §2/§12#1 — MITTLERE Prominenz]** Eigene Karte mit Akzent-Rand (sichtbar, aber keine Navy-Vollfläche/kein Hero — das wäre drängend, §1.2 verbietet künstliche Dringlichkeit). Drei Wert-Bullets mit lucide-Line-Icons (`ScanLine`/`Calculator`/`Bell`, `text-accent`, `strokeWidth=1.75` — Muster wie `how-it-works.tsx`): KI-Rechnungsscan, präziser normalisierter Vergleich inkl. Bonus/Folgejahr, laufende Überwachung. KEIN hartkodierter Preis (die 4,90 € aus §2 sind Arbeitsannahme, kommerziell offen). CTA-Button ist ein ECHTES `disabled`-Element (kein `href`, kein `onClick`) + erklärender Text „Wir informieren dich, sobald die Registrierung startet." — bewusst kein Fake-Link, der wie ein Bug aussieht; T4 ersetzt `disabled` durch den echten Stripe/Registrierungs-Flow.
  - **[VERIFIKATION, Playwright gegen den echten Dev-Server + lokalen Supabase-Stack, Desktop 1280px UND Mobile 375px]** Sparfall (4.200 kWh/24,5 ct/€9,90-Monat/PLZ 1010): grüne Headline **€ 237/Jahr** „Ersparnis/Jahr beim Wechsel zu Blitz Energie Blitz Prepaid", 10 Alternativen (7× grün „X/Jahr sparen", 3× rot „X/Jahr mehr" — kein „Ersparnis"-Wort bei den teureren), Coverage-Disclaimer + Grob-Hinweis + Abo-Karte sichtbar, CTA programmatisch `disabled`. Bereits-konkurrenzfähig-Fall (3.500 kWh/15 ct/€5-Monat/PLZ 1010, bereits im ERSTEN Versuch erreicht): „Du zahlst bereits konkurrenzfähig", KEINE grüne Zahl, alle 10 Alternativen korrekt „…mehr" (rot). 28-ct-Plausi-Fall: prominente Bernstein-Box mit dem §14-DoD-Text VOR der Ergebnis-Karte, Ergebnis darunter unverändert korrekt. **0px horizontaler Overflow** (`scrollWidth − clientWidth`) auf allen 3 Szenarien × 2 Breakpoints, **0 Konsolenfehler** über alle 6 Läufe. `pnpm lint`/`typecheck`/`build` (apps/web) grün, `pnpm -r test` monorepo-weit **224/224** grün (`engine` 144, `tariff-monitor` 62, `shared` 18) unverändert.
  - **[OFFEN, nächster T3-Teil]** Nav-Verlinkung/Produktseite (Website-Session, §4.2) — bleibt der letzte T3-Baustein.
- **[GEBAUT, außerhalb der T3-Nummerierung] Abo-Karte umplatziert + eigener Flächenton.** `gratis-check-result.tsx`: die Abo-Karte (§2/§12#1) sitzt jetzt direkt nach dem Ergebnis/der Empfehlung, VOR der Alternativen-Liste (stärkster Funnel-Moment), statt am Listenende — nur EINMAL platziert. Neuer apps/web-DESIGN.md-Token **`--color-navy-subtle`** (`#eff6ff`, Blue 50, dieselbe Methodik wie die übrigen `*-subtle`-Tokens) als eigener, dezenter Flächenton der Karte (`bg-navy-subtle` neben dem bestehenden `border-accent-border`) — hebt sie vom umgebenden Ergebnis ab, ohne mit dem grünen Ersparnis-Wert zu konkurrieren oder eine Navy-Vollfläche/Hero zu sein. Kein Token dafür existierte vorher (geprüft, dann mit Andreas abgestimmt statt einen Wert zu erfinden) — dokumentiert in `apps/web/DESIGN.md`.
- **[GEBAUT: T3 — Teil 4 von 4, ABSCHLUSS] Randfälle gehärtet + Responsive-Feinschliff — T3 damit inhaltlich VOLLSTÄNDIG.** Reine Härtung/Politur der bestehenden Oberfläche (§6/§7), keine neuen Features, `packages/tariff-monitor` unangetastet.
  - **[RANDFALL 1 — leere/nicht erreichbare Tarif-Tabelle, §7]** Zwei bisher unterschiedlich schlecht behandelte Ursachen laufen jetzt auf DENSELBEN ruhigen Zustand zusammen: (a) DB erreichbar, 0 Zeilen (Scrape-Lauf lieferte nichts) — vorher `text-negative`-Fehlertext ohne Fassung; (b) DB **nicht erreichbar** — `fetchCurrentTariffRows` wirft ABSICHTLICH bei einem echten Fehler (s. Kommentar dort), aber `app/(site)/[locale]/strom-check/page.tsx` fing das nirgends ab → ein nicht erreichbares Supabase hätte die Server-Component werfen lassen und Next' generische Fehlerseite gezeigt, nicht „ruhige Meldung". **Gefunden, nicht nur vermutet:** mit einer unerreichbaren `SUPABASE_URL` reproduziert (Fetch-Timeout, `TypeError: fetch failed`), dann per try/catch in `page.tsx` abgefangen (serverseitig `console.error` — Team-Sichtbarkeit für den §7-Robustheits-Alert —, Client bekommt ein leeres Array). Beide Ursachen zeigen jetzt identisch: neue neutrale Box (`border-line`/`bg-surface-sunken`, dasselbe `info`-Callout-Muster wie `components/wissen/callout.tsx`, KEINE Warnfarbe — kein Fehler des Nutzers), neuer Text „Vergleich derzeit nicht verfügbar…" (`Monitor.GratisCheck.unavailable.*`, ersetzt den alten `noTariffs`-Key).
  - **[RANDFALL 2 — kein sparender Kandidat, §1.3]** Bereits vorher korrekt gebaut (T3-3) — hier re-verifiziert (Playwright, `energyPriceCtPerKwh=5`), explizit gegen alle 10 echten Seed-Tarife: „Du zahlst bereits konkurrenzfähig", KEINE grüne Zahl, 0× „undefined", alle 10 Alternativen korrekt „…mehr" (rot), Abo-Karte + Alternativen-Liste vollständig, kein leerer Zustand.
  - **[RANDFALL 3 — Rückkehrer-UX, §6]** Der „Willkommen zurück"-Banner (`gratis-check-client.tsx`) hatte eine ABWEICHENDE Kasten-Geometrie (`rounded-md px-4 py-3`) gegenüber `PlausibilityWarnings`/`Callout` (`rounded-lg p-5 sm:p-6`) — jetzt vereinheitlicht, plus `variant="secondary"` (Navy-Kontur) statt `ghost` für „Neu eingeben": ein klarer, nicht zu übersehender Weg zurück zu einer frischen Eingabe.
  - **[RANDFALL 4 — Lade-/Übergangs-Zustand, §7]** Root Cause eines echten, MESSBAREN Layout-Sprungs beim Absenden gefunden: `handleValidSubmit` setzte `showWelcomeBack` synchron auf `false` — der Banner (sitzt VOR dem Formular) verschwand im selben Render wie das Ergebnis erschien, der „Jetzt vergleichen"-Button (unter dem Zeiger/Fokus) sprang sichtbar nach oben. Fix: `showWelcomeBack` bleibt nach dem Absenden bestehen (die Aussage bleibt wahr — `saveGratisCheckValues` speichert exakt die gerade abgesendeten Werte), verschwindet erst bei „Neu eingeben" (dort ist ein Sprung erwartet, das Formular wird ohnehin geleert). **Gemessen (Playwright, `boundingBox()` des Submit-Buttons vor/nach Klick):** 0px Differenz (vorher ungemessen, aber durch den synchronen State-Doppel-Update strukturell vorhanden).
  - **[RANDFALL 5 — lange Namen/extreme Zahlen, §7]** `lib/monitor/schema.ts` deckelt Verbrauch/Preis/Grundgebühr bewusst NICHT nach oben (nur `positive()`) — eine extreme Eingabe (z. B. 999.999.999.999 kWh × 99.999.999 ct/kWh) ist damit strukturell möglich, nicht nur theoretisch. `gratis-check-result.tsx`: `min-w-0 flex-1` auf dem Namens-/Kosten-Block je Alternativen-Zeile (Flex-Kinder schrumpfen sonst nicht unter ihre Inhaltsbreite — ein langer Anbietername sprengt sonst die Zeile statt umzubrechen) + `break-words` auf Headline-Zahl, Alternativen-Namen und -Badges. **Verifiziert:** ein DOM-injizierter 140-Zeichen-Anbietername bei 400px Breite UND eine echte, unbegrenzte Eingabe (Headline rendert „€ 999.999.801.098.999.800 /Jahr") bei 375px — beide **0px horizontaler Overflow**.
  - **[TEAL-TOKEN-PRÜFUNG, im Nebenbefund verlangt]** `border-accent-border` auf der Abo-Karte ist NICHT versehentlich vom Kalkulator übernommen — `apps/web/DESIGN.md` legt `--color-accent: #0f766e` (Teal 700) explizit „identisch zum Kalkulator" fest (Prinzip 7.2, „ein Produkt"-Gefühl Marketing↔Kalkulator) und dieser EINE Akzent-Token trägt in `apps/web` durchgängig CTA/Badges/Callouts/Plausi-Warnungs-Rand. Die „Grün-Akzent"-Formulierung in `apps/web/Pflichtenheft_Website_Coolin.md` Prinzip 6 beschreibt denselben gedämpften Teal-Ton umgangssprachlich (RGB-Grünanteil dominiert). **Belassen, nicht umbenannt/umgefärbt** — korrekter, kanonischer Token, keine Foreign-Value.
  - **[RESPONSIVE-FEINSCHLIFF]** Voller Durchgang @375/768/1280px über alle Zustände (Sparfall, bereits-konkurrenzfähig, Rückkehrer, lange Namen, extreme Zahlen, leere Tabelle) — **0px horizontaler Overflow** (`scrollWidth − clientWidth`) in jedem Fall, keine zusätzlichen Anpassungen nötig (bestehende `flex-wrap`/`max-w-prose`/Grid-Muster trugen bereits; nur die in Randfall 5 beschriebenen `min-w-0`/`break-words`-Ergänzungen waren echte Lücken).
  - **[VERIFIKATION, Playwright gegen den echten Dev-Server + lokalen Supabase-Stack, inkl. gezielt manipulierter DB/Env]** 0-Zeilen-Fall real hergestellt (`ALTER TABLE … DISABLE TRIGGER ALL; DELETE …` auf `tariff_snapshots`, danach wiederhergestellt via `supabase db reset` — die Append-only-Sperre (T2) verhindert das ohne den Trigger-Schalter zu Recht) UND Fetch-Fehler-Fall real hergestellt (unerreichbare `SUPABASE_URL`, bei nicht-leerer Tabelle — beweist, dass der ruhige Zustand vom ABGEFANGENEN FEHLER kommt, nicht zufällig von 0 Zeilen). Beide zeigen identisch die neue Box, 0 „echte" Konsolenfehler (der einzige eingefangene Log-Eintrag im Fetch-Fehler-Fall ist Next-Dev-Modus' bekanntes Spiegeln von Server-`console.error` in die Browser-Konsole — dev-only, kein React-Error-Boundary-Trip, in Production nicht vorhanden). `pnpm lint`/`typecheck`/`build` (apps/web) grün, `pnpm -r test` monorepo-weit **224/224** grün (`engine` 144, `tariff-monitor` 62, `shared` 18) unverändert.
  - **T3 (Gratis-Check-UI, §13) ist damit fachlich abgeschlossen:** Formular → Client-Vergleich → gestaltetes Ergebnis + Plausibilität + Abo-Teaser, dataless (§10), gegen `monitor.current_tariffs`; alle in §6/§7 benannten Randfälle behandelt.
- **T1–T3 sind damit fachlich abgeschlossen** (Tarif-Engine, Datenschicht, Gratis-Check-UI inkl. Randfall-Härtung — s. Einträge oben). **Nächster Schritt: T4 — Auth + Stripe + Entitlements** (§4.3/§13): erstes reales Portal-Fundament, geteilt mit dem künftigen Kalkulator-Portalteil; `hasActiveMonitor` aus Stripe-Webhook. Das Admin-UI für Scraper-Targets (§7) entsteht **mit** T4 (braucht denselben Auth-Container — kein vorgezogenes Sonder-Login). **T2 — Scraper-Code** (§7/§13, Extraktion) bleibt parallel offen, ist aber nicht der nächste aktive Schritt: Top-15–20-Scraper, tägliche Kadenz, Schreiben in `scrape_targets`/`scrape_runs` + Robustheits-Alerts bei 0/unplausiblen Ergebnissen — das komplette Zeitreihen-/Betriebs-Schema (inkl. `scrape_targets`/`scrape_runs`) steht bereits (s. o.), **braucht aber die echten Scraper-Targets (§12 #6, Andreas/Martin) — noch nicht geliefert**, plus einen kurzen ToS-Check (§12 #3); Extraktionsmethode ist inzwischen als KI-Default entschieden (§7 im Pflichtenheft). Sobald `TariffCostObject` einen zweiten Consumer bekommt (DB-Lesecode, bereits mit T3 geschehen), gehört der Typ voraussichtlich nach `packages/shared` (Muster: geteilte Contract-Typen liegen dort, nicht in einem Feature-Package) — nicht vorgezogen, bis es einen konkreten Anlass gibt.
  - **Offene Notizen, weiterhin unverändert mitgeführt:** `billingCycle` fließt bewusst NICHT in `ongoingYearlyCostEur` ein — `[OFFEN: billingCycle-Effekt?]` in `normalize.ts`, ungeklärt bis ein unterjähriger Abschlags-Mechanismus im Contract auftaucht. Stufe-4-Rechnungs-Rückrechnung (`checkInvoiceReconciliation`) rechnet die relative Abweichung bezogen auf `invoiceTotalEur` (den beobachteten Rechnungsbetrag), nicht auf `expected` (die eigene Schätzung) — bei T6 (KI-Rechnungsscan) bewusst bestätigen, ob das weiterhin die richtige Bezugsgröße ist.
- **[GEBAUT: T4-1 — geteiltes `platform`-Schema (Auth/Rollen/Entitlements/Stripe-Spiegel), REIN DATENBANK]** Neue Migration `supabase/migrations/20260718093043_create_platform_schema.sql` (Migrationshistorie append-only gewahrt, bestehende monitor-Migrationen unangetastet). **PRODUKTÜBERGREIFEND:** `platform` trägt Monitor UND künftigen Kalkulator-Portalteil — liegt deshalb bewusst NICHT im `monitor`-Schema (ein Produkt-Schema darf nie Abhängigkeit des Fundaments werden; Abhängigkeitsrichtung immer Produkt → Fundament). **§15-PFLICHT:** Änderungen an `platform` sind geteilte Infrastruktur und mit der Kalkulator-Doku synchron zu halten. **Kein** Auth-Code/Stripe-Code/UI — das ist T4-2/T4-3/T4-4.
  - **[SCHEMA]** 6 Tabellen: `profiles` (1:1 zu auth.users, kein E-Mail-Duplikat), `customers` (Stripe-Customer-Spiegel, `stripe_customer_id` unique), `subscriptions` (Stripe-Abo-Spiegel, `status` als text+CHECK statt Enum — Fremdvokabular), `entitlements` (**die EINE Tabelle, die App-Code zur Zugangsentscheidung liest**, PK `(user_id, product)`), `stripe_events` (append-only Idempotenz-Ledger, PK = Event-ID), `user_roles` (PK `(user_id, role)`). 2 Enums (`product_key`, `entitlement_source`). Alle 9 Funktionen mit `SET search_path = ''` (Muster wie monitor).
  - **[ENTITLEMENT-ABLEITUNG PER TRIGGER, NICHT IM HANDLER (I2)]** `sync_entitlement_from_subscription` (AFTER INSERT/UPDATE auf subscriptions) leitet die `entitlements`-Zeile ab (I9-Status-Mapping in EINER Funktion `status_grants_access`: active/trialing/past_due → aktiv, sonst inaktiv; `valid_until = current_period_end`; `cancel_at_period_end` ändert NICHTS). Upsert nur über bestehende `source=stripe`-Zeilen (`WHERE source='stripe'`) → manueller Grant (`source=manual`, Testnutzer) wird NIE überschrieben. **Hartschutz `guard_entitlement_stripe_source`:** eine `source=stripe`-Zeile kann NUR aus dem Sync-Trigger heraus entstehen (transaktionslokales Flag) — Anwendungscode (service_role) kann keine stripe-Entitlements fälschen. Grund für Trigger statt Handler: der Webhook (T4-3) kann das Nachziehen vergessen, die DB nicht.
  - **[WEITERE INVARIANTEN]** `reject_stale_subscription_update` (BEFORE UPDATE, I5: älteres `stripe_event_created_at` → `RETURN NULL`, Zeile unverändert, kein Fehler, kein Sync). `reject_stripe_event_mutation` (append-only, I6). `handle_new_user` (AFTER INSERT auf auth.users, SECURITY DEFINER, idempotent, I7). ON DELETE CASCADE durchgängig auf auth.users (I8). `is_admin` (SECURITY DEFINER → liest user_roles ohne RLS-Rekursion, I10; kein JWT-Claim, Entzug greift sofort). `has_entitlement(user, product)` (SECURITY DEFINER, die EINZIGE Funktion, die T7-Cron aufruft; sperrt bei abgelaufenem `valid_until` auch wenn `is_active`, I1/T11).
  - **[RLS/GRANTS, Least-Privilege]** RLS auf allen 6 Tabellen. `anon`: NIRGENDS Grant/Policy/Schema-Usage. `authenticated`: nur EIGENE Zeilen LESEN (I4, `select (select auth.uid())`) + Anzeigename der eigenen `profiles`-Zeile ändern (Column-Grant `update (display_name)`). KEIN Nutzer-Schreibzugriff auf customers/subscriptions/entitlements/stripe_events/user_roles (I3, der Bezahlschutz). `service_role`: genau die Webhook/Cron-Rechte (select/insert/update auf customers/subscriptions/entitlements, select/insert auf stripe_events, select auf user_roles) — **nirgends delete** (Löschung nur über auth.users-Cascade). SECURITY-DEFINER-Funktionen erst von PUBLIC entzogen, dann gezielt vergeben. **Admin-Policies bewusst NICHT vorgebaut** — kommen mit T4-4 (die Funktion `is_admin` existiert und ist getestet).
  - **[ENTSCHEIDUNG A — `set_updated_at`]** EIGENE `platform.set_updated_at()` angelegt statt `monitor.set_updated_at` zu referenzieren. Begründung: Schema-Autarkie — das Fundament darf nicht vom Produkt-Schema abhängen (genau die verbotene Richtung). Eine identische, eigenständige Funktion ist der geringere Preis. Im Migrationskommentar begründet.
  - **[ENTSCHEIDUNG B — config.toml]** `platform` NICHT in `[api].schemas` aufgenommen (Erwartung der Aufgabe bestätigt). Personenbezogene Auth-/Zahlungs-Spiegel werden von Server-Code (service_role/RLS-geschützt) gelesen, nie über die öffentliche REST-API mit anon-Key. Transport-Ebenen-Schutz zusätzlich zu RLS. `extra_search_path` ebenfalls unverändert (Funktionen sind `search_path`-autark).
  - **[AUSFÜHRBARES DB-GATE — NEU im Repo]** `packages/db-tests` (Vitest, `pg` + GoTrue-Admin-API über `fetch`). Bis dato lagen alle DB-Verifikationen nur als Prosa in Handover-Logs vor; bei Zugangsrechten/Zahlungsstatus ist das nicht vertretbar (RLS-Fehler = unsichtbares Datenleck). **12 Tests grün gegen den laufenden lokalen Stack** (T1–T11, je einer pro Invariante, + T6b Hartschutz). Jeder Test stellt den Zustand REAL her (echte Nutzer via Admin-API, echte Rollen). **RLS-Treue trotz nicht-exponiertem Schema:** die Tests setzen `request.jwt.claims` + `SET LOCAL ROLE` exakt wie PostgREST (keine Simulation — ein `supabase-js`-`.from()`-Client könnte `platform` mangels REST-Exposition gar nicht erreichen). **Eigenes `test:db`-Script (NICHT `test`)** → `pnpm -r test` zieht es nicht (Docker-freie Umgebungen bleiben verschont; die 224 bestehenden Tests unverändert). Fehlt der Stack → **harter Fehler mit klarer Meldung**, kein stiller Skip. Wiederholbar ohne `db reset`.
  - **[CI]** Separater Workflow `.github/workflows/db-gate.yml` (Trigger: `supabase/**` + `packages/db-tests/**` + manuell): Supabase-CLI, `supabase start` + `db reset`, Env aus `supabase status -o env`, `pnpm --filter db-tests test:db`, `supabase stop`. Bewusst NICHT in `test.yml` (der schnelle PR-Check bleibt schnell). Neues Root-Script `db:types` (`--schema public,monitor,platform`); `supabase/database.types.ts` regeneriert (enthält jetzt das platform-Schema).
  - **[SPEC-WIDERSPRUCH gemeldet]** Der Prompt nennt für den `status`-CHECK „neun bekannte Stripe-Werte", Invariante I9 zählt aber genau ACHT auf und Stripe kennt aktuell genau diese acht (active, past_due, unpaid, canceled, incomplete, incomplete_expired, trialing, paused). Implementiert wurden die acht realen Werte (I9 ist die verbindliche Verhaltensspec); der CHECK ist per Migration weitbar. Im Migrationskommentar + Report festgehalten. **Randfall past_due/valid_until:** I9 setzt `valid_until = current_period_end`, `has_entitlement` sperrt bei `valid_until ≤ now()`; ob ein `past_due`-Abo (Karte abgelaufen) sichtbar bleibt, hängt daran, ob Stripe `current_period_end` bei past_due in die Zukunft schiebt — ggf. Grace-Policy in T4-2/T4-3 nötig. Beide Punkte im Report unter (d).
  - **[VERIFIKATION]** `supabase db reset` grün (alle 3 Migrationen + Seed). Introspektion: 9 Funktionen `search_path=""`, 4 SECURITY DEFINER, RLS auf 6 Tabellen, Trigger/Enums wie entworfen. `pnpm lint`/`typecheck`/`build` (3 Apps) grün, `pnpm -r test` **224/224 unverändert** (`engine` 144, `tariff-monitor` 62, `shared` 18), `db-tests` **12/12** separat grün.
  - **[OFFEN → T4-2/T4-3/T4-4]** T4-2: Auth-Wiring (Supabase-Auth, Session/Middleware, profiles-Reads); `NEXT_PUBLIC_SUPABASE_*` ist dafür reserviert. T4-3: Stripe-Checkout + Webhook — schreibt `subscriptions`/`stripe_events` als service_role, die Entitlement-Zeile macht der Trigger; Idempotenz über den `stripe_events`-PK. Offene Entscheidung dort: wie Server-Code `platform` liest (service_role-Client mit `db.schema='platform'` + gezielte Exposition, oder direkte Verbindung, oder `public`-RPC-Wrapper) — bewusst nicht vorweggenommen. T4-4: Admin-UI + die dann nötigen `is_admin`-Policies. **Kalkulator-Doku (§15) für das geteilte `platform`-Schema noch nachzuziehen** (nicht im T4-1-Auftragsumfang der Monitor-Doku).
- **[NOTIZ, gegen das Vergessen — Reseller/Mandanten]** `platform.entitlements` ist für eine spätere **Reseller-/Mandanten-Struktur additiv um eine `tenant_id` erweiterbar** (ein Reseller mit eigenen Endkunden, die Zugang zum Pro-Kalkulator bekommen). **Bewusst NICHT jetzt gebaut** — es existiert kein realer erster Reseller, und eine Mandantenstruktur auf Verdacht wäre geratene Architektur (Prinzip „für Skalierung bauen, ohne Over-Engineering"). Reine Notiz, kein Code: sobald ein echter Reseller-Fall auftaucht, kommt `tenant_id` (+ RLS je Mandant) additiv dazu, kein Umbau am Bestand.
- **[GEBAUT: T4-2 — Supabase-Auth-Fundament in `apps/web`, ABGESCHLOSSEN]** Erstes reales Login der COOLiN-Plattform (Registrierung, E-Mail-Bestätigung, Login, Logout, Passwort-Reset, geschützte Kontoseite). Baut auf dem T4-1-`platform`-Schema auf; trägt später auch den Kalkulator-Portalteil. `@supabase/ssr` neu in `apps/web`. **KEIN Browser-Client / kein Supabase im Client-Bundle (J1)** — alle Auth-Vorgänge laufen server-seitig (Server Actions + ein Callback-Route-Handler), gemessen: `.next/static` enthält 0 Treffer für Key/URL/Env-Namen/`createBrowserClient`/`createServerClient`/`@supabase/ssr`/`exchangeCodeForSession`.
  - **[Aufgabe 0]** (0a) Neue Migration `20260718104300_entitlements_valid_until_constraint.sql`: CHECK, dass `source='stripe' AND is_active AND valid_until IS NULL` verboten ist — Fail-open-Schutz gegen den Stripe-**„Basil"**-Fallstrick (API 2025-03-31 verschob `current_period_end` aufs SubscriptionItem; ein naiver T4-3-Handler bekäme `undefined` → NULL → Dauerzugang trotz Kündigung). `source='manual'` bleibt mit NULL erlaubt (Lifetime/Test). +4 db-tests. (0b) Root-CLAUDE.md (Kalkulator) um den `platform`-Schema-Absatz ergänzt — **damit ist die in T4-1 offene §15-Doku-Nachziehung erledigt**. (0c) tenant_id-Notiz (oben).
  - **[Aufgabe 1 — zentrale Env-Validierung]** `apps/web/lib/env.public.ts` (nur `NEXT_PUBLIC_*`, überall importierbar) + `env.server.ts` (`import 'server-only'` — Geheimnisse können strukturell nicht ins Client-Bundle) + `env-shared.ts` (zod-Helfer). Die 5 bisherigen Fundstellen (`monitor/supabase.ts`, `site.ts`, `kontakt/deliver.ts`, `kontakt/turnstile.ts`, `turnstile-widget.tsx`) darauf umgestellt, Verhalten unverändert. `.env.example` nachgezogen.
  - **[Aufgabe 2 — Auth-Infra]** `lib/supabase/server.ts` (Server-Client für RSC + Actions/Route-Handler, ein setAll-try/catch deckt beide Kontexte) + `lib/supabase/middleware.ts` (`updateSession`). `middleware.ts` komponiert next-intl UND Supabase-Refresh (J2). `config.toml`: `enable_confirmations = true` (J4), `site_url`/`additional_redirect_urls` um `localhost`/`127.0.0.1` (+ `/**`-Globs für den Callback) ergänzt; lokal fängt **Mailpit** (früher „Inbucket", Port 54324) die Mails.
  - **[Aufgabe 3 — RPC-Wrapper]** Neue Migration `20260718104400_auth_rpc_wrappers.sql`: `public.get_my_entitlement(product)` (delegiert an `platform.has_entitlement(auth.uid(), …)`, J8) + `public.get_my_profile()` — SECURITY DEFINER, `SET search_path=''`, **nur an `authenticated` gegrantet, nie anon** (J9). Zugriff auf `platform` ausschließlich über diese public-Wrapper (J3). +5 db-tests.
  - **[Aufgabe 4 — Flows/Seiten]** Deutsche Routen unter `app/(site)/[locale]/`: `/registrieren`, `/anmelden`, `/passwort-vergessen`, `/passwort-neu`, `/konto` (server-geschützt, J6) + Callback `app/auth/callback/route.ts` (außerhalb Locale, von der Middleware ausgeschlossen). Alle Auth-Routen `noindex` (J7). Formulare: `useActionState` + Server Actions, bauen auf `Input`/`Label`/`FieldHint`/`Button`, ein Hinweis-Slot je Feld, Fokus ins erste fehlerhafte Feld (Muster `kontakt-form.tsx`). Alle Texte unter neuem Top-Level-Namespace `Konto` in `messages/de.json` (NICHT unter `Monitor`).
  - **[ENTSCHEIDUNGEN (nicht vorgegeben), mit Begründung]** (a) **Middleware-Reihenfolge:** next-intl ZUERST (baut die Response), dann schreibt Supabase die refreshten Cookies auf GENAU DIESE Response — der umgekehrte Weg verwürfe die Tokens still. (b) **`/login`** (englischer Alt-Platzhalter) → dauerhafter Redirect auf `/anmelden` (nicht gelöscht, `LOGIN_HREF` bleibt gültig; Header-Verlinkung ist Website-Session). (c) **EIN Server-Client** statt getrennt „Reader"/„Writer" — der setAll-try/catch macht ihn in RSC lese-sicher und in Actions schreibfähig (Supabase-Standardmuster). (d) **Passwort min. 8** (strenger als `config.toml` = 6, aber immer ≤ Supabase-Politik). (e) **„Sie"-Ansprache** im Konto-/Auth-Bereich (coolin.at-Shell), obwohl der Monitor-Gratis-Check „du" nutzt. (f) **Passwort-Reset & „E-Mail existiert nicht"** verraten NIE, ob eine Adresse registriert ist (J5 + Enumeration-Schutz).
  - **[ABWEICHUNG gemeldet — pinned-Image-Bug]** Das lokal/CI gepinnte `supabase/postgres:17.6.1.106` (PG 17.6) **segfault**et, wenn eine Nicht-Owner-Rolle eine `public`-Funktion OHNE Execute-Grant AUFRUFT (statt sauberem „permission denied for function") — ein Bug in einem vorgeladenen Hook (auto_explain/pgaudit/plan_filter), NICHT in unserem SQL (per Bisektion belegt; Vanilla-PG 17 wirft hier sauber, verwaltete Supabase-Cloud ist ein anderer Build). Die T4-1-Funktionen sind verschont, weil sie im nicht-exponierten `platform`-Schema liegen. **Konsequenz:** Die Grants bleiben design-korrekt authenticated-only; die db-tests prüfen die anon-Ablehnung per `has_function_privilege`-Introspektion (die Autorisierungs-Wahrheit) statt per Aufruf, der die gemeinsame Test-/CI-DB abstürzen ließe. App/Tests rufen die Wrapper nie als anon auf.
  - **[VERIFIKATION]** Ein Node-Skript treibt die ECHTEN Flows über progressive-enhancement `<form>`+Server Action (kein Browser, kein JS) gegen `next start` + lokalen Stack + Mailpit — **12/12 PASS**: Registrierung→Bestätigungsmail(Mailpit)→PKCE-Verifier-Cookie→Link→Session→`/konto` zeigt „kein aktives Abo"; `/konto` ohne Session → 307 `/anmelden` (roher Body ohne Kontoinhalt); Session überlebt Reload+Routenwechsel; Login mit unbestätigtem Account abgewiesen; voller Passwort-Reset inkl. Login mit neuem Passwort; `/strom-check` unverändert. `db reset` grün (5 Migrationen), `db-tests` **21/21**, `lint`/`typecheck`/`build` (3 Apps) grün, `pnpm -r test` **224/224** unverändert.
  - **[OFFEN → T4-3 / Deploy-Aufgabe Andreas]** T4-3 (Stripe): schreibt `subscriptions`/`stripe_events` als service_role, `current_period_end` aus dem **SubscriptionItem** (Basil) lesen (sonst greift der 0a-Constraint), `SUPABASE_SERVICE_ROLE_KEY`/`STRIPE_*` additiv ins `env.server.ts`-Schema. **Cloud-Deploy (nicht Teil dieses Prompts):** Redirect-URLs + produktionstauglicher SMTP (**Resend**, im Repo vorhanden) im Supabase-**Dashboard** setzen — der eingebaute Mailversand hat harte Rate-Limits. Turnstile auf der Registrierung ist bewusst nicht gebaut (Supabase-Rate-Limits genügen zunächst; vor Livegang neu bewerten).
- **[GEMESSEN: Cloud-Anbindung + Segfault-Probe — Deploy-Runde Andreas, REIN INFRA/MESSUNG, kein Feature-Code]** Lokales Repo mit dem Cloud-Projekt **coolin_energy** (Org „CoolIn", AWS **eu-central-1**/Frankfurt) verknüpft (`supabase link`, Ref/PW nur in gitignoreter Scratchpad-env, NICHT im Repo), alle **5 Migrationen gepusht** (`supabase db push` über den Session-Pooler). Kein Git-Commit (Andreas entscheidet über Branch/PR). Neue Repo-Datei `DEPLOYMENT.md` (Root, Env-/Dashboard-Anleitung, Namen+Fundorte, KEINE Werte) + `README_Doku-Struktur.md` nachgezogen.
  - **[SEGFAULT-PROBE — DER KERNBEFUND, T4-2-These GEMESSEN statt geschlossen]** Cloud-DB-Build ist **`17.6.1.147`** vs. lokal gepinnt **`17.6.1.106`** — anderer, aber nur minimal neuerer Build; **`shared_preload_libraries` sind IDENTISCH** (beide laden `pgaudit, auto_explain, plan_filter` — die verdächtigen Hooks). **Ergebnis der Probe über den ECHTEN Angriffsweg (anon-REST, `POST /rest/v1/rpc/get_my_profile` + `get_my_entitlement`) gegen die Cloud:** sauberes **HTTP 401 `42501 permission denied`**, **KEIN Crash, KEINE Recovery** (DB durchgehend normal, gleiche `pg_backend_pid`-erreichbar). **Byte-identisch zum lokalen 17.6.1.106-REST-Verhalten** — d. h. selbst das gepinnte Bug-Image wirft über REST sauber. **→ Die ursprüngliche Angst ist WIDERLEGT: ein unauthentifizierter HTTP-Request mit dem anon-Key kann die DB NICHT in Recovery schicken. Kein DoS über die öffentliche API.**
    - **[WO der Bug WIRKLICH sitzt — lokale Kontrollgruppe]** Der Segfault ist NICHT über REST erreichbar, sondern NUR über eine **direkte DB-Verbindung mit `SET ROLE anon; SELECT public.get_my_profile()`** (der von T4-2 per Bisektion gefundene Weg). Lokal reproduziert: Direktaufruf → „Connection terminated unexpectedly" + sofortiger Reconnect liefert **`57P03 cannot_connect_now`** (DB in Crash-Recovery), danach wieder gesund. Ein Angreifer mit bloßem anon-Key (REST) hat keine `SET ROLE`-fähige DB-Verbindung — der Bug ist über die Angriffsfläche unerreichbar.
    - **[METHODIK-KORREKTUR, für spätere Proben]** `pg_postmaster_start_time()` ist der FALSCHE Crash-Indikator: bei einem Backend-Segfault überlebt der Postmaster-Prozess (killt nur die Kind-Backends, macht Crash-Recovery), die Startzeit ändert sich NICHT. Echte Crash-Signale sind **Verbindungsabbruch + `57P03` beim Reconnect**.
    - **[NICHT gemessen — Andreas-Entscheidung offen]** Ob der Cloud-Build `17.6.1.147` den latenten Bug (nur per Direktaufruf auslösbar) noch trägt, ist NICHT definitiv geklärt: der destruktive Direktaufruf gegen die Cloud (der die leere DB kurz in Recovery schicken würde) wurde vom **Sicherheits-Klassifikator gestoppt**. Statische Evidenz (identische preloaded libs, minimaler Versionssprung) ist **inkonklusiv** — man kann NICHT schließen, dass die Cloud gepatcht ist. **Für die Sicherheit egal** (REST ist sauber). Wenn Build-Gewissheit gewünscht: Direkttest bei leerem Projekt nachholen (Andreas' Architekturentscheidung).
    - **[KONSEQUENZ für die T4-2-Workarounds — bleiben korrekt]** Die db-tests prüfen die anon-Ablehnung weiter per `has_function_privilege`-Introspektion (nicht per Aufruf) — richtig so, unabhängig vom Cloud-Build. Die Grants bleiben authenticated-only. Kein Handlungsbedarf am SQL/an den Wrappern.
    - **[GEGENPROBE authenticated — sauber]** Echter Cloud-Nutzer (GoTrue-Admin-API) → Login-Token → `get_my_profile` liefert die eigene Profil-Zeile (Auto-Profil-Trigger greift in der Cloud), `get_my_entitlement('monitor')` → `false`; Nutzer gelöscht, Cascade-Cleanup belegt (auth.users=0, platform.profiles=0).
  - **[CLOUD-VERIFIKATION Aufgabe 1 — bitgenau gegen lokal]** Introspektions-Snapshot (Schemas/Tabellen+RLS/Views/Enums/Funktionen+search_path+secdef/Grants/Policies/Trigger/Constraints) **byte-identisch** Cloud↔lokal (18 295 B je Seite): 2 Schemas, 9 Tabellen (**alle RLS aktiv**), 1 View, 2 Enums, **14 Funktionen alle `search_path=''`**, 31 Table-Grants, 7 Policies, 13 Trigger, 23 Constraints. Konkret belegt: `anon` hat **null** Grants + **keine Schema-Usage** auf `platform`; `monitor.current_tariffs` DB-seitig anon-lesbar; **`platform` NICHT exponiert** (PGRST106 „Only the following schemas are exposed: public, graphql_public").
  - **[ABWEICHUNG Cloud↔lokal #1 — handlungsrelevant] `monitor` ist in der Cloud NICHT exponiert.** `supabase db push` überträgt `[api].schemas` aus `config.toml` NICHT. Cloud exponiert nur `public, graphql_public` → ein REST-Read von `monitor.current_tariffs` liefert **404 (PGRST106 „Invalid schema: monitor")**, `/strom-check` würde auf einem Deploy nichts anzeigen. Der DB-Grant IST da; es fehlt nur die PostgREST-Exposition. **Dashboard-Aktion:** Project Settings → API → Exposed schemas → `monitor` hinzufügen (s. `DEPLOYMENT.md` §2a). `platform` bleibt bewusst draußen.
  - **[ABWEICHUNG #2 — harmlos] Cloud-Authenticator lädt zusätzlich `supautils`** (`session_preload_libraries`) — Supabase-Plattform-Tooling, erwartbar. Sonst Rollen-Config identisch.
  - **[Aufgabe 3 — Seed] 0 → 11 `tariff_snapshots`, 10 `current_tariffs`** (View dedupliziert), Historisierung belegt (nur der 22,5-ct-Snapshot in `current_tariffs`; volle Zeitreihe [23,9 · 22,5]). **OFFENER PUNKT (Prinzip 1, §12 #6):** die Seed-Tarife sind **Platzhalter** (erfundene Anbieter „Blitz Energie" …), **keine echten AT-Tarife.** `/strom-check` bleibt **noindex + unverlinkt**; **vor Verlinkung/Indexierung MÜSSEN echte Tarifdaten den Seed ersetzen** — sonst gehen erfundene Zahlen an echte Nutzer (Prinzip 1). Zusätzlich hält `NEXT_PUBLIC_SITE_URL != coolin.at` die ganze Seite per robots aus dem Index (s. `DEPLOYMENT.md`).
  - **[Aufgabe 4 — DB-Gate 21/21 gegen die Cloud GRÜN]** Ziel-DB rein per Env (`SUPABASE_DB_URL`/`_API_URL`/`_SERVICE_ROLE_KEY`), Default bleibt lokal — **kein Code-Change an `client.ts`** (der Session-Pooler wird mit `?sslmode=no-verify` sauber erreicht). Alle 12 platform- + 5 RPC-Wrapper- + 4 Constraint-Tests grün → RLS/Trigger/Constraints/Cascades greifen in der Cloud identisch. **Aufräum-Beleg:** `auth.users`=0 und die fünf user-gebundenen platform-Tabellen=0 (Cascade). **AUSNAHME/BEFUND:** `platform.stripe_events`=**2** Testzeilen (T8/T9) bleiben — die Tabelle ist **append-only** (DELETE-Trigger) UND nicht user-gebunden (kein Cascade); lokal maskiert `db reset` das, gegen eine persistente DB bleibt es liegen. Zeilen ohne Personenbezug (payload null). **Cleanup vom Klassifikator gestoppt** (er verlangt das transiente Deaktivieren des append-only-Triggers) → **Andreas-Entscheidung** (Befehl im Abschlussbericht/Scratchpad). Die CI (`db-gate.yml`) bleibt bewusst auf dem lokalen Stack (S4).
  - **[SECRET-HYGIENE S1/S2]** `git status` nach `link`/`push` unverändert sauber (link schreibt nur ins gitignorete `.temp/`); alle Credentials ausschließlich im Scratchpad außerhalb des Repos; `SUPABASE_SERVICE_ROLE_KEY` NICHT in `apps/web` verdrahtet (S3).
